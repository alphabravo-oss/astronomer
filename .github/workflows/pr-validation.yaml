name: pr-validation

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backend:
    name: enterprise backend gate
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod

      # Backend contract checks consume js-yaml from the locked frontend tree.
      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install contract dependencies
        working-directory: frontend
        run: npm ci

      - name: Install ShellCheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck

      - name: Migration roundtrip smoke (TEST-05)
        run: ./scripts/migrate-roundtrip-smoke.sh
        env:
          # Job uses service container when present; script also self-starts docker PG.
          DATABASE_URL: ${{ env.DATABASE_URL }}

      - name: Supported release-line upgrade matrix
        run: ./scripts/release-upgrade-matrix.sh

      - name: Run authoritative backend gate
        run: ./scripts/verify-enterprise.sh backend
        env:
          VERIFY_ARTIFACT_DIR: ${{ runner.temp }}/astronomer-verify-enterprise-backend

      - name: Upload commit-bound backend evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: enterprise-backend-evidence-${{ github.sha }}
          path: ${{ runner.temp }}/astronomer-verify-enterprise-backend
          if-no-files-found: error
          retention-days: 90

  stateful-qualifications:
    name: stateful · ${{ matrix.name }}
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        include:
          - {name: postgres-integrations, command: "make test-postgres-integration"}
          - {name: postgres-integrations-race, command: "POSTGRES_INTEGRATION_RACE=1 make test-postgres-integration"}
          - {name: worker-runtime, command: "make test-worker-runtime-integration"}
          - {name: worker-runtime-race, command: "WORKER_INTEGRATION_RACE=1 make test-worker-runtime-integration"}
          - {name: process-restart, command: "make test-process-restart-qualification"}
          - {name: process-restart-race, command: "PROCESS_RESTART_QUALIFICATION_RACE=1 make test-process-restart-qualification"}
          - {name: redis-outage, command: "make test-redis-outage-recovery"}
          - {name: redis-outage-race, command: "REDIS_OUTAGE_RECOVERY_RACE=1 make test-redis-outage-recovery"}
          - {name: postgres-outage, command: "make test-postgres-outage-qualification"}
          - {name: postgres-outage-race, command: "POSTGRES_OUTAGE_QUALIFICATION_RACE=1 make test-postgres-outage-qualification"}
          - {name: tunnel-owner-ha, command: "./scripts/test-tunnel-queue-ha.sh"}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
      - name: Run qualification without hidden skips
        env:
          QUALIFICATION_COMMAND: ${{ matrix.command }}
        run: |
          mkdir -p stateful-evidence
          set -o pipefail
          bash -Eeuo pipefail -c "$QUALIFICATION_COMMAND" 2>&1 | tee "stateful-evidence/${{ matrix.name }}.log"
      - name: Retain stateful qualification evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: stateful-${{ matrix.name }}-${{ github.run_id }}
          path: stateful-evidence/
          if-no-files-found: error
          retention-days: 90

  postgres-failover-certification:
    name: PostgreSQL failover RPO/RTO certification
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod

      - name: Validate failover harness contract
        run: make test-postgres-failover-static

      - name: Certify PostgreSQL streaming failover
        run: make test-postgres-failover-certification
        env:
          POSTGRES_FAILOVER_ARTIFACT_DIR: ${{ runner.temp }}/astronomer-postgres-failover
          POSTGRES_FAILOVER_RPO_MAX_ROWS: "0"
          POSTGRES_FAILOVER_RTO_MAX_SECONDS: "30"

      - name: Upload PostgreSQL failover evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: postgres-failover-evidence-${{ github.run_id }}
          path: ${{ runner.temp }}/astronomer-postgres-failover/
          if-no-files-found: error
          retention-days: 90

  frontend:
    name: enterprise frontend gate
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Run authoritative frontend gate
        run: ./scripts/verify-enterprise.sh frontend
        env:
          # Preserve the existing moderate threshold, which is stricter than
          # the enterprise high/critical minimum.
          NPM_AUDIT_LEVEL: moderate
          VERIFY_ARTIFACT_DIR: ${{ runner.temp }}/astronomer-verify-enterprise-frontend

      - name: Upload commit-bound frontend evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: enterprise-frontend-evidence-${{ github.sha }}
          path: ${{ runner.temp }}/astronomer-verify-enterprise-frontend
          if-no-files-found: error
          retention-days: 90

  frontend-e2e:
    name: Playwright end-to-end tests
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Validate flake ownership and quarantine expiry
        run: node ../scripts/test-flake-report.mjs --validate

      - name: End-to-end tests
        run: npm run test:e2e

      - name: Route-smoke crawl
        run: npm run test:e2e:smoke
        env:
          # P7.3: the crawl also writes one full-page screenshot per route to
          # frontend/gallery/ for the reviewer eyeball (non-blocking).
          SMOKE_GALLERY: "1"

      - name: Blocking desktop, tablet, and mobile visual matrix
        run: npm run test:e2e:visual

      - name: Upload screenshot gallery
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: smoke-gallery
          path: frontend/gallery/
          retention-days: 14

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: playwright-report
          path: |
            frontend/playwright-report
            frontend/test-results
          retention-days: 30

  # P7.2 live e2e tier: the live specs run through the same disposable,
  # fail-fast process harness available to developers via make.
  frontend-e2e-live:
    name: frontend live e2e
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod

      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Install Playwright browsers
        working-directory: frontend
        run: npx playwright install --with-deps chromium

      - name: Validate live-browser harness contract
        run: make test-live-browser-static

      - name: Disposable live end-to-end stack
        run: make test-live-browser
        env:
          LIVE_BROWSER_ARTIFACT_DIR: ${{ runner.temp }}/astronomer-live-browser
          LIVE_BROWSER_TRIVY_ENABLED: "1"

      - name: Upload live browser evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: live-browser-evidence-${{ github.run_id }}
          path: ${{ runner.temp }}/astronomer-live-browser/
          if-no-files-found: error
          retention-days: 90

  helm:
    name: enterprise Helm gate
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod

      - name: Set up Helm
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4 # v4
        with:
          version: v3.21.0

      - name: Run authoritative Helm gate
        run: ./scripts/verify-enterprise.sh helm
        env:
          VERIFY_ARTIFACT_DIR: ${{ runner.temp }}/astronomer-verify-enterprise-helm

      - name: Upload commit-bound Helm evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: enterprise-helm-evidence-${{ github.sha }}
          path: ${{ runner.temp }}/astronomer-verify-enterprise-helm
          if-no-files-found: error
          retention-days: 90

  container-supply-chain:
    name: image scan and sbom
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        component:
          - { name: server, context: ., dockerfile: deploy/docker/Dockerfile.server, image: astronomer-go-server }
          - { name: worker, context: ., dockerfile: deploy/docker/Dockerfile.worker, image: astronomer-go-worker }
          - { name: agent, context: ., dockerfile: deploy/docker/Dockerfile.agent, image: astronomer-go-agent }
          - { name: migrate, context: ., dockerfile: deploy/docker/Dockerfile.migrate, image: astronomer-go-migrate }
          - { name: shell, context: ., dockerfile: deploy/docker/Dockerfile.shell, image: astronomer-shell, skip_files: /usr/local/bin/kubectl }
          - { name: frontend, context: frontend, dockerfile: frontend/Dockerfile, image: astronomer-frontend }
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Build image locally
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: ${{ matrix.component.context }}
          file: ${{ matrix.component.dockerfile }}
          load: true
          tags: ${{ matrix.component.image }}:ci
          provenance: false
          sbom: false

      - name: Scan image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ matrix.component.image }}:ci
          format: table
          vuln-type: os,library
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          skip-files: ${{ matrix.component.skip_files }}
          exit-code: "1"

      - name: Install syft
        uses: anchore/sbom-action/download-syft@e22c389904149dbc22b58101806040fa8d37a610 # v0
        with:
          syft-version: "v1.16.0"

      - name: Generate SBOM
        run: |
          syft ${{ matrix.component.image }}:ci \
            -o spdx-json \
            > sbom-${{ matrix.component.name }}.spdx.json
          test -s sbom-${{ matrix.component.name }}.spdx.json
          python3 -c "import json; json.load(open('sbom-${{ matrix.component.name }}.spdx.json'))"

      - name: Upload SBOM
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sbom-${{ matrix.component.name }}-${{ github.sha }}
          path: sbom-${{ matrix.component.name }}.spdx.json
          retention-days: 30

  enterprise-signoff:
    name: same-commit PR qualification aggregate
    if: always()
    needs:
      - backend
      - stateful-qualifications
      - postgres-failover-certification
      - frontend
      - frontend-e2e
      - frontend-e2e-live
      - helm
      - container-supply-chain
    runs-on: ubuntu-24.04
    steps:
      - name: Require every same-commit qualification
        env:
          RESULTS_JSON: ${{ toJSON(needs) }}
        run: |
          python3 - <<'PY'
          import json
          import os

          results = json.loads(os.environ["RESULTS_JSON"])
          failed = {name: value["result"] for name, value in results.items() if value["result"] != "success"}
          summary = os.environ.get("GITHUB_STEP_SUMMARY")
          lines = ["## Same-commit PR qualification aggregate", "", "| Gate | Result |", "| --- | --- |"]
          lines.extend(f"| `{name}` | {value['result']} |" for name, value in sorted(results.items()))
          if summary:
              with open(summary, "a", encoding="utf-8") as handle:
                  handle.write("\n".join(lines) + "\n")
          if failed:
              raise SystemExit(f"required qualification failure: {failed}")
          PY
      - name: Require every protected external producer in the release aggregator contract
        run: |
          ./scripts/check-enterprise-qualification-producers.py > "$RUNNER_TEMP/protected-producers.json"
          python3 - <<'PY'
          import json, os
          value = json.load(open(os.path.join(os.environ["RUNNER_TEMP"], "protected-producers.json"), encoding="utf-8"))
          with open(os.environ["GITHUB_STEP_SUMMARY"], "a", encoding="utf-8") as summary:
              summary.write("\n### Protected release qualifications (validated wiring; external signed runs still required)\n\n")
              summary.write("| Producer | Workflow | Environment |\n| --- | --- | --- |\n")
              for row in value["protected_producers"]:
                  summary.write(f"| `{row['id']}` | `{row['workflow']}` | `{row['environment']}` |\n")
          PY
