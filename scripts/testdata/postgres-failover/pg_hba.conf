# Dedicated disposable qualification network only. Password authentication is
# required for both application and physical replication connections.
local   all          all                              trust
host    all          all          0.0.0.0/0           scram-sha-256
host    replication  all          0.0.0.0/0           scram-sha-256
host    all          all          ::/0                scram-sha-256
host    replication  all          ::/0                scram-sha-256
