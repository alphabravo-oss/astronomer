// Stub entry point (P1.1). P1.7 fleshes this out with the router provider,
// fontsource imports, error boundary, and stale-chunk reload handler.
import { createRoot } from 'react-dom/client';

createRoot(document.getElementById('root')!).render(<div />);
