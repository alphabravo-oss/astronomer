// Package astroclient is the generated, typed Go SDK for the Astronomer
// Management API.
//
// The bulk of this package (astroclient.gen.go) is generated by oapi-codegen
// from docs/openapi.yaml. Regenerate with `make sdk` or `go generate ./...`.
// Do not edit the generated file by hand; change the spec (or
// oapi-codegen.overlay.yaml) and regenerate.
//
// Scope: models + a net/http client only. The Astronomer HTTP server is
// hand-written on go-chi, so no server stubs are generated here.
//
// Authentication is NOT emitted by the generator. All non-/auth, non-/internal
// endpoints expect `Authorization: Bearer <token>` (a session JWT from
// POST /auth/login/ or a personal API token). Supply it via a
// RequestEditorFn, e.g.:
//
//	bearer := func(ctx context.Context, req *http.Request) error {
//		req.Header.Set("Authorization", "Bearer "+token)
//		return nil
//	}
//	c, _ := astroclient.NewClientWithResponses(baseURL,
//		astroclient.WithRequestEditorFn(bearer))
//
// Loosely typed payloads: ~30 component schemas are permissive stubs
// (type:object + additionalProperties:true, e.g. ClusterResponse,
// AllowlistResponse, CloudProviderSpec) and render as map[string]interface{}
// with no field-level type safety. Non-JSON endpoints (text/event-stream SSE,
// application/octet-stream, x-yaml, the greedy {path} k8s/proxy passthroughs)
// are reachable only via the raw *http.Response returned by the low-level
// Client methods, not the typed *WithResponse helpers.
package astroclient

// The config's paths (output, overlay, spec) are relative to the repo root,
// which is two levels up from this package, so run the generator from there.
// `make sdk` is the equivalent entrypoint.
//go:generate sh -c "cd ../.. && go run github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@v2.5.0 -config oapi-codegen.yaml docs/openapi.yaml"
