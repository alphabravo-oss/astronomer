// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: argocd_operation_events.sql

package sqlc

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
)

const createArgoCDOperationEvent = `-- name: CreateArgoCDOperationEvent :one
INSERT INTO argocd_operation_events (
    operation_id,
    level,
    stage,
    message,
    detail
)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, operation_id, level, stage, message, detail, created_at
`

type CreateArgoCDOperationEventParams struct {
	OperationID uuid.UUID       `json:"operation_id"`
	Level       string          `json:"level"`
	Stage       string          `json:"stage"`
	Message     string          `json:"message"`
	Detail      json.RawMessage `json:"detail"`
}

func (q *Queries) CreateArgoCDOperationEvent(ctx context.Context, arg CreateArgoCDOperationEventParams) (ArgocdOperationEvent, error) {
	row := q.db.QueryRow(ctx, createArgoCDOperationEvent,
		arg.OperationID,
		arg.Level,
		arg.Stage,
		arg.Message,
		arg.Detail,
	)
	var i ArgocdOperationEvent
	err := row.Scan(
		&i.ID,
		&i.OperationID,
		&i.Level,
		&i.Stage,
		&i.Message,
		&i.Detail,
		&i.CreatedAt,
	)
	return i, err
}

const listArgoCDOperationEvents = `-- name: ListArgoCDOperationEvents :many
SELECT id, operation_id, level, stage, message, detail, created_at FROM argocd_operation_events
WHERE operation_id = $1
ORDER BY created_at ASC
`

func (q *Queries) ListArgoCDOperationEvents(ctx context.Context, operationID uuid.UUID) ([]ArgocdOperationEvent, error) {
	rows, err := q.db.Query(ctx, listArgoCDOperationEvents, operationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ArgocdOperationEvent{}
	for rows.Next() {
		var i ArgocdOperationEvent
		if err := rows.Scan(
			&i.ID,
			&i.OperationID,
			&i.Level,
			&i.Stage,
			&i.Message,
			&i.Detail,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
