// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tool_operation_events.sql

package sqlc

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
)

const createToolOperationEvent = `-- name: CreateToolOperationEvent :one
INSERT INTO tool_operation_events (
    operation_id,
    level,
    stage,
    message,
    detail
)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, operation_id, level, stage, message, detail, created_at
`

type CreateToolOperationEventParams struct {
	OperationID uuid.UUID       `json:"operation_id"`
	Level       string          `json:"level"`
	Stage       string          `json:"stage"`
	Message     string          `json:"message"`
	Detail      json.RawMessage `json:"detail"`
}

func (q *Queries) CreateToolOperationEvent(ctx context.Context, arg CreateToolOperationEventParams) (ToolOperationEvent, error) {
	row := q.db.QueryRow(ctx, createToolOperationEvent,
		arg.OperationID,
		arg.Level,
		arg.Stage,
		arg.Message,
		arg.Detail,
	)
	var i ToolOperationEvent
	err := row.Scan(
		&i.ID,
		&i.OperationID,
		&i.Level,
		&i.Stage,
		&i.Message,
		&i.Detail,
		&i.CreatedAt,
	)
	return i, err
}

const listToolOperationEvents = `-- name: ListToolOperationEvents :many
SELECT id, operation_id, level, stage, message, detail, created_at FROM tool_operation_events
WHERE operation_id = $1
ORDER BY created_at ASC
`

func (q *Queries) ListToolOperationEvents(ctx context.Context, operationID uuid.UUID) ([]ToolOperationEvent, error) {
	rows, err := q.db.Query(ctx, listToolOperationEvents, operationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ToolOperationEvent{}
	for rows.Next() {
		var i ToolOperationEvent
		if err := rows.Scan(
			&i.ID,
			&i.OperationID,
			&i.Level,
			&i.Stage,
			&i.Message,
			&i.Detail,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
