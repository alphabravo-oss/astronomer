# P2/P3 Triaged Backlog — re-verification pass

**Date:** 2026-07-29
**Repo state verified against:** `main` @ `e8f8c34`
**Supersedes:** the dispositions, severities and remediation text of §6 and §7 of `PARITY_AND_HARDENING_REMEDIATION_2026-07-28.md` for the 49 findings listed here.

This document is **not a new audit**. It is a re-verification of the P2/P3 tail of the 2026-07-28 parity/hardening plan against the code as it stands after the nine post-audit commits (`2b418a1..e8f8c34`), performed read-only. Every finding in §6 and §7 of the original plan was re-read against current source, migrations, queries, the OpenAPI spec, chart templates and the frontend, by symbol rather than by the line numbers the plan quotes (most of which have drifted). Where the plan's evidence is now stale, wrong, or where its remediation would regress work that shipped after it was written, that is stated plainly below. For the findings covered here, **the dispositions in this document replace the ones in the original plan**; the original remains the source of truth for the finding narratives, acceptance criteria and remediation steps except where superseded here.

Why this pass exists: during the same session, one audit finding was found to be factually wrong (it claimed a privilege escalation required an operator-authored custom role when migration 032 seeds a built-in `RBAC Administrator` with `rbac:*`), and another's diagnosis was half wrong in a way that would have broken two working fields if patched as described. Nothing below is carried forward on the strength of the original text alone.

### Commit range, stated precisely

The task framing cites nine commits as `e8ef831..HEAD`. That range actually contains only five (`faf062b`, `a586b5c`, `29f8c94`, `0f8101c`, `e8f8c34`). The other four (`9cd99d8`, `3799088`, `2c92f45`, `37dcf42`) are ancestors of `e8ef831`. The full post-audit code range is `2b418a1..e8f8c34`, containing all nine plus two docs-only commits (`4b37848`, `e8ef831`). All nine were considered.

---

## 1. Summary

### By disposition (49 unique findings)

| Disposition | Count | Meaning |
|---|---|---|
| still-valid | 47 | Defect reproduces against `e8f8c34`; no compensating control shipped |
| partially-fixed | 2 | One symptom closed by a post-audit commit; the underlying defect survives |
| already-fixed | 0 | — |
| wrong-as-written | 0 | No finding's *defect claim* is false |
| obsolete | 0 | — |

**Findings requiring no further work: 0.** No P2/P3 finding was closed by the nine post-audit commits — they concentrated on P0/P1 authz, tunnel backpressure, catalog sync and at-rest encryption. What *did* change is the remediation text: **4 remediation blocks are wrong as written** (2 of them destructively so) and **3 evidence sub-claims are false**. Those are §3.

### By severity as of today

| Severity now | Count | Effort split |
|---|---|---|
| medium | 23 | S ×8, M ×13, L ×2 |
| low | 26 | S ×11, M ×9, L ×6 |

### Counts by source section

| Section of the original plan | Findings | still-valid | partially-fixed | medium now | low now |
|---|---|---|---|---|---|
| §6.1–6.2 core & features | 9 | 9 | 0 | 9 | 0 |
| §6.3–6.7 agent, transport, crypto, ArgoCD, gates | 21 | 21 | 0 | 14 | 7 |
| §7 P3 (first half) | 10 | 8 | 2 | 0 | 10 |
| §7 P3 (second half) | 11 | 11 | 0 | 0 | 11 |
| **Unique total** | **49** | **47** | **2** | **23** | **26** |

51 verification records were produced; `gitops-decrypt-silent-ciphertext-fallback` and `keyrotate-dex-cutover-offset-not-advanced` were each verified twice by different reviewers on a deliberate seam overlap. Both pairs agree on substance (see §7 for the line-number disagreement).

---

## 2. Duplicate-verified items

| id | Verified twice? | Agreement |
|---|---|---|
| gitops-decrypt-silent-ciphertext-fallback | yes (§7 first half + second half) | Full — same code, same line anchors (`internal/worker/tasks/gitops_sync.go:769,:774,:789-797,:799-808`) |
| keyrotate-dex-cutover-offset-not-advanced | yes (§7 first half + second half) | Substance identical; line anchors differ by 1–3 (see §7) |

---

## 3. Do not implement these

Nothing here is a finding to close. Everything here is **work somebody would otherwise waste or code they would otherwise break** by following the original plan verbatim. Read this before touching any of the four findings named.

### 3.1 Remediation steps that would break working code

| Finding | The step | What the code actually does now | Do instead |
|---|---|---|---|
| `informer-full-object-caches` | Item 2: "move Pod/ConfigMap/Secret/**Node** to metadata informers" | `a586b5c` made the heartbeat read Node objects out of the typed cache: `internal/agent/state_subscriber.go:867-883` `InventoryNodes` returns `[]*corev1.Node`, consumed at `internal/agent/health.go:327-328`, `:528-529`, `:550-551` for `node.Status.Allocatable.Cpu()/Memory()` and `node.Spec.ProviderID`. A metadata informer for Node **silently zeroes reported cluster CPU/memory capacity and breaks provider/distro detection**. | Move only ConfigMap/Secret (optionally Service). Keep Node typed and use `informers.WithTransform` to strip `managedFields` + last-applied annotation, explicitly preserving `Status.Allocatable` and `Spec.ProviderID`. |
| `agent-observability-gap` | Item 1: add 7 metrics including `astronomer_agent_tunnel_send_dropped_total` and `..._send_queue_depth` | `a586b5c` already registers both, in the `init()` at `internal/agent/metrics.go:78-87`. Re-adding them **panics at process start** on `prometheus.MustRegister` duplicate registration. Shipped label sets are `{class,reason}` and `{class}`, not the plan's `{type}`. | Item 1 shrinks to the five still-absent metrics: `tunnel_connected` gauge, `tunnel_reconnects_total`, `build_info`, `heartbeat_failures_total`, `informer_sync_failed`. Items 2–3 stand; item 2 (`/readyz` `DegradedReasons`, `internal/agent/health.go:682-687` is still a bare `hr.connected.Load()` while `collectHeartbeat` populates reasons at `:447-511`) is now the highest-value remnant. |

### 3.2 Remediation steps that are not implementable as described

| Finding | The step | What the code actually does now | Do instead |
|---|---|---|---|
| `operation-supersede-drift-kills-inflight-ops` | Item 3: "stop treating a still-running op as retryable in `operationstate.IsRetryable`" | `internal/operationstate/status.go:15-17` is `func IsRetryable(status string) bool { return status == Failed \|\| status == Superseded }`. It never treated a running op as retryable, and it receives only a status string — it **cannot** distinguish "superseded mid-execution" from "superseded before starting". | Drop item 3, or move it to the retry endpoint in the handler and key it on a re-read of `started_at`/`completed_at`. Items 1–2 (status guards on the six `Mark*Superseded` queries, all of which end `WHERE id = $1` — `internal/db/queries/tool_operations.sql:80-88` plus the five siblings) are the whole of the fix. |
| `ui-proxy-permission-mapping-ignores-argocd-verbs` | Item 1: "sync/rollback/terminate/resource-action require `argocd:sync` **OR** `clusters:update`" | `faf062b` deliberately re-anchored proxy **mutations** to GLOBAL scope: `internal/server/middleware/argocd_authz.go:85-94` resolves the local cluster for reads only and leaves `clusterID = uuid.Nil` for mutations, with the rationale at `:36-51` (a proxied request carries no parseable destination, so a cluster-scoped grant must not authorize fleet-wide delivery changes). Adding cluster-scope-evaluated alternatives **regresses that hardening**. | Keep global-scope evaluation for mutations; add `argocd:sync` / `argocd:manage` as *global-scope* alternatives there. Evaluate read alternatives (`argocd:read` OR `argocd:list`) at local-cluster scope as today. Item 3 (what scope project-scoped `argocd` grants evaluate against) becomes the load-bearing prerequisite, not an afterthought. **Effort rises S → M.** |

### 3.3 Evidence sub-claims that are false — do not repeat them in a commit message or PR body

| Finding | False claim | Truth |
|---|---|---|
| `dockerignore-omits-large-build-artifacts` | "only `bin/` is actually in `.gitignore` — `keyrotate`, `worker`, `screenshots/` are merely untracked, so one `git add -A` commits 150 MB+" | `.gitignore` (dated 2026-07-16, i.e. **before** the audit) lists `bin/`, `/server`, `/agent`, `/worker`, `/keyrotate`, `screenshots/`, `coverage.out`, `*.test`, `.worktrees/`, `.claude/`. No commit hazard, no `git status` noise. Only the Docker-build-context half survives: `.dockerignore` is 11 lines with none of these, `deploy/docker/Dockerfile.server` does `COPY . .`, and the artifacts exist today (`keyrotate` 26M, `worker` 125M, `bin/` 297M, `screenshots/` 73M) → ~470 MB of context per image × 5 images, plus layer-cache busting. |
| `parse-cluster-id-helper-ignored-87-times` | "`MaxBytesReader` appears only in `internal/tunnel` proxy paths" | It also appears at `internal/handler/apiserver_audit.go:166`, `internal/handler/service_mesh.go:634`, `internal/handler/service_proxy.go:94` (plus `internal/tunnel/proxy.go:540`). Those are three bespoke per-endpoint caps. The finding's substance is unaffected — there is still no global body cap over the ~121 `json.NewDecoder(r.Body).Decode` sites. |
| `agent-no-read-idle-deadline` | "A blocked `conn.Write` can also wedge `writeLoop` until `sendCh` saturation trips `failClose`" | After `a586b5c` only a full **CONTROL** queue trips `failClose` (`internal/agent/tunnel.go:843-861`); stream/best-effort frames drop with counters and never close the socket. A blocked write now wedges `writeLoop` until control traffic (heartbeat pong, session lifecycle) backs up — a narrower, slower recovery path than the audit described. This **strengthens** remediation step 3; only the sentence is stale. |

### 3.4 Remediation steps that carry a cost the plan did not price

| Finding | Caveat |
|---|---|
| `alertmanager-email-missing-smtp-globals` | Step 1 wants validation via `github.com/prometheus/alertmanager/config.Load`. That module is **not** in `go.mod` (only `github.com/prometheus/common v0.66.1`); adding it pulls a large new module tree. Lead with steps 2–4 (emit SMTP globals from the stored SMTP row, omit email receivers when no SMTP row exists and say so in Preview) plus a lighter structural assertion in tests. |
| `no-drift-exception-mechanism` | Step 3 (ship default `ignoreDifferences` on the platform baseline) now argues against a comment `faf062b` deliberately added at `internal/server/baseline_appsets.go:554-556`, which reasons that prune+selfHeal should stay ON *because* `targetRevision` is pinned. Raise step 3 with that comment in view; do not apply it silently. Steps 1, 2, 4 unaffected. |
| `project-quota-no-aggregate-accounting` | Two quota objects are applied per namespace today: `astronomer-managed` from the `resource_quota` JSONB blob (`internal/worker/tasks/project_reconcile.go:342-345`) and `astronomer-project-quota` from the scalar columns (`:352-357`, rendered at `:823-851`). The plan never says which becomes the per-namespace default. Decide before writing the back-compat migration or the split lands inconsistently. |
| `git-backed-chart-repos-accepted-never-synced` | If the "reject at create" option lands, **keep** the `catalog.IsGitRepo` branch and `errRepoTypeUnsupported` in `internal/worker/tasks/catalog_sync.go:183-196` for already-persisted rows, and give `CreateRepo`'s reject an escape for existing git rows or an upgrade strands them un-deletable. Also: the plan's test premise "SyncRepo returns 502 after an index.yaml 404" is now true only of the handler route (`internal/handler/catalog.go:669-673`), not the scheduled sweep — assert against both. |
| `argocd-go-3430-line-split`, `crd-controller-2919-line-split`, `newapp-1600-line-constructor`, `api-hooks-flat-2700-line-modules` | **Every line range quoted in these four remediations is stale.** `internal/handler/argocd.go` grew 3,430 → 3,548 (+118); `internal/crd/controller.go` 2,919 → 2,935; `frontend/src/lib/api.ts` 2,763 → 2,802; `internal/server/server.go` boundaries drifted +9 to +40. Re-derive every seam by symbol. Also do not confuse the split target `internal/handler/argocd.go` with the existing Argo **client** package `internal/handler/argocd/`. |
| `appproject-scoping-absent` | There are **two** project-identity assertions in `internal/server/self_manage_values.go`, at `:869` (`validateActiveSelfManagedApplicationIdentity`) and `:899`, both comparing `project != "default"`. Relaxing only one breaks self-manage. |
| `leave-local-ownership-exclusion-fails-open` | `resolveBaselineChartVersion`'s doc comment at `internal/server/baseline_appsets.go:253-258` cites this fail-open as the file's house style for bad operator data. Update that comment in the same commit or the behavior gets re-introduced. |
| `kubectl-shell-ws-reopen-skips-rbac-when-scope-flag-off` | Step 1's precondition ("after the exec-logs-verb-mismatch fix") is **already satisfied** — `37dcf42` shipped pod-verb exec/logs gating and `rbac.ResourcePods` / `rbac.VerbExec` exist (`internal/rbac/types.go:10`, `:111`). Implement directly against `(ResourcePods, VerbExec)`. |
| `argocd-internal-proxy-tls-insecure-skip-verify` | Step 4's "copy the local branch" shape exists at `internal/worker/tasks/argocd_auto_register_cluster.go:624-626` (`{Insecure: cluster.CaCertificate == "", CAData: ...}`) — but that branch is **inert today** because of `cluster-ca-never-persisted`. It is a shape to copy, not a working reference implementation. Do not conflate this with `faf062b`'s "ArgoCD TLS verification", which was the instance-API `verify_ssl` item. |

---

## 4. Still actionable

Grouped by severity as of today, then by effort. "Remediation" column: ✅ = original steps apply verbatim (modulo line drift); ⚠️ = applies with the caveat in §3.4; ❌ = must be reworked per §3.1–3.2.

### 4.1 Medium — 23 findings

#### Effort S (8)

| id | Current evidence @ `e8f8c34` | Rem. |
|---|---|---|
| `api-token-no-max-ttl` | `internal/handler/auth.go:1488-1494` still `var expiresAt pgtype.Timestamptz; if req.ExpiresInDays > 0 {…}`, NULL persisted at `:1517-1524`. Request struct `:1391` is bare `ExpiresInDays int`. `token.max_ttl_min` is defined once (`internal/handler/platform_settings.go:148`, default 525600, now MinInt/MaxInt-bounded) and referenced nowhere else repo-wide. `docs/openapi.yaml:15435` still documents "0 or omitted = no expiry" with no maximum. | ✅ |
| `alertmanager-drops-pagerduty-msteams` | Both renderers cover 3 of 5 types: `internal/handler/alerting.go:1381-1395` (`slack`/`webhook`/`email`/`default: continue`, route append at `:1397-1404` inside the same loop, so an unmatched type yields neither receiver nor route) and `internal/handler/monitoring.go:2560-2571`. Canonical list: `internal/worker/tasks/notification_dispatch.go:61-67`. Delivery is real: `alerting.go:1248` writes `astronomer-thanos-rule-alertmanagers`, `monitoring.go:2277-2281` wires it into Thanos Ruler. | ✅ |
| `cis-ingest-in-process-goroutine-loses-scans` | `internal/handler/security.go:687-695` still `go h.pollScanReport(...)`; `pollScanReport:710-717` is 60×30s under a 35-min `context.Background()` timeout. Enqueuer dead: `SetIngestQueue` called at `internal/server/routes_security.go:137`, assigned at `security.go:145-148`, `h.queue` read at zero sites. `tasks.ConfigureSecurityIngest` (`internal/worker/tasks/security_scan.go:92`) has no non-test caller. `TypeSecurityIngest` registered only in `Worker.RegisterHandlers` (`internal/worker/worker.go:247`), absent from `RegisterTunnelHandlers` (`:221-234`). No reaper in `internal/worker/scheduler.go`. | ✅ (option b) |
| `exec-and-log-binary-corruption` | `internal/agent/exec.go:235-238` still `json.Marshal(map[string]string{"stream":…, "data": string(p)})`; stdin round-trip at `:169-188`; `internal/agent/logs.go:121-123` still `map[string]string{"line": scanner.Text()}` with a 1 MiB buffer at `:118`. `internal/agent/k8sproxy.go` base64-encodes on the proxy path — the in-tree fix pattern. Two evidence nits: `Write` returns `(0, err)` on send failure (`exec.go:239-244`), and since `a586b5c` a full queue surfaces as a stream error via `recordSendDrop` (`tunnel.go:843-861`) rather than a silent success. | ✅ |
| `kubectl-shell-ws-reopen-skips-rbac-when-scope-flag-off` | `internal/handler/kubectl_shell.go:527-604` unchanged: ticket auth `:536`, `loadSessionForCluster` `:549` (ownership + cluster match only), status check `:553`, RBAC re-derivation only inside the flagged block `:576-583`. `shellScopeEnabled` (`internal/handler/kubectl_shell_scope.go:328-339`) requires `NamespaceScopedRBAC` or `shell_scope_to_caller`, both default false. Cross-pod forward `:600-604` runs with no unconditional verb check. | ⚠️ §3.4 (easier than written) |
| `leave-local-ownership-exclusion-fails-open` | Newly verified in full. `internal/server/baseline_appsets.go:413-421`: `if q == nil { return out }` and `if err != nil { return out }`, both yielding the empty exclusion map; intent documented at `:408-412`. Caller `:370` uses it unconditionally; `ensureBaselineApplicationSets` does `res.Update` at `:401` every tick; loop period 30s (`internal/server/self_manage_argocd.go:56`, driven at `:111`/`:205`). Generated Applications carry automated prune+selfHeal (`baseline_appsets.go:563-564`). | ⚠️ §3.4 |
| `api-ts-shadow-generated-types` | All seven shadow pairs present: `FeatureFlags` (`frontend/src/lib/api.ts:217` vs `openapi.generated.ts:1690`), `RegistrationStep` (`:313`/`:1742`), `RegistrationStatus` (`:335`/`:1741`), `NodeKeyValueRequest` (`:498`/`:1715`), `NodeKeyRequest` (`:503`/`:1714`), `NodeTaintRequest` (`:507`/`:1719`), `NodeTaintRemoveRequest` (`:513`/`:1718`); plus `DrainNodeRequest` (`:456`) shadowing `NodeDrainRequest` under a different name. Gate hole intact: `scripts/code-health-inventory.mjs:153` skips `openapi.generated.ts`, regex `:155-157` only matches `*Request/Response/WriteRequest`. | ✅ |
| `golangci-lint-never-runs-in-ci` | `grep -rn golangci` over `Makefile`, `scripts/`, `.github/` = 2 hits, both in the Makefile (`:88` help, `:89` `golangci-lint run ./...`). `.golangci.yml` present at repo root, invoked by nothing; `scripts/verify-enterprise.sh` never calls it; the binary is not installed in this environment. | ✅ |

#### Effort M (13)

| id | Current evidence @ `e8f8c34` | Rem. |
|---|---|---|
| `group-membership-only-refreshed-at-login` | `auth.SyncUserGroups` (`internal/auth/group_sync.go:106`) has exactly two production callers: `internal/handler/sso.go:437` (SSO callback) and `internal/handler/group_mappings.go:345` (manual admin resync). `internal/worker/scheduler.go:40-100` registers only `tasks.RefreshGroupSyncMetricsType` (@every 5m — three `COUNT(*)`s, not a reconcile). No membership-refresh task exists under `internal/worker/tasks`. The claims-unavailable no-revoke policy the remediation depends on is intact at `group_sync.go:118-127`. | ✅ |
| `project-quota-no-aggregate-accounting` | `internal/worker/tasks/project_reconcile.go:324` `reconcileProjectNamespace` applies the project quota to each namespace independently (`renderResourceQuota` SSA at `:342-345`; explicit-policy path `:352-357` via `renderProjectResourceQuota` `:823-851` copying `ResourceQuotaCpuLimit/MemoryLimit/PodCount` onto `spec.hard`). No summation, ceiling or used-limit in the file. `internal/handler/projects.go` has no aggregate validation (quota hits are the per-tenant `quota.Enforcer` for project *count*, `:105`, `:242-246`, and DTO fields `:370-396`). No migration defines `namespace_default_resource_quota` or `resource_quota_used_limit`. | ⚠️ §3.4 |
| `alertmanager-email-missing-smtp-globals` | `internal/handler/monitoring.go:2565-2568` emits `email_configs: [{to, send_resolved}]`; `:2580-2582` sets `global: {resolve_timeout: 5m}` only. Identical at `internal/handler/alerting.go:1390-1393`, `:1405-1408`. Rendered YAML ships inline as the chart `config` value (`monitoring.go:2486-2506` `sharedAlertmanagerPayload` → `applySharedAlertmanager` `:2510`). No `smtp`/`smarthost` anywhere in either renderer. Source of truth exists (`internal/handler/smtp.go`, `internal/db/sqlc/smtp.sql.go`); `AlertingHandler` already holds a `SettingsCache` (`alerting.go:97`, `:141`). | ⚠️ §3.4 |
| `alertmanager-routing-configmap-inert` | `internal/handler/alerting.go:1232-1245` applies three ConfigMaps; repo-wide grep for `alertmanager-routing` and `alert-silences` returns only those write sites. Only `astronomer-ruler-rules` is consumed (`internal/handler/monitoring.go:2265-2268`). Alertmanager still takes config inline (`monitoring.go:2499-2506` + `configmapReload.enabled=true`), computed only during Preview/Install/Upgrade/Replace (`:850`, `:873`, `:900`, `:936`). Precedent to mirror: `alerting.go:1248` writes a Secret that `monitoring.go:2277-2281` consumes by name. | ✅ (verify the chart's `configFromSecret`/`existingSecret` key against the pinned version) |
| `no-release-history-or-revision-rollback-ui` | Backend routed: `internal/server/routes_tools_controlplane.go:220-221` → `internal/handler/catalog.go:1774` `ListInstalledChartRevisions`, `:1810` `GetInstalledChartValues`. No frontend caller (grep for `revisions` under `frontend/src` hits only the ArgoCD history helper `src/lib/api.ts:2211-2217`). Only rollback affordance: `frontend/src/routes/dashboard/catalog/index.tsx:193-204` `rollback.mutate({id, revision: row.revision - 1})`, no confirm, while uninstall at `:205-215` confirms. Backend already accepts an arbitrary revision: `catalog.go:1426-1443` computes `max(current.Revision-1, 1)` but honors an explicit `{"revision": N}` body. | ✅ |
| `agent-no-read-idle-deadline` | `internal/agent/tunnel.go:995-1006` `readMessage` calls `conn.Read(ctx)` with the bare loop context; `:1008-1014` `writeMessage` likewise; `readLoop:593-595` and `writeLoop:715-743`→`writeQueued:747-755` pass it unchanged. No `lastActivity`, no idle timeout, no ping ticker in the package. `cmd/agent/main.go:364` wires only `tunnel.SetConnectionListener(health.SetConnected)`. `internal/agent/health.go:678-688`: `/healthz` unconditional 200, `/readyz` a single boolean. `a586b5c`'s `internal/agent/upgrade_watchdog.go` is a self-upgrade rollback Job, not a liveness watchdog. | ✅ (one stale sentence — §3.3) |
| `no-decommission-signal-to-offline-agent` | `internal/tunnel/server.go:897-919` `SendDecommission` returns `(nil,false,nil)` with no local connection and no sibling locator hit. `protocol.ConnectAckPayload{Accepted:false}` is constructed nowhere in `internal/tunnel` (only `:493` with `Accepted:true`, plus one test fake). Revoked-token path closes bare at `:521` with `StatusPolicyViolation` before any ACK. Agent side `internal/agent/tunnel.go:354-357` turns `!ack.Accepted` into a plain error feeding `reconnectLoop` — no terminal state, no `DecommissionHandler`. `internal/worker/tasks/cluster_decommission.go` has zero `argoproj/Application` references. Partial mitigation already present: `cluster_decommission.go:113-160` implements a 10-attempt / 15-min grace window plus a 2-min-heartbeat-staleness fast path — bounds the window, does not close it. | ✅ |
| `namespace-filtered-watch-local-path-filters-raw-chunks` | `internal/tunnel/proxy.go:332-346` base64-decodes each `K8sStreamFrameData` frame and hands the whole decoded frame to `watchEventAllowed`, dropping on `ferr != nil \|\| !keep` with no carry-over buffer. Producer is not event-aligned: `internal/agent/k8sproxy.go:164` `k8sStreamChunkSize = 16*1024`, buffer at `:256`, emit-whatever-Read-returned at `:261`. Cross-pod path does it correctly (`proxy.go:663-671`, `bufio.Scanner`, 4 MiB cap at `:665`) — the two paths disagree. | ✅ |
| `tunnel-rate-limits-keyed-on-spoofable-ip-and-run-before-auth` | `internal/server/routes.go:1071-1079` orders `rateLimit(ClassK8sProxy)` before `requireAuth`. Exec (`:1114-1115`), logs (`:1118-1119`), kubectl shell (`:1134-1135`) carry `rateLimit(ClassExecLogs)` with no `requireAuth` in the chain at all. `internal/server/middleware/api_rate_limit.go:353-369` prefers `GetAuthenticatedUser` (unpopulated at that point) then falls to `"ip:"+host(r.RemoteAddr)`. `chimiddleware.RealIP` installed unconditionally at `routes.go:355`; no `trusted_proxy_cidrs` config exists repo-wide. `internal/server/middleware/audit.go:205-239` `RemoteIPAddr` takes the first XFF entry untrusted and feeds `ConnectClientIP` and `Event.IPAddress`. | ✅ (item 1 remains the least valuable of the three) |
| `cluster-ca-never-persisted` | `ca_certificate` appears in **zero** queries under `internal/db/queries/`; every non-generated Go reference is a read: `internal/server/self_manage_argocd.go:341`, `:344-345`, drift check `:397-398`; `internal/handler/argocd.go:2354-2355`; `internal/worker/tasks/argocd_auto_register_cluster.go:625-626`. `internal/handler/clusters.go:2368` emits an always-empty `certificate-authority-data`. Manual override discarded: `argocd.go:3146-3148` uses `cluster.CaCertificate` only when `req.CAData` is empty, and nothing persists `req.CAData`. Non-local managed path returns `TLSClientConfig{Insecure: true}` unconditionally (`argocd_auto_register_cluster.go:650`). | ✅ (step 4 still won't fix managed clusters — see `argocd-internal-proxy-tls-…`) |
| `decommission-wedges-baseline-applications` | `internal/server/baseline_appsets.go:524` stamps `resources-finalizer.argocd.argoproj.io` on every generated Application (rationale `:516-524`), with automated prune+selfHeal at `:563-564`. `internal/worker/tasks/cluster_decommission.go:926` `lookupClusterSecret`, `:941` deletes the Secret, `:968` drops index rows via `DeleteArgoCDManagedClustersByCluster`; grep for `Application` in that file returns nothing. `internal/handler/argocd_orphans.go` is the only Application-aware surface and is read-only. | ✅ |
| `ui-proxy-permission-mapping-ignores-argocd-verbs` | `internal/server/middleware/argocd_authz.go:105-110` returns `(rbac.ResourceClusters, rbac.VerbUpdate)` for every mutation, `(rbac.ResourceArgoCD, rbac.VerbRead)` otherwise, never consulting argocd-native verbs; `isArgoCDMutatingPath:128-136` covers only `/sync`, `/rollback`, `/terminate`, `/resource-action`; engine call `:97` passes `uuid.Nil` for `projectID`, so project-scoped bindings can never match. Migration 098 read directly: `:15` GitOps Admin is GLOBAL with `argocd:[create,read,update,delete,list,sync,manage]` + `clusters:[read,list]`; `:48` GitOps Deployer and `:52` Workload Deployer land in `project_roles` with `argocd:[read,list,sync]`. Impact narrows post-`faf062b` (fleet-wide sync now needs a **global** `clusters:update`); the GitOps Admin 403 is if anything worse. | ❌ §3.2 — rework, S → M |
| `no-request-schema-field-gate` | `scripts/verify-enterprise.sh:55-82` `verify_contract_artifacts` runs five checks (openapi-coverage, generated frontend types, docs↔embedded byte compare, error-code docs, route-metadata JSON well-formedness); `verify_api_contract:85-108` adds route-table, error-catalog, route-security. Nothing field-level. `scripts/openapi-coverage.mjs` contains no reference to `properties`, `requestBody` or `schemas`. Live consequence still reproducible: `NodeDrainRequest` is missing `force` with every gate green. | ✅ |

#### Effort L (2)

| id | Current evidence @ `e8f8c34` | Rem. |
|---|---|---|
| `appproject-scoping-absent` | Every Argo object hard-codes the `default` project: `internal/server/baseline_appsets.go:538`, `internal/server/self_manage_migration.go:170`, `internal/crd/controller.go:1100`, `:2532`, `:2557`. No AppProject manifest under `deploy/chart/templates` (only a comment in `serviceaccount.yaml:141`). `internal/handler/argocd_project_validation.go:19-25` skips URL validation when `destination.Server == "*"`; `internal/argosecurity/policy.go:1504-1506` `ValidateSourceRepoPattern` returns nil for `"*"`; `allowProjectDestinationWildcard` still live at `policy.go:672`, `:847`. `faf062b`'s AppSet pinning and destination authorization added no AppProject. | ⚠️ §3.4 (two identity assertions) |
| `no-scheduled-cis-scans` | No schedule storage (`scan_schedule`/`cis_scan` absent from `internal/db/migrations`). No cron entry in `internal/worker/scheduler.go`. Only start path is `POST /security/scans/` → `CreateScan`, building one `astronomer-cis-<unix>` ClusterScan CR from `cluster_id` + `profile`; no `scheduledScanConfig`/`cronSchedule` in `security.go`. Wizard one-shot: `frontend/src/routes/dashboard/security/scans/new/index.tsx`. | ✅ — but this is the weakest medium in the set (pure feature gap, nothing broken; treat as medium-low) and **must** follow `cis-ingest-…`, whose step 2 (`extract startScan`) is a hard prerequisite |

### 4.2 Low — 26 findings

#### Effort S (11)

| id | Current evidence @ `e8f8c34` | Rem. |
|---|---|---|
| `clusterbaseline-gitopstarget-target-management-cluster` | `internal/crd/controller.go:1231-1238` `clusterBaselineClusterSelector` seeds `matchLabels` with only `{astronomer.io/managed-by: astronomer}` + operator labels; `gitOpsTargetClusterSelector:2748-2755` identical. Platform baseline *does* exclude local (`internal/server/baseline_appsets.go:485` sets `argoCDIsLocalLabelKey: "false"`), and `internal/argolabels/labels.go:24`/`:64` stamps `astronomer.io/is-local` on the managed-cluster Secret — so a managed-by-only selector matches the local Secret. | ✅ (use `argolabels.IsLocalLabelKey`; selector moved to `:2748`) |
| `drain-force-field-never-reaches-ui` | `internal/handler/resources.go:67-71` declares `Force bool \`json:"force,omitempty"\`` and gates on it at `:1303`. `docs/openapi.yaml` `NodeDrainRequest` lists only `ignore_daemonsets`/`delete_empty_dir_data`/`grace_period_seconds`/`dry_run`. `frontend/src/lib/api.ts:456` hand-written `DrainNodeRequest` lacks `force`; `:493` `drainNode` defaults body to `{}`; `frontend/src/routes/dashboard/clusters/$id/nodes/$nodeName/index.tsx:354` sends no body. (`resources.go:1470` `Force:true` is `kubeutil.ApplyOptions`, unrelated.) | ✅ |
| `permission-decision-unmemoized-defeats-every-usememo` | `frontend/src/lib/permission-hooks.ts:9-16` returns `explainPermission(...)` with no `useMemo`. `frontend/src/routes/dashboard/clusters/$id/$resource/index.tsx:793-808` `useClusterResourcePermissions` memoizes only `scope` (`:795`) and returns a bare object literal of nine decisions — fresh identity every render. Wasted re-render work; no incorrect behavior. | ✅ |
| `git-backed-chart-repos-accepted-never-synced` **(partially-fixed)** | `29f8c94` fixed the sweep half: `internal/worker/tasks/catalog_sync.go:183-196` now has `case catalog.IsGitRepo(repoRecord):` (helper `internal/catalog/repository.go:78-80`) returning `errRepoTypeUnsupported` with an actionable message, recorded against the row at `:101-121` and excluded from the failure count. Defect survives on both other paths: `internal/handler/catalog.go:444-450` still accepts and persists `repo_type='git'` with no allowlist; manual sync `:669-673` branches OCI-else-`fetchAndIngestRepoIndex`, so Sync still 502s. UI offers only `['helm','oci']` (`frontend/src/routes/dashboard/catalog/index.tsx:940`). Acceptance criterion "no path can persist a permanently unsyncable repo row" unmet. The new worker comment `:105-116` names this finding and states the durable fix is reject-at-create — that settles the Reject-vs-Implement choice. | ⚠️ §3.4 |
| `loki-log-query-not-surfaced` | `QueryOutput` at `internal/handler/logging.go:722-725`, routed at `internal/server/routes_tools_controlplane.go:276`. Frontend has three tabs only: `frontend/src/routes/dashboard/logging/index.tsx:47-55` `type TabKey = 'outputs' \| 'pipelines' \| 'operations'`. No caller for `QueryOutput`/`/query/` under `frontend/src`. | ✅ |
| `agent-observability-gap` **(partially-fixed)** | `a586b5c` added five metrics at `internal/agent/metrics.go:78-87` (`tunnel_send_dropped_total{class,reason}`, `tunnel_send_queue_depth{class}`, `inflight_dispatch_active{class}`, `inflight_dispatch_rejected_total{class,type}`, `response_buffer_bytes`) — two of the plan's seven plus two it never asked for. Still absent: `tunnel_connected`, `tunnel_reconnects_total`, `build_info`, `heartbeat_failures_total`, `informer_sync_failed`. `/readyz` (`internal/agent/health.go:682-687`) still a bare boolean writing `"not_connected"` despite `collectHeartbeat` populating `DegradedReasons` at `:447-511`. | ❌ §3.1 for step 1; steps 2–3 ✅ |
| `gitops-decrypt-silent-ciphertext-fallback` | `internal/worker/tasks/gitops_sync.go:789-797` `func decryptGitAuth(blob string) string` returns `blob` from the error branch, discarding `err`. Callers `:769` (`githttp.BasicAuth{Password: decryptGitAuth(...)}`) and `:774` (`gitssh.NewPublicKeys`, which fails loudly at `:775-777` — only the https_token branch is silent). `gitops_registration_sources.auth_encrypted` is in keyrotate's `rewriteTargets` (`cmd/keyrotate/main.go:169`), so a decrypt failure means key loss or a keyrotate miss. No `*_decrypt_failures_total` metric near `updateSourceGauges` (`:799-808`). | ✅ (the plan's step-2 correction — passthrough is load-bearing for pre-encryption plaintext rows, per the doc comments at `:757-760` and `:784-788` — is still accurate) |
| `keyrotate-dex-cutover-offset-not-advanced` | `cmd/keyrotate/main.go` `cutoverDexPublicClients` (from `:434`): the offset advance is still guarded by `if dryRun { offset += len(batch) }` (`:492-494`), followed by the only exit `if len(batch) < batchSize { break }`. All four failure branches `result.failed++; continue` while the predicate `WHERE public_clients_cutover_at IS NULL ORDER BY id LIMIT $1 OFFSET $2` (`:441`) re-selects them. `--batch-size` help still says "rows per transaction" while there is **no** `BeginTx` in the file — all writes are autocommit `db.ExecContext` (`:275`, `:350`, `:485`). Narrowing confirmed from data: `dex_settings` is a singleton (`internal/db/migrations/137_dex_advisory_lock_connector_lifecycle.up.sql:14,19` hardcodes `id='00000000-0000-0000-0000-000000000001'`), so at the default batch size the loop always breaks on pass one — the hang requires `--batch-size 1`. | ✅ |
| `argocd-repo-encrypt-and-discard` | `internal/handler/argocd.go:3435-3449` (was `:3317-3331`) still holds the "round-trip through the Encryptor so it lands in our request audit (when we add one)" comment and the `if h.encryptor != nil {…}` block that encrypts `req.Password`/`req.SSHPrivateKey`, discards both with `_`, and only Warns on error. Plaintext then goes to `client.CreateRepository(...)` at `:3452`; the audit record `:3456-3464` stores only `has_password`/`has_private_key`. `TestRepo` (now `:3513`) contains no copy. `audit_logs` is still absent from keyrotate's `rewriteTargets`. | ✅ (delete `:3435-3449` inclusive of the comment) |
| `operation-supersede-drift-kills-inflight-ops` | `internal/handler/operation_runner.go:70-78` treats a nil `ShouldSupersede` as always-supersede; `internal/handler/monitoring.go:3333` is the only supplier repo-wide; `tools.go:1291`, `logging.go:1216`, `catalog.go:2124`, `workloads.go:1396` set only `IsFreshRunning`. No status guard on the write: `MarkToolOperationSuperseded` (`internal/db/queries/tool_operations.sql:80-88`) ends `WHERE id = $1`, as do all five siblings and `MarkToolOperationCompleted`/`Failed` (`:60-68`, `:70-78`). | ❌ step 3 — §3.2; steps 1–2 ✅ |
| `tool-cluster-status-n-plus-1` | `internal/handler/tools.go:648-689` `ClusterStatus`: `ListEnabledTools` at `:655`, then a per-tool loop `:677-688` calling `GetLatestToolOperationForTarget` once per tool (in-code justification at `:670-675`). `ListEnabledTools` is unbounded (`internal/db/queries/tools.sql:10-11`, no LIMIT) while the adjacent `ListInstalledChartsByCluster` caps at `Limit: 200` (`:658-662`). The batch query does not exist — `internal/db/queries/tool_operations.sql` has only `GetLatestToolOperationForTarget :one` at `:34`. | ✅ (step 3 target is now `:676-688`; interface entries at `tools.go:51` and `:35`) |
| `dockerignore-omits-large-build-artifacts` | `.dockerignore` is 11 lines (node_modules, .next, out, .env*, .git, .gitignore, *.md with `!content/**/*.md`, coverage). Artifacts present at repo root: `keyrotate` 26M, `worker` 125M, `bin/` 297M, `screenshots/` 73M. `deploy/docker/Dockerfile.server` does `COPY . .`. | ✅ step 1 (entries proposed are exactly the missing ones); step 2's embedded-asset caution (`internal/handler/assets/openapi.yaml`) still applies. **Rationale must be corrected — §3.3** |

*(12 rows above; `git-backed-chart-repos` and `agent-observability-gap` are the two partially-fixed items and are counted once each in the S column.)*

#### Effort M (9)

| id | Current evidence @ `e8f8c34` | Rem. |
|---|---|---|
| `informer-full-object-caches` | `internal/agent/state_subscriber.go:392` builds the typed factory with plain `informers.NewSharedInformerFactory`; no `informers.WithTransform` in the package. `registerCore:484-494` attaches full typed Pods/Services/Nodes/ConfigMaps and, when `s.watchSecrets` (`:492`), full Secrets; `registerApps:499-506` full Deployments/ReplicaSets/StatefulSets/DaemonSets. No `GOMEMLIMIT` in `deploy/agent/install.yaml.template`. Note the new interaction for item 3: `internal/agent/membudget.go:218-229` derives the in-flight response budget from `GOMEMLIMIT` at 1/4, so `GOMEMLIMIT=400MiB` shrinks the budget 128 MiB → 100 MiB (per-request cap stays 64 MiB, so no regression — but pick the number with the divisor in mind). | ❌ item 2 — §3.1 |
| `no-default-role-no-creator-owner` | `grep -rn new_user_default` over non-doc files = 0 hits; the column does not exist. `internal/db/migrations/001_initial.up.sql:688` seeds "Standard User" with no default flag. `internal/handler/sso.go:501-531` `findOrCreateUser` calls `CreateUser` and returns — no `global_role_bindings` insert; `CreateGlobalRoleBinding` appears in none of `users.go`/`scim.go`/`sso.go` (only non-test writers repo-wide: `internal/server/routes_rbac_audit_agents.go`, `internal/handler/rbac.go`, `internal/auth/agent_ingest_token.go`). `(*ProjectHandler).Create` reaches `CreateProject` at `internal/handler/projects.go:600` with no `project_role_bindings` insert. | ✅ (step 2 creator-owner remains optional; a global binding does pass at project scope, so creators are not locked out) |
| `no-drift-exception-mechanism` | `ignoreDifferences`/`IgnoreDifferences` = zero non-test hits across `internal/`, `frontend/src/`, `deploy/`. `ApplicationSpec` (`internal/handler/argocd/applications.go:27-33`) is `{Project, Source, Sources, Destination, SyncPolicy}`. `ClusterBaselineSyncPolicy` (`internal/crd/types.go:577-581`) is `{Automated, Prune, SelfHeal}`. `selfHeal: true` hardcoded at `internal/server/baseline_appsets.go:564` and `internal/server/self_manage_migration.go:188`. Raw-patch workaround still exists: `PatchApplication` forwards the merge patch verbatim (`internal/handler/argocd.go:2727-2765`) and `internal/argosecurity/policy.go:1853` rejects only compare-options values other than `IgnoreExtraneous`. | ⚠️ step 3 — §3.4; steps 1, 2, 4 ✅ |
| `tunnel2-unfinished-migration-live-on-server` | `internal/server/server.go:435` `remoteServer := tunnel2.NewRemoteServer(...)` is unconditional; `internal/server/routes.go:1043-1053` registers `/api/v1/connect/{cluster_id}/` behind only a nil check, with only the `/v2/pods/` demo handler production-gated (`:1060-1065`). No operator switch (`tunnel2` absent from `internal/config/` and `deploy/chart/values.yaml`). `internal/agent2/client.go:82` is still `allow := func(proto, address string) bool { return true }`. `internal/tunnel2/server.go` calls `connectauth.Validate` at `:166` but has no `agentcompat`/skew/timestamp check. Agent half unreachable in production: `cmd/agent/main.go:48-75` registers `connect2` as experimental, `:119` enforces `credential_source=identity`, and `deploy/agent/install.yaml.template:592-594` hardcodes `command: [astronomer-agent, connect]`. ServiceProxy: `Hub.ServiceProxyRequest` (`internal/tunnel/originators.go:213-216`) has zero non-test callers while the handler is registered at `cmd/agent/main.go:188` and `internal/server/localcluster.go:211`. | ✅ (the plan says `:168` for the agent-side `RegisterHandler`; it is `:188`) |
| `argocd-internal-proxy-tls-insecure-skip-verify` | `StartInternalArgoCDProxy` (`internal/server/server.go:2034-2076`) calls `certutil.GenerateSelfSignedCertKey("astronomer-server.astronomer.svc.cluster.local", ...)` at `:2066` on every boot — new key per pod, never persisted or rotated. `managedClusterCredential` (`internal/worker/tasks/argocd_auto_register_cluster.go:615-651`) ends `return token, server, &argocdclient.TLSClientConfig{Insecure: true}, nil` at `:650`, follow-up noted in the comment `:640-649`. `deploy/chart/templates` has only `tls-certificate.yaml`/`tls-issuer.yaml` (public listener); no internal-proxy Certificate. The token gate remains the sole front-door control (`routes.go:1091`, `:1514` → `requireArgoCDClusterProxyToken:1902`). | ⚠️ §3.4 |
| `crd-controller-2919-line-split` | `internal/crd/controller.go` is 2,935 lines. `internal/crd` contains only controller.go, ingest_v2.go, mirror_router.go, trivy_watcher.go, types.go + tests — none of the proposed `reconciler_*.go`/`argoappset.go`/`argorollup.go`. All six reconcilers declared in one file: Cluster `:281`, Project `:478`, ClusterBaseline `:620`, ComponentBundle `:679`, AgentProfile `:719`, GitOpsTarget `:759`, bodies interleaved (`ensureGitOpsTargetApplicationSet:2275` sits far below ClusterBaseline methods at `:958-1051`). | ⚠️ §3.4 (split by symbol) |
| `parse-cluster-id-helper-ignored-87-times` | `internal/handler/image_vulns.go:474-481` defines `parseClusterID`; 12 hits total (definition + 11 call sites). `uuid.Parse(chi.URLParam(r, "cluster_id"))` = 44 hits; literal `"Invalid cluster ID"` = 87; `json.NewDecoder(r.Body).Decode` = 121 (audit said 122). Hand-rolled example: `internal/handler/tools.go:649-653`. No `internal/handler/request_params.go`. | ✅ (one evidence nit — §3.3) |
| `api-hooks-flat-2700-line-modules` | `frontend/src/lib/api.ts` is now **2,802** lines (grew +39 since the audit); `frontend/src/lib/hooks.ts` 2,216 lines with exactly 151 `^export function use`. The blessed target exists and is partial: `frontend/src/lib/api/` holds ~20 feature modules with colocated tests; `scripts/code-health-inventory.mjs:93-97` allows direct fetch/axios in `api.ts` **or** anything under `frontend/src/lib/api/`. No `frontend/src/lib/hooks/` directory at all. | ✅ — case strengthened: the monolith is growing while the sanctioned directory exists, which is exactly what the plan's step 3 gate would stop |
| `per-cluster-observability-ia-gap` | `getClusterNavGroups` (`frontend/src/components/layout/sidebar.tsx:170-245`) lists Overview, Adoption, Nodes, Namespaces, Events, Tools, Apps, Service Mesh + agent-gated Image Scans/Shell/Control-plane DR/Registries/Snapshots/Network & Access — no Monitoring, Alerts, Logging, Notifiers or CIS. `frontend/src/routes/dashboard/clusters/$id/` has no monitoring/alerting/logging/compliance directory. | ✅ — but it is an IA preference, and the plan itself sequences it as the follow-on to `monitoring-stack-lifecycle-no-ui` |

#### Effort L (6)

| id | Current evidence @ `e8f8c34` | Rem. |
|---|---|---|
| `split-brain-across-server-replicas` | `internal/tunnel/server.go:597` in-process supersession via `h.agents.Set` (per-pod sharded map); `:634` calls `loc.Set(ctx, payload.ClusterID)` unconditionally with no locator consultation first; `persistConnect:981` only marks prior DB rows disconnected. `AgentID` is always empty in production: `internal/agent/config.go:24` declares it, `:137` defaults to `""`, and `ASTRONOMER_AGENT_ID`/`agent_id` appear nowhere in `deploy/agent/install.yaml.template` (`a586b5c`'s only env addition there was `ASTRONOMER_AGENT_IMAGE_REPOSITORY`). `payload.AgentID` recorded at `server.go:582`, `:616`, `:1209`. | ✅ items 1, 3, 4 (cheap); item 2 (redis lease) stays deferred |
| `resource-table-copypaste-2855-line-route` | `frontend/src/routes/dashboard/clusters/$id/$resource/index.tsx` is still exactly 2,855 lines with 21 top-level `*Table` components. The abstractions the plan credits are present: `NamespacedActions:2011`, `RouteTable<T>:2114`, `GenericResourceTable:2582`. No post-audit commit touched the frontend routes. | ✅ (finish the RouteTable/GenericResourceTable generalization; do not invent a new abstraction) |
| `newapp-1600-line-constructor` | `internal/server/server.go:313` `func NewApp` runs to `:1943` — 1,631 lines of a 2,338-line file; 86 `handler.New*` calls in the file. `validateProductionSecurityWiring` is still the post-hoc backstop at `:208` (plan cites `:199`). | ⚠️ §3.4 (ranges drifted +9…+40) |
| `argocd-go-3430-line-split` | `internal/handler/argocd.go` is now **3,548** lines (+118). None of the four proposed files exist; `internal/argosecret` does not exist (`internal/argolabels` holds only `labels.go`). The pure-Kubernetes helpers to move are all still in place: `argoCDClusterTokenExpiry:2507`, `jwtExpiry:2520`, `decodeBase64URL:2541`, `clusterSecretNameFromServer:2548`, `mergeAstronomerManagedLabels:2556`, `astronomerManagedLabelsPatch:2570`, `stringMapEqual:2586`, `findArgoCDClusterSecretByServer:2598`. Confirmed: `grep 'jwtExpiry\|argoCDClusterTokenExpiry\|mergeAstronomerManagedLabels' internal/handler/*_test.go` = 0 — those helpers have zero direct unit coverage. | ⚠️ §3.4 |
| `duplicate-alertmanager-renderer` | `internal/handler/alerting.go:1347` `renderAlertmanagerConfig` vs `internal/handler/monitoring.go:2525` `renderSharedAlertmanagerConfig` (whose comment `:2529-2530` still says "Mirrors AlertingHandler.renderAlertmanagerConfig"). Link-loading blocks byte-identical (`alerting.go:1352-1370`, `monitoring.go:2531-2549`). Drift intact: `alerting.go:1384-1388` separate `case "slack":`/`case "webhook":` vs `monitoring.go:2561` collapsed `case "slack", "webhook":`; `alerting.go:1406` calls `h.alertmanagerTiming(ctx)` (`:172`) while `monitoring.go:2586-2592` hardcodes 30s/5m/3h behind a comment admitting the monitoring render "does not currently thread SettingsCache". Both `default: continue` on pagerduty/msteams; both emit `email_configs` with no SMTP globals. `e8f8c34` added +302 lines to monitoring.go but did not touch the renderer. | ✅ (effort listed M in the source triage; sequenced as the enabling refactor, not an independent defect — anchors moved: alerting.go `:1311-1388`→`:1347-1424`, monitoring.go `:2314-2390`→`:2525-2601`) |

---

## 5. Severity changed since the audit

All movement is downward, and every downgrade is justified by an argument the original plan already made in its own corrections — **no post-audit commit reduced any risk in this set.**

| id | Was | Now | Why |
|---|---|---|---|
| `split-brain-across-server-replicas` | medium | low | Deployment is `replicas: 1` / `strategy: Recreate`; reaching the split-brain state requires an operator to scale up or re-run join against a second cluster. `AgentID` is always empty (`internal/agent/config.go:137`), which is the fencing gap, but the trigger is operator-initiated. |
| `informer-full-object-caches` | medium | low | Memory-footprint concern with no demonstrated OOM; and the one high-value sub-item (Node) is now off the table (§3.1). |
| `clusterbaseline-gitopstarget-target-management-cluster` | medium | low | Inconsistent default / footgun, not a defect — the selector does exactly what the operator wrote; only the platform baseline's own exclusion (`internal/server/baseline_appsets.go:485`) makes the inconsistency visible. |
| `drain-force-field-never-reaches-ui` | medium | low | A missing affordance plus one dropped-options bug; no security or correctness impact. |
| `permission-decision-unmemoized-defeats-every-usememo` | medium | low | Wasted re-render work; behavior is correct. |
| `resource-table-copypaste-2855-line-route` | medium | low | Pure code health; no defect. |
| `newapp-1600-line-constructor` | medium | low | Pure structure; no demonstrated defect. `validateProductionSecurityWiring` (`internal/server/server.go:208`) remains the compensating backstop. |
| `no-scheduled-cis-scans` | medium | medium (read as medium-**low**) | Recorded medium for continuity with the plan, but it is a pure feature gap with nothing broken and it is blocked behind `cis-ingest-…`. Sequence it last within its batch. |

Two findings had their *effort* or *scope* revised rather than severity:

| id | Change |
|---|---|
| `ui-proxy-permission-mapping-ignores-argocd-verbs` | Effort **S → M**. The `faf062b` global-scope redesign makes "what scope do project-scoped `argocd` grants evaluate against" a prerequisite rather than a nice-to-have. Impact narrows (fleet-wide sync now needs a *global* `clusters:update`) while the GitOps Admin 403 gets worse. |
| `git-backed-chart-repos-accepted-never-synced` | Disposition **still-valid → partially-fixed**; the Reject-vs-Implement decision is now settled in favor of Reject by the in-code comment at `internal/worker/tasks/catalog_sync.go:105-116`. |

---

## 6. Suggested batching

This repo's pattern is one reviewable commit per subsystem. Each batch below is one commit (or one short chain), ordered by dependency inside the batch. Cross-batch prerequisites are called out.

| # | Batch | Findings (in order) | Notes |
|---|---|---|---|
| 1 | **Alertmanager rendering** | `duplicate-alertmanager-renderer` → `alertmanager-drops-pagerduty-msteams` → `alertmanager-email-missing-smtp-globals` → `alertmanager-routing-configmap-inert` | The dedupe is the enabling refactor; doing the other three first means fixing everything twice. Land the shared `RenderReceiver` + a guard test over `notification_dispatch.go:61-67` `SupportedNotificationChannels`, then the two content fixes, then the Secret-based config (mirroring `alerting.go:1248` ↔ `monitoring.go:2277-2281`). Skip the `prometheus/alertmanager` dependency (§3.4). |
| 2 | **CIS scan lifecycle** | `cis-ingest-in-process-goroutine-loses-scans` → `no-scheduled-cis-scans` | Step 2 of the first (extract `startScan`) is a hard prerequisite for the second. Do not start the second in the same commit. |
| 3 | **Agent runtime hardening** | `agent-no-read-idle-deadline` → `agent-observability-gap` (steps 2–3 only) → `informer-full-object-caches` (ConfigMap/Secret + `WithTransform`, Node untouched) | All three land in `internal/agent`. The idle deadline and the `/readyz` `DegradedReasons` work share `health.go`. Read §3.1 before touching either of the last two. |
| 4 | **Tunnel frame handling** | `exec-and-log-binary-corruption` → `namespace-filtered-watch-local-path-filters-raw-chunks` | Both are "the local/agent path mangles bytes the cross-pod path handles correctly"; both have an in-tree correct reference (`k8sproxy.go` base64, `proxy.go:663-671` scanner). |
| 5 | **Stream-endpoint authorization & limits** | `kubectl-shell-ws-reopen-skips-rbac-when-scope-flag-off` → `tunnel-rate-limits-keyed-on-spoofable-ip-and-run-before-auth` | Both edit `internal/server/routes.go` chains around exec/logs/shell. The first is now unblocked by `37dcf42` (`rbac.ResourcePods`/`rbac.VerbExec` exist). |
| 6 | **ArgoCD delivery scoping** | `appproject-scoping-absent` → `no-drift-exception-mechanism` → `clusterbaseline-gitopstarget-target-management-cluster` → `leave-local-ownership-exclusion-fails-open` | All in `internal/server/baseline_appsets.go` + `internal/crd/controller.go`. The AppProject work reshapes the same generated-Application construction that drift exceptions and the selector defaults touch. Read §3.4 for the two self-manage identity assertions and the `faf062b` selfHeal comment. |
| 7 | **ArgoCD proxy authorization** | `ui-proxy-permission-mapping-ignores-argocd-verbs` | Stands alone: `internal/server/middleware/argocd_authz.go` only, and it must not be batched with anything that would obscure a regression of `faf062b`. Decide the project-scope question first (§3.2). |
| 8 | **Cluster TLS identity** | `cluster-ca-never-persisted` → `argocd-internal-proxy-tls-insecure-skip-verify` | The first makes the second's conditional-pinning shape (`argocd_auto_register_cluster.go:624-626`) actually functional. Doing the second alone still leaves managed clusters on `Insecure: true`. |
| 9 | **Decommission lifecycle** | `decommission-wedges-baseline-applications` → `no-decommission-signal-to-offline-agent` → `split-brain-across-server-replicas` (items 1, 3, 4) | All three are "what happens to an agent/cluster we are removing or replacing". The `ConnectAckPayload{Accepted:false}` work in the second is also the natural place to stop closing bare at `server.go:521`. |
| 10 | **Secret handling & keyrotate** | `gitops-decrypt-silent-ciphertext-fallback` → `argocd-repo-encrypt-and-discard` → `keyrotate-dex-cutover-offset-not-advanced` | All three are "ciphertext that goes somewhere wrong". The first two both concern credentials that end up in unrotated stores (`audit_logs` is absent from `rewriteTargets`). |
| 11 | **API contract gates** | `api-ts-shadow-generated-types` → `no-request-schema-field-gate` → `drain-force-field-never-reaches-ui` → `golangci-lint-never-runs-in-ci` | Order matters: fix the shadow types, then add the field-level gate, then let the gate catch `force` as its first real finding (proving it works), then wire golangci-lint into `scripts/verify-enterprise.sh`. |
| 12 | **Identity & token policy** | `api-token-no-max-ttl` → `no-default-role-no-creator-owner` → `group-membership-only-refreshed-at-login` | Ascending effort, all in the auth/RBAC provisioning path. The group-refresh task can reuse whatever cache/JWT invalidation the max-TTL work introduces. |
| 13 | **Catalog & release UX** | `git-backed-chart-repos-accepted-never-synced` → `no-release-history-or-revision-rollback-ui` → `loki-log-query-not-surfaced` | Backend for the middle item already exists and already accepts an arbitrary revision (`catalog.go:1426-1443`) — this batch is mostly frontend. |
| 14 | **Handler ergonomics & N+1** | `parse-cluster-id-helper-ignored-87-times` → `tool-cluster-status-n-plus-1` → `operation-supersede-drift-kills-inflight-ops` (steps 1–2) | Three small, independently reviewable `internal/handler` + `internal/db/queries` changes with the same shape: use the thing that already exists. |
| 15 | **Frontend structure** | `permission-decision-unmemoized-defeats-every-usememo` → `api-hooks-flat-2700-line-modules` → `resource-table-copypaste-2855-line-route` → `per-cluster-observability-ia-gap` | The IA item is last because it wants `-page.tsx` extraction, which is easier after the api/hooks split lands. |
| 16 | **Backend structure** | `argocd-go-3430-line-split` → `crd-controller-2919-line-split` → `newapp-1600-line-constructor` | Pure mechanical splits, no behavior change. Re-derive every seam by symbol (§3.4). Do these when no other batch has open work in those files. |
| 17 | **Standalone / no natural batch** | `project-quota-no-aggregate-accounting` (decide the two-quota-object question first), `tunnel2-unfinished-migration-live-on-server`, `dockerignore-omits-large-build-artifacts` | Each touches a subsystem nothing else in this backlog touches. `dockerignore` is a 10-line change and can go any time. |

**Highest-value first three batches**, if capacity is limited: 1 (Alertmanager — four medium findings, one refactor), 11 (contract gates — the gate hole is what let `force` ship undetected and will let the next one ship too), 5 (stream-endpoint authz — the only medium in this set with an unauthenticated-chain component).

---

## 7. Appendix: coverage and limits

### Coverage

| Source section | Findings covered |
|---|---|
| §6.1, §6.2 (core & features) | 9 of 9 — every `### [<id>]` heading |
| §6.3–§6.7 (agent, transport, crypto/TLS, ArgoCD, code health & gates) | 21 of 21 |
| §7 (P3) | 19 of 19, split 10 + 11 across two reviewers with a deliberate one-finding overlap at the seam |
| **Total** | **49 unique** (51 verification records) |

Every disposition above comes from reading the current source, migrations, `.sql` query files, `docs/openapi.yaml`, chart templates, `deploy/agent/install.yaml.template` and the frontend routes at `e8f8c34`. Findings were located **by symbol**, not by the line numbers the plan quotes — most have drifted, several by more than 100 lines. Where a claim depended on data rather than code (seeded roles, singleton rows, shipped env vars), the migration or template was read directly: `001_initial.up.sql:688` ("Standard User"), `098_rancher_grade_role_catalog.up.sql:15,48,52` (GitOps roles), `137_dex_advisory_lock_connector_lifecycle.up.sql:14,19` (dex singleton), `deploy/agent/install.yaml.template:592-594` (`connect`, not `connect2`) and `git show a586b5c -- deploy/agent/install.yaml.template` (no `ASTRONOMER_AGENT_ID`).

### Not examined

- **Nothing was executed.** No build, no `go test`, no `make verify`, no jest, no frontend code-health gate, no runtime exercise of any path. Every claim of the form "this test would fail today" or "this gate would report X" is inferred from reading the test/gate source.
- `golangci-lint` is **not installed** in this environment, so the current lint state of the tree is unknown — that absence is itself part of `golangci-lint-never-runs-in-ci`'s evidence, but it means no claim is made about how many violations exist.
- **P0/P1 findings (§1–§5 of the plan) were not re-verified here.** This pass covers only the P2/P3 tail. The nine post-audit commits closed P0/P1 items; their dispositions live in the original plan and in the docs commits `4b37848` and `e8ef831`.
- **Structural findings were verified as unsplit, not as correctly-seamed.** For `argocd-go-3430-line-split`, `crd-controller-2919-line-split`, `newapp-1600-line-constructor` and `api-hooks-flat-2700-line-modules`, the file sizes, symbol locations and absence of the proposed target files were confirmed; no judgment is offered on whether the plan's proposed seams are the right ones beyond the observation that every quoted line range is stale.
- **Chart-version-dependent claims were not resolved against the pinned chart.** `alertmanager-routing-configmap-inert`'s remediation needs `configFromSecret` vs `existingSecret`, which varies by upstream chart version; that lookup is left to the implementer.
- **No fix was implemented and no file other than this one was created or modified.** No mutating git command was run.

### One internal inconsistency in the inputs

The two independent verifications of `keyrotate-dex-cutover-offset-not-advanced` disagree on line anchors by 1–3 lines throughout: the dry-run guard is reported as `cmd/keyrotate/main.go:496-498` by one reviewer and `:492-494` by the other; the `--batch-size` help text as `:62` vs `:59`; the SELECT predicate as `:442` vs `:441`; the unmarshal/encrypt failure branches as `:466-469`/`:476-478` vs `:466-468`/`:475-477`. Both agree on every substantive claim (the guard is `if dryRun`, the only exit is the short-batch break, all four failure branches `continue` without advancing, there is no `BeginTx`, `dex_settings` is a singleton). The second reviewer's anchors were used above; **re-derive by symbol (`cutoverDexPublicClients`) before editing.** No other cross-section disagreement was found; the duplicate verification of `gitops-decrypt-silent-ciphertext-fallback` agrees exactly.
