{
  "version": 1,
  "description": "Classifies direct Redis enqueue call sites that are not statically proven task-outbox backed by scripts/operation-task-inventory.mjs.",
  "classifications": [
    {
      "file": "internal/worker/job_metrics.go",
      "function": "EnqueueWithCorrelation",
      "classification": "wrapper-only",
      "owner": "backend/worker",
      "reason": "This is a shared instrumentation wrapper around asynq.Client.Enqueue, not an independent product action producer.",
      "validation": "Callers remain responsible for their own durable intent or best-effort classification."
    },
    {
      "file": "internal/worker/tasks/notification_dispatch.go",
      "function": "HandleNotificationSend",
      "classification": "outbox-backed",
      "owner": "backend/notifications",
      "reason": "Email delivery is handed off from the durable notification task into the PostgreSQL email_messages queue with a stable per-recipient dedupe key before the notification task succeeds.",
      "validation": "The notification intent originates in task_outbox; email.Enqueuer persists email_messages idempotently, and a persistence failure returns an error so asynq retries the durable notification task."
    },
    {
      "file": "internal/worker/tasks/cluster_template_apply.go",
      "function": "HandleClusterTemplateDriftCheck",
      "classification": "repair-backed",
      "owner": "backend/templates",
      "reason": "This direct enqueue happens inside the recovery sweep for failed template applications. If delivery fails, the durable failed row remains eligible for the next sweep after backoff.",
      "validation": "cluster_template:drift_check is scheduled on the tunnel queue and guards repeated failures with failedApplyMinBackoff."
    },
    {
      "file": "internal/worker/tasks/gitops_sync.go",
      "function": "enqueueDecommission",
      "classification": "repair-backed",
      "owner": "backend/gitops",
      "reason": "The path creates a durable cluster_decommissions operation before dispatch and prefers a deduplicated task_outbox row. If both outbox and direct acceleration are unavailable, the pending operation remains eligible for repair.",
      "validation": "cluster:decommission_all is scheduled every minute on the tunnel queue, re-enqueues pending rows, and the reconciler uses a lease claim plus persisted idempotent phase state; TestGitOpsDecommissionWritesTaskOutboxBeforeDirectEnqueue verifies the normal outbox contract."
    }
  ]
}
