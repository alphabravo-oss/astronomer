{
  "version": 1,
  "rules": [
    {
      "id": "auth-session-public-baseline",
      "methods": [
        "POST"
      ],
      "patterns": [
        "/api/v1/auth/login",
        "/api/v1/auth/logout",
        "/api/v1/auth/refresh"
      ],
      "classification": "covered-auth-flow",
      "reason": "Core auth session endpoints are public or token/cookie-gated by design and have dedicated auth/rate-limit test coverage outside the high-risk route registry."
    },
    {
      "id": "apiserver-audit-ingest",
      "methods": [
        "POST"
      ],
      "patterns": [
        "/api/v1/clusters/{cluster_id}/apiserver-audit"
      ],
      "classification": "covered-rbac-cluster-scoped",
      "reason": "The per-cluster agent POSTs batched audit.k8s.io events to this ingest endpoint, gated on the dedicated audit_ingest:create RBAC permission and idempotent on (cluster_id, auditID); it carries no high-risk blast radius beyond the owning cluster."
    },
    {
      "id": "scim-token-admin",
      "methods": [
        "POST",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/admin/scim-tokens",
        "/api/v1/admin/scim-tokens/{id}"
      ],
      "classification": "covered-superuser-gated",
      "reason": "Mint/revoke the static bearer tokens the /scim/v2 provisioning chain authenticates against; the routes sit inside the JWT auth chain and are superuser-gated inside the handler (RequireSuperuser), the same posture as /admin/settings. The plaintext token is returned once on create and only its SHA-256 hash is persisted, with create/revoke audited via RecordAuditFromRequest."
    },
    {
      "id": "cluster-agent-token-lifecycle",
      "methods": [
        "POST"
      ],
      "patterns": [
        "/api/v1/clusters/{id}/agent-token/rotate",
        "/api/v1/clusters/{id}/agent-token/revoke",
        "/api/v1/cluster-agents/{cluster_id}/self-test",
        "/api/v1/cluster-agents/{cluster_id}/upgrade",
        "/api/v1/cluster-agents/{cluster_id}/upgrade-plan"
      ],
      "classification": "covered-rbac-cluster-scoped",
      "reason": "Cluster-agent mutations are authenticated and cluster-scoped. Token rotation and revocation require clusters:update plus the write-clusters scope; upgrade planning, upgrade execution, and self-test require their dedicated cluster-agent RBAC actions. Every operation is limited to the named cluster and records an audit event."
    },
    {
      "id": "monitoring-settings-unauthenticated-read-fix",
      "methods": [
        "GET",
        "POST"
      ],
      "patterns": [
        "/api/v1/settings/monitoring/backend",
        "/api/v1/settings/monitoring/thanos/preview",
        "/api/v1/settings/monitoring/alertmanager/preview",
        "/api/v1/settings/monitoring/grafana/preview"
      ],
      "classification": "covered-rbac-global-monitoring",
      "reason": "The three routes that answered an unauthenticated caller before the 2026-07-28 fix: the backend read returned the decoded authConfig (operator-supplied backend auth material) and the two previews rendered the shared Thanos/Alertmanager helm values. They now sit behind requireAuth at the router and monitoring:read inside the handler, and each is additionally declared in docs/security-sensitive-routes.json so TestHighRiskRoutesDenyUnauthenticatedRequests asserts the 401."
    },
    {
      "id": "monitoring-shared-stack-lifecycle",
      "methods": [
        "POST",
        "PUT",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/settings/monitoring/backend",
        "/api/v1/settings/monitoring/operations/{id}/retry",
        "/api/v1/settings/monitoring/thanos/install",
        "/api/v1/settings/monitoring/thanos/upgrade",
        "/api/v1/settings/monitoring/thanos/replace",
        "/api/v1/settings/monitoring/thanos/uninstall",
        "/api/v1/settings/monitoring/alertmanager/install",
        "/api/v1/settings/monitoring/alertmanager/upgrade",
        "/api/v1/settings/monitoring/alertmanager/replace",
        "/api/v1/settings/monitoring/alertmanager/uninstall",
        "/api/v1/settings/monitoring/grafana/install",
        "/api/v1/settings/monitoring/grafana/upgrade",
        "/api/v1/settings/monitoring/grafana/replace",
        "/api/v1/settings/monitoring/grafana/uninstall"
      ],
      "classification": "covered-rbac-global-monitoring",
      "reason": "Shared Thanos/Alertmanager lifecycle against the management cluster, plus the default-backend upsert and the operation retry. All are gated on monitoring:update inside the handler (the retry additionally re-checks the operation's own target), the helm verbs carry the write-clusters scope backstop, and each enqueues an audited monitoring operation (monitoring.shared_thanos.* / monitoring.shared_alertmanager.* / monitoring.endpoint.update / monitoring.operation.retry)."
    },
    {
      "id": "monitoring-cluster-stack-lifecycle",
      "methods": [
        "POST",
        "PUT",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/clusters/{id}/monitoring/config",
        "/api/v1/clusters/{id}/monitoring/stack/preview",
        "/api/v1/clusters/{id}/monitoring/stack/install",
        "/api/v1/clusters/{id}/monitoring/stack/upgrade",
        "/api/v1/clusters/{id}/monitoring/stack/replace",
        "/api/v1/clusters/{id}/monitoring/stack/uninstall"
      ],
      "classification": "covered-rbac-cluster-scoped",
      "reason": "Per-cluster monitoring stack config and helm lifecycle. Each route carries requirePermission(monitoring, read/create/update/delete) middleware inside the authenticated /clusters group, blast radius is the owning cluster's monitoring release, and the mutations are audited (monitoring.stack.*)."
    },
    {
      "id": "monitoring-endpoint-and-metrics-queries",
      "methods": [
        "POST",
        "PUT",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/monitoring/endpoints",
        "/api/v1/monitoring/endpoints/{id}",
        "/api/v1/monitoring/metrics/query/{cluster_id}",
        "/api/v1/clusters/{cluster_id}/metrics"
      ],
      "classification": "covered-rbac-global-monitoring",
      "reason": "Monitoring backend endpoint CRUD and the two POST-shaped metric query aliases. All carry requirePermission(monitoring, create/update/delete for CRUD, read for the queries) inside the authenticated router; the query routes are POST only because the PromQL selector travels in the body, and they mutate nothing."
    },
    {
      "id": "alerting-surface-rbac",
      "methods": [
        "POST",
        "PUT",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/alerting/channels",
        "/api/v1/alerting/channels/{id}",
        "/api/v1/alerting/channels/{id}/test",
        "/api/v1/alerting/rules",
        "/api/v1/alerting/rules/{id}",
        "/api/v1/alerting/rules/{id}/enable",
        "/api/v1/alerting/rules/{id}/disable",
        "/api/v1/alerting/events/{id}/acknowledge",
        "/api/v1/alerting/events/{id}/resolve",
        "/api/v1/alerting/silences",
        "/api/v1/alerting/silences/{id}",
        "/api/v1/alerting/silences/{id}/expire",
        "/api/v1/alerts/rules/{id}/enable",
        "/api/v1/alerts/rules/{id}/disable",
        "/api/v1/alerts/silences/{id}/expire",
        "/api/v1/admin/alerting/inhibitions",
        "/api/v1/admin/alerting/inhibitions/{id}"
      ],
      "classification": "covered-rbac-global-alerts",
      "reason": "Every alerting mutator carries an explicit per-verb requirePermission on rbac.ResourceAlerts at the router (create on POSTs, update on state changes and tests, delete on DELETEs), inside the authenticated subrouter; the /alerts/* paths are frontend aliases bound to the same handlers and middleware, and the /admin/alerting/inhibitions writers additionally require the admin API-token scope."
    },
    {
      "id": "charlie-alert-policy-admin",
      "methods": [
        "PUT"
      ],
      "patterns": [
        "/api/v1/admin/charlie/alert-policy"
      ],
      "classification": "covered-rbac-charlie-admin",
      "reason": "The product-owned Charlie notification policy is feature-gated and requires authenticated admin scope plus charlie:manage at the router. Browser mutations inherit CSRF protection, the handler enforces optimistic revisions, and the mutation is recorded through the bounded Charlie audit contract without exposing recipients, channel configuration, or credentials."
    },
    {
      "id": "charlie-kubernetes-visibility-admin",
      "methods": [
        "PUT"
      ],
      "patterns": [
        "/api/v1/admin/charlie/kubernetes-visibility"
      ],
      "classification": "covered-rbac-charlie-admin-fail-closed",
      "reason": "The product-owned Kubernetes visibility policy is feature-gated and requires authenticated admin scope plus charlie:manage at the router. Browser mutations inherit CSRF protection; the handler enforces optimistic revisions; every scope change closes Charlie authority to disabled, clears capability acknowledgement, drains active work, and emits a bounded audit event before rediscovery can be reviewed and acknowledged on both sides."
    },
    {
      "id": "control-plane-controllers-superuser",
      "methods": [
        "POST",
        "PUT",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/controllers/policy",
        "/api/v1/controllers/alerts/{id}/acknowledge",
        "/api/v1/controllers/silences",
        "/api/v1/controllers/silences/{id}"
      ],
      "classification": "covered-superuser-gated",
      "reason": "Fleet-wide controller policy, alert acknowledgement and alertmanager silences are platform-admin actions; every mutator is wrapped in requireSuperuser (handler.RequireSuperuser) at the router while the paired GETs require alerts:read/list (SEC-05)."
    },
    {
      "id": "platform-admin-scope-gated-crud",
      "methods": [
        "POST",
        "PUT",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/admin/settings/{key}",
        "/api/v1/admin/dashboard-widgets",
        "/api/v1/admin/dashboard-widgets/{id}",
        "/api/v1/admin/prometheus-datasources",
        "/api/v1/admin/prometheus-datasources/{id}",
        "/api/v1/admin/prometheus-datasources/{id}/test",
        "/api/v1/admin/quota-plans",
        "/api/v1/admin/quota-plans/{name}",
        "/api/v1/admin/gitops-sources",
        "/api/v1/admin/gitops-sources/{id}",
        "/api/v1/admin/gitops-sources/{id}/sync"
      ],
      "classification": "covered-superuser-gated",
      "reason": "Platform-admin registries (global settings, dashboard widgets, Prometheus datasources, quota plans, GitOps sources). Each mutator is registered with requireAuth + requireScope(ScopeAdmin) so a read- or write-clusters-scoped API token is rejected at the router, and the handlers gate on superuser themselves so non-admin sessions get a clean 403."
    },
    {
      "id": "security-surface-rbac",
      "methods": [
        "POST",
        "PUT",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/security/templates",
        "/api/v1/security/templates/{id}",
        "/api/v1/security/policies",
        "/api/v1/security/policies/{id}",
        "/api/v1/security/policies/{id}/apply",
        "/api/v1/security/scans"
      ],
      "classification": "covered-rbac-global-security",
      "reason": "Template/policy/scan mutators carry per-verb requirePermission on rbac.ResourceSecurity (create/update/delete). ApplyPolicy and CreateScan push configuration to a managed cluster through the tunnel and additionally require the write-clusters API-token scope."
    },
    {
      "id": "gatekeeper-constraint-authoring",
      "methods": [
        "POST",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/clusters/{id}/gatekeeper/constraints",
        "/api/v1/clusters/{id}/gatekeeper/constraints/{name}",
        "/api/v1/clusters/{id}/gatekeeper/constraints/validate"
      ],
      "classification": "covered-rbac-cluster-scoped",
      "reason": "Server-side apply and delete of custom ConstraintTemplates/Constraints require the write-clusters scope AND clusters:update — the same authority as editing the cluster. The validate POST is a dry-run that only needs clusters:read."
    },
    {
      "id": "native-rbac-rule-authoring",
      "methods": [
        "POST",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/native-rbac-rules",
        "/api/v1/native-rbac-rules/{id}"
      ],
      "classification": "covered-rbac-global-rbac",
      "reason": "Authoring native per-CRD rules is an RBAC-management action, so it carries the write-rbac API-token scope plus requirePermission on rbac.ResourceRBAC (create on POST, delete on DELETE) — the same gate as role and binding CRUD."
    },
    {
      "id": "extensions-install-and-bridge",
      "methods": [
        "POST"
      ],
      "patterns": [
        "/api/v1/extensions",
        "/api/v1/extensions/validate",
        "/api/v1/extensions/verify-bundle",
        "/api/v1/extensions/{name}/enable",
        "/api/v1/extensions/{name}/disable",
        "/api/v1/extensions/{name}/data/{dataSourceId}",
        "/api/v1/extensions/{name}/token"
      ],
      "classification": "covered-rbac-global-settings",
      "reason": "Install/enable/disable/verify-bundle require ScopeAdmin plus settings:update. The tenant-facing data proxy and bridge-token mint are deliberately only settings:read at the router: the real gate is inside ProxyData/IssueTicket, which re-checks the CALLING user's own RBAC against the manifest's declared dataSource, so an extension can never exceed its user."
    },
    {
      "id": "logging-pipeline-cluster-scoped",
      "methods": [
        "POST",
        "PUT",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/logging/outputs",
        "/api/v1/logging/outputs/{id}",
        "/api/v1/logging/outputs/{id}/enable",
        "/api/v1/logging/outputs/{id}/disable",
        "/api/v1/logging/outputs/{id}/test",
        "/api/v1/logging/outputs/{id}/query",
        "/api/v1/logging/pipelines",
        "/api/v1/logging/pipelines/{id}",
        "/api/v1/logging/pipelines/{id}/enable",
        "/api/v1/logging/pipelines/{id}/disable",
        "/api/v1/logging/operations/{id}/retry"
      ],
      "classification": "covered-rbac-cluster-scoped",
      "reason": "Logging output and pipeline mutators authorize inside the handler: each resolves the owning cluster from the row and calls authorizeClusterAction with rbac.ResourceLogging (create/update/delete), so the grant is scoped to the cluster whose log shipping is being changed."
    },
    {
      "id": "tools-catalog-cluster-scoped",
      "methods": [
        "POST",
        "PUT",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/tools/{slug}/preview",
        "/api/v1/tools/{slug}/install",
        "/api/v1/tools/{slug}/upgrade",
        "/api/v1/tools/{slug}/uninstall",
        "/api/v1/tools/{slug}/adopt",
        "/api/v1/tools/operations/{id}/retry"
      ],
      "classification": "covered-rbac-cluster-scoped",
      "reason": "Tool install/upgrade/uninstall/adopt run helm against a target cluster and authorize inside the handler with authorizeClusterAction on rbac.ResourceCatalog (create for install/adopt, update for upgrade/preview, delete for uninstall) against the cluster named in the request body."
    },
    {
      "id": "chart-rating-owner-scoped",
      "methods": [
        "POST",
        "PUT",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/charts/{chart_id}/ratings",
        "/api/v1/charts/{chart_id}/ratings/{rating_id}"
      ],
      "classification": "covered-owner-scoped",
      "reason": "Per-chart ratings are user-owned content: create binds the row to the authenticated caller, and update/delete refuse unless the caller owns the rating or is a superuser. Blast radius is one review row, not platform state."
    },
    {
      "id": "image-vuln-rescan-cluster-scoped",
      "methods": [
        "POST"
      ],
      "patterns": [
        "/api/v1/clusters/{cluster_id}/vulnerabilities/rescan"
      ],
      "classification": "covered-rbac-cluster-scoped",
      "reason": "Requeues the Trivy scan job for one cluster behind requirePermission(clusters:read) — the same gate as the rest of that cluster's vulnerability surface. It creates no cluster state and its blast radius is a scan re-run on the owning cluster."
    },
    {
      "id": "stream-ticket-mint",
      "methods": [
        "POST"
      ],
      "patterns": [
        "/api/v1/streams/tickets"
      ],
      "classification": "covered-rbac-cluster-scoped",
      "reason": "Mints a short-lived single-use websocket ticket. requireAuth at the router; the handler binds the ticket to the CALLER's own user id and re-checks that caller's RBAC on the requested cluster for the stream kind (read for events/logs/registration, clusters:update for exec/shell), so a ticket can never exceed the permissions of the principal that requested it."
    },
    {
      "id": "scim-provisioning-bearer-token",
      "methods": [
        "POST",
        "PUT",
        "PATCH",
        "DELETE"
      ],
      "patterns": [
        "/scim/v2/Users",
        "/scim/v2/Users/{id}",
        "/scim/v2/Groups",
        "/scim/v2/Groups/{id}"
      ],
      "classification": "covered-token-gated",
      "reason": "The SCIM 2.0 provisioning surface is mounted outside the JWT chain because IdPs cannot present a session JWT. Every route sits under deps.SCIM.Auth, which validates the static provisioning bearer token minted by the superuser-gated /admin/scim-tokens surface (see scim-token-admin) and compares its SHA-256 hash in constant time."
    },
    {
      "id": "gitops-push-webhook-shared-secret",
      "methods": [
        "POST"
      ],
      "patterns": [
        "/api/v1/gitops/sources/{id}/webhook"
      ],
      "classification": "covered-token-gated",
      "reason": "A git provider cannot present a JWT, so this push endpoint is deliberately outside the auth chain: the handler compares the X-Astronomer-Webhook-Secret (or bearer) against the configured shared secret in constant time and 503s when no secret is configured. It triggers a sync of the one source named in the path and nothing else."
    },
    {
      "id": "flux-native-delivery-project-mutations",
      "methods": [
        "POST",
        "PATCH",
        "DELETE"
      ],
      "patterns": [
        "/api/v1/delivery/sources",
        "/api/v1/delivery/sources/{id}",
        "/api/v1/delivery/sources/{id}/rotate-credential",
        "/api/v1/delivery/sources/{id}/verify",
        "/api/v1/delivery/bundles",
        "/api/v1/delivery/bundles/{id}",
        "/api/v1/delivery/bundles/{id}/versions",
        "/api/v1/delivery/targets",
        "/api/v1/delivery/targets/{id}",
        "/api/v1/delivery/targets/{id}/preview",
        "/api/v1/delivery/targets/{id}/rollouts",
        "/api/v1/delivery/rollouts/{id}/pause",
        "/api/v1/delivery/rollouts/{id}/resume",
        "/api/v1/delivery/rollouts/{id}/abort",
        "/api/v1/delivery/rollouts/{id}/retry",
        "/api/v1/delivery/deployments/{id}/reconcile",
        "/api/v1/delivery/deployments/{id}/suspend",
        "/api/v1/delivery/deployments/{id}/resume"
      ],
      "classification": "covered-rbac-project-scoped",
      "reason": "Flux-native delivery mutations are authenticated, require the projects:write API-token scope, and authorize a dedicated delivery resource and verb against one normalized project. Middleware rejects conflicting query, header, and body scopes before RBAC or persistence; target, rollout, and deployment updates additionally use strong If-Match compare-and-swap tokens. Replay-safe operations run behind authorization and credential plaintext is accepted only by the write-only encrypted source boundary."
    },
    {
      "id": "flux-native-delivery-critical-decisions",
      "methods": [
        "POST"
      ],
      "patterns": [
        "/api/v1/delivery/rollouts/{id}/approve",
        "/api/v1/delivery/rollouts/{id}/rollback",
        "/api/v1/delivery/targets/{id}/orphan"
      ],
      "classification": "covered-rbac-high-risk",
      "reason": "Approval, rollback, and orphan decisions have distinct delivery_approvals:approve, delivery_rollbacks:rollback, and delivery_orphans:orphan grants instead of inheriting ordinary rollout or target update. Every request is project-normalized, authenticated, audited, idempotency-protected, and bound to an exact strong If-Match generation; approvals also bind an exact frozen digest and bounded expiry."
    }
  ]
}
