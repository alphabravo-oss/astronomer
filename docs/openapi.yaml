openapi: 3.1.0
info:
  title: Astronomer Management API
  version: '1.0'
  summary: Multi-cluster Kubernetes management plane
  description: |
    REST API surface for the Astronomer management plane. Covers the
    cluster onboarding wizard, agent registration, image vulnerability
    scans, kubectl shell sessions, helm catalog, and platform admin.

    Auth: all endpoints under `/api/v1/` outside `/auth/` and the
    `/internal/` namespace require either a session JWT issued by
    POST `/auth/login/` or a personal API token. Pass the token as
    `Authorization: Bearer <token>`.

    RBAC: write endpoints check the caller's permissions against the
    platform RBAC engine. Routes annotated `superuser` require
    `users.is_superuser = true`; the rest gate on resource verbs
    (clusters:update, clusters:read, etc.).
  contact:
    name: Astronomer engineering
    url: https://github.com/alphabravocompany/astronomer-go
servers:
  - url: http://astronomer.5.78.101.247.nip.io:8080
    description: Live dev (.247)
  - url: http://localhost:8080
    description: Local
x-astronomer-durable-polling-headers: &durable-polling-headers
  Location: {description: Exact durable operation status resource, schema: {type: string, format: uri-reference}}
  Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
components:
  securitySchemes:
    bearerJWT:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        JWT issued by `POST /api/v1/auth/login/` (session token) or
        personal API token from `/api/v1/settings/tokens/`.
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Alias of bearerJWT; session JWT or personal API token.
    gitOpsWebhookSecret:
      type: apiKey
      in: header
      name: X-Astronomer-Webhook-Secret
      description: Shared secret configured for external Git provider push hooks.
  parameters:
    Limit:
      name: limit
      in: query
      required: false
      description: Bounded page size; the server clamps ordinary lists to at most 200.
      schema: {type: integer, minimum: 1, maximum: 200, default: 20}
    Offset:
      name: offset
      in: query
      required: false
      description: Zero-based offset into the authorized, filtered result.
      schema: {type: integer, minimum: 0, default: 0}
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      description: Stable caller key used to replay a committed mutation without duplicating durable intent.
      schema: {type: string, minLength: 1, maxLength: 128}
    RequiredIdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      description: Required stable caller key used to replay a committed durable mutation without duplicating intent.
      schema: {type: string, minLength: 1, maxLength: 128}
    MonitoringManagementClusterID:
      name: clusterId
      in: query
      required: false
      description: Management-cluster target; used when managementClusterId is omitted from the request body.
      schema: {type: string, format: uuid}
  schemas:
    CharlieOnboardingRequest:
      type: object
      additionalProperties: false
      description: |
        Prefer Charlie endpoint plus a one-time connect token issued by Charlie
        Admin. The legacy package fields remain for air-gapped file import.
      properties:
        endpoint:
          type: string
          format: uri
          writeOnly: true
          description: Charlie central HTTPS origin, matching the issued connect token.
        connect_token:
          type: string
          writeOnly: true
          description: One-time charlie.connect.v1 token issued by Charlie Admin.
        package:
          type: object
          writeOnly: true
          description: Signed charlie.onboarding/v1 package; may contain one-time credentials.
        signing_public_key:
          type: string
          writeOnly: true
          description: Base64url Ed25519 public key confirmed out of band by the operator.
        confirmed_signing_key_id:
          type: string
        confirmed_signing_fingerprint:
          type: string
          pattern: '^[a-f0-9]{64}$'
        expected_deployment_id:
          type: string
        expected_route_id:
          type: string
    CharlieOnboardingStatus:
      type: object
      additionalProperties: false
      required:
        - package_id
        - product_id
        - product_slug
        - deployment_id
        - logical_agent_id
        - integration_id
        - mcp_url
        - route_id
        - allowed_route_ids
        - schema
        - central_api_version
        - central_trust_fingerprint
        - signing_key_id
        - signing_fingerprint
        - package_digest
        - artifact
        - replica_count
        - issued_at
        - expires_at
        - state
        - idempotent
      properties:
        package_id:
          type: string
        product_id: {type: string}
        product_slug: {type: string, enum: [astronomer]}
        deployment_id:
          type: string
        logical_agent_id: {type: string}
        integration_id: {type: string}
        mcp_url: {type: string, format: uri}
        route_id:
          type: string
        allowed_route_ids: {type: array, items: {type: string}}
        schema: {type: string, enum: [charlie.onboarding/v1]}
        central_api_version: {type: string, enum: [charlie/v1]}
        central_trust_fingerprint: {type: string, pattern: '^[a-f0-9]{64}$'}
        signing_key_id: {type: string}
        signing_fingerprint: {type: string, pattern: '^[a-f0-9]{64}$'}
        package_digest: {type: string, pattern: '^[a-f0-9]{64}$'}
        artifact:
          type: object
          additionalProperties: false
          required: [image, manifest_digest, chart, chart_digest]
          properties:
            image: {type: string}
            manifest_digest: {type: string, pattern: '^sha256:[a-f0-9]{64}$'}
            chart: {type: string}
            chart_digest: {type: string, pattern: '^sha256:[a-f0-9]{64}$'}
        replica_count: {type: integer, minimum: 2, maximum: 20}
        issued_at: {type: string, format: date-time}
        expires_at: {type: string, format: date-time}
        state:
          type: string
          enum: [validated, consumed]
        idempotent:
          type: boolean
    CharlieSessionResource:
      type: object
      additionalProperties: false
      required: [type, id, required_verb]
      properties:
        type:
          type: string
          enum: [installation, management_component, alert, backup, self_management_application, agent_connection_record, cluster_agents, tunnel]
        id:
          type: string
          maxLength: 255
        required_verb:
          type: string
          const: read
    CharlieSessionCreateRequest:
      type: object
      additionalProperties: false
      required: [client_session_id, intent]
      properties:
        client_session_id: {type: string, format: uuid}
        intent: {type: string, minLength: 1, maxLength: 4096}
        trigger: {type: string, maxLength: 128}
        current_ui_context: {type: string, maxLength: 255}
        resources:
          type: array
          maxItems: 25
          uniqueItems: true
          items: {$ref: '#/components/schemas/CharlieSessionResource'}
    CharlieMessageRequest:
      type: object
      additionalProperties: false
      required: [client_message_id, message]
      properties:
        client_message_id: {type: string, format: uuid}
        message: {type: string, minLength: 1, maxLength: 32768}
    CharlieCommandRequest:
      type: object
      additionalProperties: false
      required: [id, version, arguments]
      description: Product-validated shortcut selection. It does not grant authority or bypass Charlie policy.
      properties:
        id: {type: string, pattern: '^[a-z][a-z0-9._-]{0,63}$'}
        version: {type: string, pattern: '^[1-9][0-9]{0,8}$'}
        arguments:
          type: object
          maxProperties: 4
          propertyNames: {pattern: '^[a-z][a-z0-9_]{0,31}$'}
          additionalProperties: {type: string, maxLength: 512}
    CharlieCommandDescriptor:
      type: object
      additionalProperties: false
      required: [id, version, name, label, description, category, execution, effect, required_mode, example]
      properties:
        id: {type: string}
        version: {type: string}
        name: {type: string}
        aliases: {type: array, uniqueItems: true, items: {type: string}}
        label: {type: string}
        description: {type: string}
        category: {type: string}
        execution: {type: string, enum: [agent, client]}
        effect: {type: string, enum: [read, local]}
        required_mode: {type: string, const: read_only}
        example: {type: string}
        argument:
          type: object
          additionalProperties: false
          required: [name, placeholder, required]
          properties:
            name: {type: string}
            placeholder: {type: string}
            required: {type: boolean}
    CharlieCommandCatalog:
      type: object
      additionalProperties: false
      required: [schema, version, commands]
      properties:
        schema: {type: string, const: astronomer.charlie-command-catalog/v1}
        version: {type: integer, const: 1}
        commands: {type: array, items: {$ref: '#/components/schemas/CharlieCommandDescriptor'}}
    CharlieThreadMessageRequest:
      type: object
      additionalProperties: false
      required: [client_message_id, message]
      properties:
        client_message_id: {type: string, format: uuid}
        message: {type: string, minLength: 1, maxLength: 32768}
        trigger: {type: string, maxLength: 128}
        current_ui_context: {type: string, maxLength: 255}
        resources:
          type: array
          maxItems: 25
          items: {$ref: '#/components/schemas/CharlieSessionResource'}
        command: {$ref: '#/components/schemas/CharlieCommandRequest'}
    CharlieThreadMetadata:
      type: object
      additionalProperties: false
      required: [id, title, state, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        title: {type: string, maxLength: 120}
        state: {type: string, enum: [active, archived]}
        current_session_id: {type: string, format: uuid, nullable: true}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        archived_at: {type: string, format: date-time, nullable: true}
    CharlieAbortRequest:
      type: object
      additionalProperties: false
      required: [request_id]
      properties:
        request_id: {type: string, format: uuid}
    CharlieSessionMetadata:
      type: object
      additionalProperties: false
      required: [id, client_session_id, intent, resource_scope_summary, state, visibility, central_revision, source, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        client_session_id: {type: string, format: uuid}
        intent: {type: string}
        resource_scope_summary: {type: string}
        state:
          type: string
          enum: [creating, active, waiting_approval, completed, aborted, failed]
        visibility: {type: string, enum: [private, incident]}
        central_revision: {type: integer, format: int64, minimum: 0}
        source: {type: string, enum: [user, event]}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    CharlieFindingResource:
      type: object
      additionalProperties: false
      required: [type, id, required_verb]
      properties:
        type: {type: string, enum: [installation, management_component, alert, backup, self_management_application, agent_connection_record, cluster_agents, tunnel]}
        id: {type: string, minLength: 1, maxLength: 255}
        required_verb: {type: string, enum: [read]}
    CharlieFindingSummary:
      type: object
      additionalProperties: false
      required: [id, title, severity, state, summary, reason_no_action, repeat_count, updated_at, affected_resource, workflow_state, available_decisions]
      properties:
        id: {type: string, format: uuid}
        title: {type: string, maxLength: 256}
        severity: {type: string, enum: [info, low, medium, high, critical]}
        state: {type: string, enum: [open, acknowledged, dismissed, resolved, expired]}
        summary: {type: string, maxLength: 2048}
        reason_no_action: {type: string, maxLength: 64}
        repeat_count: {type: integer, minimum: 1}
        updated_at: {type: string, format: date-time}
        affected_resource: {$ref: '#/components/schemas/CharlieFindingResource'}
        workflow_state:
          type: string
          enum: [approval_pending, manual_remediation_required, remediation_in_progress, verification_pending, resolved, rejected, dismissed, expired]
          description: Product-authorized operator workflow derived from durable finding state; it is not execution authority.
        available_decisions:
          type: array
          uniqueItems: true
          maxItems: 3
          items:
            type: string
            enum: [acknowledge, start_remediation, request_verification, dismiss, resolve]
          description: Exact currently legal decisions. Clients must not infer or add decisions from model content.
    CharlieFindingVerification:
      type: object
      additionalProperties: false
      required: [method, steps]
      properties:
        method: {type: string, pattern: '^[a-z][a-z0-9_.:-]{0,127}$'}
        steps:
          type: array
          minItems: 1
          maxItems: 16
          items: {type: string, minLength: 1, maxLength: 512}
    CharlieFindingManualRemediation:
      type: object
      additionalProperties: false
      required: [steps, expected_impact, verification]
      properties:
        preconditions:
          type: array
          maxItems: 16
          items: {type: string, minLength: 1, maxLength: 256}
        steps:
          type: array
          minItems: 1
          maxItems: 16
          items: {type: string, minLength: 1, maxLength: 512}
        expected_impact: {type: string, minLength: 1, maxLength: 1024}
        rollback: {type: string, minLength: 1, maxLength: 1024}
        verification: {$ref: '#/components/schemas/CharlieFindingVerification'}
    CharlieFindingAdvisoryDetail:
      type: object
      additionalProperties: false
      required: [evidence_summary, diagnosis, confidence, risk_impact, operator_checks, verification_steps]
      properties:
        evidence_summary:
          type: array
          maxItems: 20
          items: {type: string, minLength: 1, maxLength: 8192}
        diagnosis: {type: string, minLength: 1, maxLength: 4096}
        confidence: {type: number, minimum: 0, maximum: 1}
        risk_impact: {type: string, minLength: 1, maxLength: 2048}
        preconditions:
          type: array
          maxItems: 16
          items: {type: string, minLength: 1, maxLength: 256}
        rollback: {type: string, minLength: 1, maxLength: 1024}
        operator_checks:
          type: array
          minItems: 1
          maxItems: 16
          items: {type: string, minLength: 1, maxLength: 512}
        verification_steps:
          type: array
          minItems: 1
          maxItems: 16
          items: {type: string, minLength: 1, maxLength: 512}
        manual_remediation: {$ref: '#/components/schemas/CharlieFindingManualRemediation'}
    CharlieFindingDetail:
      allOf:
        - {$ref: '#/components/schemas/CharlieFindingSummary'}
        - type: object
          properties:
            risk_impact: {type: string, maxLength: 1024}
            verification_summary: {type: string, maxLength: 1024}
            detail:
              allOf:
                - {$ref: '#/components/schemas/CharlieFindingAdvisoryDetail'}
              description: Strict advisory evidence returned only after current product authorization. It cannot contain approval, manifest, signature, action, argument, or authorization fields.
    CharlieFindingTransitionRequest:
      type: object
      additionalProperties: false
      required: [request_id]
      properties:
        request_id: {type: string, format: uuid}
    CharlieAdminConnection:
      type: object
      additionalProperties: false
      required: [connected, disclosure_acknowledged]
      properties:
        connected: {type: boolean}
        endpoint: {type: string, format: uri}
        product_id: {type: string}
        product_slug: {type: string}
        deployment_id: {type: string}
        route_id: {type: string}
        central_version: {type: string}
        signing_key_id: {type: string}
        signing_fingerprint: {type: string}
        package_digest: {type: string}
        disclosure_digest: {type: string}
        disclosure_acknowledged: {type: boolean}
        updated_at: {type: string, format: date-time}
    CharlieAdminAgent:
      type: object
      additionalProperties: false
      required: [application_state, desired_replicas, ready_replicas, standby_replicas, replicas]
      properties:
        application_state: {type: string, enum: [not_installed, inactive, installing, ready, degraded, unavailable, disconnected]}
        desired_replicas: {type: integer, format: int32, minimum: 0}
        ready_replicas: {type: integer, format: int32, minimum: 0}
        leader_replica: {type: string}
        standby_replicas: {type: array, items: {type: string}}
        replicas:
          type: array
          maxItems: 20
          items: {$ref: '#/components/schemas/CharlieAdminAgentReplica'}
        fencing_epoch: {type: integer, format: int64, minimum: 0}
        last_heartbeat_at: {type: string, format: date-time}
        agent_version: {type: string}
        chart_version: {type: string}
        chart_digest: {type: string}
        image_digest: {type: string}
    CharlieAdminAgentReplica:
      type: object
      additionalProperties: false
      required: [ordinal, role, state]
      properties:
        ordinal: {type: integer, minimum: 0, maximum: 19}
        instance_id: {type: string, description: Opaque product-agent instance identifier; never a credential.}
        role: {type: string, enum: [leader, standby, unknown]}
        state: {type: string, enum: [ready, degraded, unavailable, unknown]}
        last_heartbeat_at: {type: string, format: date-time}
        version: {type: string}
    CharlieAdminMode:
      type: object
      additionalProperties: false
      required: [requested, authoritative, revision, emergency_disabled, effects, auto_readiness, workload_ceiling, workload_ceiling_ready]
      properties:
        requested: {type: string, enum: [disabled, read_only, approval, auto]}
        authoritative: {type: string, enum: [disabled, read_only, approval, auto]}
        revision: {type: integer, format: int64, minimum: 0}
        emergency_disabled: {type: boolean}
        disable_pending: {type: boolean}
        disclosure_digest: {type: string}
        acknowledged_disclosure_digest: {type: string}
        effects: {type: array, items: {type: string}}
        auto_readiness: {$ref: '#/components/schemas/CharlieAdminAutoReadiness'}
        workload_ceiling: {type: string, enum: [disabled, read_only, approval, auto], description: Product-owned ceiling currently rendered in the agent's Flux delivery workload.}
        workload_ceiling_ready: {type: boolean, description: True only after a non-pruning rollout and exact readback from both ready product-agent replicas.}
    CharlieKubernetesVisibility:
      type: object
      additionalProperties: false
      required: [schema, profile, revision, state, instance_id, namespaces, product_owned_only, cluster_scoped, pod_logs, downstream_targets, secret_values, exec, attach, port_forward, api_proxy, requires_rediscovery, requires_central_review, requires_product_acknowledgement, available_profiles, scope_summary]
      properties:
        schema: {type: string, enum: [charlie.kubernetes-visibility/v1]}
        profile: {type: string, enum: [disabled, product_namespace, cluster_diagnostics]}
        revision: {type: integer, format: int64, minimum: 0}
        state: {type: string, enum: [not_configured, disabled, enabled]}
        instance_id: {type: string, maxLength: 128}
        namespaces: {type: array, maxItems: 32, uniqueItems: true, items: {type: string, maxLength: 63}}
        product_owned_only: {type: boolean, enum: [true]}
        cluster_scoped: {type: boolean}
        pod_logs: {type: boolean}
        downstream_targets: {type: boolean, enum: [false]}
        secret_values: {type: boolean, enum: [false]}
        exec: {type: boolean, enum: [false]}
        attach: {type: boolean, enum: [false]}
        port_forward: {type: boolean, enum: [false]}
        api_proxy: {type: boolean, enum: [false]}
        requires_rediscovery: {type: boolean}
        requires_central_review: {type: boolean}
        requires_product_acknowledgement: {type: boolean}
        candidate_disclosure_digest: {type: string, pattern: '^[a-f0-9]{64}$'}
        available_profiles: {type: array, minItems: 3, maxItems: 3, uniqueItems: true, items: {type: string, enum: [disabled, product_namespace, cluster_diagnostics]}}
        scope_summary: {type: string, maxLength: 512}
    CharlieKubernetesVisibilityRequest:
      type: object
      additionalProperties: false
      required: [profile, pod_logs, revision]
      properties:
        profile: {type: string, enum: [disabled, product_namespace, cluster_diagnostics]}
        pod_logs: {type: boolean}
        revision: {type: integer, format: int64, minimum: 0}
    CharlieAdminAutoReadiness:
      type: object
      additionalProperties: false
      required: [ready, blockers]
      properties:
        ready: {type: boolean}
        blockers:
          type: array
          maxItems: 8
          items:
            type: object
            additionalProperties: false
            required: [code, message, next_action]
            properties:
              code: {type: string, maxLength: 64}
              message: {type: string, maxLength: 512}
              next_action: {type: string, maxLength: 512}
    CharlieAdminStatus:
      type: object
      additionalProperties: false
      required: [connection, agent, mode]
      properties:
        connection: {$ref: '#/components/schemas/CharlieAdminConnection'}
        agent: {$ref: '#/components/schemas/CharlieAdminAgent'}
        mode: {$ref: '#/components/schemas/CharlieAdminMode'}
    CharlieAdminActionRequest:
      type: object
      additionalProperties: false
      required: [confirmation]
      properties:
        confirmation: {type: string, minLength: 1, maxLength: 64}
    CharlieModeRequest:
      type: object
      additionalProperties: false
      properties:
        mode: {type: string, enum: [disabled, read_only, approval, auto]}
        revision: {type: integer, format: int64, minimum: 0}
        emergency_disable: {type: boolean}
        acknowledge_disclosure_digest: {type: string, minLength: 64, maxLength: 71}
      description: Supply mode and revision together, or acknowledge_disclosure_digest alone.
    CharlieAdminTriggerRule:
      type: object
      additionalProperties: false
      required: [name, source_type, enabled, severities, scopes, cooldown_seconds, grace_period_seconds, flap_window_seconds, flap_count, fleet_threshold_percent, suppressed, maximum_attempts, dead_letter_enabled, mode_ceiling]
      properties:
        id: {type: string, format: uuid}
        name: {type: string, minLength: 1, maxLength: 128}
        source_type: {type: string, minLength: 1, maxLength: 64}
        enabled: {type: boolean}
        severities: {type: array, minItems: 1, maxItems: 5, uniqueItems: true, items: {type: string, enum: [info, low, medium, high, critical]}}
        scopes: {type: array, maxItems: 25, uniqueItems: true, items: {type: string, maxLength: 255}}
        cooldown_seconds: {type: integer, format: int32, minimum: 1}
        grace_period_seconds: {type: integer, format: int32, minimum: 1}
        flap_window_seconds: {type: integer, format: int32, minimum: 1}
        flap_count: {type: integer, format: int32, minimum: 1}
        estate_threshold_percent: {type: integer, format: int32, minimum: 0, maximum: 100}
        fleet_threshold_percent: {type: integer, format: int32, minimum: 0, maximum: 100, deprecated: true, description: Deprecated compatibility alias for estate_threshold_percent.}
        minimum_agent_version: {type: string, maxLength: 64}
        suppressed: {type: boolean}
        maximum_attempts: {type: integer, format: int32, minimum: 1}
        dead_letter_enabled: {type: boolean}
        service_identity: {type: string, readOnly: true}
        mode_ceiling: {type: string, enum: [read_only, approval, auto], default: read_only}
    CharlieAdminAutomation:
      type: object
      additionalProperties: false
      required: [rules, action_policies, defaults_revision, service_identity_enabled]
      properties:
        rules: {type: array, maxItems: 100, items: {$ref: '#/components/schemas/CharlieAdminTriggerRule'}}
        action_policies: {type: array, maxItems: 64, items: {$ref: '#/components/schemas/CharlieAdminActionPolicy'}}
        defaults_revision: {type: integer, format: int64, minimum: 0}
        service_identity_enabled: {type: boolean}
    CharlieAdminActionPolicy:
      type: object
      additionalProperties: false
      required: [capability, effect, risk, auto_eligible, central_allowlisted, central_state, enabled, revision, max_actions_per_incident, max_actions_per_window, budget_window_seconds, cooldown_seconds, scope_summary, preconditions, verification, circuit_state]
      properties:
        capability: {type: string, pattern: '^[a-z][a-z0-9_.:-]{0,127}$'}
        effect: {type: string, enum: [write]}
        risk: {type: string, enum: [low, medium, high, critical]}
        auto_eligible: {type: boolean}
        central_allowlisted: {type: boolean}
        central_state: {type: string, enum: [verified, unavailable]}
        enabled: {type: boolean}
        revision: {type: integer, format: int64, minimum: 0}
        max_actions_per_incident: {type: integer, format: int32, minimum: 1, maximum: 100}
        max_actions_per_window: {type: integer, format: int32, minimum: 1, maximum: 100}
        budget_window_seconds: {type: integer, format: int32, minimum: 60, maximum: 86400}
        cooldown_seconds: {type: integer, format: int32, minimum: 30, maximum: 604800}
        scope_summary: {type: string, maxLength: 512}
        preconditions: {type: array, maxItems: 16, items: {type: string, maxLength: 512}}
        verification: {type: string, maxLength: 512}
        circuit_state: {type: string, enum: [closed, open, unknown]}
    CharlieAdminActionPolicyInput:
      type: object
      additionalProperties: false
      required: [enabled, max_actions_per_incident, max_actions_per_window, budget_window_seconds, cooldown_seconds]
      properties:
        enabled: {type: boolean}
        max_actions_per_incident: {type: integer, format: int32, minimum: 1, maximum: 100}
        max_actions_per_window: {type: integer, format: int32, minimum: 1, maximum: 100}
        budget_window_seconds: {type: integer, format: int32, minimum: 60, maximum: 86400}
        cooldown_seconds: {type: integer, format: int32, minimum: 30, maximum: 604800}
    CharlieAdminAlertChannel:
      type: object
      additionalProperties: false
      required: [id, name, type, enabled, destination_configured]
      properties:
        id: {type: string, format: uuid}
        name: {type: string, maxLength: 128}
        type: {type: string, enum: [slack, pagerduty, msteams, webhook]}
        enabled: {type: boolean}
        destination_configured: {type: boolean, description: Indicates a local destination exists without returning its value.}
    CharlieAdminAlertPolicy:
      type: object
      additionalProperties: false
      required: [enabled, minimum_severity, dedupe_window_seconds, escalation_after_seconds, quiet_hours_enabled, quiet_hours_start, quiet_hours_end, quiet_hours_timezone, revision, channel_ids, channels, available_channels, in_app_enabled]
      properties:
        enabled: {type: boolean}
        minimum_severity: {type: string, enum: [info, low, medium, warning, high, critical]}
        dedupe_window_seconds: {type: integer, format: int32, minimum: 60, maximum: 604800}
        escalation_after_seconds: {type: integer, format: int32, minimum: 0, maximum: 604800}
        quiet_hours_enabled: {type: boolean}
        quiet_hours_start: {type: string, pattern: '^([01][0-9]|2[0-3]):[0-5][0-9]$'}
        quiet_hours_end: {type: string, pattern: '^([01][0-9]|2[0-3]):[0-5][0-9]$'}
        quiet_hours_timezone: {type: string, minLength: 1, maxLength: 64}
        revision: {type: integer, format: int64, minimum: 0}
        channel_ids: {type: array, maxItems: 32, uniqueItems: true, items: {type: string, format: uuid}}
        channels: {type: array, maxItems: 32, items: {$ref: '#/components/schemas/CharlieAdminAlertChannel'}}
        available_channels: {type: array, maxItems: 1000, items: {$ref: '#/components/schemas/CharlieAdminAlertChannel'}}
        in_app_enabled: {type: boolean, readOnly: true, description: Durable findings remain visible in Astronomer regardless of external routing.}
    CharlieAdminAlertPolicyInput:
      type: object
      additionalProperties: false
      required: [revision, enabled, minimum_severity, dedupe_window_seconds, escalation_after_seconds, quiet_hours_enabled, quiet_hours_start, quiet_hours_end, quiet_hours_timezone, channel_ids]
      properties:
        revision: {type: integer, format: int64, minimum: 0, description: Exact revision read by the administrator; zero creates the first policy.}
        enabled: {type: boolean}
        minimum_severity: {type: string, enum: [info, low, medium, warning, high, critical]}
        dedupe_window_seconds: {type: integer, format: int32, minimum: 60, maximum: 604800}
        escalation_after_seconds: {type: integer, format: int32, minimum: 0, maximum: 604800}
        quiet_hours_enabled: {type: boolean}
        quiet_hours_start: {type: string, pattern: '^([01][0-9]|2[0-3]):[0-5][0-9]$'}
        quiet_hours_end: {type: string, pattern: '^([01][0-9]|2[0-3]):[0-5][0-9]$'}
        quiet_hours_timezone: {type: string, minLength: 1, maxLength: 64}
        channel_ids: {type: array, maxItems: 32, uniqueItems: true, items: {type: string, format: uuid}}
    CharlieAdminAlertDeliveryProof:
      type: object
      additionalProperties: false
      required: [delivery_id, finding_id, delivery_kind, status, template_identity, deep_link_valid, content_free, attempt_count, maximum_attempts, created_at, updated_at]
      description: Metadata-only proof. Notification subject, body, destination, channel identity, dedupe bucket, and provider errors are never returned.
      properties:
        delivery_id: {type: string, format: uuid}
        finding_id: {type: string, format: uuid}
        delivery_kind: {type: string, enum: [initial, escalation]}
        status: {type: string, enum: [queued, delivering, retry, delivered, suppressed, dead]}
        template_identity: {type: string, enum: [charlie.finding.initial/v1, charlie.finding.escalation/v1, unrecognized]}
        deep_link_valid: {type: boolean}
        content_free: {type: boolean, description: True only when the persisted subject/body exactly match Astronomer's fixed content-free template and finding-bound deep link.}
        attempt_count: {type: integer, format: int32, minimum: 0}
        maximum_attempts: {type: integer, format: int32, minimum: 1, maximum: 20}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        delivered_at: {type: string, format: date-time}
    CharlieAdminAlertDeliveryProofView:
      type: object
      additionalProperties: false
      required: [finding_id, finding_block_code, finding_workflow_state, delivery_count, dedupe_valid, deliveries]
      properties:
        finding_id: {type: string, format: uuid}
        finding_block_code: {type: string, maxLength: 64}
        finding_workflow_state: {type: string, maxLength: 64}
        delivery_count: {type: integer, minimum: 0, maximum: 32}
        dedupe_valid: {type: boolean}
        deliveries: {type: array, maxItems: 32, items: {$ref: '#/components/schemas/CharlieAdminAlertDeliveryProof'}}
    CharlieDiscoveryQualificationRequest:
      type: object
      additionalProperties: false
      required: [scenario]
      properties:
        scenario: {type: string, enum: [mixed_catalog, malformed_catalog]}
    CharlieAdminDiscoveryQualification:
      type: object
      additionalProperties: false
      required: [scenario, candidate_enabled, accepted_count, rejected_count, accepted_names, catalog_bound, malformed_rejected]
      properties:
        scenario: {type: string, enum: [mixed_catalog, malformed_catalog]}
        candidate_enabled: {type: boolean, description: Whether this fixed candidate generation is eligible to activate; it does not report or modify current integration activation.}
        accepted_count: {type: integer, minimum: 0, maximum: 2}
        rejected_count: {type: integer, minimum: 0, maximum: 1}
        accepted_names: {type: array, maxItems: 2, uniqueItems: true, items: {type: string, pattern: '^[a-z][a-z0-9_.:-]{0,127}$'}}
        disclosure_digest: {type: string, pattern: '^[a-f0-9]{64}$'}
        catalog_bound: {type: boolean}
        malformed_rejected: {type: boolean}
    CharlieAdminTriggerEvent:
      type: object
      additionalProperties: false
      required: [id, rule_id, event_type, resource_type, resource_id, state, repeat_count, attempt_count, first_occurred_at, last_occurred_at, updated_at]
      description: UI-safe lifecycle metadata; summary, fingerprint, session, origin, and outbox payload are intentionally omitted.
      properties:
        id: {type: string, format: uuid}
        retry_of_event_id: {type: string, format: uuid}
        rule_id: {type: string, format: uuid}
        event_type: {type: string, maxLength: 64}
        resource_type: {type: string, maxLength: 64}
        resource_id: {type: string, maxLength: 255}
        state: {type: string, enum: [pending, dispatching, dispatched, retry, dead, completed, suppressed]}
        repeat_count: {type: integer, format: int32, minimum: 1}
        attempt_count: {type: integer, format: int32, minimum: 0}
        last_error_code: {type: string, maxLength: 64}
        first_occurred_at: {type: string, format: date-time}
        last_occurred_at: {type: string, format: date-time}
        dead_lettered_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    CharlieTriggerRetryRequest:
      type: object
      additionalProperties: false
      properties:
        request_id: {type: string, format: uuid, deprecated: true, description: Legacy correlation identifier; Idempotency-Key is authoritative.}
    CharlieTriggerRetryReceipt:
      type: object
      additionalProperties: false
      required: [operation_id, event_id, status, status_url]
      properties:
        operation_id: {type: string, format: uuid}
        event_id: {type: string, format: uuid}
        status: {type: string, enum: [pending, dispatching, dispatched, retry, dead, completed, suppressed]}
        status_url: {type: string}
    CharlieAdminPermission:
      type: object
      additionalProperties: false
      required: [permission, scope, source]
      properties:
        permission: {type: string}
        scope: {type: string}
        source: {type: string}
    CharlieAdminAccess:
      type: object
      additionalProperties: false
      required: [effective_permissions, automation_grants]
      properties:
        effective_permissions: {type: array, items: {$ref: '#/components/schemas/CharlieAdminPermission'}}
        automation_grants: {type: array, items: {$ref: '#/components/schemas/CharlieAdminPermission'}}
    CharlieAccessRequest:
      type: object
      additionalProperties: false
      required: [automation_service_identity_enabled]
      properties:
        automation_service_identity_enabled: {type: boolean}
    CharlieAdminDiagnosticCheck:
      type: object
      additionalProperties: false
      required: [id, label, state, summary, next_action]
      properties:
        id: {type: string}
        label: {type: string}
        state: {type: string, enum: [healthy, degraded, unavailable, inactive, ready, unknown]}
        summary: {type: string}
        next_action: {type: string}
        checked_at: {type: string, format: date-time}
        expires_at: {type: string, format: date-time}
    CharlieAdminDiagnostics:
      type: object
      additionalProperties: false
      required: [overall, checks]
      properties:
        overall: {type: string, enum: [healthy, degraded, unavailable, inactive, ready, unknown]}
        checks: {type: array, maxItems: 32, items: {$ref: '#/components/schemas/CharlieAdminDiagnosticCheck'}}
        correlation_id: {type: string}
    CharlieContextSearchResult:
      type: object
      additionalProperties: false
      required: [type, id, required_verb, label, summary]
      properties:
        type: {type: string, enum: [installation, management_component, alert, backup, self_management_application, agent_connection_record, cluster_agents, tunnel]}
        id: {type: string, minLength: 1, maxLength: 255}
        required_verb: {type: string, const: read}
        label: {type: string, maxLength: 256}
        summary: {type: string, maxLength: 1024}
    CharlieOperationStatus:
      type: object
      additionalProperties: false
      required: [operation_id, capability, effect, state, result_status, created_at, updated_at]
      properties:
        operation_id: {type: string}
        capability: {type: string}
        effect: {type: string, enum: [read, write]}
        state: {type: string}
        result_status: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        dispatched_at: {type: string, format: date-time}
        verified_at: {type: string, format: date-time}
    CharlieApprovalSummary:
      type: object
      additionalProperties: false
      required: [id, title, state, eligible, capability, target, risk, effect, requiredPermission, expiresAt]
      description: Bounded display metadata only; signed authority material never crosses the browser boundary.
      properties:
        id: {type: string, minLength: 1, maxLength: 128}
        title: {type: string, maxLength: 256}
        state: {type: string, enum: [pending, approved, denied, expired]}
        eligible: {type: boolean}
        capability: {type: string}
        target: {type: string}
        risk: {type: string}
        effect: {type: string, enum: [write]}
        requiredPermission: {type: string}
        expiresAt: {type: string, format: date-time}
        reason: {type: string, maxLength: 512}
        review: {$ref: '#/components/schemas/CharlieApprovalReviewSummary'}
    CharlieApprovalReviewSummary:
      type: object
      additionalProperties: false
      required: [argumentsWithheld]
      description: Product-projected human review content; raw action arguments and authority material are excluded.
      properties:
        description: {type: string, minLength: 1, maxLength: 4096}
        expectedImpact: {type: string, minLength: 1, maxLength: 4096}
        reversible: {type: boolean}
        rollback: {type: string, minLength: 1, maxLength: 4096}
        destructive: {type: boolean}
        argumentsWithheld: {type: boolean, const: true}
    CharlieApprovalDecisionRequest:
      type: object
      additionalProperties: false
      required: [request_id, decision]
      properties:
        request_id: {type: string, format: uuid}
        decision: {type: string, enum: [approve, reject, deny]}
        rationale: {type: string, maxLength: 512}
    LoggingSavedSearch:
      type: object
      additionalProperties: false
      required: [id, output_id, name, query, namespaces, limit, direction, live_tail, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        output_id: {type: string, format: uuid}
        name: {type: string, minLength: 1, maxLength: 120}
        query: {type: string, maxLength: 16384}
        namespaces:
          type: array
          maxItems: 20
          uniqueItems: true
          items: {type: string, minLength: 1, maxLength: 63}
        limit: {type: integer, format: int32, minimum: 1, maximum: 1000}
        direction: {type: string, enum: [forward, backward]}
        live_tail: {type: boolean}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    CreateLoggingSavedSearchRequest:
      type: object
      additionalProperties: false
      required: [output_id, name]
      properties:
        output_id: {type: string, format: uuid}
        name: {type: string, minLength: 1, maxLength: 120}
        query: {type: string, maxLength: 16384, default: ""}
        namespaces:
          type: array
          maxItems: 20
          uniqueItems: true
          items: {type: string, minLength: 1, maxLength: 63}
        limit: {type: integer, format: int32, minimum: 1, maximum: 1000, default: 100}
        direction: {type: string, enum: [forward, backward], default: backward}
        live_tail: {type: boolean, default: false}
    UpdateLoggingSavedSearchRequest:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: {type: string, minLength: 1, maxLength: 120}
        query: {type: string, maxLength: 16384, default: ""}
        namespaces:
          type: array
          maxItems: 20
          uniqueItems: true
          items: {type: string, minLength: 1, maxLength: 63}
        limit: {type: integer, format: int32, minimum: 1, maximum: 1000, default: 100}
        direction: {type: string, enum: [forward, backward], default: backward}
        live_tail: {type: boolean, default: false}
    LoggingSavedSearchEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingSavedSearch'}
    LoggingSavedSearchListEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data:
          type: array
          items: {$ref: '#/components/schemas/LoggingSavedSearch'}
    LoggingOutputWriteRequest:
      type: object
      additionalProperties: false
      required: [name, output_type]
      properties:
        name: {type: string, minLength: 1, maxLength: 255}
        output_type:
          type: string
          enum: [elasticsearch, opensearch, loki, splunk, cloudwatch, datadog, s3, syslog]
        configuration:
          type: object
          additionalProperties: true
          description: Provider-specific configuration. Credential fields are write-only and redacted from reads.
        cluster_id: {type: string, format: uuid}
        enabled: {type: boolean, default: false}
    LoggingPipelineWriteRequest:
      type: object
      additionalProperties: false
      required: [name, output_ids]
      properties:
        name: {type: string, minLength: 1, maxLength: 255}
        cluster_id: {type: string, format: uuid}
        namespaces:
          type: array
          maxItems: 200
          items: {type: string, minLength: 1, maxLength: 63}
        labels:
          type: object
          additionalProperties: {type: string}
        filters:
          description: Bounded provider-independent filter configuration.
        output_ids:
          type: array
          minItems: 1
          maxItems: 200
          uniqueItems: true
          items: {type: string, format: uuid}
          description: Destinations on the same cluster that receive records selected by this pipeline.
        enabled: {type: boolean, default: false}
    LoggingQueryRequest:
      type: object
      additionalProperties: false
      properties:
        query: {type: string, maxLength: 16384, default: ""}
        limit: {type: integer, minimum: 1, maximum: 1000, default: 100}
        start: {type: string, description: RFC3339 timestamp or Loki nanosecond epoch.}
        end: {type: string, description: RFC3339 timestamp or Loki nanosecond epoch.}
        direction: {type: string, enum: [forward, backward], default: backward}
        namespaces:
          type: array
          maxItems: 20
          uniqueItems: true
          items: {type: string, minLength: 1, maxLength: 63}
    LoggingOutputCapabilities:
      type: object
      additionalProperties: false
      required: [ship, test, query, tail, aggregate, link_out, retention_visibility, query_mode]
      properties:
        ship: {type: boolean}
        test: {type: boolean}
        query: {type: boolean}
        tail: {type: boolean}
        aggregate: {type: boolean}
        link_out: {type: boolean}
        link_out_url: {type: string, format: uri}
        retention_visibility: {type: boolean}
        query_mode: {type: string, enum: [native, link_out, shipping_only]}
    LoggingOutput:
      type: object
      additionalProperties: false
      required: [id, name, output_type, configuration, cluster_id, enabled, created_at, updated_at, is_system, capabilities]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        output_type: {type: string}
        configuration:
          type: object
          additionalProperties: true
          description: Redacted provider configuration; credential values are never returned.
        cluster_id: {type: string, format: uuid, nullable: true}
        enabled: {type: boolean}
        created_by_id: {type: string, format: uuid, nullable: true}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        is_system: {type: boolean}
        capabilities: {$ref: '#/components/schemas/LoggingOutputCapabilities'}
    LoggingPipeline:
      type: object
      additionalProperties: false
      required: [id, name, cluster_id, namespaces, labels, filters, output_ids, output_names, enabled, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        cluster_id: {type: string, format: uuid}
        namespaces: {type: array, items: {type: string}}
        labels: {type: object, additionalProperties: {type: string}}
        filters: {}
        output_ids:
          type: array
          items: {type: string, format: uuid}
        output_names:
          type: array
          items: {type: string}
        enabled: {type: boolean}
        created_by_id: {type: string, format: uuid, nullable: true}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    LoggingOperationEvent:
      type: object
      additionalProperties: false
      required: [id, level, stage, message, detail, createdAt]
      properties:
        id: {type: string, format: uuid}
        level: {type: string}
        stage: {type: string}
        message: {type: string}
        detail: {type: object, additionalProperties: true}
        createdAt: {type: string, format: date-time}
    LoggingOperation:
      type: object
      additionalProperties: false
      required: [id, targetType, targetKey, operationType, status, attemptCount, errorMessage, createdAt, updatedAt]
      properties:
        id: {type: string, format: uuid}
        targetType: {type: string, enum: [output, pipeline]}
        targetKey: {type: string}
        operationType: {type: string}
        status: {type: string, enum: [pending, running, completed, failed, superseded]}
        attemptCount: {type: integer, format: int32, minimum: 0}
        startedAt: {type: string, format: date-time, nullable: true}
        completedAt: {type: string, format: date-time, nullable: true}
        errorMessage: {type: string}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
        events: {type: array, items: {$ref: '#/components/schemas/LoggingOperationEvent'}}
    LoggingQueryResult:
      type: object
      additionalProperties: false
      required: [backend, query, limit, start, end, data]
      properties:
        backend: {type: string}
        query: {type: string}
        limit: {type: integer, minimum: 1, maximum: 1000}
        start: {type: string, format: date-time}
        end: {type: string, format: date-time}
        data: {}
    LoggingOutputEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingOutput'}
    LoggingOutputMutationReceipt:
      type: object
      additionalProperties: false
      required: [output, operation]
      properties:
        output: {$ref: '#/components/schemas/LoggingOutput'}
        operation: {$ref: '#/components/schemas/LoggingOperation'}
    LoggingOutputMutationReceiptEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingOutputMutationReceipt'}
    LoggingPipelineEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingPipeline'}
    LoggingPipelineMutationReceipt:
      type: object
      additionalProperties: false
      required: [pipeline, operation]
      properties:
        pipeline: {$ref: '#/components/schemas/LoggingPipeline'}
        operation: {$ref: '#/components/schemas/LoggingOperation'}
    LoggingPipelineMutationReceiptEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingPipelineMutationReceipt'}
    LoggingOperationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingOperation'}
    LoggingOutputPage:
      allOf:
        - {$ref: '#/components/schemas/PaginatedEnvelope'}
        - type: object
          properties:
            data: {type: array, items: {$ref: '#/components/schemas/LoggingOutput'}}
    LoggingPipelinePage:
      allOf:
        - {$ref: '#/components/schemas/PaginatedEnvelope'}
        - type: object
          properties:
            data: {type: array, items: {$ref: '#/components/schemas/LoggingPipeline'}}
    LoggingOperationListEnvelope:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/LoggingOperation'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    LoggingTestResult:
      type: object
      additionalProperties: false
      required: [success, message, operation]
      properties:
        success: {type: boolean}
        message: {type: string}
        operation: {$ref: '#/components/schemas/LoggingOperation'}
    LoggingTestEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingTestResult'}
    LoggingQueryEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingQueryResult'}
    LoggingTokenRotation:
      type: object
      additionalProperties: false
      required: [clusterId, token, rotatedAt, operation]
      properties:
        clusterId: {type: string, format: uuid}
        token: {type: string, writeOnly: true, description: Plaintext ingest token returned exactly once.}
        rotatedAt: {type: string, format: date-time}
        operation: {$ref: '#/components/schemas/LoggingOperation'}
    LoggingTokenRotationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingTokenRotation'}
    LoggingAttachStatus:
      type: object
      additionalProperties: false
      required: [clusterId, ingestPublic, status, host, attached]
      properties:
        clusterId: {type: string, format: uuid}
        ingestPublic: {type: boolean}
        status: {type: string}
        host: {type: string}
        attached: {type: boolean}
    LoggingAttachStatusEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingAttachStatus'}
    LoggingAttachResult:
      allOf:
        - {$ref: '#/components/schemas/LoggingOutput'}
        - type: object
          properties:
            token: {type: string, writeOnly: true, description: Plaintext ingest token returned only when minted.}
    LoggingAttachResultEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingAttachResult'}
    LoggingAttachMutationReceipt:
      type: object
      additionalProperties: false
      required: [output, operation]
      properties:
        output: {$ref: '#/components/schemas/LoggingAttachResult'}
        operation: {$ref: '#/components/schemas/LoggingOperation'}
    LoggingAttachMutationReceiptEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingAttachMutationReceipt'}
    LoggingFluentBitConfig:
      type: object
      additionalProperties: false
      required: [cluster_id, config]
      properties:
        cluster_id: {type: string, format: uuid}
        config: {type: string}
    LoggingFluentBitConfigEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LoggingFluentBitConfig'}
    LoggingControllerStatusEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data:
          type: object
          additionalProperties: false
          required: [reconciler, health, healthReasons, outputs, pipelines, operations, recentFailureCount, recentOperations]
          properties:
            reconciler: {type: object, additionalProperties: true}
            health: {type: string}
            healthReasons: {type: array, items: {type: string}}
            outputs: {type: object, additionalProperties: true}
            pipelines: {type: object, additionalProperties: true}
            operations: {type: object, additionalProperties: {type: integer}}
            recentFailureCount: {type: integer, minimum: 0}
            recentOperations: {type: array, items: {$ref: '#/components/schemas/LoggingOperation'}}
            latestFailure: {nullable: true}
    PodSecurityTemplate:
      type: object
      additionalProperties: false
      required: [id, name, description, is_default, is_builtin, enforce_level, enforce_version, audit_level, audit_version, warn_level, warn_version, exempt_usernames, exempt_runtime_classes, exempt_namespaces, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        description: {type: string}
        is_default: {type: boolean}
        is_builtin: {type: boolean}
        enforce_level: {type: string, enum: [privileged, baseline, restricted]}
        enforce_version: {type: string}
        audit_level: {type: string, enum: [privileged, baseline, restricted]}
        audit_version: {type: string}
        warn_level: {type: string, enum: [privileged, baseline, restricted]}
        warn_version: {type: string}
        exempt_usernames: {type: array, items: {type: string}}
        exempt_runtime_classes: {type: array, items: {type: string}}
        exempt_namespaces: {type: array, items: {type: string}}
        created_by_id: {type: string, format: uuid, nullable: true}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    PodSecurityTemplateWriteRequest:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: {type: string, minLength: 1}
        description: {type: string}
        is_default: {type: boolean}
        enforce_level: {type: string, enum: [privileged, baseline, restricted]}
        enforce_version: {type: string}
        audit_level: {type: string, enum: [privileged, baseline, restricted]}
        audit_version: {type: string}
        warn_level: {type: string, enum: [privileged, baseline, restricted]}
        warn_version: {type: string}
        exempt_usernames: {type: array, items: {type: string}}
        exempt_runtime_classes: {type: array, items: {type: string}}
        exempt_namespaces: {type: array, items: {type: string}}
    ClusterSecurityPolicy:
      type: object
      additionalProperties: false
      required: [id, cluster_id, template_id, sync_status, error_message, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid}
        template_id: {type: string, format: uuid}
        applied_at: {type: string, format: date-time, nullable: true}
        sync_status: {type: string, enum: [synced, pending, failed, unknown]}
        error_message: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    ClusterSecurityPolicyCreateRequest:
      type: object
      additionalProperties: false
      required: [cluster_id, template_id]
      properties:
        cluster_id: {type: string, format: uuid}
        template_id: {type: string, format: uuid}
    CISScanCreateRequest:
      type: object
      additionalProperties: false
      required: [cluster_id]
      properties:
        cluster_id: {type: string, format: uuid}
        profile:
          type: string
          description: Installed ClusterScanProfile name; omitted to select the newest compatible distribution profile.
        scan_type:
          type: string
          deprecated: true
          description: Compatibility alias for profile.
    CISProfile:
      type: object
      additionalProperties: false
      required: [name, benchmarkVersion]
      properties:
        name: {type: string}
        benchmarkVersion: {type: string}
    CISProfilesResponse:
      type: object
      additionalProperties: false
      required: [items, source]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/CISProfile'}}
        source: {type: string, enum: [cluster, fallback]}
        error: {type: string}
    CISFinding:
      type: object
      additionalProperties: false
      required: [test_id, severity, status, description, remediation]
      properties:
        test_id: {type: string}
        severity: {type: string, enum: [critical, high, medium, low, info]}
        status: {type: string, enum: [pass, fail, warn, skip, info]}
        description: {type: string}
        remediation: {type: string}
    CISScan:
      type: object
      additionalProperties: true
      required: [id, cluster_id, scan_type, status, passed, failed, warned, skipped, started_at, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid}
        scan_type: {type: string}
        status: {type: string, enum: [pending, running, completed, failed, cancelled]}
        passed: {type: integer, minimum: 0}
        failed: {type: integer, minimum: 0}
        warned: {type: integer, minimum: 0}
        skipped: {type: integer, minimum: 0}
        started_at: {type: string, format: date-time}
        completed_at: {type: string, format: date-time, nullable: true}
        cluster_scan_name: {type: string}
        initiated_by_id: {type: string, format: uuid, nullable: true}
        terminal_reason: {type: string}
        poll_attempt: {type: integer, minimum: 0}
        summary: {}
        results: {}
        findings: {type: array, items: {$ref: '#/components/schemas/CISFinding'}}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    CISScanEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/CISScan'}
    CISScanListEnvelope:
      allOf:
        - {$ref: '#/components/schemas/PaginatedEnvelope'}
        - type: object
          properties:
            data: {type: array, items: {$ref: '#/components/schemas/CISScan'}}
    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
          required:
            - code
            - message
      required:
        - error
    MonitoringStackRequest:
      type: object
      additionalProperties: false
      description: Optional overrides for a per-cluster kube-prometheus-stack lifecycle action.
      properties:
        releaseName: {type: string}
        namespace: {type: string}
        retention: {type: string}
        storageClass: {type: string}
        storageSize: {type: string}
        scrapeInterval: {type: string}
        clusterLabel: {type: string}
        clusterLabelValue: {type: string}
        prometheusVersion: {type: string}
        chartVersion: {type: string}
        storageConfigId: {type: string, format: uuid}
        objectStorageSecretName: {type: string}
        enableGrafana: {type: boolean}
        enableAlertmanager: {type: boolean}
        thanosSidecarEnabled: {type: boolean}
        autoRollbackOnFailure: {type: boolean}
    SharedThanosStackRequest:
      type: object
      additionalProperties: false
      description: managementClusterId may be supplied as the clusterId query parameter instead.
      required: [storageConfigId]
      properties:
        managementClusterId: {type: string, format: uuid}
        namespace: {type: string}
        releaseName: {type: string}
        chartVersion: {type: string}
        storageConfigId: {type: string, format: uuid}
        objectStorageSecretName: {type: string}
        queryReplicas: {type: integer, format: int32, minimum: 0}
        storeGatewayReplicas: {type: integer, format: int32, minimum: 0}
        compactorReplicas: {type: integer, format: int32, minimum: 0}
        autoRollbackOnFailure: {type: boolean}
    SharedAlertmanagerStackRequest:
      type: object
      additionalProperties: false
      description: managementClusterId may be supplied as the clusterId query parameter instead.
      properties:
        managementClusterId: {type: string, format: uuid}
        namespace: {type: string}
        releaseName: {type: string}
        chartVersion: {type: string}
        replicas: {type: integer, format: int32, minimum: 0}
        storageClass: {type: string}
        storageSize: {type: string}
        autoRollbackOnFailure: {type: boolean}
    SharedGrafanaStackRequest:
      type: object
      additionalProperties: false
      description: managementClusterId may be supplied as the clusterId query parameter instead.
      properties:
        managementClusterId: {type: string, format: uuid}
        namespace: {type: string}
        releaseName: {type: string}
        chartVersion: {type: string}
        replicas: {type: integer, format: int32, minimum: 0}
        storageClass: {type: string}
        storageSize: {type: string}
        ingressHost: {type: string}
        logDatasourceUrl: {type: string, format: uri}
        autoRollbackOnFailure: {type: boolean}
    SharedLokiStackRequest:
      type: object
      additionalProperties: false
      description: managementClusterId may be supplied as the clusterId query parameter instead.
      required: [storageConfigId, ingestHostname]
      properties:
        managementClusterId: {type: string, format: uuid}
        namespace: {type: string}
        releaseName: {type: string}
        chartVersion: {type: string}
        storageConfigId: {type: string, format: uuid}
        objectStorageSecretName: {type: string}
        ingestHostname: {type: string}
        storageClass: {type: string}
        walStorageSize: {type: string}
        mode: {type: string, enum: [singleBinary, simpleScalable]}
        retention: {type: string}
        skipDiskCheck: {type: boolean}
        autoRollbackOnFailure: {type: boolean}
    UpdateClusterMonitoringConfigRequest:
      type: object
      additionalProperties: false
      properties:
        backendId: {type: string, format: uuid}
        clusterLabel: {type: string}
        clusterLabelValue: {type: string}
        scrapeIntervalSeconds: {type: integer, format: int32, minimum: 0}
        retention: {type: string}
        stackNamespace: {type: string}
        prometheusReleaseName: {type: string}
        thanosSidecarEnabled: {type: boolean}
        storageConfigId: {type: string, format: uuid}
        objectStorageSecretName: {type: string}
        storageClass: {type: string}
        storageSize: {type: string}
        status: {type: string}
    UpdateMonitoringBackendRequest:
      type: object
      additionalProperties: false
      required: [queryUrl]
      properties:
        backendType: {type: string}
        queryUrl: {type: string, format: uri}
        alertmanagerUrl: {type: string, format: uri}
        tenantId: {type: string}
        authType: {type: string}
        authConfig: {type: object, additionalProperties: true, writeOnly: true}
        defaultStepSeconds: {type: integer, format: int32, minimum: 0}
        timeoutSeconds: {type: integer, format: int32, minimum: 0}
        defaultAutoRollbackOnFailure: {type: boolean}
        maxRetryAttempts: {type: integer, format: int32, minimum: 0}
    MonitoringOperationEvent:
      type: object
      additionalProperties: false
      required: [id, level, stage, message, detail, createdAt]
      properties:
        id: {type: string, format: uuid}
        level: {type: string}
        stage: {type: string}
        message: {type: string}
        detail: {type: object, additionalProperties: true}
        createdAt: {type: string, format: date-time}
    MonitoringOperation:
      type: object
      additionalProperties: false
      required: [id, targetType, targetKey, operationType, status, attemptCount, errorMessage, createdAt, updatedAt]
      properties:
        id: {type: string, format: uuid}
        targetType: {type: string, enum: [cluster_stack, shared_thanos, shared_alertmanager, shared_grafana, shared_loki]}
        targetKey: {type: string}
        operationType: {type: string, enum: [install, upgrade, replace, uninstall]}
        status: {type: string, enum: [pending, running, completed, failed, superseded]}
        attemptCount: {type: integer, format: int32, minimum: 0}
        startedAt: {type: string, format: date-time, nullable: true}
        completedAt: {type: string, format: date-time, nullable: true}
        errorMessage: {type: string}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
        events: {type: array, items: {$ref: '#/components/schemas/MonitoringOperationEvent'}}
    MonitoringObservedRelease:
      type: object
      additionalProperties: false
      required: [clusterId, namespace, releaseName, observedAt, status]
      properties:
        clusterId: {type: string, format: uuid}
        namespace: {type: string}
        releaseName: {type: string}
        observedAt: {type: string, format: date-time}
        status: {type: string}
        revision: {type: integer, format: int32}
        error: {type: string}
    MonitoringStackPreview:
      type: object
      additionalProperties: false
      required: [clusterId, chart, values, desiredSpecHash, requiresReplace, replaceReasons]
      properties:
        clusterId: {type: string, format: uuid}
        chart:
          type: object
          additionalProperties: false
          required: [repoUrl, chartName]
          properties:
            repoUrl: {type: string, format: uri}
            chartName: {type: string}
        values: {type: object, additionalProperties: true}
        desiredSpecHash: {type: string}
        requiresReplace: {type: boolean}
        replaceReasons: {type: array, nullable: true, items: {type: string}}
    MonitoringStackStatus:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status: {type: string}
        managementClusterId: {type: string, format: uuid}
        namespace: {type: string}
        releaseName: {type: string}
        chartVersion: {type: string}
        desiredSpecHash: {type: string}
        observedRelease: {$ref: '#/components/schemas/MonitoringObservedRelease'}
        drifted: {type: boolean}
        driftReasons: {type: array, items: {type: string}}
        pods: {type: integer, minimum: 0}
        operation: {$ref: '#/components/schemas/MonitoringOperation'}
        retention: {type: string}
        thanosSidecarEnabled: {type: boolean}
        storageConfigId: {type: string, format: uuid, nullable: true}
        objectStorageSecretName: {type: string}
        storageClass: {type: string}
        storageSize: {type: string}
        lastObservedStatus: {type: string}
        lastObservedRevision: {type: integer, format: int32}
        lastObservedAt: {type: string, format: date-time, nullable: true}
        lastDriftDetectedAt: {type: string, format: date-time, nullable: true}
        lastHealthyAt: {type: string, format: date-time, nullable: true}
        queryReplicas: {type: integer, format: int32}
        storeGatewayReplicas: {type: integer, format: int32}
        compactorReplicas: {type: integer, format: int32}
        replicas: {type: integer, format: int32}
        ingressHost: {type: string}
        logDatasourceUrl: {type: string, format: uri}
        grafanaHost: {type: string}
        authMode: {type: string}
        autoRollbackOnFailure: {type: boolean}
        managedAssetHashes: {type: object, additionalProperties: true}
        alertingAssetHashes: {type: object, additionalProperties: true}
        ingestHostname: {type: string}
        ingestPublic: {type: boolean}
        walStorageSize: {type: string}
        mode: {type: string}
        skipDiskCheck: {type: boolean}
        computedLokiPrefix: {type: string}
        lastSizerVerdict: {type: object, additionalProperties: true}
        queryUrl: {type: string}
        authUrl: {type: string}
    ClusterMonitoringConfig:
      type: object
      additionalProperties: false
      required: [id, clusterId, backendId, clusterLabel, clusterLabelValue, scrapeIntervalSeconds, retention, stackNamespace, prometheusReleaseName, thanosSidecarEnabled, objectStorageSecretName, storageClass, storageSize, lastAppliedSpecHash, lastObservedStatus, lastObservedRevision, status]
      properties:
        id: {type: string, format: uuid}
        clusterId: {type: string, format: uuid}
        backendId: {type: string, format: uuid}
        clusterLabel: {type: string}
        clusterLabelValue: {type: string}
        scrapeIntervalSeconds: {type: integer, format: int32}
        retention: {type: string}
        stackNamespace: {type: string}
        prometheusReleaseName: {type: string}
        thanosSidecarEnabled: {type: boolean}
        storageConfigId: {type: string, format: uuid, nullable: true}
        objectStorageSecretName: {type: string}
        storageClass: {type: string}
        storageSize: {type: string}
        lastAppliedSpecHash: {type: string}
        lastObservedStatus: {type: string}
        lastObservedRevision: {type: integer, format: int32}
        lastObservedAt: {type: string, format: date-time, nullable: true}
        lastDriftDetectedAt: {type: string, format: date-time, nullable: true}
        status: {type: string}
        lastHealthyAt: {type: string, format: date-time, nullable: true}
    MonitoringBackend:
      type: object
      additionalProperties: false
      required: [id, name, backendType, queryUrl, alertmanagerUrl, tenantId, authType, authConfig, authConfigKeys, operationPolicies, defaultStepSeconds, timeoutSeconds, isDefault]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        backendType: {type: string}
        queryUrl: {type: string}
        alertmanagerUrl: {type: string}
        tenantId: {type: string}
        authType: {type: string}
        authConfig: {type: object, additionalProperties: true, readOnly: true}
        authConfigKeys: {type: array, items: {type: string}, readOnly: true}
        operationPolicies: {type: object, additionalProperties: true}
        defaultStepSeconds: {type: integer, format: int32}
        timeoutSeconds: {type: integer, format: int32}
        isDefault: {type: boolean}
    MonitoringSizerVerdict:
      type: object
      additionalProperties: false
      required: [result, reasons]
      properties:
        result: {type: string}
        mode: {type: string, nullable: true}
        reasons: {type: array, items: {type: string}}
        warnings: {type: array, items: {type: string}}
    MonitoringSizerObjectStorage:
      type: object
      additionalProperties: false
      required: [configured, storageConfigId, computedLokiPrefix]
      properties:
        configured: {type: boolean}
        storageConfigId: {type: string}
        computedLokiPrefix: {type: string}
    MonitoringSizerResponse:
      type: object
      additionalProperties: false
      required: [managementClusterId, isLocal, kubernetesVersion, nodes, requestsInUse, podListTruncated, leftover, reserve, usable, storageClass, objectStorage, connectedClusters, thanos, estimates, skipDiskCheck, verdicts, caps]
      properties:
        managementClusterId: {type: string}
        isLocal: {type: boolean}
        kubernetesVersion: {type: string}
        nodes: {type: object, additionalProperties: true}
        requestsInUse: {type: object, additionalProperties: true}
        podListTruncated: {type: boolean}
        leftover: {type: object, additionalProperties: true}
        reserve: {type: object, additionalProperties: true}
        usable: {type: object, additionalProperties: true}
        storageClass: {type: object, additionalProperties: true}
        objectStorage: {$ref: '#/components/schemas/MonitoringSizerObjectStorage'}
        connectedClusters: {type: integer, minimum: 0}
        thanos: {type: object, additionalProperties: true}
        estimates: {type: object, additionalProperties: true}
        skipDiskCheck: {type: boolean}
        verdicts:
          type: object
          additionalProperties: true
          required: [grafana, loki, thanosReceive]
          properties:
            grafana: {$ref: '#/components/schemas/MonitoringSizerVerdict'}
            loki: {$ref: '#/components/schemas/MonitoringSizerVerdict'}
            thanosReceive: {$ref: '#/components/schemas/MonitoringSizerVerdict'}
        caps: {type: object, additionalProperties: true}
    MonitoringOperationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/MonitoringOperation'}
    MonitoringOperationPage:
      allOf:
        - {$ref: '#/components/schemas/PaginatedEnvelope'}
        - type: object
          properties:
            data: {type: array, items: {$ref: '#/components/schemas/MonitoringOperation'}}
    MonitoringStackPreviewEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/MonitoringStackPreview'}
    MonitoringStackStatusEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/MonitoringStackStatus'}
    ClusterMonitoringConfigEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data:
          description: Empty when this cluster has no monitoring configuration.
          oneOf:
            - {$ref: '#/components/schemas/ClusterMonitoringConfig'}
            - {type: object, additionalProperties: false, maxProperties: 0}
    MonitoringBackendEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data:
          description: Empty when no default monitoring backend is configured.
          oneOf:
            - {$ref: '#/components/schemas/MonitoringBackend'}
            - {type: object, additionalProperties: false, maxProperties: 0}
    MonitoringBackendPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/MonitoringBackend'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
    MonitoringMetricsEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data:
          type: object
          description: Cluster, node, or workload metrics keyed by metric name. Live backends may add provider-native series fields.
          additionalProperties: true
    LegacyMonitoringMetricsResult:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status: {type: string, enum: [success, error]}
        data: {type: object, additionalProperties: true}
        error: {type: string}
    LegacyMonitoringMetricsEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/LegacyMonitoringMetricsResult'}
    MonitoringSizerEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/MonitoringSizerResponse'}
    DashboardWidgetRequest:
      type: object
      additionalProperties: false
      required: [name, widget_type, scope]
      properties:
        name: {type: string, minLength: 1, maxLength: 128}
        description: {type: string}
        widget_type: {type: string, enum: [grafana_panel, prom_sparkline, prom_stat, url_iframe]}
        spec: {type: object, additionalProperties: true}
        scope: {type: string, enum: [global, cluster, project]}
        scope_ids: {type: array, items: {type: string, format: uuid}}
        grid:
          type: object
          additionalProperties: false
          properties:
            x: {type: integer, format: int32, minimum: 0}
            y: {type: integer, format: int32, minimum: 0}
            w: {type: integer, format: int32, minimum: 0}
            h: {type: integer, format: int32, minimum: 0}
        refresh_seconds: {type: integer, format: int32, minimum: 0}
        enabled: {type: boolean}
    DashboardWidgetGrid:
      type: object
      additionalProperties: false
      required: [x, y, w, h]
      properties:
        x: {type: integer, format: int32, minimum: 0}
        y: {type: integer, format: int32, minimum: 0}
        w: {type: integer, format: int32, minimum: 0}
        h: {type: integer, format: int32, minimum: 0}
    DashboardWidget:
      type: object
      additionalProperties: false
      required: [id, name, description, widget_type, spec, scope, scope_ids, grid, refresh_seconds, enabled, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        description: {type: string}
        widget_type: {type: string, enum: [grafana_panel, prom_sparkline, prom_stat, url_iframe]}
        spec: {type: object, additionalProperties: true}
        scope: {type: string, enum: [global, cluster, project]}
        scope_ids: {type: array, items: {type: string, format: uuid}}
        grid: {$ref: '#/components/schemas/DashboardWidgetGrid'}
        refresh_seconds: {type: integer, format: int32, minimum: 0}
        enabled: {type: boolean}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    DashboardWidgetEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DashboardWidget'}
    DashboardWidgetPage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/DashboardWidget'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    PrometheusDatasource:
      type: object
      additionalProperties: false
      required: [id, name, url, has_auth, tls_skip_verify, enabled, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        url: {type: string, format: uri}
        has_auth: {type: boolean}
        tls_skip_verify: {type: boolean}
        enabled: {type: boolean}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    PrometheusDatasourceEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/PrometheusDatasource'}
    PrometheusDatasourcePage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/PrometheusDatasource'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    PrometheusDatasourceTestResult:
      type: object
      additionalProperties: false
      required: [ok, message]
      properties:
        ok: {type: boolean}
        message: {type: string}
    PrometheusDatasourceTestResultEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/PrometheusDatasourceTestResult'}
    RenderedDashboardWidgetData:
      type: object
      additionalProperties: false
      properties:
        sparkline_svg: {type: string}
        stat_value: {type: number, format: double}
        stat_unit: {type: string}
        stat_format: {type: string}
        stat_ok: {type: boolean}
        error: {type: string}
    RenderedDashboardWidget:
      type: object
      additionalProperties: false
      required: [id, name, widget_type, spec_resolved, grid, refresh_seconds]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        widget_type: {type: string, enum: [grafana_panel, prom_sparkline, prom_stat, url_iframe]}
        spec_resolved: {type: object, additionalProperties: true}
        grid: {$ref: '#/components/schemas/DashboardWidgetGrid'}
        refresh_seconds: {type: integer, format: int32, minimum: 0}
        data: {$ref: '#/components/schemas/RenderedDashboardWidgetData'}
    RenderedDashboardWidgetListEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/RenderedDashboardWidget'}}
    GitOpsSourceRequest:
      type: object
      additionalProperties: false
      description: name and repo_url are required on create; omitted update fields preserve or default according to the endpoint contract.
      properties:
        name: {type: string}
        repo_url: {type: string, format: uri}
        branch: {type: string}
        path_prefix: {type: string}
        auth_mode: {type: string, enum: [none, https_token, ssh_key]}
        auth: {type: string, writeOnly: true}
        sync_mode: {type: string, enum: [manual, interval]}
        sync_interval_seconds: {type: integer, format: int32, minimum: 0}
        on_delete: {type: string, enum: [log, tombstone, decommission]}
        enabled: {type: boolean}
        allow_mass_decommission: {type: boolean}
    GitOpsSource:
      type: object
      additionalProperties: false
      required: [id, name, repo_url, branch, path_prefix, auth_mode, auth, auth_configured, sync_mode, sync_interval_seconds, on_delete, enabled, allow_mass_decommission, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        repo_url: {type: string, format: uri}
        branch: {type: string}
        path_prefix: {type: string}
        auth_mode: {type: string, enum: [none, https_token, ssh_key]}
        auth: {type: string, readOnly: true, description: Empty or the literal redaction sentinel '<encrypted>'; secret material is never returned.}
        auth_configured: {type: boolean}
        sync_mode: {type: string, enum: [manual, interval]}
        sync_interval_seconds: {type: integer, format: int32, minimum: 0}
        on_delete: {type: string, enum: [log, tombstone, decommission]}
        last_synced_at: {type: string, format: date-time}
        last_synced_sha: {type: string}
        last_error: {type: string}
        enabled: {type: boolean}
        allow_mass_decommission: {type: boolean}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    GitOpsSourceEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/GitOpsSource'}
    GitOpsSourcePage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/GitOpsSource'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    GitOpsManagedCluster:
      type: object
      additionalProperties: false
      required: [cluster_id, repo_path, last_yaml_sha, last_applied_at, status]
      properties:
        cluster_id: {type: string, format: uuid}
        cluster_name: {type: string}
        display_name: {type: string}
        repo_path: {type: string}
        last_yaml_sha: {type: string}
        last_applied_at: {type: string, format: date-time}
        status: {type: string, enum: [active, tombstoned]}
        tombstoned_at: {type: string, format: date-time}
    GitOpsManagedClusterPage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/GitOpsManagedCluster'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    GitOpsApplyPreview:
      type: object
      additionalProperties: false
      required: [cluster_name, repo_path, created, updated, no_op, template_bound, registries, tool_presets, restored_active]
      properties:
        cluster_name: {type: string}
        cluster_id: {type: string, format: uuid}
        repo_path: {type: string}
        created: {type: boolean}
        updated: {type: boolean}
        no_op: {type: boolean}
        template_bound: {type: boolean}
        registries: {type: array, items: {type: string}}
        tool_presets: {type: array, items: {type: string}}
        project: {type: string}
        restored_active: {type: boolean}
    GitOpsPreviewResult:
      type: object
      additionalProperties: false
      required: [head_sha, source_id, source_name, applies, would_miss, would_restore, on_delete_policy]
      properties:
        head_sha: {type: string}
        source_id: {type: string, format: uuid}
        source_name: {type: string}
        applies: {type: array, items: {$ref: '#/components/schemas/GitOpsApplyPreview'}}
        would_miss: {type: array, items: {type: string}}
        would_restore: {type: array, items: {type: string}}
        on_delete_policy: {type: string, enum: [log, tombstone, decommission]}
    GitOpsPreviewResultEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/GitOpsPreviewResult'}
    GitOpsSyncResult:
      type: object
      additionalProperties: false
      required: [status, task_id]
      properties:
        status: {type: string, enum: [queued]}
        task_id: {type: string, format: uuid}
    GitOpsSyncResultEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/GitOpsSyncResult'}
    GitOpsSyncReceipt:
      type: object
      additionalProperties: false
      required: [source_id, task_id, status]
      properties:
        source_id: {type: string, format: uuid}
        task_id: {type: string, format: uuid}
        status: {type: string, enum: [queued]}
    GitOpsSyncReceiptEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/GitOpsSyncReceipt'}
    PrometheusDatasourceRequest:
      type: object
      additionalProperties: false
      required: [name, url]
      properties:
        name: {type: string, minLength: 1, maxLength: 64}
        url: {type: string, format: uri, pattern: '^https?://'}
        basic_auth_user: {type: string, writeOnly: true}
        basic_auth_pass: {type: string, writeOnly: true}
        bearer_token: {type: string, writeOnly: true}
        tls_skip_verify: {type: boolean}
        enabled: {type: boolean}
    QuotaPlanRequest:
      type: object
      additionalProperties: false
      description: name is required on create and ignored on update, where the path name is authoritative.
      properties:
        name: {type: string}
        enforcement: {type: string, enum: [soft, hard]}
        description: {type: string}
        max_clusters_per_project: {type: integer, format: int32, minimum: 0}
        max_namespaces_per_project: {type: integer, format: int32, minimum: 0}
        max_members_per_project: {type: integer, format: int32, minimum: 0}
        max_projects_per_user: {type: integer, format: int32, minimum: 0}
        max_tokens_per_user: {type: integer, format: int32, minimum: 0}
        max_streams_per_user: {type: integer, format: int32, minimum: 0}
        max_total_clusters: {type: integer, format: int32, minimum: 0}
        max_total_users: {type: integer, format: int32, minimum: 0}
    QuotaPlan:
      type: object
      additionalProperties: false
      required: [name, enforcement, description, max_clusters_per_project, max_namespaces_per_project, max_members_per_project, max_projects_per_user, max_tokens_per_user, max_streams_per_user, max_total_clusters, max_total_users]
      properties:
        name: {type: string}
        enforcement: {type: string, enum: [soft, hard]}
        description: {type: string}
        max_clusters_per_project: {type: integer, format: int32, minimum: 0}
        max_namespaces_per_project: {type: integer, format: int32, minimum: 0}
        max_members_per_project: {type: integer, format: int32, minimum: 0}
        max_projects_per_user: {type: integer, format: int32, minimum: 0}
        max_tokens_per_user: {type: integer, format: int32, minimum: 0}
        max_streams_per_user: {type: integer, format: int32, minimum: 0}
        max_total_clusters: {type: integer, format: int32, minimum: 0}
        max_total_users: {type: integer, format: int32, minimum: 0}
    QuotaPlanEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/QuotaPlan'}
    QuotaPlanPage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/QuotaPlan'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    QuotaUsageGlobal:
      type: object
      additionalProperties: false
      required: [total_clusters, max_total_clusters, total_users, max_total_users]
      properties:
        total_clusters: {type: integer, format: int64, minimum: 0}
        max_total_clusters: {type: integer, format: int32, minimum: 0}
        total_users: {type: integer, format: int64, minimum: 0}
        max_total_users: {type: integer, format: int32, minimum: 0}
    ProjectQuotaOffender:
      type: object
      additionalProperties: false
      required: [project_id, project_name, quota_plan, limit, current, maximum, usage_pct]
      properties:
        project_id: {type: string, format: uuid}
        project_name: {type: string}
        quota_plan: {type: string}
        limit: {type: string, enum: [max_clusters_per_project, max_namespaces_per_project, max_members_per_project]}
        current: {type: integer, format: int64, minimum: 0}
        maximum: {type: integer, format: int32, minimum: 1}
        usage_pct: {type: number, format: double, minimum: 0}
    UserQuotaOffender:
      type: object
      additionalProperties: false
      required: [user_id, username, quota_plan, limit, current, maximum, usage_pct]
      properties:
        user_id: {type: string, format: uuid}
        username: {type: string}
        quota_plan: {type: string}
        limit: {type: string, enum: [max_projects_per_user, max_tokens_per_user]}
        current: {type: integer, format: int64, minimum: 0}
        maximum: {type: integer, format: int32, minimum: 1}
        usage_pct: {type: number, format: double, minimum: 0}
    QuotaUsageSnapshot:
      type: object
      additionalProperties: false
      required: [global, project_offenders, user_offenders]
      properties:
        global: {$ref: '#/components/schemas/QuotaUsageGlobal'}
        project_offenders: {type: array, items: {$ref: '#/components/schemas/ProjectQuotaOffender'}}
        user_offenders: {type: array, items: {$ref: '#/components/schemas/UserQuotaOffender'}}
    QuotaUsageSnapshotEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/QuotaUsageSnapshot'}
    ProjectQuotaDimensions:
      type: object
      additionalProperties: false
      required: [max_clusters_per_project, max_namespaces_per_project, max_members_per_project]
      properties:
        max_clusters_per_project: {type: integer, format: int64, minimum: 0}
        max_namespaces_per_project: {type: integer, format: int64, minimum: 0}
        max_members_per_project: {type: integer, format: int64, minimum: 0}
    UserQuotaDimensions:
      type: object
      additionalProperties: false
      required: [max_projects_per_user, max_tokens_per_user, max_streams_per_user]
      properties:
        max_projects_per_user: {type: integer, format: int64, minimum: 0}
        max_tokens_per_user: {type: integer, format: int64, minimum: 0}
        max_streams_per_user: {type: integer, format: int64, minimum: 0}
    ProjectQuotaPercentages:
      type: object
      additionalProperties: false
      required: [max_clusters_per_project, max_namespaces_per_project, max_members_per_project]
      properties:
        max_clusters_per_project: {type: number, format: double, minimum: 0}
        max_namespaces_per_project: {type: number, format: double, minimum: 0}
        max_members_per_project: {type: number, format: double, minimum: 0}
    UserQuotaPercentages:
      type: object
      additionalProperties: false
      required: [max_projects_per_user, max_tokens_per_user, max_streams_per_user]
      properties:
        max_projects_per_user: {type: number, format: double, minimum: 0}
        max_tokens_per_user: {type: number, format: double, minimum: 0}
        max_streams_per_user: {type: number, format: double, minimum: 0}
    ProjectQuotaSnapshot:
      type: object
      additionalProperties: false
      required: [project_id, quota_plan, enforcement, limits, usage, usage_pct, overrides]
      properties:
        project_id: {type: string, format: uuid}
        quota_plan: {type: string}
        enforcement: {type: string, enum: [soft, hard]}
        limits: {$ref: '#/components/schemas/ProjectQuotaDimensions'}
        usage: {$ref: '#/components/schemas/ProjectQuotaDimensions'}
        usage_pct: {$ref: '#/components/schemas/ProjectQuotaPercentages'}
        overrides: {type: object, additionalProperties: true}
    ProjectQuotaSnapshotEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ProjectQuotaSnapshot'}
    UserQuotaSnapshot:
      type: object
      additionalProperties: false
      required: [user_id, quota_plan, enforcement, limits, usage, usage_pct, overrides]
      properties:
        user_id: {type: string, format: uuid}
        quota_plan: {type: string}
        enforcement: {type: string, enum: [soft, hard]}
        limits: {$ref: '#/components/schemas/UserQuotaDimensions'}
        usage: {$ref: '#/components/schemas/UserQuotaDimensions'}
        usage_pct: {$ref: '#/components/schemas/UserQuotaPercentages'}
        overrides: {type: object, additionalProperties: true}
    UserQuotaSnapshotEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/UserQuotaSnapshot'}
    PlatformSetting:
      type: object
      additionalProperties: false
      required: [key, value, description, type, default, is_default]
      properties:
        key: {type: string, minLength: 1}
        value: {}
        description: {type: string}
        type: {type: string, enum: [string, bool, int, enum]}
        default: {}
        updated_by: {type: string, format: uuid}
        updated_at: {type: string, format: date-time}
        is_default: {type: boolean}
    PlatformSettingEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/PlatformSetting'}
    PlatformSettingListEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data:
          type: array
          items: {$ref: '#/components/schemas/PlatformSetting'}
    PlatformSettingUpdateRequest:
      type: object
      additionalProperties: false
      required: [value]
      properties:
        value: {}
    PlatformSettingsBatchUpdateRequest:
      type: object
      additionalProperties: false
      required: [updates]
      properties:
        updates:
          type: object
          minProperties: 1
          additionalProperties: true
    AlertChannelRequest:
      type: object
      additionalProperties: false
      description: name is required on create; channel_type/type and configuration/config are compatibility pairs.
      properties:
        name: {type: string}
        channel_type: {type: string}
        type: {type: string}
        configuration: {type: object, additionalProperties: true}
        config: {type: object, additionalProperties: true}
        enabled: {type: boolean}
    AlertRuleRequest:
      type: object
      additionalProperties: false
      description: name is required on create; update accepts partial-compatible defaults from the existing rule.
      properties:
        name: {type: string}
        description: {type: string}
        cluster_id: {type: string, format: uuid, nullable: true}
        rule_type: {type: string}
        type: {type: string}
        configuration: {type: object, additionalProperties: true}
        query: {type: string}
        threshold: {type: number, nullable: true}
        duration: {type: string}
        labels: {type: object, additionalProperties: {type: string}}
        annotations: {type: object, additionalProperties: {type: string}}
        notificationChannelIds: {type: array, items: {type: string}}
        severity: {type: string}
        enabled: {type: boolean}
        cooldown_minutes: {type: integer, format: int32}
        rule_kind: {type: string, enum: [threshold, anomaly]}
        metric: {type: string}
        anomaly_stddev: {type: number, nullable: true}
        anomaly_window_seconds: {type: integer, format: int32, nullable: true}
        anomaly_min_samples: {type: integer, format: int32, nullable: true}
        anomaly_direction: {type: string, enum: [above, below, either]}
    AlertSilenceRequest:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        rule_id: {type: string, format: uuid, nullable: true}
        cluster_id: {type: string, format: uuid, nullable: true}
        reason: {type: string, minLength: 1}
        starts_at: {type: string, format: date-time}
        ends_at: {type: string, format: date-time}
        duration: {type: string}
        matchers: {type: object, additionalProperties: {type: string}}
    AlertRule:
      type: object
      additionalProperties: false
      required: [id, name, description, type, severity, clusterId, clusterName, namespace, enabled, query, threshold, duration, activeAlerts, labels, annotations, notificationChannelIds, ruleKind, metric, anomalyStddev, anomalyWindowSeconds, anomalyMinSamples, anomalyDirection, createdAt, updatedAt]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        description: {type: string}
        type: {type: string, description: Evaluator type such as promql, threshold, absence, deadman, or change.}
        severity: {type: string, enum: [critical, warning, info]}
        clusterId: {type: string, format: uuid, nullable: true}
        clusterName: {type: string, nullable: true}
        namespace: {type: string}
        enabled: {type: boolean}
        query: {type: string}
        threshold: {type: number, nullable: true}
        duration: {type: string}
        activeAlerts: {type: integer, minimum: 0}
        labels: {type: object, additionalProperties: {type: string}}
        annotations: {type: object, additionalProperties: {type: string}}
        notificationChannelIds: {type: array, items: {type: string, format: uuid}}
        ruleKind: {type: string, enum: [threshold, anomaly]}
        metric: {type: string}
        anomalyStddev: {type: number, nullable: true}
        anomalyWindowSeconds: {type: number, nullable: true}
        anomalyMinSamples: {type: number, nullable: true}
        anomalyDirection: {type: string, enum: ['', above, below, either]}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
    AlertRuleEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/AlertRule'}
    AlertRulePage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/AlertRule'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    AlertEvent:
      type: object
      additionalProperties: false
      required: [id, ruleId, ruleName, severity, status, message, clusterId, clusterName, namespace, resource, labels, firedAt, acknowledgedAt, acknowledgedBy, resolvedAt, resolvedBy]
      properties:
        id: {type: string, format: uuid}
        ruleId: {type: string, format: uuid}
        ruleName: {type: string}
        severity: {type: string, enum: [critical, warning, info]}
        status: {type: string, enum: [firing, acknowledged, resolved, silenced]}
        message: {type: string}
        clusterId: {type: string, format: uuid, nullable: true}
        clusterName: {type: string, nullable: true}
        namespace: {type: string}
        resource: {type: string}
        labels: {type: object, additionalProperties: {type: string}}
        firedAt: {type: string, format: date-time}
        acknowledgedAt: {type: string, format: date-time, nullable: true}
        acknowledgedBy: {type: string, format: uuid, nullable: true}
        resolvedAt: {type: string, format: date-time, nullable: true}
        resolvedBy: {type: string, format: uuid, nullable: true}
    AlertEventEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/AlertEvent'}
    AlertEventPage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/AlertEvent'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    AlertSilence:
      type: object
      additionalProperties: false
      required: [id, reason, matchers, startsAt, endsAt, duration, createdBy, createdAt]
      properties:
        id: {type: string, format: uuid}
        reason: {type: string}
        matchers: {type: object, additionalProperties: {type: string}}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        duration: {type: string}
        createdBy: {type: string, format: uuid, nullable: true}
        createdAt: {type: string, format: date-time}
    AlertSilenceEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/AlertSilence'}
    AlertSilencePage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/AlertSilence'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    AnomalyBaseline:
      type: object
      additionalProperties: false
      required: [id, clusterId, metric, windowSeconds, sampleCount, mean, stddev, min, max, p50, p95, p99, lastValue, lastValueAt, updatedAt, recentSampleBytes]
      properties:
        id: {type: string, format: uuid}
        clusterId: {type: string, format: uuid}
        metric: {type: string}
        windowSeconds: {type: integer, format: int32, minimum: 1}
        sampleCount: {type: integer, format: int32, minimum: 0}
        mean: {type: number}
        stddev: {type: number, minimum: 0}
        min: {type: number}
        max: {type: number}
        p50: {type: number}
        p95: {type: number}
        p99: {type: number}
        lastValue: {type: number}
        lastValueAt: {type: string, format: date-time, nullable: true}
        updatedAt: {type: string, format: date-time}
        recentSampleBytes: {type: integer, minimum: 0, description: Byte length of the retained internal sample ring, not the sample data itself.}
    AnomalyBaselineEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/AnomalyBaseline'}
    AnomalyBaselinePage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/AnomalyBaseline'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    NotificationChannelTestResult:
      type: object
      additionalProperties: false
      required: [success, message]
      properties:
        success: {type: boolean}
        message: {type: string}
    NotificationChannelTestResultEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/NotificationChannelTestResult'}
    PublicBrandingSettings:
      type: object
      additionalProperties: false
      required: [branding.product_name, branding.logo_url, branding.primary_color, branding.support_url, branding.copyright]
      properties:
        'branding.product_name': {type: string}
        'branding.logo_url': {type: string}
        'branding.primary_color': {type: string, pattern: '^#[0-9A-Fa-f]{6}$'}
        'branding.support_url': {type: string}
        'branding.copyright': {type: string}
    PublicBrandingSettingsEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/PublicBrandingSettings'}
    PublicBannerSettings:
      type: object
      additionalProperties: false
      required: [banner.login_text, banner.global_text, banner.global_color]
      properties:
        'banner.login_text': {type: string}
        'banner.global_text': {type: string}
        'banner.global_color': {type: string, enum: [info, warning, critical]}
    PublicBannerSettingsEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/PublicBannerSettings'}
    PublicRegistrationSettings:
      type: object
      additionalProperties: false
      required: [registration.tls_mode, registration.ca_bundle]
      properties:
        'registration.tls_mode': {type: string, enum: [public_ca, private_ca, insecure]}
        'registration.ca_bundle': {type: string}
    PublicRegistrationSettingsEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/PublicRegistrationSettings'}
    SSOPresetField:
      type: object
      additionalProperties: false
      required: [name, label, placeholder, kind, required]
      properties:
        name: {type: string}
        label: {type: string}
        placeholder: {type: string}
        kind: {type: string, enum: [text, url, tenant]}
        required: {type: boolean}
    SSOPreset:
      type: object
      additionalProperties: false
      required: [key, display_name, type, default_scopes, callback_path_hint, required_fields, logo_slug]
      properties:
        key: {type: string, enum: [github, google, azure-ad, gitlab, okta]}
        display_name: {type: string}
        type: {type: string, enum: [oauth2, oidc]}
        issuer_url_template: {type: string}
        default_scopes: {type: array, items: {type: string}}
        callback_path_hint: {type: string}
        required_fields: {type: array, items: {$ref: '#/components/schemas/SSOPresetField'}}
        docs_url: {type: string, format: uri}
        logo_slug: {type: string}
    SSOPresetListEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/SSOPreset'}}
    ControlPlanePolicyRequest:
      type: object
      additionalProperties: false
      properties:
        monitoringQueueDepthThreshold: {type: integer, format: int32}
        deliveryQueueDepthThreshold: {type: integer, format: int32}
        toolsQueueDepthThreshold: {type: integer, format: int32}
        catalogQueueDepthThreshold: {type: integer, format: int32}
        monitoringStaleRunningThreshold: {type: integer, format: int32}
        deliveryStaleRunningThreshold: {type: integer, format: int32}
        toolsStaleRunningThreshold: {type: integer, format: int32}
        catalogStaleRunningThreshold: {type: integer, format: int32}
        monitoringRecentFailureThreshold: {type: integer, format: int32}
        deliveryRecentFailureThreshold: {type: integer, format: int32}
        toolsRecentFailureThreshold: {type: integer, format: int32}
        catalogRecentFailureThreshold: {type: integer, format: int32}
        recentFailureWindowMinutes: {type: integer, format: int32}
    ControlPlanePolicy:
      type: object
      additionalProperties: false
      required:
        - monitoringQueueDepthThreshold
        - deliveryQueueDepthThreshold
        - toolsQueueDepthThreshold
        - catalogQueueDepthThreshold
        - monitoringStaleRunningThreshold
        - deliveryStaleRunningThreshold
        - toolsStaleRunningThreshold
        - catalogStaleRunningThreshold
        - monitoringRecentFailureThreshold
        - deliveryRecentFailureThreshold
        - toolsRecentFailureThreshold
        - catalogRecentFailureThreshold
        - recentFailureWindowMinutes
      properties:
        monitoringQueueDepthThreshold: {type: integer, format: int32, minimum: 1}
        deliveryQueueDepthThreshold: {type: integer, format: int32, minimum: 1}
        toolsQueueDepthThreshold: {type: integer, format: int32, minimum: 1}
        catalogQueueDepthThreshold: {type: integer, format: int32, minimum: 1}
        monitoringStaleRunningThreshold: {type: integer, format: int32, minimum: 1}
        deliveryStaleRunningThreshold: {type: integer, format: int32, minimum: 1}
        toolsStaleRunningThreshold: {type: integer, format: int32, minimum: 1}
        catalogStaleRunningThreshold: {type: integer, format: int32, minimum: 1}
        monitoringRecentFailureThreshold: {type: integer, format: int32, minimum: 1}
        deliveryRecentFailureThreshold: {type: integer, format: int32, minimum: 1}
        toolsRecentFailureThreshold: {type: integer, format: int32, minimum: 1}
        catalogRecentFailureThreshold: {type: integer, format: int32, minimum: 1}
        recentFailureWindowMinutes: {type: integer, format: int32, minimum: 1}
    ControlPlanePolicyEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ControlPlanePolicy'}
    ControlPlaneAlert:
      type: object
      additionalProperties: false
      required: [id, controller, conditionType, status, message, detail, firedAt, createdAt, updatedAt]
      properties:
        id: {type: string, format: uuid}
        controller: {type: string}
        conditionType: {type: string}
        status: {type: string}
        message: {type: string}
        detail: {type: object, additionalProperties: true}
        firedAt: {type: string, format: date-time}
        resolvedAt: {type: string, format: date-time, nullable: true}
        acknowledgedAt: {type: string, format: date-time, nullable: true}
        acknowledgedBy: {type: string, format: uuid, nullable: true}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
    ControlPlaneAlertEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ControlPlaneAlert'}
    ControlPlaneAlertPage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ControlPlaneAlert'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    ControlPlaneSilence:
      type: object
      additionalProperties: false
      required: [id, controller, conditionType, reason, startsAt, endsAt, createdAt]
      properties:
        id: {type: string, format: uuid}
        controller: {type: string}
        conditionType: {type: string}
        reason: {type: string}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        createdBy: {type: string, format: uuid, nullable: true}
        createdAt: {type: string, format: date-time}
    ControlPlaneSilenceEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ControlPlaneSilence'}
    ControlPlaneSilencePage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ControlPlaneSilence'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    ControlPlaneSummary:
      type: object
      additionalProperties: false
      required: [controllers, queueDepth, staleRunningCount, controllersWithFailures, degradedControllers, health]
      properties:
        controllers: {type: integer, minimum: 0}
        queueDepth: {type: integer, minimum: 0}
        staleRunningCount: {type: integer, minimum: 0}
        controllersWithFailures: {type: integer, minimum: 0}
        degradedControllers: {type: integer, minimum: 0}
        health: {type: string, enum: [healthy, degraded]}
    ControlPlaneStatus:
      type: object
      additionalProperties: false
      required: [policy, summary]
      properties:
        monitoring: {type: object, additionalProperties: true}
        tools: {type: object, additionalProperties: true}
        catalog: {type: object, additionalProperties: true}
        backups: {type: object, additionalProperties: true}
        logging: {type: object, additionalProperties: true}
        security: {type: object, additionalProperties: true}
        policy: {$ref: '#/components/schemas/ControlPlanePolicy'}
        summary: {$ref: '#/components/schemas/ControlPlaneSummary'}
    ControlPlaneStatusEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ControlPlaneStatus'}
    ControlPlaneSilenceRequest:
      type: object
      additionalProperties: false
      required: [controller, reason]
      properties:
        controller: {type: string, minLength: 1}
        conditionType: {type: string}
        reason: {type: string, minLength: 1}
        duration: {type: string}
    NativeRBACRuleRequest:
      type: object
      additionalProperties: false
      required: [userId, resource, verbs]
      properties:
        userId: {type: string, format: uuid}
        clusterId: {type: string, format: uuid}
        namespace: {type: string}
        apiGroup: {type: string}
        resource: {type: string, minLength: 1}
        verbs: {type: array, minItems: 1, items: {type: string, enum: [read, list, watch, create, update, delete, '*']}}
    NativeRBACRule:
      type: object
      additionalProperties: false
      required: [id, userId, namespace, apiGroup, resource, verbs, createdAt]
      properties:
        id: {type: string, format: uuid}
        userId: {type: string, format: uuid}
        clusterId: {type: string, format: uuid}
        namespace: {type: string}
        apiGroup: {type: string}
        resource: {type: string}
        verbs: {type: array, items: {type: string, enum: [read, list, watch, create, update, delete, '*']}}
        createdAt: {type: string, format: date-time}
        createdBy: {type: string, format: uuid}
    NativeRBACRuleEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/NativeRBACRule'}
    NativeRBACRuleListEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/NativeRBACRule'}}
    GrafanaTicketRedeemRequest:
      type: object
      additionalProperties: false
      required: [ticket]
      properties:
        ticket: {type: string, minLength: 1, writeOnly: true}
    GrafanaTicketIdentity:
      type: object
      additionalProperties: false
      required: [email, role, ttl, explore, admin, clusterIds]
      properties:
        email: {type: string, format: email}
        role: {type: string, enum: [Admin, Editor, Viewer]}
        ttl: {type: integer, format: int64, minimum: 1, description: Grafana session cookie lifetime in nanoseconds.}
        explore: {type: boolean}
        admin: {type: boolean}
        clusterIds: {type: array, items: {type: string, format: uuid}, nullable: true}
    GrafanaTicketIdentityEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/GrafanaTicketIdentity'}
    SCIMToken:
      type: object
      additionalProperties: false
      required: [id, name, prefix, last_used_at, created_at]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        prefix: {type: string}
        last_used_at: {type: string, format: date-time, nullable: true}
        created_at: {type: string, format: date-time}
    SCIMTokenCreated:
      type: object
      additionalProperties: false
      required: [id, name, prefix, last_used_at, created_at, token]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        prefix: {type: string}
        last_used_at: {type: string, format: date-time, nullable: true}
        created_at: {type: string, format: date-time}
        token:
          type: string
          description: Plaintext bearer token, returned only on create.
    SCIMTokenCreatedEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/SCIMTokenCreated'}
    SCIMTokenList:
      type: object
      additionalProperties: false
      required: [tokens]
      properties:
        tokens: {type: array, items: {$ref: '#/components/schemas/SCIMToken'}}
    SCIMTokenListEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/SCIMTokenList'}
    SCIMGroupCreateRequest:
      type: object
      additionalProperties: false
      required: [displayName]
      properties:
        displayName: {type: string, minLength: 1}
        members: {type: array, items: {$ref: '#/components/schemas/SCIMGroupMember'}}
        roleId: {type: string, format: uuid}
    SCIMGroupPatchOperation:
      type: object
      additionalProperties: false
      required: [op]
      properties:
        op: {type: string, enum: [add, remove, replace]}
        path: {type: string}
        value: {}
    SCIMGroupPatchRequest:
      type: object
      additionalProperties: false
      required: [Operations]
      properties:
        schemas: {type: array, items: {type: string}}
        Operations: {type: array, items: {$ref: '#/components/schemas/SCIMGroupPatchOperation'}}
    ClusterSecurityPolicyEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ClusterSecurityPolicy'}
    ControllerReconcilerStatus:
      type: object
      additionalProperties: false
      required: [enabled]
      properties:
        enabled: {type: boolean}
        queueDepth: {type: integer, minimum: 0}
        staleRunningCount: {type: integer, minimum: 0}
        staleThresholdSecond: {type: integer, minimum: 0}
    SecurityControllerStatus:
      type: object
      additionalProperties: false
      required: [reconciler, health, healthReasons, templates, policies, scans]
      properties:
        reconciler: {$ref: '#/components/schemas/ControllerReconcilerStatus'}
        health: {type: string, enum: [healthy, degraded]}
        healthReasons: {type: array, items: {type: string}}
        templates:
          type: object
          additionalProperties: false
          required: [total]
          properties:
            total: {type: integer, minimum: 0}
        policies:
          type: object
          additionalProperties: false
          required: [total, failedCount, statuses]
          properties:
            total: {type: integer, minimum: 0}
            failedCount: {type: integer, minimum: 0}
            statuses: {type: object, additionalProperties: {type: integer, minimum: 0}}
        scans:
          type: object
          additionalProperties: false
          required: [total, runningCount, failedCount, statuses]
          properties:
            total: {type: integer, minimum: 0}
            runningCount: {type: integer, minimum: 0}
            failedCount: {type: integer, minimum: 0}
            statuses: {type: object, additionalProperties: {type: integer, minimum: 0}}
    SecurityControllerStatusEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/SecurityControllerStatus'}
    ToolControllerStatus:
      type: object
      additionalProperties: false
      required: [reconciler, tools, operations, recentFailureCount, recentOperations, latestFailure]
      properties:
        reconciler: {$ref: '#/components/schemas/ControllerReconcilerStatus'}
        tools:
          type: object
          additionalProperties: false
          required: [catalogCount, installedCount]
          properties:
            catalogCount: {type: integer, format: int64, minimum: 0}
            installedCount: {type: integer, format: int64, minimum: 0, nullable: true}
        operations: {type: object, additionalProperties: {type: integer, minimum: 0}}
        recentFailureCount: {type: integer, minimum: 0}
        recentOperations:
          type: array
          items:
            type: object
            description: Tool operation preview; may include a bounded eventsPreview array.
            additionalProperties: true
        latestFailure:
          type: object
          nullable: true
          additionalProperties: true
    ToolControllerStatusEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ToolControllerStatus'}
    CompliancePostureIssue:
      type: object
      additionalProperties: false
      required: [cluster_id, cluster_name, severity, category, message]
      properties:
        cluster_id: {type: string, format: uuid}
        cluster_name: {type: string}
        severity: {type: string}
        category: {type: string}
        message: {type: string}
    ClusterCompliancePosture:
      type: object
      additionalProperties: false
      required: [cluster_id, cluster_name, overall_score, cis_score, vulns_score, netpol_score]
      properties:
        cluster_id: {type: string, format: uuid}
        cluster_name: {type: string}
        overall_score: {type: number, format: double, minimum: 0, maximum: 100}
        cis_score: {type: number, format: double, minimum: 0, maximum: 100}
        vulns_score: {type: number, format: double, minimum: 0, maximum: 100}
        netpol_score: {type: number, format: double, minimum: 0, maximum: 100}
    CompliancePosture:
      type: object
      additionalProperties: false
      required: [overall_score, cis_score, vulns_score, netpol_score, audit_score, cluster_count, clusters, top_issues, computed_at, audit_retention_months]
      properties:
        overall_score: {type: number, format: double, minimum: 0, maximum: 100}
        cis_score: {type: number, format: double, minimum: 0, maximum: 100}
        vulns_score: {type: number, format: double, minimum: 0, maximum: 100}
        netpol_score: {type: number, format: double, minimum: 0, maximum: 100}
        audit_score: {type: number, format: double, minimum: 0, maximum: 100}
        cluster_count: {type: integer, minimum: 0}
        clusters: {type: array, items: {$ref: '#/components/schemas/ClusterCompliancePosture'}}
        top_issues: {type: array, maxItems: 10, items: {$ref: '#/components/schemas/CompliancePostureIssue'}}
        computed_at: {type: string, format: date-time}
        audit_retention_months: {type: integer, minimum: 0}
    CompliancePostureEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/CompliancePosture'}
    VulnerabilitySeverityCounts:
      type: object
      additionalProperties: false
      required: [critical, high, medium, low, unknown]
      properties:
        critical: {type: integer, format: int64, minimum: 0}
        high: {type: integer, format: int64, minimum: 0}
        medium: {type: integer, format: int64, minimum: 0}
        low: {type: integer, format: int64, minimum: 0}
        unknown: {type: integer, format: int64, minimum: 0}
    VulnerabilityAggregate:
      type: object
      additionalProperties: false
      required: [critical, high, medium, low, unknown, report_count, last_scanned_at]
      properties:
        critical: {type: integer, format: int64, minimum: 0}
        high: {type: integer, format: int64, minimum: 0}
        medium: {type: integer, format: int64, minimum: 0}
        low: {type: integer, format: int64, minimum: 0}
        unknown: {type: integer, format: int64, minimum: 0}
        report_count: {type: integer, format: int64, minimum: 0}
        last_scanned_at: {type: string, format: date-time, nullable: true}
    VulnerabilityAggregateEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/VulnerabilityAggregate'}
    FleetVulnerabilityAggregate:
      type: object
      additionalProperties: false
      required: [critical, high, medium, low, unknown, report_count, cluster_count, last_scanned_at]
      properties:
        critical: {type: integer, format: int64, minimum: 0}
        high: {type: integer, format: int64, minimum: 0}
        medium: {type: integer, format: int64, minimum: 0}
        low: {type: integer, format: int64, minimum: 0}
        unknown: {type: integer, format: int64, minimum: 0}
        report_count: {type: integer, format: int64, minimum: 0}
        cluster_count: {type: integer, format: int64, minimum: 0}
        last_scanned_at: {type: string, format: date-time, nullable: true}
    FleetVulnerabilityAggregateEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/FleetVulnerabilityAggregate'}
    ImageVulnerabilityReport:
      type: object
      additionalProperties: false
      required: [id, cluster_id, report_name, namespace, workload_kind, workload_name, container_name, image_registry, image_repo, image_tag, image_digest, scanner, scanner_version, critical_count, high_count, medium_count, low_count, unknown_count, scanned_at, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid}
        report_name: {type: string}
        namespace: {type: string}
        workload_kind: {type: string}
        workload_name: {type: string}
        container_name: {type: string}
        image_registry: {type: string}
        image_repo: {type: string}
        image_tag: {type: string}
        image_digest: {type: string}
        scanner: {type: string}
        scanner_version: {type: string}
        critical_count: {type: integer, minimum: 0}
        high_count: {type: integer, minimum: 0}
        medium_count: {type: integer, minimum: 0}
        low_count: {type: integer, minimum: 0}
        unknown_count: {type: integer, minimum: 0}
        scanned_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    ImageVulnerability:
      type: object
      additionalProperties: false
      required: [id, report_id, vulnerability_id, severity, pkg_name, installed_version, fixed_version, primary_link, cvss_score, title, description, created_at]
      properties:
        id: {type: string, format: uuid}
        report_id: {type: string, format: uuid}
        vulnerability_id: {type: string}
        severity: {type: string}
        pkg_name: {type: string}
        installed_version: {type: string}
        fixed_version: {type: string}
        primary_link: {type: string}
        cvss_score: {type: number, format: double, nullable: true}
        title: {type: string}
        description: {type: string}
        created_at: {type: string, format: date-time}
    ImageVulnerabilityReportDetail:
      type: object
      additionalProperties: false
      required: [report, vulnerabilities, vulnerability_total, severity_filter, limit, offset]
      properties:
        report: {$ref: '#/components/schemas/ImageVulnerabilityReport'}
        vulnerabilities: {type: array, items: {$ref: '#/components/schemas/ImageVulnerability'}}
        vulnerability_total: {type: integer, format: int64, minimum: 0}
        severity_filter: {type: string}
        limit: {type: integer, minimum: 1, maximum: 500}
        offset: {type: integer, minimum: 0}
    ImageVulnerabilityReportDetailEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ImageVulnerabilityReportDetail'}
    VulnerabilityHistoryPoint:
      type: object
      additionalProperties: false
      required: [scanned_at, critical, high, medium, low, unknown, report_count]
      properties:
        scanned_at: {type: string, format: date-time}
        critical: {type: integer, format: int64, minimum: 0}
        high: {type: integer, format: int64, minimum: 0}
        medium: {type: integer, format: int64, minimum: 0}
        low: {type: integer, format: int64, minimum: 0}
        unknown: {type: integer, format: int64, minimum: 0}
        report_count: {type: integer, format: int64, minimum: 0}
    VulnerabilityHistory:
      type: object
      additionalProperties: false
      required: [cluster_id, since, snapshots, total_count]
      properties:
        cluster_id: {type: string, format: uuid}
        since: {type: string, format: date-time}
        snapshots: {type: array, items: {$ref: '#/components/schemas/VulnerabilityHistoryPoint'}}
        total_count: {type: integer, minimum: 0}
    VulnerabilityHistoryEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/VulnerabilityHistory'}
    VulnerabilityDiffBucket:
      type: object
      additionalProperties: false
      required: [critical, high, medium, low, unknown, scanned_at]
      properties:
        critical: {type: integer, minimum: 0}
        high: {type: integer, minimum: 0}
        medium: {type: integer, minimum: 0}
        low: {type: integer, minimum: 0}
        unknown: {type: integer, minimum: 0}
        scanned_at: {type: string, format: date-time}
    VulnerabilityDiffDelta:
      type: object
      additionalProperties: false
      required: [critical, high, medium, low, unknown]
      properties:
        critical: {type: integer}
        high: {type: integer}
        medium: {type: integer}
        low: {type: integer}
        unknown: {type: integer}
    VulnerabilityDiff:
      type: object
      additionalProperties: false
      required: [cluster_id, has_comparison, prior_hours]
      properties:
        cluster_id: {type: string, format: uuid}
        has_comparison: {type: boolean}
        prior_hours: {type: integer, minimum: 1, maximum: 8760}
        latest: {$ref: '#/components/schemas/VulnerabilityDiffBucket'}
        prior: {$ref: '#/components/schemas/VulnerabilityDiffBucket'}
        delta: {$ref: '#/components/schemas/VulnerabilityDiffDelta'}
    VulnerabilityDiffEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/VulnerabilityDiff'}
    VulnerabilityReportHistoryPoint:
      type: object
      additionalProperties: false
      required: [scanned_at, critical, high, medium, low, unknown]
      properties:
        scanned_at: {type: string, format: date-time}
        critical: {type: integer, minimum: 0}
        high: {type: integer, minimum: 0}
        medium: {type: integer, minimum: 0}
        low: {type: integer, minimum: 0}
        unknown: {type: integer, minimum: 0}
    VulnerabilityReportHistory:
      type: object
      additionalProperties: false
      required: [report_id, snapshots, total_count]
      properties:
        report_id: {type: string, format: uuid}
        snapshots: {type: array, items: {$ref: '#/components/schemas/VulnerabilityReportHistoryPoint'}}
        total_count: {type: integer, minimum: 0}
    VulnerabilityReportHistoryEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/VulnerabilityReportHistory'}
    VulnerabilityScanProgress:
      type: object
      additionalProperties: false
      required: [scanning, active_jobs, completed_jobs, failed_jobs, reports_count, trivy_operator_ready, last_scan_age_seconds]
      properties:
        scanning: {type: boolean}
        active_jobs: {type: integer, minimum: 0}
        completed_jobs: {type: integer, minimum: 0}
        failed_jobs: {type: integer, minimum: 0}
        reports_count: {type: integer, format: int64, minimum: 0}
        trivy_operator_ready: {type: boolean}
        last_scan_age_seconds: {type: integer, minimum: 0, nullable: true}
    VulnerabilityScanProgressEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/VulnerabilityScanProgress'}
    VulnerabilityRescanResult:
      type: object
      additionalProperties: false
      required: [operation_id, operation_url, cluster_id, status, requested_at]
      properties:
        operation_id: {type: string, format: uuid}
        operation_url: {type: string, pattern: '^/api/v1/workloads/operations/[0-9a-f-]+/$'}
        cluster_id: {type: string, format: uuid}
        status: {type: string, enum: [pending, running, completed, failed]}
        requested_at: {type: string, format: date-time}
    VulnerabilityRescanResultEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/VulnerabilityRescanResult'}
    VulnerableClusterSummary:
      type: object
      additionalProperties: false
      required: [cluster_id, critical, high, medium, low, report_count, last_scanned_at]
      properties:
        cluster_id: {type: string, format: uuid}
        critical: {type: integer, format: int64, minimum: 0}
        high: {type: integer, format: int64, minimum: 0}
        medium: {type: integer, format: int64, minimum: 0}
        low: {type: integer, format: int64, minimum: 0}
        report_count: {type: integer, format: int64, minimum: 0}
        last_scanned_at: {type: string, format: date-time, nullable: true}
    VulnerableClusterSummaryListEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/VulnerableClusterSummary'}}
    BackupDrillResult:
      type: object
      additionalProperties: false
      required: [id, started_at, finished_at, status, backup_key, schema_version, error_message, created_at]
      properties:
        id: {type: string, format: uuid}
        started_at: {type: string, format: date-time}
        finished_at: {type: string, format: date-time, nullable: true}
        status: {type: string, enum: [success, failure, partial, running]}
        backup_key: {type: string}
        schema_version: {type: integer, format: int32, nullable: true}
        error_message: {type: string}
        created_at: {type: string, format: date-time}
    BackupDrillLatestResponse:
      type: object
      additionalProperties: false
      required: [latest, latest_success, latest_success_age_seconds]
      properties:
        latest:
          nullable: true
          allOf: [{$ref: '#/components/schemas/BackupDrillResult'}]
        latest_success:
          nullable: true
          allOf: [{$ref: '#/components/schemas/BackupDrillResult'}]
        latest_success_age_seconds: {type: number, format: double, nullable: true}
    ManagementCronJobStatus:
      type: object
      additionalProperties: false
      required: [name, schedule, suspended]
      properties:
        name: {type: string}
        schedule: {type: string}
        suspended: {type: boolean}
        last_schedule_time: {type: string, format: date-time}
        last_successful_time: {type: string, format: date-time}
    ManagementBackupEndpoint:
      type: object
      additionalProperties: false
      required: [bucket, prefix, region]
      properties:
        bucket: {type: string}
        prefix: {type: string}
        region: {type: string}
        endpoint: {type: string}
    ManagementBackupRetention:
      type: object
      additionalProperties: false
      properties:
        daily: {type: string}
        weekly: {type: string}
        monthly: {type: string}
    ManagementBackupJob:
      type: object
      additionalProperties: false
      required: [name, succeeded, failed, active]
      properties:
        name: {type: string}
        start_time: {type: string, format: date-time}
        completion_time: {type: string, format: date-time}
        succeeded: {type: integer, format: int32}
        failed: {type: integer, format: int32}
        active: {type: integer, format: int32}
        duration_seconds: {type: integer, format: int64}
    ManagementBackupDestination:
      type: object
      additionalProperties: false
      required: [id, name, source, bucket, prefix, region, schedule, enabled, keep_daily, keep_weekly, keep_monthly, has_credentials, read_only]
      properties:
        id: {type: string}
        name: {type: string}
        source: {type: string, enum: [ui, helm]}
        bucket: {type: string}
        prefix: {type: string}
        region: {type: string}
        endpoint: {type: string}
        schedule: {type: string}
        enabled: {type: boolean}
        keep_daily: {type: integer, format: int32}
        keep_weekly: {type: integer, format: int32}
        keep_monthly: {type: integer, format: int32}
        has_credentials: {type: boolean}
        access_key: {type: string}
        secret_key: {type: string}
        cronjob: {$ref: '#/components/schemas/ManagementCronJobStatus'}
        last_job: {$ref: '#/components/schemas/ManagementBackupJob'}
        read_only: {type: boolean}
        desired_generation: {type: integer, format: int64}
        applied_generation: {type: integer, format: int64}
        desired_state: {type: string}
        reconcile_status: {type: string}
        last_error: {type: string}
    ManagementBackupStatusResponse:
      type: object
      additionalProperties: false
      required: [enabled, destinations, encryption_key_backup]
      properties:
        enabled: {type: boolean}
        reason: {type: string}
        destinations:
          type: array
          nullable: true
          items: {$ref: '#/components/schemas/ManagementBackupDestination'}
        encryption_key_backup:
          type: object
          additionalProperties: false
          required: [wrapping_configured]
          properties:
            wrapping_configured: {type: boolean}
        drill: {$ref: '#/components/schemas/ManagementCronJobStatus'}
        cronjob: {$ref: '#/components/schemas/ManagementCronJobStatus'}
        destination: {$ref: '#/components/schemas/ManagementBackupEndpoint'}
        retention: {$ref: '#/components/schemas/ManagementBackupRetention'}
        last_job: {$ref: '#/components/schemas/ManagementBackupJob'}
    ManagementBackupDestinationWriteRequest:
      type: object
      additionalProperties: false
      required: [name, bucket]
      properties:
        name: {type: string, maxLength: 255}
        bucket: {type: string, maxLength: 255}
        prefix: {type: string}
        region: {type: string}
        endpoint_url: {type: string}
        access_key: {type: string}
        secret_key: {type: string}
        schedule: {type: string}
        enabled: {type: boolean}
        keep_daily: {type: integer, format: int32}
        keep_weekly: {type: integer, format: int32}
        keep_monthly: {type: integer, format: int32}
    ManagementBackupDeleteReceipt:
      type: object
      additionalProperties: false
      required: [destination_id, generation, desired_state, status]
      properties:
        destination_id: {type: string, format: uuid}
        generation: {type: integer, format: int64}
        desired_state: {type: string, enum: [deleted]}
        status: {type: string}
    ManagementBackupDeleteReceiptEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ManagementBackupDeleteReceipt'}
    ManagementBackupDestinationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ManagementBackupDestination'}
    ManagementBackupOperation:
      type: object
      additionalProperties: false
      required: [id, destination_id, operation_type, status]
      properties:
        id: {type: string, format: uuid}
        destination_id: {type: string}
        operation_type: {type: string, enum: [management_backup_test, management_backup_run]}
        status: {type: string}
        operation_url: {type: string}
        attempt_count: {type: integer, format: int32}
        error_message: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    NotificationTemplateVariable:
      type: object
      additionalProperties: false
      required: [name, description, required, example]
      properties:
        name: {type: string}
        description: {type: string}
        required: {type: boolean}
        example: {type: string}
    NotificationTemplateListItem:
      type: object
      additionalProperties: false
      required: [key, channel, description, body_format, has_override, enabled]
      properties:
        key: {type: string}
        channel: {type: string, enum: [email, webhook]}
        description: {type: string}
        body_format: {type: string, enum: [text, markdown, html, json]}
        has_override: {type: boolean}
        enabled: {type: boolean}
        updated_at: {type: string, format: date-time}
    NotificationTemplateDetail:
      type: object
      additionalProperties: false
      required: [key, channel, description, body_format, default_subject, default_body, subject, body, has_override, enabled, variables]
      properties:
        key: {type: string}
        channel: {type: string, enum: [email, webhook]}
        description: {type: string}
        body_format: {type: string, enum: [text, markdown, html, json]}
        default_subject: {type: string}
        default_body: {type: string}
        subject: {type: string}
        body: {type: string}
        has_override: {type: boolean}
        enabled: {type: boolean}
        updated_at: {type: string, format: date-time}
        updated_by: {type: string, format: uuid}
        variables: {type: array, items: {$ref: '#/components/schemas/NotificationTemplateVariable'}}
    NotificationTemplateUpsertRequest:
      type: object
      additionalProperties: false
      required: [body]
      properties:
        subject: {type: string}
        body: {type: string}
        body_format: {type: string, enum: [text, markdown, html, json]}
        enabled: {type: boolean}
    NotificationTemplatePreviewRequest:
      type: object
      additionalProperties: false
      required: [variables]
      properties:
        subject: {type: string}
        body: {type: string}
        body_format: {type: string, enum: [text, markdown, html, json]}
        variables: {type: object, additionalProperties: true}
    NotificationTemplatePreviewResponse:
      type: object
      additionalProperties: false
      required: [subject, body]
      properties:
        subject: {type: string}
        body: {type: string}
    ReadAuditPolicyResponse:
      type: object
      additionalProperties: false
      required: [id, name, description, path_pattern, verbs, sample_rate, enabled, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        description: {type: string}
        path_pattern: {type: string}
        verbs: {type: string}
        sample_rate: {type: number, format: double, minimum: 0, maximum: 1}
        enabled: {type: boolean}
        created_by: {type: string, format: uuid}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    ReadAuditPolicyListResponse:
      type: object
      additionalProperties: false
      required: [items, total]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/ReadAuditPolicyResponse'}}
        total: {type: integer, minimum: 0}
    ReadAuditPolicyCreateRequest:
      type: object
      additionalProperties: false
      required: [name, path_pattern]
      properties:
        name: {type: string, minLength: 1}
        description: {type: string}
        path_pattern: {type: string, minLength: 1}
        verbs: {type: string, default: GET}
        sample_rate: {type: number, format: double, minimum: 0, maximum: 1, default: 1}
        enabled: {type: boolean, default: true}
    ReadAuditPolicyUpdateRequest:
      type: object
      additionalProperties: false
      properties:
        description: {type: string}
        path_pattern: {type: string, minLength: 1}
        verbs: {type: string}
        sample_rate: {type: number, format: double, minimum: 0, maximum: 1}
        enabled: {type: boolean}
    ComplianceQuotaPlanSpec:
      type: object
      additionalProperties: false
      required: [name, enforcement]
      properties:
        name: {type: string}
        enforcement: {type: string, enum: [soft, hard]}
        description: {type: string}
        max_clusters_per_project: {type: integer}
        max_namespaces_per_project: {type: integer}
        max_members_per_project: {type: integer}
        max_projects_per_user: {type: integer}
        max_tokens_per_user: {type: integer}
        max_streams_per_user: {type: integer}
        max_total_clusters: {type: integer}
        max_total_users: {type: integer}
    ComplianceMaintenanceWindowSpec:
      type: object
      additionalProperties: false
      required: [name, start_hour, start_minute, duration_min]
      properties:
        name: {type: string}
        description: {type: string}
        days_of_week: {type: array, items: {type: integer, minimum: 1, maximum: 7}}
        start_hour: {type: integer, minimum: 0, maximum: 23}
        start_minute: {type: integer, minimum: 0, maximum: 59}
        duration_min: {type: integer, minimum: 1}
    ComplianceAlertRuleSpec:
      type: object
      additionalProperties: false
      required: [name, rule_type, severity, configuration]
      properties:
        name: {type: string}
        rule_type: {type: string}
        severity: {type: string}
        cooldown_minutes: {type: integer, minimum: 0}
        configuration: {type: object, additionalProperties: true}
    ComplianceBaselineSpec:
      type: object
      additionalProperties: false
      required: [audit_retention_days]
      properties:
        audit_retention_days: {type: integer, minimum: 0}
        quota_plans: {type: array, items: {$ref: '#/components/schemas/ComplianceQuotaPlanSpec'}}
        pss_profile: {type: string}
        maintenance_window_template: {$ref: '#/components/schemas/ComplianceMaintenanceWindowSpec'}
        alert_rules: {type: array, items: {$ref: '#/components/schemas/ComplianceAlertRuleSpec'}}
        platform_settings: {type: object, additionalProperties: {type: string}}
        required_webhooks: {type: array, items: {type: string}}
        required_smtp: {type: boolean}
        required_totp: {type: boolean}
        read_audit_policies: {type: array, items: {type: string}}
    ComplianceBaseline:
      type: object
      additionalProperties: false
      required: [id, slug, name, description, version, enabled, spec, active]
      properties:
        id: {type: string, format: uuid}
        slug: {type: string, enum: [pci_dss_4_0, hipaa, fedramp_moderate, soc2]}
        name: {type: string}
        description: {type: string}
        version: {type: string}
        enabled: {type: boolean}
        spec: {$ref: '#/components/schemas/ComplianceBaselineSpec'}
        active: {type: boolean}
    ComplianceBaselineDiff:
      type: object
      additionalProperties: false
      required: [baseline_id, baseline_slug, baseline_name, current, target, changes]
      properties:
        baseline_id: {type: string, format: uuid}
        baseline_slug: {type: string}
        baseline_name: {type: string}
        current: {type: object, additionalProperties: true}
        target: {type: object, additionalProperties: true}
        changes: {type: array, items: {type: string}}
    ComplianceBaselineApplication:
      type: object
      additionalProperties: false
      required: [id, baseline_id, baseline_slug, baseline_name, applied_at, status, notes]
      properties:
        id: {type: string, format: uuid}
        baseline_id: {type: string, format: uuid}
        baseline_slug: {type: string}
        baseline_name: {type: string}
        applied_by: {type: string, nullable: true}
        applied_at: {type: string, format: date-time}
        status: {type: string, enum: [applied, reverted]}
        reverted_at: {type: string, format: date-time, nullable: true}
        reverted_by: {type: string, nullable: true}
        notes: {type: string}
        previous_state: {}
    ComplianceBaselineApplyRequest:
      type: object
      additionalProperties: false
      properties:
        notes: {type: string}
    ComplianceBaselineApplyReceipt:
      type: object
      additionalProperties: false
      required: [application_id, baseline_id, slug]
      properties:
        application_id: {type: string, format: uuid}
        baseline_id: {type: string, format: uuid}
        slug: {type: string}
    ComplianceBaselineRevertReceipt:
      type: object
      additionalProperties: false
      required: [application_id, status]
      properties:
        application_id: {type: string, format: uuid}
        status: {type: string, enum: [reverted]}
    SIEMForwarderRequest:
      type: object
      additionalProperties: false
      properties:
        name: {type: string}
        transport: {type: string, enum: [syslog_udp, syslog_tcp, syslog_tls, splunk_hec, ndjson_https]}
        endpoint: {type: string}
        auth: {type: string}
        event_filters: {type: array, items: {type: string}}
        format: {type: string, enum: ['', rfc5424, rfc3164, cef, ndjson]}
        tls_skip_verify: {type: boolean}
        ca_cert_pem: {type: string}
        batch_size: {type: integer, minimum: 1, maximum: 10000}
        flush_interval_ms: {type: integer, minimum: 100, maximum: 600000}
        timeout_seconds: {type: integer, minimum: 1, maximum: 300}
        enabled: {type: boolean}
    SIEMForwarderResponse:
      type: object
      additionalProperties: false
      required: [id, name, transport, endpoint, auth, auth_configured, event_filters, format, tls_skip_verify, ca_cert_configured, batch_size, flush_interval_ms, timeout_seconds, enabled, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        transport: {type: string}
        endpoint: {type: string}
        auth: {type: string}
        auth_configured: {type: boolean}
        event_filters: {type: array, items: {type: string}}
        format: {type: string}
        tls_skip_verify: {type: boolean}
        ca_cert_configured: {type: boolean}
        batch_size: {type: integer}
        flush_interval_ms: {type: integer}
        timeout_seconds: {type: integer}
        enabled: {type: boolean}
        created_by: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    SIEMForwarderList:
      type: object
      additionalProperties: false
      required: [items, total]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/SIEMForwarderResponse'}}
        total: {type: integer, minimum: 0}
    SIEMForwarderTestReceipt:
      type: object
      additionalProperties: false
      required: [operation_id, forwarder_id, status, status_url, created_at]
      properties:
        operation_id: {type: string, format: uuid}
        forwarder_id: {type: string, format: uuid}
        status: {type: string, enum: [pending, succeeded, failed]}
        status_url: {type: string}
        error_code: {type: string}
        created_at: {type: string, format: date-time}
        completed_at: {type: string, format: date-time}
    SIEMForwarderStatusResponse:
      type: object
      additionalProperties: false
      required: [forwarder_id, last_sent_at, last_error, queue_depth, dropped_total, dispatched_total, updated_at]
      properties:
        forwarder_id: {type: string, format: uuid}
        last_sent_at: {type: string, format: date-time, nullable: true}
        last_error: {type: string}
        queue_depth: {type: integer, minimum: 0}
        dropped_total: {type: integer, minimum: 0}
        dispatched_total: {type: integer, minimum: 0}
        updated_at: {type: string, format: date-time}
    DataEnvelope:
      type: object
      description: Standard success envelope. The handler payload is nested under `data`.
      properties:
        data: {}
      required:
        - data
    AdminQueueOperation:
      type: object
      additionalProperties: false
      required: [id, action, queue, task_id, status, attempt_count, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        action: {type: string, enum: [retry, discard]}
        queue: {type: string, minLength: 1}
        task_id: {type: string, minLength: 1}
        status: {type: string, enum: [pending, running, retrying, failed, succeeded]}
        attempt_count: {type: integer, format: int32, minimum: 0}
        last_error:
          type: string
          description: Sanitized bounded failure code; never a Redis connection error or task payload.
        completed_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    AdminQueueOperationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/AdminQueueOperation'}
    ResourceOperation:
      type: object
      additionalProperties: false
      required: [id, cluster_id, resource_type, resource_name, action, status, generation, observed_generation, attempt_count, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid}
        resource_type: {type: string, minLength: 1}
        namespace: {type: string}
        resource_name: {type: string, minLength: 1}
        action: {type: string, enum: [apply, delete]}
        status: {type: string, enum: [pending, running, retrying, failed, succeeded]}
        generation: {type: integer, format: int64, minimum: 1}
        observed_generation: {type: integer, format: int64, minimum: 0}
        attempt_count: {type: integer, format: int32, minimum: 0}
        error_code:
          type: string
          maxLength: 128
          description: Sanitized terminal category; never manifest data or a Kubernetes response body.
        observed_status_code: {type: integer, format: int32, minimum: 100, maximum: 599}
        observed_resource_version: {type: string, maxLength: 255}
        completed_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    ResourceOperationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ResourceOperation'}
    GroupMapping:
      type: object
      additionalProperties: false
      required: [id, connector_id, group_name, scope, role_id, cluster_id, project_id, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        connector_id: {type: string, description: Connector UUID, or an empty string for a connector-agnostic mapping.}
        group_name: {type: string, minLength: 1}
        scope: {type: string, enum: [global, cluster, project]}
        role_id: {type: string, format: uuid}
        cluster_id: {type: string, description: Cluster UUID for cluster scope; otherwise an empty string.}
        project_id: {type: string, description: Project UUID for project scope; otherwise an empty string.}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    GroupMappingWriteRequest:
      type: object
      additionalProperties: false
      required: [group_name, scope, role_id]
      properties:
        connector_id: {type: string, description: Optional connector UUID; empty or omitted means any connector.}
        group_name: {type: string, minLength: 1}
        scope: {type: string, enum: [global, cluster, project]}
        role_id: {type: string, format: uuid}
        cluster_id: {type: string, description: Required as a UUID only when scope is cluster.}
        project_id: {type: string, description: Required as a UUID only when scope is project.}
    GroupMappingEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/GroupMapping'}
    GroupMappingListEnvelope:
      type: object
      additionalProperties: false
      required: [data, count, next, previous]
      properties:
        data:
          type: array
          items: {$ref: '#/components/schemas/GroupMapping'}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
    RouteResponseEnvelope:
      type: object
      description: |
        Forward-compatible JSON response envelope used by route-inventory
        contracts. Domain operations may return either an enveloped payload or
        a documented bare object while fields are promoted additively into a
        named component schema.
      additionalProperties: true
    KubernetesProxyResponse:
      type: object
      description: Unwrapped Kubernetes API response forwarded byte-for-byte by the authenticated cluster proxy.
      additionalProperties: true
    ServiceProxyResponse:
      type: string
      format: binary
      description: Unwrapped upstream service response. Content type and bytes are forwarded from the selected in-cluster service.
    ResourcePolicyMetadata:
      type: object
      additionalProperties: false
      required: [secret, privilege_escalating, destructive_delete, force_conflict_permission]
      properties:
        secret: {type: boolean}
        privilege_escalating: {type: boolean}
        destructive_delete: {type: boolean}
        force_conflict_permission: {type: boolean}
    ResourceDiscoveryEntry:
      type: object
      additionalProperties: false
      required: [resource_type, api_base, api_version, kind, plural, namespaced, verbs, policy, source]
      properties:
        resource_type: {type: string}
        api_base: {type: string}
        api_group: {type: string}
        api_version: {type: string}
        kind: {type: string}
        plural: {type: string}
        namespaced: {type: boolean}
        verbs: {type: array, items: {type: string}, uniqueItems: true}
        short_names: {type: array, items: {type: string}, uniqueItems: true}
        categories: {type: array, items: {type: string}, uniqueItems: true}
        policy: {$ref: '#/components/schemas/ResourcePolicyMetadata'}
        source: {type: string, enum: [kubernetes_discovery, astronomer_fallback]}
    ResourceDiscoveryResponse:
      type: object
      additionalProperties: false
      required: [cluster_id, resources, crds, partial, errors]
      properties:
        cluster_id: {type: string, format: uuid}
        resources: {type: array, items: {$ref: '#/components/schemas/ResourceDiscoveryEntry'}}
        crds: {type: array, items: {type: object, additionalProperties: true}}
        partial: {type: boolean}
        errors: {type: object, additionalProperties: {type: string}}
    ResourceDiscoveryEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ResourceDiscoveryResponse'}
    ResourceSchemaResponse:
      type: object
      additionalProperties: false
      required: [resource, schema, schema_available]
      properties:
        resource: {$ref: '#/components/schemas/ResourceDiscoveryEntry'}
        schema: {type: object, additionalProperties: true}
        schema_name: {type: string}
        schema_available: {type: boolean}
        definitions:
          type: object
          description: Bounded transitive closure of local OpenAPI schemas referenced by `schema`.
          additionalProperties: {type: object, additionalProperties: true}
        definitions_truncated:
          type: boolean
          description: True when the 256-definition response bound was reached.
    ResourceSchemaEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ResourceSchemaResponse'}
    MirroredResourceMetadata:
      type: object
      additionalProperties: {type: string}
    MirroredIngressClass:
      type: object
      additionalProperties: false
      required: [name, controller, parameters, is_default, labels, annotations, last_seen_at, created_at, updated_at]
      properties:
        name: {type: string}
        controller: {type: string}
        parameters: {}
        is_default: {type: boolean}
        labels: {$ref: '#/components/schemas/MirroredResourceMetadata'}
        annotations: {$ref: '#/components/schemas/MirroredResourceMetadata'}
        last_seen_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    MirroredGatewayClass:
      type: object
      additionalProperties: false
      required: [name, controller_name, description, parameters, accepted_status, labels, annotations, last_seen_at, created_at, updated_at]
      properties:
        name: {type: string}
        controller_name: {type: string}
        description: {type: string}
        parameters: {}
        accepted_status: {type: string}
        labels: {$ref: '#/components/schemas/MirroredResourceMetadata'}
        annotations: {$ref: '#/components/schemas/MirroredResourceMetadata'}
        last_seen_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    MirroredNetworkPolicy:
      type: object
      additionalProperties: false
      required: [namespace, name, pod_selector, policy_types, ingress_rules, egress_rules, labels, annotations, is_managed, last_seen_at, created_at, updated_at]
      properties:
        namespace: {type: string}
        name: {type: string}
        pod_selector: {}
        policy_types: {}
        ingress_rules: {}
        egress_rules: {}
        labels: {$ref: '#/components/schemas/MirroredResourceMetadata'}
        annotations: {$ref: '#/components/schemas/MirroredResourceMetadata'}
        is_managed: {type: boolean}
        last_seen_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    MirroredResourceQuota:
      type: object
      additionalProperties: false
      required: [namespace, name, hard, used, scopes, labels, annotations, last_seen_at, created_at, updated_at]
      properties:
        namespace: {type: string}
        name: {type: string}
        hard: {}
        used: {}
        scopes: {}
        labels: {$ref: '#/components/schemas/MirroredResourceMetadata'}
        annotations: {$ref: '#/components/schemas/MirroredResourceMetadata'}
        last_seen_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    MirroredLimitRange:
      type: object
      additionalProperties: false
      required: [namespace, name, limits, labels, annotations, last_seen_at, created_at, updated_at]
      properties:
        namespace: {type: string}
        name: {type: string}
        limits: {}
        labels: {$ref: '#/components/schemas/MirroredResourceMetadata'}
        annotations: {$ref: '#/components/schemas/MirroredResourceMetadata'}
        last_seen_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    MirroredIngressClassPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/MirroredIngressClass'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
    MirroredGatewayClassPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/MirroredGatewayClass'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
    MirroredNetworkPolicyPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/MirroredNetworkPolicy'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
    MirroredResourceQuotaPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/MirroredResourceQuota'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
    MirroredLimitRangePage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/MirroredLimitRange'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
    ErrorEnvelope:
      type: object
      description: Standard error envelope.
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            request_id:
              type: string
              description: Correlation id, present only when set by request middleware.
          required:
            - code
            - message
      required:
        - error
    PaginatedEnvelope:
      type: object
      description: DRF-style paginated list envelope. `data` is the page of items.
      properties:
        data:
          type: array
          items: {}
        count:
          type: integer
          format: int64
          description: Total number of items across all pages.
        next:
          type: string
          nullable: true
          description: URL of the next page, or null on the last page.
        previous:
          type: string
          nullable: true
          description: URL of the previous page, or null on the first page.
      required:
        - data
        - count
        - next
        - previous
    ListEnvelope:
      type: object
      description: Forward-looking list envelope with explicit pagination metadata.
      properties:
        data:
          type: array
          items: {}
        pagination:
          $ref: '#/components/schemas/PaginationMetadata'
      required: [data, pagination]
    LoginRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
    LoginResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            token:
              type: string
              description: Access JWT (~1h TTL)
            refresh:
              type: string
              description: Refresh JWT (~7d TTL)
            user:
              $ref: '#/components/schemas/User'
    AuthUserRoleBinding:
      type: object
      additionalProperties: false
      required: [id, role_id, role_name, role_rules, group]
      properties:
        id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        role_name:
          type: string
        role_rules:
          type: array
          items:
            $ref: '#/components/schemas/RBACRule'
        group:
          type: string
        cluster_id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
    AuthUserRoles:
      type: object
      additionalProperties: false
      required: [global, cluster, project]
      properties:
        global:
          type: array
          items:
            $ref: '#/components/schemas/AuthUserRoleBinding'
        cluster:
          type: array
          items:
            $ref: '#/components/schemas/AuthUserRoleBinding'
        project:
          type: array
          items:
            $ref: '#/components/schemas/AuthUserRoleBinding'
    User:
      type: object
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        email:
          type: string
          format: email
        first_name:
          type: string
        last_name:
          type: string
        is_active:
          type: boolean
        is_staff:
          type: boolean
        is_superuser:
          type: boolean
        date_joined:
          type: string
          format: date-time
        last_login:
          type: string
          format: date-time
          nullable: true
        must_change_password:
          type: boolean
        roles:
          $ref: '#/components/schemas/AuthUserRoles'
    StreamTicketRequest:
      type: object
      additionalProperties: false
      required: [stream_type]
      properties:
        stream_type:
          type: string
          enum: [events, registration, logs, exec, shell]
        cluster_id:
          type: string
          format: uuid
          description: Required for every stream type except events.
    StreamTicketResponse:
      type: object
      additionalProperties: false
      required: [ticket, expires_at]
      properties:
        ticket:
          type: string
        expires_at:
          type: string
          format: date-time
    Cluster:
      type: object
      additionalProperties: false
      required:
        - id
        - name
        - display_name
        - description
        - status
        - api_server_url
        - ca_certificate
        - environment
        - region
        - provider
        - labels
        - annotations
        - distribution
        - agent_version
        - kubernetes_version
        - node_count
        - created_at
        - updated_at
        - is_local
        - cluster_uid
        - registration_phase
        - managed_by
        - external_ref_api_version
        - external_ref_kind
        - external_ref_namespace
        - external_ref_name
        - observed_generation
        - cpu_percentage
        - memory_percentage
        - pod_count
        - metrics_server_present
        - decommissioning
        - agent_privilege_profile
        - downstream_impersonation
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: RFC-1123 cluster slug
        display_name:
          type: string
        description:
          type: string
        status:
          type: string
          enum:
            - pending
            - active
            - disconnected
            - error
        api_server_url: {type: string, format: uri}
        ca_certificate: {type: string, description: Public CA bundle used to verify the Kubernetes API endpoint; never an authentication credential.}
        environment:
          type: string
          enum:
            - production
            - staging
            - development
            - dev
        region:
          type: string
        provider:
          type: string
          description: eks / gke / aks / k3d / kind / other
        distribution:
          type: string
          description: vanilla / k3s / openshift / ...
        labels:
          type: object
          additionalProperties:
            type: string
        annotations:
          type: object
          additionalProperties:
            type: string
        registration_phase:
          type: string
          enum:
            - created
            - awaiting_agent
            - connected
            - provisioning
            - ready
            - failed
        install_baseline:
          type: boolean
          nullable: true
          description: Explicit registration choice; true selects the default-enabled components in the signed built-in bundle catalog.
        agent_version:
          type: string
        kubernetes_version:
          type: string
        node_count:
          type: integer
        last_heartbeat:
          type: string
          nullable: true
          format: date-time
        is_local:
          type: boolean
        decommissioned_at:
          type: string
          nullable: true
          format: date-time
        created_by_id: {type: string, format: uuid, nullable: true}
        cluster_uid: {type: string}
        group_id: {type: string, format: uuid, nullable: true}
        registration_started_at: {type: string, format: date-time, nullable: true}
        registration_completed_at: {type: string, format: date-time, nullable: true}
        managed_by: {type: string, enum: [api, ui, crd, system]}
        external_ref_api_version: {type: string}
        external_ref_kind: {type: string}
        external_ref_namespace: {type: string}
        external_ref_name: {type: string}
        observed_generation: {type: integer, format: int64, minimum: 0}
        agent_privilege_profile:
          type: string
          enum:
            - viewer
            - operator
            - namespace-viewer
            - namespace-operator
            - custom
            - admin
        downstream_impersonation:
          type: string
          description: >-
            Tri-state downstream-impersonation mode for this cluster
            (docs/design/downstream-impersonation.md §5), stored in
            `annotations` under `astronomer.io/downstream-impersonation`.
            Writable by superusers only, and `enforce` additionally requires the
            agent to have advertised the impersonation capability. Absent,
            blank and unrecognized all read as `off`, which is the default and
            — in Phase 0 — the only mode any code path acts on.
          enum:
            - "off"
            - attribute
            - enforce
        cpu_percentage:
          type: number
        memory_percentage:
          type: number
        pod_count:
          type: integer
        metrics_server_present: {type: boolean}
        decommissioning: {type: boolean}
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateClusterRequest:
      type: object
      additionalProperties: false
      required:
        - name
      properties:
        name:
          type: string
          description: RFC-1123 slug; must be lowercase + hyphens
        display_name:
          type: string
        description:
          type: string
        environment:
          type: string
        region:
          type: string
        provider:
          type: string
        distribution:
          type: string
        labels:
          type: object
          additionalProperties: {type: string}
        annotations:
          type: object
          additionalProperties: {type: string}
        api_server_url:
          type: string
          format: uri
          description: Optional externally reachable HTTPS Kubernetes API origin; only ports 443 and 6443 are accepted.
        ca_certificate:
          type: string
          description: Optional PEM CA bundle used to verify the direct Kubernetes API endpoint. Never a credential.
    RegistrationStatus:
      type: object
      additionalProperties: false
      required: [cluster_id, phase, steps]
      properties:
        cluster_id:
          type: string
          format: uuid
        phase:
          type: string
          enum:
            - created
            - awaiting_agent
            - connected
            - provisioning
            - ready
            - failed
        install_baseline:
          type: boolean
          nullable: true
          description: Explicit registration choice; true waits for the catalog-defined Flux baseline to finish before registration becomes ready.
        started_at:
          type: string
          format: date-time
          nullable: true
        completed_at:
          type: string
          format: date-time
          nullable: true
        steps:
          type: array
          items:
            $ref: '#/components/schemas/RegistrationStep'
    RegistrationStep:
      type: object
      additionalProperties: false
      required: [id, step_name, label, status, progress_pct, step_order, created_at]
      properties:
        id:
          type: string
          format: uuid
        step_name:
          type: string
        label:
          type: string
        status:
          type: string
          enum:
            - pending
            - running
            - success
            - failed
            - skipped
        progress_pct:
          type: integer
          minimum: 0
          maximum: 100
        detail:
          type: object
          additionalProperties: true
        started_at:
          type: string
          format: date-time
          nullable: true
        completed_at:
          type: string
          format: date-time
          nullable: true
        error_message:
          type: string
        step_order:
          type: integer
        created_at:
          type: string
          format: date-time
    SetOptionsRequest:
      type: object
      required:
        - install_baseline
      properties:
        install_baseline:
          type: boolean
          description: Select the release-pinned, default-enabled built-in delivery components for this imported cluster.
    VulnSummary:
      type: object
      properties:
        critical:
          type: integer
        high:
          type: integer
        medium:
          type: integer
        low:
          type: integer
        unknown:
          type: integer
        report_count:
          type: integer
        last_scanned_at:
          type: string
          format: date-time
          nullable: true
    VulnImage:
      type: object
      properties:
        id:
          type: string
          format: uuid
        cluster_id:
          type: string
          format: uuid
        report_name:
          type: string
        namespace:
          type: string
        workload_kind:
          type: string
        workload_name:
          type: string
        container_name:
          type: string
        image_registry:
          type: string
        image_repo:
          type: string
        image_tag:
          type: string
        image_digest:
          type: string
        scanner:
          type: string
        scanner_version:
          type: string
        critical_count:
          type: integer
        high_count:
          type: integer
        medium_count:
          type: integer
        low_count:
          type: integer
        unknown_count:
          type: integer
        scanned_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    KubectlSession:
      type: object
      additionalProperties: false
      required: [id, cluster_id, user_id, status, pod_name, pod_namespace, container, started_at, last_input_at, expires_at, idle_timeout_seconds]
      properties:
        id:
          type: string
          format: uuid
        cluster_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        status:
          type: string
          enum:
            - starting
            - active
            - closed
            - expired
            - failed
        pod_name:
          type: string
        pod_namespace:
          type: string
        container:
          type: string
        started_at:
          type: string
          format: date-time
        last_input_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        idle_timeout_seconds:
          type: integer
          format: int64
        command_count:
          type: integer
          format: int64
          minimum: 0
          description: Omitted when the session has not recorded a command.
    KubectlRecordedCommand:
      type: object
      additionalProperties: false
      required: [command_at, command_line]
      properties:
        command_at: {type: string, format: date-time}
        command_line: {type: string}
    KubectlCloseReceipt:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status: {type: string, enum: [closed]}
    KubectlSessionEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/KubectlSession'}
    KubectlSessionListEnvelope:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/KubectlSession'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    KubectlCloseEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/KubectlCloseReceipt'}
    KubectlRecordedCommandPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/KubectlRecordedCommand'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
    OwnershipTransferResponse:
      type: object
      additionalProperties: false
      required: [id, managed_by, transferred]
      properties:
        id:
          type: string
          format: uuid
        managed_by:
          type: string
          enum:
            - api
            - ui
            - crd
            - system
        transferred:
          type: boolean
    FeatureFlags:
      type: object
      additionalProperties: false
      properties:
        feature.catalog:
          type: boolean
        feature.projects:
          type: boolean
        feature.monitoring:
          type: boolean
        feature.shared_grafana:
          type: boolean
        feature.fleet_grafana:
          type: boolean
          deprecated: true
          description: Deprecated compatibility alias for feature.shared_grafana.
        feature.hosted_loki:
          type: boolean
        feature.security:
          type: boolean
        feature.backups:
          type: boolean
        feature.charlie:
          type: boolean
        feature.extensions:
          type: boolean
    ExtensionCSP:
      type: object
      additionalProperties: false
      properties:
        scriptSrc: {type: array, items: {type: string}}
        connectSrc: {type: array, items: {type: string}}
        frameSrc: {type: array, items: {type: string}}
        imageSrc: {type: array, items: {type: string}}
    ExtensionManifest:
      type: object
      required:
        - apiVersion
        - name
        - version
        - compatibleAstronomer
        - entry
        - permissions
        - extensionPoints
      properties:
        apiVersion:
          type: string
          enum:
            - extensions.astronomer.io/v1alpha1
        name:
          type: string
        displayName:
          type: string
        version:
          type: string
        compatibleAstronomer:
          type: string
        entry:
          type: string
        permissions:
          type: array
          items:
            type: string
        backendApiScopes:
          type: array
          items:
            type: string
        csp:
          $ref: '#/components/schemas/ExtensionCSP'
        extensionPoints:
          type: object
          additionalProperties: true
    ExtensionValidation:
      type: object
      properties:
        valid:
          type: boolean
        compatibility_status:
          type: string
          enum:
            - compatible
            - incompatible
            - unknown
        checksum:
          type: string
        manifest:
          $ref: '#/components/schemas/ExtensionManifest'
        warnings:
          type: array
          items:
            type: object
            additionalProperties: true
        errors:
          type: array
          items:
            type: object
            additionalProperties: true
    ExtensionRecord:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        display_name:
          type: string
        version:
          type: string
        source:
          type: string
        checksum:
          type: string
        enabled:
          type: boolean
        compatibility_status:
          type: string
        manifest:
          $ref: '#/components/schemas/ExtensionManifest'
        installed_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ExtensionMount:
      type: object
      description: >-
        Render-only projection of one enabled extension point for the host
        loader. Upstream dataSource paths/methods/rbac are NOT exposed; only the
        dataSource id + shape cross to the browser.
      properties:
        extension:
          type: string
        displayName:
          type: string
        point:
          type: string
          enum:
            - sidebar
            - dashboardWidget
            - clusterTab
            - settingsPage
        pointId:
          type: string
        title:
          type: string
        tier:
          type: integer
          description: 1 = declarative (Tier 1), 2 = signed bundle (Tier 2).
          enum:
            - 1
            - 2
        render:
          type: object
          additionalProperties: true
        dataSources:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              shape:
                type: string
    ExtensionMountsResponse:
      type: object
      description: >-
        Enabled-extensions surface (§HostMounts), indexed by mount point. Only
        enabled, compatible extensions appear; Tier-2 points additionally
        require a verified bundle.
      properties:
        sidebar:
          type: array
          items:
            $ref: '#/components/schemas/ExtensionMount'
        dashboardWidgets:
          type: array
          items:
            $ref: '#/components/schemas/ExtensionMount'
        clusterTabs:
          type: array
          items:
            $ref: '#/components/schemas/ExtensionMount'
        settings:
          type: array
          items:
            $ref: '#/components/schemas/ExtensionMount'
    ServiceMeshInventory:
      type: object
      properties:
        cluster_id:
          type: string
          format: uuid
        mesh:
          type: string
        total_count:
          type: integer
        notice:
          type: string
        resources:
          type: array
          items:
            type: object
            properties:
              kind:
                type: string
              api_version:
                type: string
              plural:
                type: string
              count:
                type: integer
              notice:
                type: string
              items:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                    managed_by:
                      type: string
                    read_only:
                      type: boolean
                    reason:
                      type: string
    ServiceMeshPolicyValidation:
      type: object
      properties:
        cluster_id:
          type: string
          format: uuid
        valid:
          type: boolean
        api_version:
          type: string
        kind:
          type: string
        name:
          type: string
        namespace:
          type: string
        managed_by:
          type: string
        read_only:
          type: boolean
        apply_allowed:
          type: boolean
        warnings:
          type: array
          items:
            type: object
            additionalProperties: true
        errors:
          type: array
          items:
            type: object
            additionalProperties: true
    AgentOfflineBehavior:
      type: object
      additionalProperties: false
      required: [state, stale, message, permitted_queued_operations, blocked_operations]
      properties:
        state:
          type: string
        last_known_at:
          type: string
          format: date-time
          nullable: true
        stale:
          type: boolean
        message:
          type: string
        permitted_queued_operations:
          type: array
          items:
            type: string
        blocked_operations:
          type: array
          items:
            type: string
    ClusterAgentItem:
      type: object
      additionalProperties: false
      required: [cluster_id, cluster_name, cluster_display_name, cluster_status, is_local, agent_status, node_count, privilege_profile, capabilities, compatibility_status]
      properties:
        cluster_id:
          type: string
          format: uuid
        cluster_name:
          type: string
        cluster_display_name:
          type: string
        cluster_status:
          type: string
        is_local:
          type: boolean
        agent_status:
          type: string
          enum:
            - connected
            - degraded
            - disconnected
        agent_id:
          type: string
        session_id:
          type: string
        agent_version:
          type: string
        kubernetes_version:
          type: string
        distribution:
          type: string
        node_count:
          type: integer
        connected_at:
          type: string
          format: date-time
          nullable: true
        last_ping:
          type: string
          format: date-time
          nullable: true
        last_heartbeat:
          type: string
          format: date-time
          nullable: true
        disconnected_at:
          type: string
          format: date-time
          nullable: true
        pod_name:
          type: string
        node_name:
          type: string
        channel_name:
          type: string
        privilege_profile:
          type: string
          enum:
            - viewer
            - operator
            - namespace-viewer
            - namespace-operator
            - custom
            - admin
        capabilities:
          type: object
          additionalProperties:
            type: boolean
        compatibility_status:
          type: string
          enum:
            - supported
            - deprecated
            - blocked
            - unknown
        compatibility_message:
          type: string
        degraded_reasons:
          type: array
          items:
            type: string
        recommended_action:
          type: string
        offline_behavior:
          $ref: '#/components/schemas/AgentOfflineBehavior'
    ClusterAgentResponse:
      type: object
      additionalProperties: false
      required: [summary, items, limit, offset]
      properties:
        summary:
          type: object
          additionalProperties: false
          required: [total_clusters, connected, degraded, disconnected, versions, profiles, statuses, compatibility, server_version, minimum_supported_agent_version, minimum_compatible_agent_version, generated_at]
          properties:
            total_clusters:
              type: integer
            connected:
              type: integer
            degraded:
              type: integer
            disconnected:
              type: integer
            versions:
              type: object
              additionalProperties:
                type: integer
            profiles:
              type: object
              additionalProperties:
                type: integer
            statuses:
              type: object
              additionalProperties:
                type: integer
            compatibility:
              type: object
              additionalProperties:
                type: integer
            server_version:
              type: string
            minimum_supported_agent_version:
              type: string
            minimum_compatible_agent_version:
              type: string
            maximum_supported_agent_version_exclusive:
              type: string
            generated_at:
              type: string
              format: date-time
        items:
          type: array
          items:
            $ref: '#/components/schemas/ClusterAgentItem'
        limit:
          type: integer
        offset:
          type: integer
    AgentConnectionDiagnostic:
      type: object
      additionalProperties: false
      required: [id, agent_id, session_id, status, connected_at]
      properties:
        id: {type: string, format: uuid}
        agent_id: {type: string, format: uuid}
        session_id: {type: string, format: uuid}
        status: {type: string}
        agent_version: {type: string}
        connected_at: {type: string, format: date-time}
        last_ping: {type: string, format: date-time, nullable: true}
        disconnected_at: {type: string, format: date-time, nullable: true}
        pod_name: {type: string}
        node_name: {type: string}
        channel_name: {type: string}
    AgentClusterConditionDiagnostic:
      type: object
      additionalProperties: false
      required: [type, status, last_transition_time]
      properties:
        type: {type: string}
        status: {type: string}
        reason: {type: string}
        message: {type: string}
        last_transition_time: {type: string, format: date-time}
        last_probe_time: {type: string, format: date-time, nullable: true}
    AgentSelfTestCheck:
      type: object
      additionalProperties: false
      required: [name, status, message]
      properties:
        name: {type: string}
        status: {type: string, enum: [passed, warning, failed]}
        message: {type: string}
    AgentLivePodDiagnostic:
      type: object
      additionalProperties: false
      required: [name, namespace, phase, ready, restart_count]
      properties:
        name: {type: string}
        namespace: {type: string}
        phase: {type: string}
        node_name: {type: string}
        ready: {type: boolean}
        restart_count: {type: integer}
        container_images: {type: array, items: {type: string}}
    AgentLiveEventDiagnostic:
      type: object
      additionalProperties: false
      properties:
        type: {type: string}
        reason: {type: string}
        message: {type: string}
        time: {type: string, format: date-time}
    AgentLiveLogDiagnostic:
      type: object
      additionalProperties: false
      required: [pod_name, lines, truncated]
      properties:
        pod_name: {type: string}
        lines: {type: array, items: {type: string}}
        truncated: {type: boolean}
    AgentLiveDiagnostics:
      type: object
      additionalProperties: false
      required: [collected_at]
      properties:
        collected_at: {type: string, format: date-time}
        deployment: {type: object, additionalProperties: true}
        pods: {type: array, items: {$ref: '#/components/schemas/AgentLivePodDiagnostic'}}
        events: {type: array, items: {$ref: '#/components/schemas/AgentLiveEventDiagnostic'}}
        logs: {type: array, items: {$ref: '#/components/schemas/AgentLiveLogDiagnostic'}}
        discovery: {type: object, additionalProperties: true}
        checks: {type: array, items: {$ref: '#/components/schemas/AgentSelfTestCheck'}}
        errors: {type: array, items: {type: string}}
    AgentUpgradeRecommendation:
      type: object
      additionalProperties: false
      required: [status, message]
      properties:
        current_version: {type: string}
        status: {type: string}
        message: {type: string}
    AgentDiagnostics:
      type: object
      additionalProperties: false
      required: [generated_at, agent, recent_connections, conditions, recommendations, redactions, upgrade_recommendation]
      properties:
        generated_at:
          type: string
          format: date-time
        agent:
          $ref: '#/components/schemas/ClusterAgentItem'
        recent_connections:
          type: array
          items:
            $ref: '#/components/schemas/AgentConnectionDiagnostic'
        conditions:
          type: array
          items:
            $ref: '#/components/schemas/AgentClusterConditionDiagnostic'
        live:
          $ref: '#/components/schemas/AgentLiveDiagnostics'
        recommendations:
          type: array
          items:
            type: string
        redactions:
          type: array
          items:
            type: string
        upgrade_recommendation:
          $ref: '#/components/schemas/AgentUpgradeRecommendation'
    AgentSelfTest:
      type: object
      additionalProperties: false
      required: [generated_at, cluster_id, cluster_name, status, checks]
      properties:
        generated_at:
          type: string
          format: date-time
        cluster_id:
          type: string
          format: uuid
        cluster_name:
          type: string
        status:
          type: string
          enum:
            - passed
            - warning
            - failed
        checks:
          type: array
          items:
            $ref: '#/components/schemas/AgentSelfTestCheck'
        recommendations:
          type: array
          items:
            type: string
    AgentDiagnosticsBundle:
      type: object
      additionalProperties: false
      required: [version, generated_at, cluster_id, cluster_name, diagnostics, notes]
      properties:
        version:
          type: string
        generated_at:
          type: string
          format: date-time
        cluster_id:
          type: string
          format: uuid
        cluster_name:
          type: string
        diagnostics:
          $ref: '#/components/schemas/AgentDiagnostics'
        notes:
          type: array
          items:
            type: string
    AgentUpgradePlanRequest:
      type: object
      additionalProperties: false
      properties:
        target_version:
          type: string
        target_image:
          type: string
        strategy:
          type: string
          default: agent_self_rollout
        canary_cluster_ids:
          type: array
          items:
            type: string
        batch_size:
          type: integer
          default: 1
          minimum: 1
        max_unavailable:
          type: integer
          default: 1
          minimum: 1
        rollback_image:
          type: string
    AgentUpgradePlan:
      type: object
      additionalProperties: false
      required: [cluster_id, cluster_name, target_version, target_image, privilege_profile, strategy, batch_size, max_unavailable, ready, preflight_checks, steps, post_upgrade_health_checks, validation, rollback]
      properties:
        cluster_id:
          type: string
          format: uuid
        cluster_name:
          type: string
        current_version:
          type: string
        target_version:
          type: string
        current_image:
          type: string
        target_image:
          type: string
        rollback_image:
          type: string
        privilege_profile:
          type: string
        strategy:
          type: string
          default: agent_self_rollout
        canary_cluster_ids:
          type: array
          items:
            type: string
        batch_size:
          type: integer
        max_unavailable:
          type: integer
        ready:
          type: boolean
        blockers:
          type: array
          items:
            type: string
        preflight_checks:
          type: array
          items:
            type: string
        steps:
          type: array
          items:
            type: string
        post_upgrade_health_checks:
          type: array
          items:
            type: string
        validation:
          type: array
          items:
            type: string
        rollback:
          type: array
          items:
            type: string
    AgentLifecycleOperation:
      type: object
      additionalProperties: false
      required: [id, cluster_id, operation_type, status, target_version, target_image, strategy, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        cluster_id:
          type: string
          format: uuid
        operation_type:
          type: string
          enum:
            - agent_upgrade
        status:
          type: string
          enum:
            - pending
            - running
            - succeeded
            - failed
            - cancelled
        target_version:
          type: string
        target_image:
          type: string
        current_version:
          type: string
        strategy:
          type: string
        operation_spec:
          type: object
          additionalProperties: true
        requested_by:
          type: string
          format: uuid
          nullable: true
        started_at:
          type: string
          format: date-time
          nullable: true
        completed_at:
          type: string
          format: date-time
          nullable: true
        last_error:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AgentLifecycleOperationsResponse:
      type: object
      additionalProperties: false
      required: [items, limit, offset]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AgentLifecycleOperation'
        limit:
          type: integer
        offset:
          type: integer
    AgentUpgradeOperationResponse:
      type: object
      additionalProperties: false
      required: [operation, plan]
      properties:
        operation:
          $ref: '#/components/schemas/AgentLifecycleOperation'
        plan:
          $ref: '#/components/schemas/AgentUpgradePlan'
    NodeDrainRequest:
      type: object
      properties:
        ignore_daemonsets:
          type: boolean
          default: true
        delete_empty_dir_data:
          type: boolean
          default: false
        grace_period_seconds:
          type: integer
          format: int64
        dry_run:
          type: boolean
          default: false
        force:
          type: boolean
          default: false
          description: >-
            Mirrors `kubectl drain --force`: evict standalone pods that have no
            ownerReferences. Without it such pods are reported as blockers
            instead, because deleting them is irreversible — no controller
            exists to recreate them.
    NodeDrainPodRef:
      type: object
      properties:
        namespace:
          type: string
        name:
          type: string
        reason:
          type: string
    NodeDrainResponse:
      type: object
      properties:
        node:
          type: string
        status:
          type: string
          enum:
            - dry_run
            - draining
            - drained
            - blocked
            - partial
        message:
          type: string
        evicted:
          type: array
          items:
            $ref: '#/components/schemas/NodeDrainPodRef'
        skipped:
          type: array
          items:
            $ref: '#/components/schemas/NodeDrainPodRef'
        failed:
          type: array
          items:
            $ref: '#/components/schemas/NodeDrainPodRef'
        blockers:
          type: array
          items:
            type: string
    NodeOperation:
      type: object
      required: [id, cluster_id, node_name, action, status, generation, observed_generation, attempt_count, progress, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid}
        node_name: {type: string}
        action:
          type: string
          enum: [cordon, uncordon, set_label, remove_label, set_annotation, remove_annotation, add_taint, remove_taint, drain]
        status: {type: string, enum: [pending, running, retrying, blocked, failed, succeeded]}
        generation: {type: integer, format: int64, minimum: 1}
        observed_generation: {type: integer, format: int64, minimum: 0}
        attempt_count: {type: integer, format: int32, minimum: 0}
        error_code: {type: string}
        progress:
          type: object
          additionalProperties: true
          description: Sanitized, resumable evidence. Drain is explicitly non-atomic; cordon and individual evictions may succeed while later evictions remain blocked or retrying. Never includes request parameters.
        completed_at: {type: string, format: date-time, nullable: true}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    NodeOperationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/NodeOperation'}
    NodeKeyValueRequest:
      type: object
      required:
        - key
      properties:
        key:
          type: string
        value:
          type: string
    NodeKeyRequest:
      type: object
      required:
        - key
      properties:
        key:
          type: string
    NodeTaintRequest:
      type: object
      required:
        - key
        - effect
      properties:
        key:
          type: string
        value:
          type: string
        effect:
          type: string
          enum:
            - NoSchedule
            - PreferNoSchedule
            - NoExecute
    NodeTaintRemoveRequest:
      type: object
      required:
        - key
      properties:
        key:
          type: string
        effect:
          type: string
          enum:
            - NoSchedule
            - PreferNoSchedule
            - NoExecute
    NodeActionResponse:
      type: object
      properties:
        node:
          type: string
        status:
          type: string
        key:
          type: string
        value:
          type: string
        removed:
          type: boolean
    NodeTaintActionResponse:
      type: object
      properties:
        node:
          type: string
        status:
          type: string
        taint:
          $ref: '#/components/schemas/NodeTaintRequest'
    PaginatedClusters:
      type: object
      additionalProperties: false
      required: [data, count, next, previous]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Cluster'
        count:
          type: integer
        next:
          type: string
          nullable: true
        previous:
          type: string
          nullable: true
    SnapshotSpec:
      type: object
      properties:
        includedNamespaces:
          type: array
          items:
            type: string
        excludedNamespaces:
          type: array
          items:
            type: string
        includedResources:
          type: array
          items:
            type: string
        excludedResources:
          type: array
          items:
            type: string
        labelSelector:
          type: string
        snapshotVolumes:
          type: boolean
          nullable: true
        ttl:
          type: string
          description: Velero-style duration e.g. 168h
        storageLocation:
          type: string
        volumeSnapshotLocations:
          type: array
          items:
            type: string
    SnapshotCreateRequest:
      allOf:
        - $ref: '#/components/schemas/SnapshotSpec'
        - type: object
          properties:
            source:
              type: string
              default: manual
            velero_name:
              type: string
            velero_namespace:
              type: string
    SnapshotResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        cluster_id:
          type: string
          format: uuid
        velero_name:
          type: string
        velero_namespace:
          type: string
        source:
          type: string
        spec:
          $ref: '#/components/schemas/SnapshotSpec'
        phase:
          type: string
        start_time:
          type: string
          format: date-time
          nullable: true
        completion_time:
          type: string
          format: date-time
          nullable: true
        expires_at:
          type: string
          format: date-time
          nullable: true
        warnings_count:
          type: integer
          format: int32
        errors_count:
          type: integer
          format: int32
        last_poll_at:
          type: string
          format: date-time
          nullable: true
        last_poll_error:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    SnapshotRestoreRequest:
      type: object
      properties:
        target_cluster_id:
          type: string
          format: uuid
          description: Optional; defaults to snapshot's cluster
        velero_namespace:
          type: string
        spec:
          type: object
          properties:
            includedNamespaces:
              type: array
              items:
                type: string
            excludedNamespaces:
              type: array
              items:
                type: string
            namespaceMapping:
              type: object
              additionalProperties:
                type: string
            labelSelector:
              type: string
            restorePVs:
              type: boolean
              nullable: true
    SnapshotRestoreResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        snapshot_id:
          type: string
          format: uuid
        target_cluster_id:
          type: string
          format: uuid
        velero_name:
          type: string
        velero_namespace:
          type: string
        spec:
          type: object
        phase:
          type: string
        start_time:
          type: string
          format: date-time
          nullable: true
        completion_time:
          type: string
          format: date-time
          nullable: true
        warnings_count:
          type: integer
          format: int32
        errors_count:
          type: integer
          format: int32
        last_poll_at:
          type: string
          format: date-time
          nullable: true
        last_poll_error:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    SnapshotScheduleRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
        cron_schedule:
          type: string
          description: Standard 5-field cron
        spec:
          $ref: '#/components/schemas/SnapshotSpec'
        enabled:
          type: boolean
          nullable: true
          default: true
    SnapshotScheduleUpdateRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
        cron_schedule:
          type: string
          description: Standard 5-field cron
        spec:
          $ref: '#/components/schemas/SnapshotSpec'
        enabled:
          type: boolean
          nullable: true
    SnapshotScheduleResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        cluster_id:
          type: string
          format: uuid
        name:
          type: string
        cron_schedule:
          type: string
        spec:
          $ref: '#/components/schemas/SnapshotSpec'
        enabled:
          type: boolean
        last_run_at:
          type: string
          format: date-time
          nullable: true
        last_run_status:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    VeleroStatusResponse:
      type: object
      properties:
        installed:
          type: boolean
        namespace:
          type: string
        storage_ready:
          type: boolean
        storage_locations:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              provider:
                type: string
              default:
                type: boolean
              phase:
                type: string
              bucket:
                type: string
        reason:
          type: string
    BackupCreateRequest:
      type: object
      required:
        - name
        - storage_id
      properties:
        name:
          type: string
        storage_id:
          type: string
          format: uuid
        backup_type:
          type: string
        database_tables:
          description: JSON array of table specs
        included_namespaces:
          type: array
          items:
            type: string
        excluded_namespaces:
          type: array
          items:
            type: string
    BackupResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        storage_id:
          type: string
          format: uuid
        backup_type:
          type: string
        status:
          type: string
        file_path:
          type: string
        file_size_bytes:
          type: integer
          format: int64
        database_tables: {}
        started_at:
          type: string
          format: date-time
          nullable: true
        completed_at:
          type: string
          format: date-time
          nullable: true
        error_message:
          type: string
        created_by_id:
          type: string
          format: uuid
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        cluster_id:
          type: string
          format: uuid
          nullable: true
        velero_backup_name:
          type: string
        velero_namespace:
          type: string
        included_namespaces: {}
        excluded_namespaces: {}
        poll_attempts:
          type: integer
          format: int32
        last_polled_at:
          type: string
          format: date-time
          nullable: true
    BackupRestoreRequest:
      type: object
      properties:
        included_namespaces:
          type: array
          items:
            type: string
        namespace_mapping:
          type: object
          additionalProperties:
            type: string
    RestoreOperationResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        backup_id:
          type: string
          format: uuid
        status:
          type: string
        started_at:
          type: string
          format: date-time
          nullable: true
        completed_at:
          type: string
          format: date-time
          nullable: true
        error_message:
          type: string
        initiated_by_id:
          type: string
          format: uuid
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        cluster_id:
          type: string
          format: uuid
          nullable: true
        velero_namespace:
          type: string
        velero_restore_name:
          type: string
        included_namespaces: {}
        namespace_mapping: {}
        poll_attempts:
          type: integer
          format: int32
        last_polled_at:
          type: string
          format: date-time
          nullable: true
    BackupScheduleRequest:
      type: object
      required:
        - name
        - storage_id
        - cron_expression
      properties:
        name:
          type: string
        storage_id:
          type: string
          format: uuid
        backup_type:
          type: string
        cron_expression:
          type: string
        retention_count:
          type: integer
          format: int32
        enabled:
          type: boolean
        cluster_id:
          type: string
          format: uuid
        velero_namespace:
          type: string
        included_namespaces:
          type: array
          items:
            type: string
        excluded_namespaces:
          type: array
          items:
            type: string
        ttl:
          type: string
    BackupScheduleResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        storage_id:
          type: string
          format: uuid
        backup_type:
          type: string
        cron_expression:
          type: string
        retention_count:
          type: integer
          format: int32
        enabled:
          type: boolean
        last_backup_id:
          type: string
          format: uuid
          nullable: true
        created_by_id:
          type: string
          format: uuid
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        cluster_id:
          type: string
          format: uuid
          nullable: true
        velero_namespace:
          type: string
        velero_schedule_name:
          type: string
        included_namespaces: {}
        excluded_namespaces: {}
        ttl:
          type: string
    BackupStorageConfigRequest:
      type: object
      required:
        - name
        - bucket
      properties:
        name:
          type: string
        storage_type:
          type: string
        bucket:
          type: string
        prefix:
          type: string
        region:
          type: string
        endpoint_url:
          type: string
        access_key:
          type: string
        secret_key:
          type: string
        is_default:
          type: boolean
        cluster_id:
          type: string
          format: uuid
        velero_namespace:
          type: string
        bsl_name:
          type: string
    BackupStorageConfigResponse:
      type: object
      description: Credentials are never returned; only has_credentials boolean.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        storage_type:
          type: string
        bucket:
          type: string
        prefix:
          type: string
        region:
          type: string
        endpoint_url:
          type: string
        is_default:
          type: boolean
        velero_namespace:
          type: string
        bsl_name:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        has_credentials:
          type: boolean
        cluster_id:
          type: string
          format: uuid
    StorageTestResult:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
    BackupControllerStatus:
      type: object
      properties:
        reconciler:
          type: object
          properties:
            enabled:
              type: boolean
            engine:
              type: string
              example: velero
        health:
          type: string
          enum:
            - healthy
            - degraded
        healthReasons:
          type: array
          items:
            type: string
        storage:
          type: object
          properties:
            count:
              type: integer
              format: int64
        backups:
          type: object
          properties:
            total:
              type: integer
            runningCount:
              type: integer
            failedCount:
              type: integer
            statuses:
              type: object
              additionalProperties:
                type: integer
        schedules:
          type: object
          properties:
            total:
              type: integer
            enabledCount:
              type: integer
        restores:
          type: object
          properties:
            total:
              type: integer
            runningCount:
              type: integer
            failedCount:
              type: integer
    HelmRepositoryAuthConfig:
      type: object
      description: |
        Credential document for a chart repository. This is the ONLY place
        credentials are accepted; the top level of the request body is not.

        At rest the secret-valued keys live in a Fernet envelope
        (`helm_repositories.auth_config_encrypted`, migration 145) and only the
        non-secret keys stay queryable as JSONB. Responses replace every
        non-empty secret with `__ASTRONOMER_SECRET_UNCHANGED__` and never carry
        the ciphertext.
      additionalProperties: true
      properties:
        username:
          type: string
        password:
          type: string
          description: write-only; read back as the redaction sentinel
        token:
          type: string
          description: write-only; read back as the redaction sentinel
        charts:
          type: array
          description: OCI only — the chart names to ingest from the registry.
          items:
            type: string
    HelmRepository:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        url:
          type: string
        repo_type:
          type: string
        description:
          type: string
        is_default:
          type: boolean
        auth_type:
          type: string
        auth_config:
          type: object
        enabled:
          type: boolean
        last_synced_at:
          description: |
            Last SUCCESSFUL ingest. A failed sync attempt deliberately does not
            advance it, so a repository that has been failing keeps reporting
            its real (stale) freshness.
          type: string
          format: date-time
          nullable: true
        last_sync_attempted_at:
          description: |
            Last time the scheduled sweep or the Sync button tried this
            repository, successfully or not. Compare with last_synced_at to
            tell "failing" from "no longer being visited".
          type: string
          format: date-time
          nullable: true
        last_sync_error:
          description: |
            Why the most recent sync attempt failed; empty when the last
            attempt succeeded. The scheduled catalog sweep isolates failures
            per repository, so this is the per-repo reason a repository is not
            refreshing while the rest of the catalog is.
          type: string
        created_by_id:
          type: string
          format: uuid
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        owner_project_id:
          type: string
          format: uuid
          nullable: true
        chart_count:
          description: |
            Number of charts ingested from this repository, used by the catalog
            Repositories table. Enrichment computed per response, not a stored
            column: a repository that has never synced reports 0.
          type: integer
          format: int64
    HelmChart:
      type: object
      properties:
        id:
          type: string
          format: uuid
        repository_id:
          type: string
          format: uuid
        name:
          type: string
        display_name:
          type: string
        description:
          type: string
        icon_url:
          type: string
        home_url:
          type: string
        category:
          type: string
        keywords:
          type: array
          items:
            type: string
        maintainers:
          type: array
          items:
            type: object
        deprecated:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    HelmChartVersion:
      type: object
      properties:
        id:
          type: string
          format: uuid
        chart_id:
          type: string
          format: uuid
        version:
          type: string
        app_version:
          type: string
        digest:
          type: string
        urls:
          type: array
          items:
            type: string
        values_schema:
          type: object
        default_values:
          type: string
        readme:
          type: string
        created_at_upstream:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    HelmChartVersionPage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data:
          type: array
          items: {$ref: '#/components/schemas/HelmChartVersion'}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    ChartRecommendation:
      type: object
      additionalProperties: false
      required: [chart_id, rating_count, avg_stars, bayesian_score]
      properties:
        chart_id:
          type: string
          format: uuid
        rating_count:
          type: integer
          format: int32
          minimum: 0
        avg_stars:
          type: number
          format: double
        bayesian_score:
          type: number
          format: double
        weight:
          type: integer
          format: int32
          minimum: 0
    ChartRating:
      type: object
      additionalProperties: false
      required: [id, chart_id, user_id, stars, note, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        chart_id: {type: string, format: uuid}
        installation_id: {type: string, format: uuid, nullable: true}
        user_id: {type: string, format: uuid}
        stars: {type: integer, format: int32, minimum: 1, maximum: 5}
        note: {type: string, maxLength: 280}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    ChartRatingWriteRequest:
      type: object
      additionalProperties: false
      required: [stars]
      properties:
        stars: {type: integer, format: int32, minimum: 1, maximum: 5}
        installation_id: {type: string, format: uuid}
        note: {type: string, maxLength: 280}
    ChartRatingAggregate:
      type: object
      additionalProperties: false
      required: [rating_count, avg_stars, bayesian_score, histogram]
      properties:
        rating_count: {type: integer, format: int32, minimum: 0}
        avg_stars: {type: number, format: double, minimum: 0, maximum: 5}
        bayesian_score: {type: number, format: double, minimum: 0, maximum: 5}
        histogram:
          type: array
          minItems: 5
          maxItems: 5
          items: {type: integer, format: int64, minimum: 0}
    ToolChart:
      type: object
      additionalProperties: false
      required: [chart_name, repo_url, namespace, order]
      properties:
        chart_name: {type: string}
        repo_url: {type: string, format: uri}
        namespace: {type: string}
        order: {type: integer}
    ToolFormField:
      type: object
      additionalProperties: false
      required: [path, label, type, group]
      properties:
        path: {type: string}
        label: {type: string}
        type: {type: string, enum: [string, number, boolean, select, storage]}
        group: {type: string}
        default: {type: string}
        options: {type: array, items: {type: string}}
        help: {type: string}
        placeholder: {type: string}
        storage_class_path: {type: string}
    ToolFormSchema:
      type: object
      additionalProperties: false
      required: [fields]
      properties:
        fields:
          type: array
          items: {$ref: '#/components/schemas/ToolFormField'}
    ClusterTool:
      type: object
      additionalProperties: false
      required:
        - id
        - slug
        - name
        - description
        - icon
        - category
        - charts
        - version_constraint
        - default_namespace
        - is_builtin
        - is_enabled
        - presets
        - service_name
        - service_port
        - service_path
        - sub_services
        - created_at
        - updated_at
      properties:
        id: {type: string, format: uuid}
        slug: {type: string}
        name: {type: string}
        description: {type: string}
        icon: {type: string}
        category: {type: string}
        charts: {type: array, items: {$ref: '#/components/schemas/ToolChart'}}
        version_constraint: {type: string}
        default_namespace: {type: string}
        is_builtin: {type: boolean}
        is_enabled: {type: boolean}
        helm_chart_id: {type: string, format: uuid, nullable: true}
        presets:
          type: object
          additionalProperties:
            type: object
            additionalProperties: true
        service_name: {type: string}
        service_port: {type: integer, format: int32, nullable: true}
        service_path: {type: string}
        sub_services:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [name, service, port]
            properties:
              name: {type: string}
              service: {type: string}
              port: {type: integer, format: int32}
        form_schema:
          allOf: [{$ref: '#/components/schemas/ToolFormSchema'}]
          nullable: true
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    ClusterToolPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous]
      properties:
        data:
          type: array
          items: {$ref: '#/components/schemas/ClusterTool'}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
    ToolActionRequest:
      type: object
      additionalProperties: false
      required: [cluster_id]
      properties:
        cluster_id: {type: string, format: uuid}
        preset: {type: string}
        values_override: {type: string}
        release_name: {type: string}
    ToolUninstallRequest:
      type: object
      additionalProperties: false
      required: [cluster_id]
      properties:
        cluster_id: {type: string, format: uuid}
    ToolPreview:
      type: object
      additionalProperties: false
      required: [charts, preset]
      properties:
        charts:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [chart_name, chart_version, namespace, values_yaml]
            properties:
              chart_name: {type: string}
              chart_version: {type: string}
              namespace: {type: string}
              values_yaml: {type: string}
        preset: {type: string}
    ToolOperationEvent:
      type: object
      additionalProperties: false
      required: [id, level, stage, message, createdAt]
      properties:
        id: {type: string, format: uuid}
        level: {type: string, enum: [info, warn, error]}
        stage: {type: string}
        message: {type: string}
        detail: {type: object, additionalProperties: true, nullable: true}
        createdAt: {type: string, format: date-time}
    ToolOperation:
      type: object
      additionalProperties: false
      required: [id, targetType, targetKey, operationType, status, attemptCount, errorMessage, createdAt, updatedAt]
      properties:
        id: {type: string, format: uuid}
        targetType: {type: string}
        targetKey: {type: string}
        operationType: {type: string, enum: [install, upgrade, uninstall, adopt]}
        status: {type: string, enum: [pending, running, completed, failed, superseded]}
        attemptCount: {type: integer, minimum: 0}
        startedAt: {type: string, format: date-time, nullable: true}
        completedAt: {type: string, format: date-time, nullable: true}
        errorMessage: {type: string}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
        events: {type: array, items: {$ref: '#/components/schemas/ToolOperationEvent'}}
    ClusterToolStatus:
      type: object
      additionalProperties: false
      required: [slug, name, status, release_name, namespace, preset_used, error]
      properties:
        slug: {type: string}
        name: {type: string}
        status: {type: string, enum: [not_installed, installed, installed_unmanaged, installing, upgrading, failed, uninstalling, unknown]}
        release_name: {type: string, nullable: true}
        namespace: {type: string, nullable: true}
        preset_used: {type: string, nullable: true}
        error: {type: string, nullable: true}
        operation: {$ref: '#/components/schemas/ToolOperation'}
    InstalledChart:
      type: object
      properties:
        id:
          type: string
          format: uuid
        cluster_id:
          type: string
          format: uuid
        chart_version_id:
          type: string
          format: uuid
          nullable: true
        release_name:
          type: string
        namespace:
          type: string
        values_override:
          type: string
        status:
          type: string
        revision:
          type: integer
        notes:
          type: string
        installed_by_id:
          type: string
          format: uuid
          nullable: true
        request_id:
          type: string
          format: uuid
          nullable: true
        tool_slug:
          type: string
          nullable: true
        preset_used:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CatalogOperation:
      type: object
      properties:
        id:
          type: string
          format: uuid
        targetType:
          type: string
        targetKey:
          type: string
        operationType:
          type: string
          description: install / upgrade / rollback / uninstall
        status:
          type: string
        attemptCount:
          type: integer
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        errorMessage:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CatalogOperationEvent:
      type: object
      properties:
        id:
          type: string
          format: uuid
        level:
          type: string
        stage:
          type: string
        message:
          type: string
        detail:
          type: object
        createdAt:
          type: string
          format: date-time
    CatalogOperationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/CatalogOperation'}
    CatalogRepositorySyncReceipt:
      type: object
      additionalProperties: false
      required: [repository_id, task_id, status]
      properties:
        repository_id: {type: string, format: uuid}
        task_id: {type: string, format: uuid}
        status: {type: string, enum: [pending, delivering, delivered, failed, dead]}
    CatalogRepositorySyncReceiptEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/CatalogRepositorySyncReceipt'}
    CatalogInstallationAcceptedEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data:
          type: object
          additionalProperties: false
          required: [installation, operation]
          properties:
            installation: {$ref: '#/components/schemas/InstalledChart'}
            operation: {$ref: '#/components/schemas/CatalogOperation'}
    HelmReleaseRevision:
      type: object
      additionalProperties: false
      required: [revision]
      properties:
        revision: {type: integer, minimum: 1}
        updated: {type: string}
        status: {type: string}
        chart: {type: string}
        app_version: {type: string}
        description: {type: string}
    HelmReleaseHistory:
      type: object
      additionalProperties: false
      required: [release_name, namespace, revisions]
      properties:
        release_name: {type: string}
        namespace: {type: string}
        revisions: {type: array, items: {$ref: '#/components/schemas/HelmReleaseRevision'}}
    HelmReleaseHistoryEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/HelmReleaseHistory'}
    InstalledAppEnriched:
      type: object
      properties:
        id:
          type: string
          format: uuid
        cluster_id:
          type: string
          format: uuid
        chart_id:
          type: string
          format: uuid
        chart_version_id:
          type: string
          format: uuid
        release_name:
          type: string
        namespace:
          type: string
        status:
          type: string
        revision:
          type: integer
        values_override:
          type: string
        tool_slug:
          type: string
        preset_used:
          type: string
        source_kind:
          type: string
          enum:
            - app
            - tool
        display_name:
          type: string
        chart_name:
          type: string
        chart_version:
          type: string
        chart_app_version:
          type: string
        chart_description:
          type: string
        chart_icon_url:
          type: string
        chart_category:
          type: string
        repo_name:
          type: string
        repo_type:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    Project:
      type: object
      description: Project row (cluster-scoped namespace policy container).
      additionalProperties: false
      required: [id, name, display_name, description, cluster_id, namespaces, resource_quota, limit_range, network_policy_mode, pod_security_profile, resource_quota_cpu_limit, resource_quota_memory_limit, resource_quota_pod_count, created_by_id, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        display_name:
          type: string
        cluster_id:
          type: string
          format: uuid
        description:
          type: string
        namespaces:
          type: array
          items:
            type: string
        resource_quota:
          type: object
          additionalProperties: true
        limit_range:
          type: object
          additionalProperties: true
        pod_security_profile:
          type: string
        network_policy_mode:
          type: string
          enum: [isolated, allow-same-project, none]
        resource_quota_cpu_limit:
          type: string
        resource_quota_memory_limit:
          type: string
        resource_quota_pod_count:
          type: integer
        created_by_id:
          type: string
          format: uuid
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateProjectRequest:
      type: object
      additionalProperties: false
      required:
        - name
        - cluster_id
      properties:
        name:
          type: string
        display_name:
          type: string
        cluster_id:
          type: string
          format: uuid
        description:
          type: string
        namespaces:
          type: array
          items:
            type: string
        pod_security_profile:
          type: string
        resource_quota:
          type: object
          additionalProperties: true
        limit_range:
          type: object
          additionalProperties: true
        network_policy_mode:
          type: string
        resource_quota_cpu_limit:
          type: string
        resource_quota_memory_limit:
          type: string
        resource_quota_pod_count:
          type: integer
          minimum: 0
    UpdateProjectRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          deprecated: true
          description: Deprecated compatibility field; project names are immutable on update.
        display_name:
          type: string
        description:
          type: string
        namespaces:
          type: array
          items:
            type: string
        pod_security_profile:
          type: string
        resource_quota:
          type: object
          additionalProperties: true
        limit_range:
          type: object
          additionalProperties: true
        network_policy_mode:
          type: string
        resource_quota_cpu_limit:
          type: string
        resource_quota_memory_limit:
          type: string
        resource_quota_pod_count:
          type: integer
          minimum: 0
        resource_quota_cpu: {type: string, deprecated: true, description: Deprecated alias for resource_quota_cpu_limit.}
        resource_quota_memory: {type: string, deprecated: true, description: Deprecated alias for resource_quota_memory_limit.}
        resource_quota_pods: {type: integer, deprecated: true, description: Deprecated alias for resource_quota_pod_count.}
    UpdateProjectPolicyRequest:
      type: object
      additionalProperties: false
      properties:
        pod_security_profile:
          type: string
        network_policy_mode:
          type: string
          enum: [isolated, allow-same-project, none]
        resource_quota_cpu_limit:
          type: string
        resource_quota_memory_limit:
          type: string
        resource_quota_pod_count:
          type: integer
          minimum: 0
        resource_quota_cpu: {type: string, deprecated: true, description: Deprecated alias for resource_quota_cpu_limit.}
        resource_quota_memory: {type: string, deprecated: true, description: Deprecated alias for resource_quota_memory_limit.}
        resource_quota_pods: {type: integer, deprecated: true, description: Deprecated alias for resource_quota_pod_count.}
    ProjectNamespaceRequest:
      type: object
      required:
        - namespace
      properties:
        namespace:
          type: string
    ProjectCatalog:
      type: object
      description: Helm catalog visible to a project, with a visibility discriminator.
      additionalProperties: true
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        url:
          type: string
        visibility:
          type: string
          enum:
            - global
            - own
            - subscribed
    CreateProjectCatalogRequest:
      type: object
      additionalProperties: false
      required:
        - name
        - url
      properties:
        name:
          type: string
        url:
          type: string
        description:
          type: string
        repo_type:
          type: string
        auth_type:
          type: string
        auth_config:
          type: object
          additionalProperties: true
        enabled:
          type: boolean
    ProjectCatalogSubscription:
      type: object
      properties:
        id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        catalog_id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
    CloudCredential:
      type: object
      description: Project cloud-provider credential (secret values redacted).
      additionalProperties: true
      properties:
        id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        name:
          type: string
        provider:
          type: string
        description:
          type: string
        data:
          type: object
          additionalProperties:
            type: string
        target_refs:
          type: array
          items:
            type: object
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CloudCredentialRequest:
      type: object
      required:
        - name
        - provider
      properties:
        name:
          type: string
        provider:
          type: string
        description:
          type: string
        data:
          type: object
          additionalProperties:
            type: string
        target_refs:
          type: array
          items:
            type: object
    RBACRole:
      type: object
      additionalProperties: true
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        display_name:
          type: string
        description:
          type: string
        scope:
          type: string
          enum:
            - global
            - cluster
            - project
        is_builtin:
          type: boolean
        rules:
          type: array
          items:
            $ref: '#/components/schemas/RBACRule'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    RBACRoleRequest:
      type: object
      additionalProperties: false
      required:
        - name
      properties:
        name:
          type: string
        display_name:
          type: string
        displayName:
          type: string
          deprecated: true
          x-go-name: DisplayNameLegacy
          description: Deprecated camelCase alias for display_name.
        description:
          type: string
        permissions:
          type: object
          additionalProperties: true
        rules:
          type: array
          items:
            $ref: '#/components/schemas/RBACRule'
        scope:
          type: string
          enum: [global, cluster, project]
          deprecated: true
          description: Deprecated compatibility field; scope is selected by the role collection path.
    RBACGlobalRoleBinding:
      type: object
      additionalProperties: true
      properties:
        id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
          nullable: true
        group:
          type: string
        created_at:
          type: string
          format: date-time
    RBACClusterRoleBinding:
      type: object
      additionalProperties: true
      properties:
        id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        cluster_id:
          type: string
          format: uuid
        namespace:
          type: string
          description: >-
            Optional Kubernetes namespace this binding is scoped to. Empty
            string means the binding applies to the full cluster scope.
        user_id:
          type: string
          format: uuid
          nullable: true
        group:
          type: string
        created_at:
          type: string
          format: date-time
    RBACProjectRoleBinding:
      type: object
      additionalProperties: true
      properties:
        id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
          nullable: true
        group:
          type: string
        created_at:
          type: string
          format: date-time
    RBACBindingRequest:
      type: object
      required:
        - role_id
      properties:
        role_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        group:
          type: string
    RBACClusterBindingRequest:
      type: object
      required:
        - role_id
        - cluster_id
      properties:
        role_id:
          type: string
          format: uuid
        cluster_id:
          type: string
          format: uuid
        namespace:
          type: string
          description: >-
            Optional Kubernetes namespace to scope the binding to. Empty or
            omitted means the binding applies to the full cluster scope.
        user_id:
          type: string
          format: uuid
        group:
          type: string
    RBACProjectBindingRequest:
      type: object
      required:
        - role_id
        - project_id
      properties:
        role_id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        group:
          type: string
    RBACEffectivePermissions:
      type: object
      additionalProperties: false
      required:
        - subject
        - superuser
        - context
        - bindings
        - permissions
      properties:
        subject:
          $ref: '#/components/schemas/RBACEffectivePermissionSubject'
        superuser:
          type: boolean
        context:
          $ref: '#/components/schemas/RBACEffectivePermissionContext'
        bindings:
          type: array
          items:
            $ref: '#/components/schemas/RBACEffectivePermissionBinding'
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/RBACEffectivePermissionGrant'
    RBACEffectivePermissionSubject:
      type: object
      additionalProperties: false
      required:
        - user_id
        - self
      properties:
        user_id:
          type: string
          format: uuid
        self:
          type: boolean
    RBACEffectivePermissionContext:
      type: object
      additionalProperties: false
      required:
        - namespace_scoped_bindings_supported
      properties:
        cluster_id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        namespace:
          type: string
        namespace_scoped_bindings_supported:
          type: boolean
        warnings:
          type: array
          items:
            type: string
    RBACRule:
      type: object
      additionalProperties: false
      required:
        - verbs
      properties:
        resource:
          type: string
        resources:
          type: array
          items:
            type: string
        verbs:
          type: array
          items:
            type: string
        api_groups:
          type: array
          items:
            type: string
    RBACEffectivePermissionSource:
      type: object
      additionalProperties: false
      required:
        - scope
      properties:
        scope:
          type: string
        binding_id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        role_name:
          type: string
        cluster_id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        namespace:
          type: string
    RBACEffectivePermissionBinding:
      type: object
      additionalProperties: false
      required:
        - scope
        - rules
      properties:
        scope:
          type: string
        binding_id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        role_name:
          type: string
        group:
          type: string
        cluster_id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        namespace:
          type: string
        superuser:
          type: boolean
        rules:
          type: array
          items:
            $ref: '#/components/schemas/RBACRule'
    RBACEffectivePermissionGrant:
      type: object
      additionalProperties: false
      required:
        - resource
        - verb
        - applies_to_context
        - sources
      properties:
        resource:
          type: string
        verb:
          type: string
        applies_to_context:
          type: boolean
        inherited:
          type: boolean
        inherited_from:
          type: string
        sources:
          type: array
          items:
            $ref: '#/components/schemas/RBACEffectivePermissionSource'
    RBACPermissionPreviewRequest:
      type: object
      additionalProperties: false
      properties:
        template_name:
          type: string
        role_id:
          type: string
          format: uuid
        scope:
          type: string
          enum:
            - global
            - cluster
            - project
          default: global
        cluster_id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        rules:
          type: array
          items:
            $ref: '#/components/schemas/RBACRule'
    RBACPermissionPreview:
      type: object
      additionalProperties: false
      required:
        - scope
        - risk_level
        - permissions
        - sensitive_flags
      properties:
        scope:
          type: string
        role_id:
          type: string
          format: uuid
        role_name:
          type: string
        template_name:
          type: string
        risk_level:
          type: string
        warnings:
          type: array
          items:
            type: string
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/RBACEffectivePermissionGrant'
        sensitive_flags:
          $ref: '#/components/schemas/RBACPermissionSensitiveFlags'
    RBACEffectivePermissionsEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/RBACEffectivePermissions'}
    RBACPermissionPreviewEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/RBACPermissionPreview'}
    RBACPermissionSensitiveFlags:
      type: object
      additionalProperties: false
      required:
        - wildcard
        - can_mutate
        - can_delete
        - can_exec
        - can_proxy
        - can_read_secrets
        - can_manage_rbac
        - can_restore
      properties:
        wildcard:
          type: boolean
        can_mutate:
          type: boolean
        can_delete:
          type: boolean
        can_exec:
          type: boolean
        can_proxy:
          type: boolean
        can_read_secrets:
          type: boolean
        can_manage_rbac:
          type: boolean
        can_restore:
          type: boolean
    RBACTemplate:
      type: object
      additionalProperties: true
      properties:
        name:
          type: string
        display_name:
          type: string
        description:
          type: string
        scope:
          type: string
          enum:
            - global
            - cluster
            - project
        rules:
          type: array
          items:
            type: object
            properties:
              resource:
                type: string
              verbs:
                type: array
                items:
                  type: string
    ApiTokenListItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        prefix:
          type: string
        expires_at:
          type: string
          format: date-time
          nullable: true
        last_used_at:
          type: string
          format: date-time
          nullable: true
        is_revoked:
          type: boolean
        created_at:
          type: string
          format: date-time
        scopes:
          type: array
          items:
            type: string
        allowed_cidrs:
          type: string
        last_seen_remote_ip:
          type: string
    ApiTokenCreated:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        token:
          type: string
          description: Plaintext, returned once.
        prefix:
          type: string
        expires_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        scopes:
          type: array
          items:
            type: string
        allowed_cidrs:
          type: string
    DexConnectorType:
      type: object
      additionalProperties: false
      properties:
        type:
          type: string
        display_hint:
          type: string
        required:
          type: array
          items:
            type: string
        optional:
          type: array
          items:
            type: string
        secret:
          type: array
          items:
            type: string
        nested:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [parent, keys]
            properties:
              parent:
                type: string
              keys:
                type: array
                items:
                  type: string
      required: [type, display_hint, required, optional, secret, nested]
    DexConnector:
      type: object
      additionalProperties: true
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        type:
          type: string
        display_name:
          type: string
        config:
          type: object
          additionalProperties: true
        enabled:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
      required: [id, name, type, display_name, config, enabled, created_at, updated_at]
    DexConnectorRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
        type:
          type: string
        display_name:
          type: string
        config:
          type: object
          additionalProperties: true
        enabled:
          type: boolean
    DexSettings:
      type: object
      additionalProperties: true
      properties:
        configured:
          type: boolean
        issuer_url:
          type: string
        namespace:
          type: string
        release_name:
          type: string
        chart_release_name:
          type: string
        deployment_name:
          type: string
        service_name:
          type: string
        runtime_secret_name:
          type: string
        configmap_name:
          type: string
          deprecated: true
          description: Compatibility alias of runtime_secret_name; never identifies a credential-bearing ConfigMap.
        public_clients:
          type: array
          items:
            type: object
            additionalProperties: false
            properties:
              id:
                type: string
              name:
                type: string
              redirectURIs:
                type: array
                minItems: 1
                items:
                  type: string
                  format: uri
              secret:
                type: string
                description: Always redacted to an empty string on reads.
              secret_configured:
                type: boolean
              public:
                type: boolean
              trustedPeers:
                type: array
                items:
                  type: string
        cluster_id:
          type: string
          format: uuid
          nullable: true
        updated_at:
          type: string
          format: date-time
        expiry:
          $ref: '#/components/schemas/DexExpiry'
        extra:
          $ref: '#/components/schemas/DexExtra'
        runtime_generation: { type: integer, format: int64 }
        runtime_phase: { type: string, enum: [fresh, prepare, cutover] }
        runtime_staged_generation: { type: integer, format: int64 }
        runtime_applied_generation: { type: integer, format: int64 }
    DexSettingsRequest:
      type: object
      additionalProperties: false
      required:
        - issuer_url
      properties:
        issuer_url:
          type: string
        namespace:
          type: string
        release_name:
          type: string
        chart_release_name:
          type: string
        deployment_name:
          type: string
        service_name:
          type: string
        runtime_secret_name:
          type: string
        configmap_name:
          type: string
          deprecated: true
        public_clients:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [id]
            properties:
              id: { type: string }
              name: { type: string }
              redirectURIs:
                type: array
                minItems: 1
                items: { type: string, format: uri }
              secret: { type: string, format: password, writeOnly: true }
              secret_configured: { type: boolean, deprecated: true }
              secretConfigured:
                type: boolean
                deprecated: true
                x-go-name: SecretConfiguredLegacy
              public: { type: boolean }
              trustedPeers:
                type: array
                items: { type: string }
        cluster_id:
          type: string
          format: uuid
        expiry:
          $ref: '#/components/schemas/DexExpiry'
        extra:
          $ref: '#/components/schemas/DexExtra'
    DexExpiry:
      type: object
      additionalProperties: false
      properties:
        idTokens: { type: string, pattern: '^[0-9]+(ns|us|ms|s|m|h)$' }
        signingKeys: { type: string, pattern: '^[0-9]+(ns|us|ms|s|m|h)$' }
        refreshTokens:
          type: object
          additionalProperties: false
          properties:
            reuseInterval: { type: string, pattern: '^[0-9]+(ns|us|ms|s|m|h)$' }
            validIfNotUsedFor: { type: string, pattern: '^[0-9]+(ns|us|ms|s|m|h)$' }
            absoluteLifetime: { type: string, pattern: '^[0-9]+(ns|us|ms|s|m|h)$' }
    DexExtra:
      type: object
      additionalProperties: false
      properties:
        logger:
          type: object
          additionalProperties: false
          properties:
            level: { type: string }
            format: { type: string }
        frontend:
          type: object
          additionalProperties: false
          properties:
            issuer: { type: string }
            logoURL: { type: string, format: uri }
            dir: { type: string }
            theme: { type: string }
        grpc:
          type: object
          additionalProperties: false
          properties:
            addr: { type: string }
        telemetry:
          type: object
          additionalProperties: false
          properties:
            http: { type: string }
    DexRegisterSSOResult:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        provider:
          type: string
        client_id:
          type: string
        display_name:
          type: string
        issuer_url: { type: string, format: uri }
        is_enabled: { type: boolean }
        verified: { type: boolean }
        staged: { type: boolean }
        applied: { type: boolean }
        runtime_state: { type: string, enum: [staged, applied] }
        secret_resource_version: { type: string }
        runtime_changed: { type: boolean }
        runtime_generation: { type: integer, format: int64 }
        created: { type: boolean }
        updated: { type: boolean }
    DexOperation:
      type: object
      additionalProperties: false
      required: [operation_id, action, target_id, runtime_generation, status, phase, attempt_count, status_url, created_at, updated_at]
      properties:
        operation_id: { type: string, format: uuid }
        action: { type: string, enum: [apply, register_sso] }
        target_id: { type: string, format: uuid }
        runtime_generation: { type: integer, format: int64, minimum: 0 }
        status: { type: string, enum: [pending, running, retrying, failed, succeeded] }
        phase: { type: string, enum: [queued, reconciling, finalizing, completed] }
        attempt_count: { type: integer, minimum: 0 }
        error_code: { type: string }
        status_url: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        completed_at: { type: string, format: date-time }
    UsersSettingsUserListItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        email:
          type: string
          format: email
        displayName:
          type: string
        provider:
          type: string
          example: local
        globalRoles:
          type: array
          items:
            type: string
        is_superuser:
          type: boolean
        enabled:
          type: boolean
        lastLogin:
          type: string
          description: RFC3339 or empty string.
        createdAt:
          type: string
          format: date-time
    UsersSettingsUpdateUserRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        username:
          type: string
        first_name:
          type: string
        last_name:
          type: string
        is_active:
          type: boolean
          nullable: true
    UsersSettingsGeneral:
      type: object
      properties:
        platformName:
          type: string
        agentHeartbeatInterval:
          type: integer
        defaultSessionTimeout:
          type: integer
        enableAuditLogging:
          type: boolean
        metricsCollection:
          type: boolean
    UsersSettingsAuditLogEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
        action:
          type: string
        resourceType:
          type: string
        resourceName:
          type: string
        user:
          type: string
          nullable: true
          format: uuid
        userAgent:
          type: string
        sourceIP:
          type: string
        status:
          type: string
          example: success
        details: {}
        timestamp:
          type: string
          format: date-time
    UsersSettingsSSOProvider:
      type: object
      properties:
        id:
          type: string
          format: uuid
        provider:
          type: string
        type:
          type: string
        name:
          type: string
        enabled:
          type: boolean
        config:
          type: object
          additionalProperties:
            type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    UsersSettingsTokenListItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        prefix:
          type: string
        expires_at:
          type: string
          format: date-time
          nullable: true
        last_used_at:
          type: string
          format: date-time
          nullable: true
        is_revoked:
          type: boolean
        created_at:
          type: string
          format: date-time
        scopes:
          type: array
          items:
            type: string
        allowed_cidrs:
          type: string
        last_seen_remote_ip:
          type: string
    UsersSettingsCreatedToken:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        token:
          type: string
          description: Plaintext, returned once.
        prefix:
          type: string
        expires_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        scopes:
          type: array
          items:
            type: string
        allowed_cidrs:
          type: string
    SmtpSettings:
      type: object
      properties:
        enabled:
          type: boolean
        host:
          type: string
        port:
          type: integer
        username:
          type: string
        password:
          type: string
          description: Always the sentinel `<encrypted>`.
        password_configured:
          type: boolean
        from_address:
          type: string
          format: email
        from_name:
          type: string
        auth_mechanism:
          type: string
          enum:
            - plain
            - login
            - cram-md5
            - none
        encryption:
          type: string
          enum:
            - starttls
            - tls
            - none
        require_tls:
          type: boolean
        timeout_seconds:
          type: integer
        updated_at:
          type: string
          format: date-time
    VaultConnection:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        addr:
          type: string
        auth_method:
          type: string
          enum:
            - token
            - approle
            - kubernetes
        auth:
          type: object
          additionalProperties:
            type: string
          description: Method-specific; secret parts redacted to `<encrypted>`.
        namespace:
          type: string
        tls_skip_verify:
          type: boolean
        ca_cert_pem:
          type: string
        default_mount:
          type: string
        enabled:
          type: boolean
        last_health_at:
          type: string
          format: date-time
        last_health_ok:
          type: boolean
        last_error:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    VaultConnectionRequest:
      type: object
      required:
        - name
        - addr
        - auth_method
      properties:
        name:
          type: string
          maxLength: 128
        description:
          type: string
        addr:
          type: string
          description: Must start with http:// (dev, requires tls_skip_verify) or https://.
        auth_method:
          type: string
          enum:
            - token
            - approle
            - kubernetes
        auth:
          type: object
          additionalProperties:
            type: string
          description: Method-specific; send `<encrypted>` to preserve a stored secret on PUT.
        namespace:
          type: string
        tls_skip_verify:
          type: boolean
        ca_cert_pem:
          type: string
        default_mount:
          type: string
          default: secret
        enabled:
          type: boolean
    VaultTestResult:
      type: object
      properties:
        ok:
          type: boolean
        reachable:
          type: boolean
        auth_ok:
          type: boolean
        latency_ms:
          type: integer
          format: int64
        message:
          type: string
        probe_path:
          type: string
    ShellSession:
      type: object
      properties:
        id:
          type: string
          format: uuid
        cluster_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        status:
          type: string
        pod_name:
          type: string
        pod_namespace:
          type: string
        container:
          type: string
        started_at:
          type: string
          format: date-time
        last_input_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        idle_timeout_seconds:
          type: integer
          format: int64
        command_count:
          type: integer
          format: int64
    WebhookSubscription:
      type: object
      additionalProperties: false
      required: [id, name, url, secret, secret_configured, event_filters, payload_template, extra_headers, enabled, max_retries, timeout_seconds, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        url:
          type: string
        secret:
          type: string
          description: Always the sentinel `<encrypted>`.
        secret_configured:
          type: boolean
        event_filters:
          type: array
          items:
            type: string
        payload_template:
          type: string
        extra_headers:
          type: object
          additionalProperties:
            type: string
        enabled:
          type: boolean
        max_retries:
          type: integer
        timeout_seconds:
          type: integer
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    WebhookSubscriptionRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          maxLength: 128
        url:
          type: string
          description: Valid http(s) URL with a host.
        secret:
          type: string
          description: Required on create; send `<encrypted>` on PUT to keep existing.
        event_filters:
          type: array
          items:
            type: string
        payload_template:
          type: string
        extra_headers:
          type: object
          additionalProperties:
            type: string
        enabled:
          type: boolean
        max_retries:
          type: integer
          minimum: 0
          maximum: 50
        timeout_seconds:
          type: integer
          minimum: 1
          maximum: 300
    WebhookDelivery:
      type: object
      additionalProperties: false
      required: [id, event_name, event_id, status, attempts, payload_size, response_status, response_body, last_error, delivered_at, next_attempt_at, created_at]
      properties:
        id:
          type: string
          format: uuid
        event_name:
          type: string
        event_id:
          type: string
        status:
          type: string
        attempts:
          type: integer
        payload_size:
          type: integer
        response_status:
          type: integer
        response_body:
          type: string
        last_error:
          type: string
        delivered_at:
          type: string
          format: date-time
          nullable: true
        next_attempt_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
    AuditLogEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
          nullable: true
        user:
          type: string
          description: Actor UUID, or 'system' when unset.
        source:
          type: string
        correlation_id:
          type: string
        action:
          type: string
        action_class:
          type: string
        resource_type:
          type: string
        resource_id:
          type: string
        resource_name:
          type: string
        detail:
          description: Raw JSON detail payload.
          nullable: true
        details:
          description: Duplicate of detail (back-compat alias).
          nullable: true
        actor_auth_method:
          type: string
        http_method:
          type: string
        path:
          type: string
        status_code:
          type: integer
          format: int32
        status:
          type: string
          description: 'Derived label: success / failure / error.'
        duration_ms:
          type: integer
          format: int64
        ip_address:
          type: string
          nullable: true
        source_ip:
          type: string
          description: Same as ip_address; empty when unset.
        user_agent:
          type: string
        request_id:
          type: string
        timestamp:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
          description: Mirrors created_at; audit rows are append-only.
    NodeSummary:
      type: object
      additionalProperties: true
      properties:
        name:
          type: string
        status:
          type: string
        roles:
          type: array
          items:
            type: string
        kubernetesVersion:
          type: string
        os:
          type: string
        architecture:
          type: string
        containerRuntime:
          type: string
        cpuCapacity:
          type: integer
        cpuUsage:
          type: integer
        memoryCapacity:
          type: integer
          format: int64
        memoryUsage:
          type: integer
          format: int64
        podCapacity:
          type: integer
        podCount:
          type: integer
        conditions:
          type: array
          items:
            type: object
        createdAt:
          type: string
          format: date-time
    NodeSummaryListEnvelope:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/NodeSummary'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    NodeDetail:
      type: object
      additionalProperties: true
      properties:
        name:
          type: string
        status:
          type: string
        roles:
          type: array
          items:
            type: string
        labels:
          type: object
          additionalProperties:
            type: string
        annotations:
          type: object
          additionalProperties:
            type: string
        createdAt:
          type: string
          format: date-time
        nodeInfo:
          type: object
          additionalProperties: true
        kubeletVersion:
          type: string
        cpuCapacity:
          type: integer
        cpuUsage:
          type: integer
        memoryCapacity:
          type: integer
          format: int64
        memoryUsage:
          type: integer
          format: int64
        podCapacity:
          type: integer
        podCount:
          type: integer
        addresses:
          type: array
          items:
            type: object
        conditions:
          type: array
          items:
            type: object
        taints:
          type: array
          items:
            type: object
        images:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              sizeBytes:
                type: integer
                format: int64
        pods:
          type: array
          items:
            type: object
        events:
          type: array
          items:
            type: object
        unschedulable:
          type: boolean
    Pod:
      type: object
      additionalProperties: true
      properties:
        name:
          type: string
        namespace:
          type: string
        clusterId:
          type: string
        phase:
          type: string
        status:
          type: string
        ready:
          type: string
          description: '"n/m" ready ratio'
        restarts:
          type: integer
        node:
          type: string
        ip:
          type: string
        containers:
          type: array
          items:
            type: object
        conditions:
          type: array
          items:
            type: object
        createdAt:
          type: string
          format: date-time
        age:
          type: string
        images:
          type: array
          items:
            type: string
    Namespace:
      type: object
      additionalProperties: true
      properties:
        name:
          type: string
        clusterId:
          type: string
        status:
          type: string
        labels:
          type: object
          additionalProperties:
            type: string
        annotations:
          type: object
          additionalProperties:
            type: string
        podCount:
          type: integer
        cpuUsage:
          type: integer
        cpuLimit:
          type: integer
        memoryUsage:
          type: integer
        memoryLimit:
          type: integer
        createdAt:
          type: string
          format: date-time
    ClusterEvent:
      type: object
      additionalProperties: true
      properties:
        id:
          type: string
        type:
          type: string
        reason:
          type: string
        message:
          type: string
        involvedObject:
          type: object
          properties:
            kind:
              type: string
            name:
              type: string
            namespace:
              type: string
        count:
          type: integer
        firstTimestamp:
          type: string
          format: date-time
        lastTimestamp:
          type: string
          format: date-time
    Workload:
      type: object
      additionalProperties: true
      properties:
        name:
          type: string
        namespace:
          type: string
        kind:
          type: string
        clusterId:
          type: string
        clusterName:
          type: string
        status:
          type: string
        ready:
          type: string
        upToDate:
          type: integer
        available:
          type: integer
        replicas:
          type: integer
        desiredReplicas:
          type: integer
        images:
          type: array
          items:
            type: string
        labels:
          type: object
          additionalProperties:
            type: string
        annotations:
          type: object
          additionalProperties:
            type: string
        createdAt:
          type: string
          format: date-time
        age:
          type: string
    WorkloadOperation:
      type: object
      additionalProperties: true
      properties:
        id:
          type: string
          format: uuid
        targetType:
          type: string
        targetKey:
          type: string
        operationType:
          type: string
        status:
          type: string
        attemptCount:
          type: integer
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        errorMessage:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    WorkloadOperationEvent:
      type: object
      additionalProperties: true
      properties:
        id:
          type: string
          format: uuid
        level:
          type: string
        stage:
          type: string
        message:
          type: string
        detail:
          type: object
        createdAt:
          type: string
          format: date-time
    PodLogEntry:
      type: object
      properties:
        timestamp:
          type: string
        message:
          type: string
        container:
          type: string
    ResourceRow:
      type: object
      description: Flattened resource row emitted by flattenGeneric and the type-specific flatten helpers. Base fields are always present; additional fields vary by resource type (e.g. dataCount, schedule, minReplicas, replicas, ready, status, phase, age, type).
      properties:
        name:
          type: string
        namespace:
          type: string
        clusterId:
          type: string
        labels:
          type: object
          additionalProperties:
            type: string
        annotations:
          type: object
          additionalProperties:
            type: string
        createdAt:
          type: string
          description: creationTimestamp from metadata
      additionalProperties: true
    ResourceSearchResult:
      type: object
      description: Cross-cluster search payload (the value of `data`).
      properties:
        results:
          type: array
          description: Merged, capped resource rows. Each row also carries cluster_id, cluster_name, clusterId and clusterName for provenance.
          items:
            allOf:
              - $ref: '#/components/schemas/ResourceRow'
              - type: object
                properties:
                  cluster_id:
                    type: string
                  cluster_name:
                    type: string
                  clusterName:
                    type: string
        errors:
          type: array
          description: Per-cluster failures surfaced inline.
          items:
            type: object
            properties:
              cluster_id:
                type: string
              cluster_name:
                type: string
              error:
                type: string
            required:
              - cluster_id
              - cluster_name
              - error
        clusters_queried:
          type: integer
        clusters_failed:
          type: integer
        type:
          type: string
        truncated:
          type: boolean
      required:
        - results
        - errors
        - clusters_queried
        - clusters_failed
        - type
        - truncated
    AllowlistResponse:
      type: object
      additionalProperties: false
      required: [cluster_id, operator_cidrs, astronomer_egress, emergency, desired, effective, mode, detected_provider, sync_status, drift, capability]
      properties:
        cluster_id: {type: string, format: uuid}
        operator_cidrs: {type: array, items: {type: string}}
        astronomer_egress: {type: array, items: {type: string}}
        emergency: {type: array, items: {type: string}}
        desired: {type: array, items: {type: string}}
        effective: {type: array, items: {type: string}}
        mode: {type: string, enum: [monitor, enforce, disabled]}
        detected_provider: {type: string}
        sync_status: {type: string}
        last_error: {type: string}
        last_reconciled_at: {type: string, format: date-time}
        drift: {type: boolean}
        capability:
          type: object
          additionalProperties: false
          required: [provider, can_monitor, can_enforce]
          properties:
            provider: {type: string}
            can_monitor: {type: boolean}
            can_enforce: {type: boolean}
            reason: {type: string}
            required_metadata: {type: array, items: {type: string}}
    AllowlistSnapshotResponse:
      type: object
      additionalProperties: false
      required: [id, cluster_id, captured_at, effective_cidrs, desired_cidrs, drift]
      properties:
        id: {type: integer, format: int64}
        cluster_id: {type: string, format: uuid}
        captured_at: {type: string, format: date-time}
        effective_cidrs: {type: array, items: {type: string}}
        desired_cidrs: {type: array, items: {type: string}}
        drift: {type: boolean}
    AllowlistUpdateRequest:
      type: object
      additionalProperties: false
      required: [cidrs, mode]
      properties:
        cidrs:
          type: array
          maxItems: 100
          uniqueItems: true
          items: {type: string}
        mode: {type: string, enum: [monitor, enforce, disabled]}
        force_apply: {type: boolean, default: false}
    ApplyClusterTemplateRequest:
      type: object
      additionalProperties: false
      required: [template_id]
      properties:
        template_id: {type: string, format: uuid}
    ApplyNetworkPolicyRequest:
      type: object
      additionalProperties: false
      required: [template_id]
      properties:
        template_id: {type: string, format: uuid}
        namespace: {type: string}
        namespaces:
          type: array
          uniqueItems: true
          items: {type: string}
    CloudProviderSpec:
      type: object
      description: >-
        Schema not yet fully enumerated; shape is the handler's JSON payload.
        Permissive object placeholder so every $ref in this spec resolves.
      additionalProperties: true
    ClusterConditionResponse:
      type: object
      additionalProperties: false
      required: [type, status, reason, message, last_transition_time, last_probe_time]
      properties:
        type: {type: string}
        status: {type: string, enum: ['True', 'False', 'Unknown']}
        reason: {type: string}
        message: {type: string}
        last_transition_time: {type: string, format: date-time}
        last_probe_time: {type: string, format: date-time}
    ClusterConditionRemediationAttempt:
      type: object
      additionalProperties: false
      required: [id, cluster_id, condition_type, action, outcome, error, detail, attempted_at]
      properties:
        id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid}
        condition_type: {type: string}
        action: {type: string}
        outcome: {type: string, enum: [success, failed, skipped]}
        error: {type: string}
        detail:
          type: object
          additionalProperties: true
          nullable: true
        attempted_at: {type: string, format: date-time}
    ClusterConditionListEnvelope:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ClusterConditionResponse'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    ClusterConditionRemediationListEnvelope:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ClusterConditionRemediationAttempt'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    ClusterGroupResponse:
      type: object
      additionalProperties: false
      required: [id, name, slug, description, color, icon, enabled, created_at, updated_at, cluster_count, cluster_count_tree]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        slug: {type: string}
        description: {type: string}
        parent_id: {type: string, format: uuid}
        color: {type: string}
        icon: {type: string}
        enabled: {type: boolean}
        created_by: {type: string, format: uuid}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        cluster_count: {type: integer, format: int64}
        cluster_count_tree: {type: integer, format: int64}
        depth:
          type: integer
          format: int32
          description: Present on the tree-list response and omitted on single/create/update responses.
    ClusterRegistryConfigLegacy:
      type: object
      description: >-
        Schema not yet fully enumerated; shape is the handler's JSON payload.
        Permissive object placeholder so every $ref in this spec resolves.
      additionalProperties: true
    ClusterRegistryRequest:
      type: object
      additionalProperties: false
      properties:
        private_registry_url: {type: string}
        registry_username: {type: string}
        registry_password: {type: string, writeOnly: true}
        insecure: {type: boolean, default: false}
        ca_bundle: {type: string}
        namespaces:
          type: array
          uniqueItems: true
          items: {type: string}
        inject_default_sa: {type: boolean}
        secret_name: {type: string}
    ClusterRegistryResponse:
      type: object
      additionalProperties: false
      required: [id, cluster_id, private_registry_url, registry_username, registry_password, insecure, ca_bundle, namespaces, inject_default_sa, secret_name, last_apply_error, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid}
        private_registry_url: {type: string}
        registry_username: {type: string}
        registry_password: {type: string, readOnly: true}
        insecure: {type: boolean}
        ca_bundle: {type: string}
        namespaces: {type: array, items: {type: string}}
        inject_default_sa: {type: boolean}
        secret_name: {type: string}
        last_applied_at: {type: string, format: date-time}
        last_apply_error: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    ClusterRegistryTestResponse:
      type: object
      additionalProperties: false
      required: [ok, status_code, message]
      properties:
        ok: {type: boolean}
        status_code: {type: integer}
        message: {type: string}
    ClusterResponse:
      $ref: '#/components/schemas/Cluster'
    ClusterTemplateApplicationResponse:
      type: object
      additionalProperties: false
      required: [cluster_id, template_id, status, spec_snapshot, created_at, updated_at]
      properties:
        cluster_id: {type: string, format: uuid}
        template_id: {type: string, format: uuid}
        template_name: {type: string}
        status: {type: string}
        spec_snapshot: {}
        last_error: {type: string}
        applied_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        drift: {type: string, enum: [synced, drift, '']}
    ClusterTemplateResponse:
      type: object
      description: >-
        Schema not yet fully enumerated; shape is the handler's JSON payload.
        Permissive object placeholder so every $ref in this spec resolves.
      additionalProperties: true
    CreateClusterGroupRequest:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: {type: string, minLength: 1}
        slug: {type: string}
        description: {type: string}
        parent_id: {type: string, format: uuid}
        color: {type: string}
        icon: {type: string}
    CreateClusterTemplateRequest:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: {type: string, minLength: 1}
        description: {type: string}
        spec: {type: object, additionalProperties: true}
    CreateNetworkPolicyTemplateRequest:
      type: object
      additionalProperties: false
      required: [name, spec_template]
      properties:
        slug: {type: string}
        name: {type: string, minLength: 1}
        description: {type: string}
        spec_template: {type: string, minLength: 1}
        enabled: {type: boolean}
        clone_from: {type: string}
    DecommissionStatusResponse:
      type: object
      description: >-
        Schema not yet fully enumerated; shape is the handler's JSON payload.
        Permissive object placeholder so every $ref in this spec resolves.
      additionalProperties: true
    MTLSBreakdownResponse:
      type: object
      additionalProperties: false
      required: [cluster_id, mesh, mtls_coverage_pct, total_count, rows]
      properties:
        cluster_id: {type: string, format: uuid}
        mesh: {type: string}
        mtls_coverage_pct: {type: integer}
        total_count: {type: integer}
        notice: {type: string}
        rows:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [namespace, mode, rules]
            properties:
              namespace: {type: string}
              mode: {type: string}
              rules: {type: integer}
    MoveClustersRequest:
      type: object
      additionalProperties: false
      required: [cluster_ids]
      properties:
        cluster_ids:
          type: array
          minItems: 1
          uniqueItems: true
          items: {type: string, format: uuid}
    InhibitionMatcher:
      type: object
      properties:
        label:
          type: string
        value:
          type: string
        is_regex:
          type: boolean
      required:
        - label
        - value
    InhibitionMatcherResponse:
      type: object
      additionalProperties: false
      required: [label, value, is_regex]
      properties:
        label: {type: string}
        value: {type: string}
        is_regex: {type: boolean}
    InhibitionRequest:
      type: object
      description: Alert inhibition rule create/update body (P-03).
      properties:
        name:
          type: string
        source_matchers:
          type: array
          items:
            $ref: '#/components/schemas/InhibitionMatcher'
        target_matchers:
          type: array
          items:
            $ref: '#/components/schemas/InhibitionMatcher'
        equal_labels:
          type: array
          items:
            type: string
        enabled:
          type: boolean
      required:
        - name
    PaginationMetadata:
      type: object
      additionalProperties: false
      required: [limit, offset, has_more, next_offset]
      properties:
        total: {type: integer, format: int64, minimum: 0}
        limit: {type: integer, minimum: 1, maximum: 200}
        offset: {type: integer, minimum: 0}
        has_more: {type: boolean}
        next_offset: {type: integer, minimum: 0, nullable: true}
    DeliverySourceVerification:
      type: object
      additionalProperties: false
      required: [id, source_id, status]
      properties:
        id: {type: string, format: uuid}
        source_id: {type: string, format: uuid}
        status: {type: string, enum: [pending, running, ready, failed]}
    DeliverySourceVerificationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliverySourceVerification'}
    NotificationChannel:
      type: object
      additionalProperties: false
      required: [id, name, type, enabled, config, createdAt, updatedAt]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        type: {type: string, enum: [slack, pagerduty, msteams, webhook, email]}
        enabled: {type: boolean}
        config:
          type: object
          description: Secret-shaped values are replaced by the literal `[redacted]` marker.
          additionalProperties: true
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
    NotificationChannelEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/NotificationChannel'}
    NotificationChannelPage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/NotificationChannel'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    InhibitionResponse:
      type: object
      additionalProperties: false
      required: [id, name, source_matchers, target_matchers, equal_labels, enabled, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        source_matchers: {type: array, items: {$ref: '#/components/schemas/InhibitionMatcherResponse'}}
        target_matchers: {type: array, items: {$ref: '#/components/schemas/InhibitionMatcherResponse'}}
        equal_labels: {type: array, items: {type: string}}
        enabled: {type: boolean}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    InhibitionEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/InhibitionResponse'}
    InhibitionPage:
      type: object
      additionalProperties: false
      required: [data, pagination]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/InhibitionResponse'}}
        pagination: {$ref: '#/components/schemas/PaginationMetadata'}
    GatekeeperConstraintSummary:
      type: object
      additionalProperties: false
      required: [name, kind, api_version]
      properties:
        name: {type: string}
        kind: {type: string}
        api_version: {type: string}
        enforcement_action: {type: string}
        violation_count: {type: integer, minimum: 0}
        yaml: {type: string}
        created_by: {type: string, format: uuid, nullable: true}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        desired_state: {type: string, enum: [present, absent]}
        sync_status: {type: string, enum: [pending, synced, failed]}
        generation: {type: integer, format: int64, minimum: 1}
        observed_generation: {type: integer, format: int64, minimum: 0}
        last_error: {type: string}
        last_reconciled_at: {type: string, format: date-time, nullable: true}
    GatekeeperConstraintList:
      type: object
      additionalProperties: false
      required: [bundle, custom]
      properties:
        bundle: {type: array, items: {$ref: '#/components/schemas/GatekeeperConstraintSummary'}}
        custom: {type: array, items: {$ref: '#/components/schemas/GatekeeperConstraintSummary'}}
    SCIMMeta:
      type: object
      additionalProperties: false
      required: [resourceType]
      properties:
        resourceType: {type: string}
    SCIMName:
      type: object
      additionalProperties: false
      properties:
        givenName: {type: string}
        familyName: {type: string}
    SCIMEmail:
      type: object
      additionalProperties: false
      required: [value]
      properties:
        value: {type: string, format: email}
        primary: {type: boolean}
    SCIMUser:
      type: object
      additionalProperties: false
      required: [schemas, id, userName, name, active, meta]
      properties:
        schemas: {type: array, items: {type: string}}
        id: {type: string}
        userName: {type: string}
        name: {$ref: '#/components/schemas/SCIMName'}
        emails: {type: array, items: {$ref: '#/components/schemas/SCIMEmail'}}
        active: {type: boolean}
        meta: {$ref: '#/components/schemas/SCIMMeta'}
    SCIMGroupMember:
      type: object
      additionalProperties: false
      required: [value]
      properties:
        value: {type: string}
        display: {type: string}
    SCIMGroup:
      type: object
      additionalProperties: false
      required: [schemas, id, displayName, meta]
      properties:
        schemas: {type: array, items: {type: string}}
        id: {type: string}
        displayName: {type: string}
        members: {type: array, items: {$ref: '#/components/schemas/SCIMGroupMember'}}
        meta: {$ref: '#/components/schemas/SCIMMeta'}
    SCIMResourceType:
      type: object
      additionalProperties: false
      required: [schemas, id, name, endpoint, schema, meta]
      properties:
        schemas: {type: array, items: {type: string}}
        id: {type: string}
        name: {type: string}
        endpoint: {type: string}
        description: {type: string}
        schema: {type: string}
        meta: {$ref: '#/components/schemas/SCIMMeta'}
    SCIMSchemaAttribute:
      type: object
      additionalProperties: false
      required: [name, type, multiValued, required, mutability, returned, uniqueness]
      properties:
        name: {type: string}
        type: {type: string}
        multiValued: {type: boolean}
        required: {type: boolean}
        mutability: {type: string}
        returned: {type: string}
        uniqueness: {type: string}
    SCIMSchema:
      type: object
      additionalProperties: false
      required: [id, name, description, attributes]
      properties:
        id: {type: string}
        name: {type: string}
        description: {type: string}
        attributes: {type: array, items: {$ref: '#/components/schemas/SCIMSchemaAttribute'}}
    SCIMListResponse:
      type: object
      additionalProperties: false
      required: [schemas, totalResults, startIndex, itemsPerPage, Resources]
      properties:
        schemas: {type: array, items: {type: string}}
        totalResults: {type: integer, format: int64, minimum: 0}
        startIndex: {type: integer, minimum: 1}
        itemsPerPage: {type: integer, minimum: 0}
        Resources:
          type: array
          items:
            oneOf:
              - {$ref: '#/components/schemas/SCIMUser'}
              - {$ref: '#/components/schemas/SCIMGroup'}
              - {$ref: '#/components/schemas/SCIMResourceType'}
              - {$ref: '#/components/schemas/SCIMSchema'}
    SCIMSupportedFeature:
      type: object
      additionalProperties: true
      required: [supported]
      properties:
        supported: {type: boolean}
        maxOperations: {type: integer}
        maxPayloadSize: {type: integer}
        maxResults: {type: integer}
    SCIMAuthenticationScheme:
      type: object
      additionalProperties: false
      required: [type, name, description, primary]
      properties:
        type: {type: string}
        name: {type: string}
        description: {type: string}
        primary: {type: boolean}
    SCIMServiceProviderConfig:
      type: object
      additionalProperties: false
      required: [schemas, documentationUri, patch, bulk, filter, changePassword, sort, etag, authenticationSchemes, meta]
      properties:
        schemas: {type: array, items: {type: string}}
        documentationUri: {type: string, format: uri}
        patch: {$ref: '#/components/schemas/SCIMSupportedFeature'}
        bulk: {$ref: '#/components/schemas/SCIMSupportedFeature'}
        filter: {$ref: '#/components/schemas/SCIMSupportedFeature'}
        changePassword: {$ref: '#/components/schemas/SCIMSupportedFeature'}
        sort: {$ref: '#/components/schemas/SCIMSupportedFeature'}
        etag: {$ref: '#/components/schemas/SCIMSupportedFeature'}
        authenticationSchemes: {type: array, items: {$ref: '#/components/schemas/SCIMAuthenticationScheme'}}
        meta: {$ref: '#/components/schemas/SCIMMeta'}
    ConstraintYAMLRequest:
      type: object
      additionalProperties: false
      description: Gatekeeper constraint authoring body (P-04).
      properties:
        yaml:
          type: string
          description: A ConstraintTemplate or Gatekeeper Constraint YAML document.
      required:
        - yaml
    ConstraintValidationResponse:
      type: object
      additionalProperties: false
      description: Result of validating (and optionally applying) a constraint (P-04).
      required: [valid, errors, applied]
      properties:
        valid:
          type: boolean
        errors:
          type: array
          items:
            type: string
        applied:
          type: boolean
        name:
          type: string
        kind:
          type: string
        status:
          type: string
          enum: [pending, synced, failed]
        task_id:
          type: string
          format: uuid
    ConstraintValidationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ConstraintValidationResponse'}
    GatekeeperConstraintMutationResponse:
      type: object
      additionalProperties: false
      required: [name, status]
      properties:
        name: {type: string}
        status: {type: string, enum: [pending, synced, failed]}
        task_id: {type: string, format: uuid}
    GatekeeperConstraintMutationEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/GatekeeperConstraintMutationResponse'}
    NetworkPolicyApplicationResponse:
      type: object
      description: >-
        Schema not yet fully enumerated; shape is the handler's JSON payload.
        Permissive object placeholder so every $ref in this spec resolves.
      additionalProperties: true
    NetworkPolicyTemplateResponse:
      type: object
      description: >-
        Schema not yet fully enumerated; shape is the handler's JSON payload.
        Permissive object placeholder so every $ref in this spec resolves.
      additionalProperties: true
    ServiceMeshDetectionResponse:
      type: object
      additionalProperties: false
      required: [cluster_id, detected_mesh, detected_version, control_plane_namespace, gateway_count, virtual_service_count, destination_rule_count, peer_authentication_count, service_profile_count, server_auth_count, mtls_coverage_pct]
      properties:
        cluster_id: {type: string, format: uuid}
        detected_mesh: {type: string}
        detected_version: {type: string}
        control_plane_namespace: {type: string}
        gateway_count: {type: integer}
        virtual_service_count: {type: integer}
        destination_rule_count: {type: integer}
        peer_authentication_count: {type: integer}
        service_profile_count: {type: integer}
        server_auth_count: {type: integer}
        mtls_coverage_pct: {type: integer}
        last_detected_at: {type: string, format: date-time}
        last_error: {type: string}
    UpdateClusterGroupRequest:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: {type: string, minLength: 1}
        slug: {type: string}
        description: {type: string}
        parent_id: {type: string, format: uuid}
        color: {type: string}
        icon: {type: string}
    UpdateClusterRequest:
      type: object
      additionalProperties: false
      description: Partial cluster update. Omitted fields are preserved by an atomic row-locked merge; explicit empty strings or objects clear the corresponding field.
      properties:
        display_name: {type: string}
        description: {type: string}
        environment: {type: string}
        region: {type: string}
        labels: {type: object, additionalProperties: {type: string}}
        annotations: {type: object, additionalProperties: {type: string}}
        api_server_url:
          description: HTTPS Kubernetes API origin. Send an empty string together with an empty ca_certificate to disable direct access; omission preserves the current value.
          oneOf:
            - {type: string, const: ""}
            - {type: string, format: uri, pattern: '^https://'}
        ca_certificate:
          type: string
          description: Optional PEM CA identity pin. Send an empty string with an empty api_server_url to disable direct access; omission preserves the current value.
    UpdateRegistryConfigRequest:
      type: object
      additionalProperties: false
      properties:
        private_registry_url: {type: string}
        registry_username: {type: string}
        registry_password: {type: string, writeOnly: true}
        insecure: {type: boolean}
        ca_bundle: {type: string}
    DeliveryLabelExpression:
      type: object
      additionalProperties: false
      required: [key, operator]
      properties:
        key: {type: string}
        operator: {type: string, enum: [In, NotIn, Exists, DoesNotExist]}
        values: {type: array, maxItems: 256, uniqueItems: true, items: {type: string}}
    DeliveryPlacement:
      type: object
      additionalProperties: false
      required: [all_clusters]
      properties:
        project_ids: {type: array, maxItems: 10000, uniqueItems: true, items: {type: string, format: uuid}}
        cluster_ids: {type: array, maxItems: 10000, uniqueItems: true, items: {type: string, format: uuid}}
        cluster_group_ids: {type: array, maxItems: 10000, uniqueItems: true, items: {type: string, format: uuid}}
        match_labels: {type: object, maxProperties: 256, additionalProperties: {type: string}}
        match_expressions: {type: array, maxItems: 256, items: {$ref: '#/components/schemas/DeliveryLabelExpression'}}
        exclude_cluster_ids: {type: array, maxItems: 10000, uniqueItems: true, items: {type: string, format: uuid}}
        all_clusters: {type: boolean}
    DeliveryPlacementInput:
      type: object
      additionalProperties: false
      properties:
        project_ids: {type: array, maxItems: 10000, uniqueItems: true, items: {type: string, format: uuid}}
        cluster_ids: {type: array, maxItems: 10000, uniqueItems: true, items: {type: string, format: uuid}}
        cluster_group_ids: {type: array, maxItems: 10000, uniqueItems: true, items: {type: string, format: uuid}}
        match_labels: {type: object, maxProperties: 256, additionalProperties: {type: string}}
        match_expressions: {type: array, maxItems: 256, items: {$ref: '#/components/schemas/DeliveryLabelExpression'}}
        exclude_cluster_ids: {type: array, maxItems: 10000, uniqueItems: true, items: {type: string, format: uuid}}
        all_clusters: {type: boolean}
    DeliveryReconciliationPolicy:
      type: object
      additionalProperties: false
      required: [interval, retry_interval, timeout, prune, wait, drift]
      properties:
        interval: {type: string}
        retry_interval: {type: string}
        timeout: {type: string}
        prune: {type: boolean}
        wait: {type: boolean}
        drift: {type: string, enum: [ignore, detect, repair]}
    DeliveryReconciliationPolicyInput:
      type: object
      additionalProperties: false
      properties:
        interval: {type: string}
        retry_interval: {type: string}
        timeout: {type: string}
        prune: {type: boolean}
        wait: {type: boolean}
        drift: {type: string, enum: [ignore, detect, repair]}
    DeliveryRolloutPolicy:
      type: object
      additionalProperties: false
      required: [approval_required]
      properties:
        approval_required: {type: boolean}
    DeliveryRolloutPolicyInput:
      type: object
      additionalProperties: false
      properties:
        approval_required: {type: boolean}
    DeliveryCapabilityRequirement:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: {type: string}
        constraint: {type: string, maxLength: 128}
    DeliveryKustomizeRenderer:
      type: object
      additionalProperties: false
      required: [path, target_namespace]
      properties:
        path: {type: string}
        target_namespace: {type: string}
        patches: {type: array, items: {type: string}}
    DeliveryHelmRenderer:
      type: object
      additionalProperties: false
      required: [chart, chart_version, release_name, target_namespace, install_retries, upgrade_retries, test]
      properties:
        chart: {type: string}
        chart_version: {type: string}
        release_name: {type: string}
        target_namespace: {type: string}
        values: {type: object, additionalProperties: true}
        install_retries: {type: integer, minimum: 0, maximum: 255}
        upgrade_retries: {type: integer, minimum: 0, maximum: 255}
        test: {type: boolean}
    DeliveryRendererSpec:
      type: object
      additionalProperties: false
      required: [kind]
      properties:
        kind: {type: string, enum: [kustomize, helm]}
        kustomize: {$ref: '#/components/schemas/DeliveryKustomizeRenderer'}
        helm: {$ref: '#/components/schemas/DeliveryHelmRenderer'}
    DeliveryBundleVersionDraft:
      type: object
      additionalProperties: false
      properties:
        source_id: {type: string, format: uuid}
        requested_revision: {type: string}
        renderer: {$ref: '#/components/schemas/DeliveryRendererSpec'}
        scope: {type: string, enum: [namespace, platform]}
        reconciliation_policy: {$ref: '#/components/schemas/DeliveryReconciliationPolicy'}
        required_capabilities: {type: array, maxItems: 128, items: {$ref: '#/components/schemas/DeliveryCapabilityRequirement'}}
    DeliveryAmount:
      type: object
      additionalProperties: false
      required: [type, value]
      properties:
        type: {type: string, enum: [count, percent]}
        value: {type: integer, minimum: 0}
    DeliveryCanaryStrategy:
      type: object
      additionalProperties: false
      required: [size, approval_after_canary, soak]
      properties:
        size: {$ref: '#/components/schemas/DeliveryAmount'}
        cluster_ids: {type: array, maxItems: 10000, uniqueItems: true, items: {type: string, format: uuid}}
        approval_after_canary: {type: boolean}
        soak: {type: string}
    DeliveryRolloutPartition:
      type: object
      additionalProperties: false
      required: [name, selector, approval_required, soak]
      properties:
        name: {type: string}
        selector: {$ref: '#/components/schemas/DeliveryPlacement'}
        approval_required: {type: boolean}
        soak: {type: string}
    DeliveryRolloutStrategy:
      type: object
      additionalProperties: false
      required: [type, max_concurrent, max_unavailable, min_ready, progress_deadline, failure_threshold, on_failure, respect_maintenance_windows]
      properties:
        type: {type: string, enum: [all_at_once, rolling, canary, partitioned]}
        max_concurrent: {type: integer, minimum: 1}
        max_unavailable: {$ref: '#/components/schemas/DeliveryAmount'}
        min_ready: {type: string}
        progress_deadline: {type: string}
        failure_threshold: {$ref: '#/components/schemas/DeliveryAmount'}
        on_failure: {type: string, enum: [pause, abort, rollback]}
        respect_maintenance_windows: {type: boolean}
        shuffle_seed: {type: string, maxLength: 128}
        canary: {$ref: '#/components/schemas/DeliveryCanaryStrategy'}
        partitions: {type: array, items: {$ref: '#/components/schemas/DeliveryRolloutPartition'}}
    DeliveryRolloutStrategyInput:
      type: object
      additionalProperties: false
      properties:
        type: {type: string, enum: [all_at_once, rolling, canary, partitioned]}
        max_concurrent: {type: integer, minimum: 1}
        max_unavailable: {$ref: '#/components/schemas/DeliveryAmount'}
        min_ready: {type: string}
        progress_deadline: {type: string}
        failure_threshold: {$ref: '#/components/schemas/DeliveryAmount'}
        on_failure: {type: string, enum: [pause, abort, rollback]}
        respect_maintenance_windows: {type: boolean}
        shuffle_seed: {type: string, maxLength: 128}
        canary: {$ref: '#/components/schemas/DeliveryCanaryStrategy'}
        partitions: {type: array, items: {$ref: '#/components/schemas/DeliveryRolloutPartition'}}
    DeliveryImmutableRevision:
      type: object
      additionalProperties: false
      required: [kind, value, artifact_digest]
      properties:
        kind: {type: string, enum: [git_commit, oci_digest, helm_chart]}
        value: {type: string}
        artifact_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
    DeliveryResolvedSource:
      type: object
      additionalProperties: false
      required: [source_id, type, url, auth_mode, trust_policy, revision]
      properties:
        source_id: {type: string, format: uuid}
        type: {type: string, enum: [git, oci_artifact, helm_http, helm_oci]}
        url: {type: string, format: uri}
        auth_mode: {type: string, enum: [none, basic, bearer, ssh, workload_identity]}
        trust_policy: {$ref: '#/components/schemas/DeliveryTrustPolicy'}
        revision: {$ref: '#/components/schemas/DeliveryImmutableRevision'}
    DeliveryVersionIdentity:
      type: object
      additionalProperties: false
      required: [bundle_version_id, spec_digest, source]
      properties:
        bundle_version_id: {type: string, format: uuid}
        spec_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        source: {$ref: '#/components/schemas/DeliveryResolvedSource'}
    DeliveryApprovalBinding:
      type: object
      additionalProperties: false
      required: [required, digest]
      properties:
        required: {type: boolean}
        digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
    DeliveryRolloutCohort:
      type: object
      additionalProperties: false
      required: [index, name, cluster_ids, approval_required, soak_after]
      properties:
        index: {type: integer, minimum: 0}
        name: {type: string}
        cluster_ids: {type: array, items: {type: string, format: uuid}}
        approval_required: {type: boolean}
        approval_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        soak_after: {type: string}
    DeliveryPreviousDeployment:
      type: object
      additionalProperties: false
      required: [version, generation]
      properties:
        version: {$ref: '#/components/schemas/DeliveryVersionIdentity'}
        generation: {type: integer, format: int64, minimum: 1}
    DeliveryPlannedCluster:
      type: object
      additionalProperties: false
      required: [cluster_id, cohort, order]
      properties:
        cluster_id: {type: string, format: uuid}
        cohort: {type: integer, minimum: 0}
        order: {type: integer, minimum: 0}
        previous: {$ref: '#/components/schemas/DeliveryPreviousDeployment'}
    DeliveryCondition:
      type: object
      additionalProperties: false
      required: [type, status, observed_generation, last_transition_time]
      properties:
        type: {type: string, enum: [Ready, Reconciling, Stalled, Drifted]}
        status: {type: string, enum: ['True', 'False', 'Unknown']}
        reason: {type: string}
        message: {type: string}
        observed_generation: {type: integer, format: int64, minimum: 0}
        last_transition_time: {type: string, format: date-time}
    DeliveryTargetWrite:
      type: object
      additionalProperties: false
      required: [name, bundle_version_id, placement, rollout_policy, reconciliation_policy]
      properties:
        project_id: {type: string, format: uuid}
        name: {type: string, minLength: 1, maxLength: 128}
        description: {type: string, maxLength: 4096}
        bundle_version_id: {type: string, format: uuid}
        placement: {$ref: '#/components/schemas/DeliveryPlacementInput'}
        rollout_policy: {$ref: '#/components/schemas/DeliveryRolloutPolicyInput'}
        reconciliation_policy: {$ref: '#/components/schemas/DeliveryReconciliationPolicyInput'}
        maintenance_window_policy: {type: object, additionalProperties: true}
        suspended: {type: boolean}
    DeliveryTargetPatch:
      type: object
      additionalProperties: false
      properties:
        project_id: {type: string, format: uuid}
        description: {type: string, maxLength: 4096}
        bundle_version_id: {type: string, format: uuid}
        placement: {$ref: '#/components/schemas/DeliveryPlacementInput'}
        rollout_policy: {$ref: '#/components/schemas/DeliveryRolloutPolicyInput'}
        reconciliation_policy: {$ref: '#/components/schemas/DeliveryReconciliationPolicyInput'}
        maintenance_window_policy: {type: object, additionalProperties: true}
        suspended: {type: boolean}
    DeliveryRolloutStart:
      type: object
      additionalProperties: false
      required: [preview_digest, confirm_all_clusters, strategy]
      properties:
        project_id: {type: string, format: uuid}
        preview_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        confirm_all_clusters: {type: boolean}
        strategy: {$ref: '#/components/schemas/DeliveryRolloutStrategyInput'}
    DeliveryRolloutAction:
      type: object
      additionalProperties: false
      properties:
        project_id: {type: string, format: uuid}
        reason_code: {type: string, maxLength: 96}
    DeliveryRolloutApproval:
      type: object
      additionalProperties: false
      required: [cohort, binding_digest, decision, expires_at]
      properties:
        project_id: {type: string, format: uuid}
        cohort: {type: integer, minimum: -1}
        binding_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        decision: {type: string, enum: [approved, rejected]}
        expires_at: {type: string, format: date-time}
    DeliveryTrustPolicy:
      type: object
      additionalProperties: false
      required: [allow_unsigned]
      properties:
        allow_unsigned: {type: boolean}
        provider: {type: string, enum: [cosign_key, cosign_keyless, git]}
        identity: {type: string, maxLength: 2048}
        issuer: {type: string, maxLength: 2048}
        key_ref: {type: string, maxLength: 253}
    DeliverySourceCredentialInput:
      type: object
      description: Write-only credential material; never returned by the API.
      additionalProperties: false
      properties:
        username: {type: string, writeOnly: true, maxLength: 262144}
        password: {type: string, writeOnly: true, maxLength: 262144}
        token: {type: string, writeOnly: true, maxLength: 262144}
        private_key: {type: string, writeOnly: true, maxLength: 262144}
        known_hosts: {type: string, writeOnly: true, maxLength: 262144}
        passphrase: {type: string, writeOnly: true, maxLength: 262144}
    DeliverySourceWrite:
      type: object
      additionalProperties: false
      required: [name, type, url, auth_mode, trust_policy]
      properties:
        project_id: {type: string, format: uuid}
        name: {type: string, minLength: 1, maxLength: 128}
        description: {type: string, maxLength: 4096}
        type: {type: string, enum: [git, oci_artifact, helm_http, helm_oci]}
        url: {type: string, format: uri, maxLength: 2048}
        auth_mode: {type: string, enum: [none, basic, bearer, ssh, workload_identity]}
        credential: {$ref: '#/components/schemas/DeliverySourceCredentialInput'}
        ca_bundle: {type: string, writeOnly: true, maxLength: 262144}
        proxy_ref: {type: string, maxLength: 128}
        trust_policy: {$ref: '#/components/schemas/DeliveryTrustPolicy'}
    DeliverySourcePatch:
      type: object
      additionalProperties: false
      properties:
        project_id: {type: string, format: uuid}
        description: {type: string, maxLength: 4096}
        url: {type: string, format: uri, maxLength: 2048}
        proxy_ref: {type: string, maxLength: 128}
        trust_policy: {$ref: '#/components/schemas/DeliveryTrustPolicy'}
        ca_bundle: {type: string, writeOnly: true, maxLength: 262144}
    DeliverySourceCredentialRotate:
      type: object
      additionalProperties: false
      required: [auth_mode, credential]
      properties:
        project_id: {type: string, format: uuid}
        auth_mode: {type: string, enum: [basic, bearer, ssh]}
        credential: {$ref: '#/components/schemas/DeliverySourceCredentialInput'}
    DeliverySource:
      type: object
      additionalProperties: false
      required: [id, project_id, name, type, url, auth_mode, credential, trust_policy, status, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        project_id: {type: string, format: uuid}
        name: {type: string}
        description: {type: string}
        type: {type: string, enum: [git, oci_artifact, helm_http, helm_oci]}
        url: {type: string, format: uri}
        auth_mode: {type: string, enum: [none, basic, bearer, ssh, workload_identity]}
        credential:
          type: object
          additionalProperties: false
          required: [configured, key_version, epoch]
          properties:
            configured: {type: boolean}
            key_version: {type: integer, format: int32, minimum: 0}
            epoch: {type: integer, format: int64, minimum: 0}
        proxy_ref: {type: string}
        trust_policy: {$ref: '#/components/schemas/DeliveryTrustPolicy'}
        status: {type: string, enum: [pending, ready, degraded, revoked]}
        last_resolved_at: {type: string, format: date-time, nullable: true}
        last_error_code: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    DeliverySourceEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties: {data: {$ref: '#/components/schemas/DeliverySource'}}
    DeliverySourcePage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous, total_known]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/DeliverySource'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
        total_known: {type: boolean}
    DeliveryBundleWrite:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        project_id: {type: string, format: uuid}
        name: {type: string, minLength: 1, maxLength: 128}
        description: {type: string, maxLength: 4096}
    DeliveryBundleVersionWrite:
      type: object
      additionalProperties: false
      required: [version, spec]
      properties:
        project_id: {type: string, format: uuid}
        version: {type: string, minLength: 1, maxLength: 128}
        spec: {$ref: '#/components/schemas/DeliveryBundleVersionDraft'}
        dependency_bundle_ids: {type: array, maxItems: 128, uniqueItems: true, items: {type: string, format: uuid}}
    DeliveryBundle:
      type: object
      additionalProperties: false
      required: [id, project_id, name, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        project_id: {type: string, format: uuid}
        name: {type: string}
        description: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    DeliveryBundleVersion:
      type: object
      additionalProperties: false
      required: [id, bundle_id, source_id, version, renderer, scope, requested_revision, renderer_spec, reconciliation_policy, required_capabilities, dependency_bundle_ids, spec_digest, verification_status, state, created_at]
      properties:
        id: {type: string, format: uuid}
        bundle_id: {type: string, format: uuid}
        source_id: {type: string, format: uuid}
        version: {type: string}
        renderer: {type: string, enum: [kustomize, helm]}
        scope: {type: string, enum: [namespace, platform]}
        requested_revision: {type: string}
        resolved_revision: {type: string}
        artifact_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        renderer_spec: {$ref: '#/components/schemas/DeliveryRendererSpec'}
        reconciliation_policy: {$ref: '#/components/schemas/DeliveryReconciliationPolicy'}
        required_capabilities: {type: array, items: {$ref: '#/components/schemas/DeliveryCapabilityRequirement'}}
        dependency_bundle_ids: {type: array, items: {type: string, format: uuid}}
        spec_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        verification_status: {type: string, enum: [pending, verified, failed, unsigned]}
        verification_identity: {type: string}
        state: {type: string, enum: [resolving, ready, failed, revoked]}
        last_error_code: {type: string}
        created_at: {type: string, format: date-time}
    DeliveryBundleEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties: {data: {$ref: '#/components/schemas/DeliveryBundle'}}
    DeliveryBundleVersionEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties: {data: {$ref: '#/components/schemas/DeliveryBundleVersion'}}
    DeliveryBundlePage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous, total_known]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/DeliveryBundle'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
        total_known: {type: boolean}
    DeliveryBundleVersionPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous, total_known]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/DeliveryBundleVersion'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
        total_known: {type: boolean}
    DeliverySourceVerify:
      type: object
      additionalProperties: false
      required: [requested_revision]
      properties:
        project_id: {type: string, format: uuid}
        requested_revision: {type: string, minLength: 1, maxLength: 256}
        chart: {type: string, maxLength: 253}
    DeliveryTarget:
      type: object
      additionalProperties: false
      required: [id, project_id, name, bundle_version_id, placement, rollout_policy, reconciliation_policy, maintenance_window_policy, suspended, generation, resource_version, deletion_state, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        project_id: {type: string, format: uuid}
        name: {type: string}
        description: {type: string}
        bundle_version_id: {type: string, format: uuid}
        placement: {$ref: '#/components/schemas/DeliveryPlacement'}
        rollout_policy: {$ref: '#/components/schemas/DeliveryRolloutPolicy'}
        reconciliation_policy: {$ref: '#/components/schemas/DeliveryReconciliationPolicy'}
        maintenance_window_policy: {type: object, additionalProperties: true}
        suspended: {type: boolean}
        generation: {type: integer, format: int64, minimum: 1}
        resource_version: {type: integer, format: int64, minimum: 1}
        deletion_state: {type: string, enum: [active, deleting, deleted]}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    DeliveryTargetEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliveryTarget'}
    DeliveryTargetPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous, total_known]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/DeliveryTarget'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
        total_known: {type: boolean}
    DeliveryTargetDeletion:
      type: object
      additionalProperties: false
      required: [id, deletion_state, resource_version]
      properties:
        id: {type: string, format: uuid}
        deletion_state: {type: string, enum: [deleting, deleted]}
        resource_version: {type: integer, format: int64, minimum: 1}
        deployment_count: {type: integer, format: int64, minimum: 0}
    DeliveryTargetDeletionEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliveryTargetDeletion'}
    DeliveryPreviewDecision:
      type: object
      additionalProperties: false
      required: [cluster_id, reason]
      properties:
        cluster_id: {type: string, format: uuid}
        project_id: {type: string, format: uuid}
        cluster_name: {type: string}
        reason: {type: string, enum: [selected, excluded_by_selector, excluded_explicitly, unauthorized, disconnected, incompatible, missing_capability, decommissioning]}
        match_reasons:
          type: array
          items: {type: string, enum: [explicit_cluster, all_clusters, cluster_group, match_labels, match_expressions]}
        matched_group_ids: {type: array, items: {type: string, format: uuid}}
        missing_capabilities: {type: array, items: {type: string}}
        compatibility_reason: {type: string}
    DeliveryTargetPreview:
      type: object
      additionalProperties: false
      required: [target_id, target_generation, bundle_version_id, preview_digest, selected_count, excluded_count, requires_all_confirmation, decisions, decision_count, decision_offset, decision_page_size, has_more_decisions, next_cursor, risks]
      properties:
        target_id: {type: string, format: uuid}
        target_generation: {type: integer, format: int64, minimum: 1}
        bundle_version_id: {type: string, format: uuid}
        preview_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        selected_count: {type: integer, minimum: 0}
        excluded_count: {type: integer, minimum: 0}
        requires_all_confirmation: {type: boolean}
        decisions: {type: array, items: {$ref: '#/components/schemas/DeliveryPreviewDecision'}}
        decision_count: {type: integer, minimum: 0, maximum: 10000}
        decision_offset: {type: integer, minimum: 0, maximum: 10000}
        decision_page_size: {type: integer, minimum: 1, maximum: 500}
        has_more_decisions: {type: boolean}
        next_cursor:
          type: string
          description: Digest-bound opaque cursor; empty on the final page.
        risks: {type: array, items: {type: string}}
    DeliveryTargetPreviewEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliveryTargetPreview'}
    DeliveryRollout:
      type: object
      required: [id, target_id, target_generation, to_bundle_version_id, placement_digest, strategy, plan_digest, state, fencing_generation, total_clusters, ready_clusters, failed_clusters, blocked_clusters, released_clusters, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        target_id: {type: string, format: uuid}
        target_generation: {type: integer, format: int64, minimum: 1}
        from_bundle_version_id: {type: string, format: uuid, nullable: true}
        to_bundle_version_id: {type: string, format: uuid}
        placement_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        placement_snapshot: {type: object, additionalProperties: true}
        strategy: {$ref: '#/components/schemas/DeliveryRolloutStrategy'}
        strategy_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        approval_policy: {type: object, additionalProperties: true}
        request_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        plan_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        frozen_plan: {type: object, additionalProperties: true}
        state: {type: string}
        fencing_generation: {type: integer, format: int64, minimum: 1}
        total_clusters: {type: integer, format: int32, minimum: 0}
        ready_clusters: {type: integer, format: int32, minimum: 0}
        failed_clusters: {type: integer, format: int32, minimum: 0}
        blocked_clusters: {type: integer, format: int32, minimum: 0}
        released_clusters: {type: integer, format: int32, minimum: 0}
        progress_deadline: {type: string, format: date-time, nullable: true}
        started_at: {type: string, format: date-time, nullable: true}
        completed_at: {type: string, format: date-time, nullable: true}
        last_error_code: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    DeliveryFrozenRollout:
      type: object
      additionalProperties: false
      required: [id, target_id, project_id, target_generation, desired, placement_digest, strategy, strategy_digest, approval, actor, idempotency_key, request_digest, created_at, deadline, cohorts, clusters, plan_digest]
      properties:
        id: {type: string, format: uuid}
        target_id: {type: string, format: uuid}
        project_id: {type: string, format: uuid}
        target_generation: {type: integer, format: int64, minimum: 1}
        desired: {$ref: '#/components/schemas/DeliveryVersionIdentity'}
        placement_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        strategy: {$ref: '#/components/schemas/DeliveryRolloutStrategy'}
        strategy_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        approval: {$ref: '#/components/schemas/DeliveryApprovalBinding'}
        actor: {type: string}
        idempotency_key: {type: string}
        request_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        created_at: {type: string, format: date-time}
        deadline: {type: string, format: date-time}
        cohorts: {type: array, items: {$ref: '#/components/schemas/DeliveryRolloutCohort'}}
        clusters: {type: array, items: {$ref: '#/components/schemas/DeliveryPlannedCluster'}}
        plan_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
    DeliveryFrozenRolloutEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliveryFrozenRollout'}
    DeliveryRolloutEvent:
      type: object
      additionalProperties: false
      required: [id, rollout_id, event_type, from_state, to_state, reason_code, fence, occurred_at, created_at]
      properties:
        id: {type: string, format: uuid}
        rollout_id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid, nullable: true}
        decision_digest: {type: string}
        event_type: {type: string}
        from_state: {type: string}
        to_state: {type: string}
        reason_code: {type: string}
        fence: {type: integer, format: int64}
        occurred_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
    DeliveryRolloutApprovalRecord:
      type: object
      additionalProperties: false
      required: [id, rollout_id, cohort, binding_digest, decision, decided_at, expires_at, created_at]
      properties:
        id: {type: string, format: uuid}
        rollout_id: {type: string, format: uuid}
        cohort: {type: integer, format: int32}
        binding_digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        decision: {type: string, enum: [approved, rejected]}
        decided_by: {type: string, format: uuid, nullable: true}
        decided_at: {type: string, format: date-time}
        expires_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
    DeliveryRolloutCluster:
      type: object
      additionalProperties: false
      required: [id, rollout_id, cluster_id, cohort, release_order, desired_bundle_version_id, desired_spec_digest, state, assignment_action, attempt, fence, last_error_code, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        rollout_id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid}
        cohort: {type: integer, format: int32}
        release_order: {type: integer, format: int32, minimum: 0}
        previous_bundle_version_id: {type: string, format: uuid, nullable: true}
        desired_bundle_version_id: {type: string, format: uuid}
        desired_spec_digest: {type: string}
        state: {type: string}
        assignment_action: {type: string, enum: [apply, rollback]}
        attempt: {type: integer, format: int32, minimum: 0}
        fence: {type: integer, format: int64, minimum: 0}
        released_at: {type: string, format: date-time, nullable: true}
        acknowledged_at: {type: string, format: date-time, nullable: true}
        ready_at: {type: string, format: date-time, nullable: true}
        completed_at: {type: string, format: date-time, nullable: true}
        deadline: {type: string, format: date-time, nullable: true}
        last_error_code: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    DeliveryRolloutPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous, total_known]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/DeliveryRollout'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
        total_known: {type: boolean}
    DeliveryRolloutClusterPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous, total_known]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/DeliveryRolloutCluster'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
        total_known: {type: boolean}
    DeliveryRolloutEventPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous, total_known]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/DeliveryRolloutEvent'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
        total_known: {type: boolean}
    DeliveryRolloutDetail:
      type: object
      additionalProperties: false
      required: [rollout, frozen_plan, approvals, timeline]
      properties:
        rollout: {$ref: '#/components/schemas/DeliveryRollout'}
        frozen_plan: {$ref: '#/components/schemas/DeliveryFrozenRollout'}
        approvals: {type: array, items: {$ref: '#/components/schemas/DeliveryRolloutApprovalRecord'}}
        timeline: {type: array, items: {$ref: '#/components/schemas/DeliveryRolloutEvent'}}
    DeliveryRolloutDetailEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliveryRolloutDetail'}
    DeliveryRolloutControl:
      type: object
      additionalProperties: false
      required: [rollout, event]
      properties:
        rollout: {$ref: '#/components/schemas/DeliveryRollout'}
        event: {$ref: '#/components/schemas/DeliveryRolloutEvent'}
        approval: {$ref: '#/components/schemas/DeliveryRolloutApprovalRecord'}
    DeliveryRolloutControlEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliveryRolloutControl'}
    DeliveryAsyncOperationReceipt:
      type: object
      additionalProperties: false
      required: [operation_id, operation, resource, resource_id, project_id, status, status_url, accepted_at]
      properties:
        operation_id: {type: string, format: uuid}
        operation: {type: string, minLength: 1}
        resource: {type: string, minLength: 1}
        resource_id: {type: string, format: uuid}
        project_id: {type: string, format: uuid}
        status: {type: string, enum: [accepted]}
        status_url: {type: string, minLength: 1}
        accepted_at: {type: string, format: date-time}
    DeliveryAsyncOperationReceiptEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliveryAsyncOperationReceipt'}
    ClusterDeployment:
      type: object
      additionalProperties: false
      required: [id, target_id, cluster_id, desired_generation, observed_generation, desired_spec_digest, observed_spec_digest, desired_revision, observed_revision, action, phase, conditions, source_kind, source_name, reconciler_kind, reconciler_name, inventory, agent_session_id, agent_sequence, last_error_code, last_message, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        target_id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid}
        current_rollout_id: {type: string, format: uuid, nullable: true}
        desired_bundle_version_id: {type: string, format: uuid, nullable: true}
        previous_bundle_version_id: {type: string, format: uuid, nullable: true}
        desired_generation: {type: integer, format: int64, minimum: 0}
        observed_generation: {type: integer, format: int64, minimum: 0}
        desired_spec_digest: {type: string}
        observed_spec_digest: {type: string}
        desired_revision: {type: string}
        observed_revision: {type: string}
        action: {type: string, enum: [apply, delete, suspend]}
        phase: {type: string, enum: [pending, blocked, applying, ready, degraded, failed, suspended, deleting, removed, unknown]}
        conditions: {type: array, items: {$ref: '#/components/schemas/DeliveryCondition'}}
        source_kind: {type: string}
        source_name: {type: string}
        reconciler_kind: {type: string}
        reconciler_name: {type: string}
        inventory: {type: object, additionalProperties: true}
        agent_session_id: {type: string}
        agent_sequence: {type: integer, format: int64, minimum: 0}
        last_error_code: {type: string}
        last_message: {type: string}
        last_observed_at: {type: string, format: date-time, nullable: true}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    ClusterDeploymentEvent:
      type: object
      additionalProperties: false
      required: [id, deployment_id, event_type, from_phase, to_phase, generation, spec_digest, reason_code, message, observed_at, created_at]
      properties:
        id: {type: string, format: uuid}
        deployment_id: {type: string, format: uuid}
        rollout_id: {type: string, format: uuid, nullable: true}
        event_type: {type: string}
        from_phase: {type: string}
        to_phase: {type: string}
        generation: {type: integer, format: int64, minimum: 0}
        spec_digest: {type: string}
        reason_code: {type: string}
        message: {type: string}
        observed_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
    ClusterDeploymentPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous, total_known]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ClusterDeployment'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
        total_known: {type: boolean}
    ClusterDeploymentEventPage:
      type: object
      additionalProperties: false
      required: [data, count, next, previous, total_known]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ClusterDeploymentEvent'}}
        count: {type: integer, format: int64, minimum: 0}
        next: {type: string, nullable: true}
        previous: {type: string, nullable: true}
        total_known: {type: boolean}
    ClusterDeploymentDetail:
      type: object
      additionalProperties: false
      required: [deployment, events]
      properties:
        deployment: {$ref: '#/components/schemas/ClusterDeployment'}
        events: {type: array, items: {$ref: '#/components/schemas/ClusterDeploymentEvent'}}
    ClusterDeploymentControl:
      type: object
      additionalProperties: false
      required: [deployment, event]
      properties:
        deployment: {$ref: '#/components/schemas/ClusterDeployment'}
        event: {$ref: '#/components/schemas/ClusterDeploymentEvent'}
    ClusterDeploymentDetailEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ClusterDeploymentDetail'}
    ClusterDeploymentControlEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ClusterDeploymentControl'}
    DeliveryControllerInventory:
      type: object
      additionalProperties: false
      required: [cluster_id, agent_version, flux_version, components, api_versions, distribution_digest, kubernetes_version, ready, compatibility_status, error_code, updated_at]
      properties:
        cluster_id: {type: string, format: uuid}
        agent_version: {type: string}
        flux_version: {type: string}
        components: {type: object, additionalProperties: {type: string}}
        api_versions: {type: array, items: {type: string}}
        distribution_digest: {type: string}
        kubernetes_version: {type: string}
        ready: {type: boolean}
        compatibility_status: {type: string, enum: [compatible, incompatible, upgrade_required, degraded]}
        error_code: {type: string}
        observed_at: {type: string, format: date-time, nullable: true}
        updated_at: {type: string, format: date-time}
    DeliveryClusterInventory:
      type: object
      additionalProperties: false
      required: [controller_inventory, deployments, deployment_count]
      properties:
        controller_inventory: {$ref: '#/components/schemas/DeliveryControllerInventory'}
        deployments: {type: array, items: {$ref: '#/components/schemas/ClusterDeployment'}}
        deployment_count: {type: integer, format: int64, minimum: 0}
    DeliveryClusterInventoryEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliveryClusterInventory'}
    DeliveryCompatibilityCount:
      type: object
      additionalProperties: false
      required: [compatibility_status, cluster_count]
      properties:
        compatibility_status: {type: string}
        cluster_count: {type: integer, format: int64, minimum: 0}
    DeliveryCompatibilityContract:
      type: object
      additionalProperties: false
      required: [summary, flux_version, flux_components, flux_apis, kubernetes_minimum, kubernetes_maximum, agent_protocol, required_capabilities]
      properties:
        summary: {type: string}
        flux_version: {type: string}
        flux_components: {type: object, additionalProperties: {type: string}}
        flux_apis: {type: array, items: {type: string}}
        kubernetes_minimum: {type: string}
        kubernetes_maximum: {type: string}
        agent_protocol: {type: string}
        required_capabilities: {type: array, items: {type: string}}
    DeliverySystemRelease:
      type: object
      properties:
        id: {type: string, format: uuid}
        release_sequence: {type: integer, format: int64}
        version: {type: string}
        artifact_url: {type: string}
        artifact_digest: {type: string}
        distribution_digest: {type: string}
        agent_version: {type: string}
        agent_image: {type: string}
        minimum_kubernetes: {type: string}
        maximum_kubernetes: {type: string}
        crd_storage_version: {type: string}
        previous_storage_version: {type: string}
        interval: {type: string}
        timeout: {type: string}
        verification_policy: {type: object, additionalProperties: true}
        credential_key_version: {type: integer, format: int32}
        credential_epoch: {type: integer, format: int64}
        spec_digest: {type: string}
        state: {type: string}
        released_at: {type: string, format: date-time, nullable: true}
        retired_at: {type: string, format: date-time, nullable: true}
        created_by: {type: string, format: uuid, nullable: true}
        created_at: {type: string, format: date-time}
    DeliverySystemRollout:
      type: object
      properties:
        id: {type: string, format: uuid}
        release_id: {type: string, format: uuid}
        previous_release_id: {type: string, format: uuid, nullable: true}
        strategy: {type: object, additionalProperties: true}
        strategy_digest: {type: string}
        state: {type: string}
        fencing_generation: {type: integer, format: int64}
        total_clusters: {type: integer, format: int32, minimum: 0}
        ready_clusters: {type: integer, format: int32, minimum: 0}
        failed_clusters: {type: integer, format: int32, minimum: 0}
        released_clusters: {type: integer, format: int32, minimum: 0}
        progress_deadline: {type: string, format: date-time, nullable: true}
        started_at: {type: string, format: date-time, nullable: true}
        completed_at: {type: string, format: date-time, nullable: true}
        last_error_code: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    DeliverySystemCompatibility:
      type: object
      additionalProperties: false
      required: [contract, current_release, current_rollout, observed_inventory]
      properties:
        contract: {$ref: '#/components/schemas/DeliveryCompatibilityContract'}
        current_release:
          allOf: [{$ref: '#/components/schemas/DeliverySystemRelease'}]
          nullable: true
        current_rollout:
          allOf: [{$ref: '#/components/schemas/DeliverySystemRollout'}]
          nullable: true
        observed_inventory: {type: array, items: {$ref: '#/components/schemas/DeliveryCompatibilityCount'}}
    DeliverySystemCompatibilityEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliverySystemCompatibility'}
    DeliveryEstateCount:
      type: object
      additionalProperties: false
      required: [key, count]
      properties:
        key: {type: string}
        count: {type: integer, format: int64, minimum: 0}
    DeliveryEstateSummary:
      type: object
      additionalProperties: false
      required: [adopted_clusters, flux_ready, incompatible, disconnected, stale, assignments, drifted, failed, degraded, active_rollouts]
      properties:
        adopted_clusters: {type: integer, format: int64, minimum: 0}
        flux_ready: {type: integer, format: int64, minimum: 0}
        incompatible: {type: integer, format: int64, minimum: 0}
        disconnected: {type: integer, format: int64, minimum: 0}
        stale: {type: integer, format: int64, minimum: 0}
        assignments: {type: integer, format: int64, minimum: 0}
        drifted: {type: integer, format: int64, minimum: 0}
        failed: {type: integer, format: int64, minimum: 0}
        degraded: {type: integer, format: int64, minimum: 0}
        active_rollouts: {type: integer, format: int64, minimum: 0}
    DeliveryEstateCluster:
      type: object
      additionalProperties: false
      required: [id, name, display_name, is_local, connected, stale, privilege_profile, kubernetes_version, agent_version, flux_version, compatibility_status, inventory_ready, inventory_error_code, assignment_count, ready_count, failed_count, degraded_count, drifted_count, last_heartbeat, inventory_observed_at, last_observed_at]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        display_name: {type: string}
        is_local: {type: boolean}
        connected: {type: boolean}
        stale: {type: boolean}
        privilege_profile: {type: string}
        kubernetes_version: {type: string}
        agent_version: {type: string}
        flux_version: {type: string}
        compatibility_status: {type: string}
        inventory_ready: {type: boolean}
        inventory_error_code: {type: string}
        assignment_count: {type: integer, format: int64, minimum: 0}
        ready_count: {type: integer, format: int64, minimum: 0}
        failed_count: {type: integer, format: int64, minimum: 0}
        degraded_count: {type: integer, format: int64, minimum: 0}
        drifted_count: {type: integer, format: int64, minimum: 0}
        last_heartbeat: {type: string, format: date-time, nullable: true}
        inventory_observed_at: {type: string, format: date-time, nullable: true}
        last_observed_at: {type: string, format: date-time, nullable: true}
    DeliveryEstateAttention:
      type: object
      additionalProperties: false
      required: [cluster_id, cluster_name, severity, reason, detail]
      properties:
        cluster_id: {type: string, format: uuid}
        cluster_name: {type: string}
        severity: {type: string, enum: [error, warning]}
        reason: {type: string}
        detail: {type: string}
    DeliveryEstateDistributions:
      type: object
      additionalProperties: false
      required: [compatibility, privilege, assignment_phases]
      properties:
        compatibility: {type: array, items: {$ref: '#/components/schemas/DeliveryEstateCount'}}
        privilege: {type: array, items: {$ref: '#/components/schemas/DeliveryEstateCount'}}
        assignment_phases: {type: array, items: {$ref: '#/components/schemas/DeliveryEstateCount'}}
    DeliveryEstate:
      type: object
      additionalProperties: false
      required: [summary, clusters, attention, distributions]
      properties:
        summary: {$ref: '#/components/schemas/DeliveryEstateSummary'}
        clusters: {type: array, items: {$ref: '#/components/schemas/DeliveryEstateCluster'}}
        attention: {type: array, items: {$ref: '#/components/schemas/DeliveryEstateAttention'}}
        distributions: {$ref: '#/components/schemas/DeliveryEstateDistributions'}
    DeliveryEstateEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/DeliveryEstate'}
    TaskOutboxEntryWire:
      type: object
      additionalProperties: false
      required: [id, task_type, queue_name, max_retry, timeout_seconds, unique_seconds, max_delivery_attempts, status, attempt_count, payload_size]
      properties:
        id: {type: string, format: uuid}
        dedupe_key: {type: string}
        task_type: {type: string}
        queue_name: {type: string}
        max_retry: {type: integer, format: int32}
        timeout_seconds: {type: integer, format: int32}
        unique_seconds: {type: integer, format: int32}
        max_delivery_attempts: {type: integer, format: int32}
        status: {type: string, enum: [pending, delivering, failed, delivered, dead]}
        attempt_count: {type: integer, format: int32}
        next_attempt_at: {type: string, format: date-time}
        locked_until: {type: string, format: date-time}
        delivered_at: {type: string, format: date-time}
        last_error: {type: string}
        payload_size: {type: integer}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    TaskOutboxEntryEnvelope:
      type: object
      additionalProperties: false
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/TaskOutboxEntryWire'}
    CreateControlPlaneSnapshotRequestWire:
      type: object
      additionalProperties: false
      properties:
        name: {type: string, minLength: 1, maxLength: 253}
        location: {type: string, enum: [local, s3], default: local}
    ControlPlaneSnapshotWire:
      type: object
      additionalProperties: false
      required: [id, cluster_id, name, status, location, created_at]
      properties:
        id: {type: string, format: uuid}
        cluster_id: {type: string, format: uuid}
        name: {type: string}
        status: {type: string, enum: [pending, running, completed, failed]}
        location: {type: string}
        size_bytes: {type: integer, format: int64}
        requested_by_id: {type: string, format: uuid}
        error: {type: string}
        created_at: {type: string, format: date-time}
        completed_at: {type: string, format: date-time}
    ControlPlaneRestoreRunbookWire:
      type: object
      additionalProperties: false
      required: [automated, distribution, snapshot_name, summary, steps, warning, docs_url]
      properties:
        automated: {type: boolean, enum: [false]}
        distribution: {type: string}
        snapshot_name: {type: string}
        summary: {type: string}
        steps: {type: array, items: {type: string}}
        warning: {type: string}
        docs_url: {type: string, format: uri}
    AdminQueueSummaryWire:
      type: object
      additionalProperties: false
      required: [name, size, active, pending, scheduled, retry, archived, completed, paused, as_of]
      properties:
        name: {type: string}
        size: {type: integer}
        active: {type: integer}
        pending: {type: integer}
        scheduled: {type: integer}
        retry: {type: integer}
        archived: {type: integer}
        completed: {type: integer}
        paused: {type: boolean}
        as_of: {type: string, format: date-time}
    AdminQueueDLQEntryWire:
      type: object
      additionalProperties: false
      required: [id, type, retried, last_err, last_failed_at]
      properties:
        id: {type: string}
        type: {type: string}
        retried: {type: integer}
        last_err: {type: string}
        last_failed_at: {type: string, format: date-time}
    AdminQueueDLQWire:
      type: object
      additionalProperties: false
      required: [queue, dlq, count]
      properties:
        queue: {type: string}
        dlq: {type: array, items: {$ref: '#/components/schemas/AdminQueueDLQEntryWire'}}
        count: {type: integer}
  x-delivery-rollout-action: &delivery-rollout-action
    tags: [Delivery]
    summary: Apply a CAS-protected typed rollout transition
    x-rbac: delivery_rollouts:update
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      - {name: If-Match, in: header, required: true, schema: {type: string, pattern: '^"[1-9][0-9]*"$'}}
      - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
    requestBody:
      content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryRolloutAction'}}}
    responses:
      '202':
        description: Transition, durable event, and worker wake-up committed
        headers:
          Location: {description: Existing rollout status resource, schema: {type: string}}
          Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryAsyncOperationReceiptEnvelope'}}}
      '412': {description: Stale fencing generation}
      '428': {description: If-Match required}
  x-delivery-deployment-action: &delivery-deployment-action
    tags: [Delivery]
    summary: Apply a CAS-protected deployment reconcile or suspend intent
    x-rbac: delivery_deployments:update
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      - {name: If-Match, in: header, required: true, schema: {type: string, pattern: '^"[0-9]+"$'}}
      - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
    requestBody:
      content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryRolloutAction'}}}
    responses:
      '202':
        description: Deployment generation and durable event committed
        headers:
          Location: {description: Existing deployment status resource, schema: {type: string}}
          Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryAsyncOperationReceiptEnvelope'}}}
      '412': {description: Stale desired generation}
      '428': {description: If-Match required}
  responses:
    BadRequest:
      description: Request validation failed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Unauthorized:
      description: Missing or invalid token
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Forbidden:
      description: Authenticated but not authorized for this resource
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Conflict:
      description: Mutation conflicts with the current resource state
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    ServiceUnavailable:
      description: Optional Charlie integration is inactive or unavailable
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    ErrorResponse:
      description: Error response (standard error envelope).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
security:
  - bearerJWT: []
tags:
  - name: Auth
    description: Login, tokens, password reset
  - name: Settings
    description: Authenticated runtime settings subsets
  - name: Clusters
    description: Cluster CRUD + lookup
  - name: Agents
    description: Adopted-cluster agent inventory, diagnostics, and lifecycle intents
  - name: Nodes
    description: Imported-cluster node operations
  - name: Registration
    description: Onboarding wizard phase machine
  - name: Projects
    description: Project CRUD, namespace policy, and ownership
  - name: Vulnerabilities
    description: Trivy image-scan results
  - name: Shell
    description: In-browser kubectl shell sessions
  - name: K8s
    description: Tunnel-passthrough to managed cluster's k8s API
  - name: Tools
    description: Cluster tool catalog + installs
  - name: Platform
    description: Platform health, diagnostics, and internal transport endpoints
  - name: Extensions
    description: UI extension manifest validation and registry controls
  - name: ServiceMesh
    description: Service mesh detection, inventory, and policy validation
  - name: Alerting
    description: Alert rules + notification channels (Slack/PagerDuty/MS Teams/webhook/email)
  - name: Delivery
    description: Flux-native targets, authoritative placement, rollouts, deployments, and compatibility
paths:
  /api/v1/admin/task-outbox/dead:
    get:
      operationId: getAdminTaskOutboxDead
      tags: [TaskOutbox]
      summary: "Read admin/task-outbox/dead"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RouteResponseEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/group-mappings:
    get:
      operationId: getAdminGroupMappings
      tags: [GroupMappings]
      summary: "Read admin/group-mappings"
      description: Lists SSO identity-group mappings after enforcing the handler's superuser gate.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "superuser check inside handler"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GroupMappingListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      operationId: postAdminGroupMappings
      tags: [GroupMappings]
      summary: "Create or execute admin/group-mappings"
      description: Creates an SSO identity-group mapping after enforcing admin token scope and the handler's superuser gate.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-auth: "bearer/session auth with admin API token scope"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/GroupMappingWriteRequest'}
      responses:
        '201':
          description: Group mapping created.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GroupMappingEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/group-mappings/{id}:
    delete:
      operationId: deleteAdminGroupMappingsById
      tags: [GroupMappings]
      summary: "Delete admin/group-mappings/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-auth: "bearer/session auth with admin API token scope"
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      operationId: getAdminGroupMappingsById
      tags: [GroupMappings]
      summary: "Read admin/group-mappings/{id}"
      description: Returns one SSO identity-group mapping after enforcing the handler's superuser gate.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "superuser check inside handler"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GroupMappingEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/resources/operations/{id}:
    get:
      operationId: getResourceOperation
      tags: [resources-explorer]
      summary: Poll a durable member-cluster resource mutation
      description: Returns sanitized status and terminal evidence only. The route cluster must match the operation and current cluster-read RBAC is re-evaluated on every poll, so revoked access fails closed.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Current durable operation state.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ResourceOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/queues:
    get:
      operationId: getAdminQueues
      tags: [Queues]
      summary: "Read admin/queues"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {type: array, items: {$ref: '#/components/schemas/AdminQueueSummaryWire'}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/queues/{queue}/dlq:
    get:
      operationId: getAdminQueuesByQueueDlq
      tags: [Queues]
      summary: "Read admin/queues/{queue}/dlq"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: queue, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/AdminQueueDLQWire'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/queues/{queue}/dlq/{id}:
    delete:
      operationId: deleteAdminQueuesByQueueDlqById
      x-astronomer-operation-status-path: /api/v1/admin/queues/operations/{id}
      tags: [Queues]
      summary: Queue a durable DLQ discard operation
      description: Atomically commits an operation receipt, identifier-only task intent, and mandatory audit intent before the worker removes the archived task from Redis.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: queue, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Durable discard operation accepted; the Redis effect has not yet been claimed as complete.
          headers:
            Location: {description: Exact admin queue operation status resource, schema: {type: string, format: uri-reference}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AdminQueueOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/queues/{queue}/dlq/{id}/retry:
    post:
      operationId: postAdminQueuesByQueueDlqByIdRetry
      x-astronomer-operation-status-path: /api/v1/admin/queues/operations/{id}
      tags: [Queues]
      summary: Queue a durable DLQ retry operation
      description: Atomically commits an operation receipt, identifier-only task intent, and mandatory audit intent before the worker moves the archived task in Redis.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: queue, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Durable retry operation accepted; the Redis effect has not yet been claimed as complete.
          headers:
            Location: {description: Exact admin queue operation status resource, schema: {type: string, format: uri-reference}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AdminQueueOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/queues/operations/{id}:
    get:
      operationId: getAdminQueuesOperationsById
      tags: [Queues]
      summary: Read a durable DLQ operation receipt
      description: Returns the current PostgreSQL-observed state for an accepted retry or discard operation.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AdminQueueOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/task-outbox/:
    get:
      operationId: getAdminTaskOutbox
      tags: [Queues]
      summary: List durable task delivery outbox entries
      x-astronomer-route-class: public-api
      x-astronomer-auth: bearer-or-session
      x-astronomer-rbac: superuser check inside handler
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [pending, delivering, failed, delivered, dead]}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 50}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Paginated task outbox entries
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/PaginatedEnvelope'}
                  - type: object
                    properties:
                      data: {type: array, items: {$ref: '#/components/schemas/TaskOutboxEntryWire'}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/task-outbox/{id}/:
    get:
      operationId: getAdminTaskOutboxById
      tags: [Queues]
      summary: Get exact durable task-outbox status
      description: Returns the exact committed task-outbox row referenced by a retry receipt.
      x-astronomer-route-class: public-api
      x-astronomer-auth: bearer-or-session
      x-astronomer-rbac: superuser check inside handler
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Exact durable task-outbox state.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/TaskOutboxEntryEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/task-outbox/{id}/retry/:
    post:
      operationId: postAdminTaskOutboxByIdRetry
      x-astronomer-operation-status-path: /api/v1/admin/task-outbox/{id}/
      tags: [Queues]
      summary: Requeue a failed task outbox entry
      x-astronomer-route-class: public-api
      x-astronomer-auth: bearer-or-session
      x-astronomer-rbac: superuser check inside handler
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Entry reset for asynchronous delivery; identical replay returns this exact receipt.
          headers:
            Location: {description: Exact task-outbox status resource, schema: {type: string, format: uri-reference}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/TaskOutboxEntryEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/control-plane-snapshots/:
    get:
      operationId: listControlPlaneSnapshots
      tags: [Clusters]
      summary: List control-plane snapshot intents
      x-astronomer-route-class: public-api
      x-astronomer-auth: bearer-or-session
      x-astronomer-rbac: clusters:read
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 50}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Snapshot page
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [items, total, limit, offset]
                        properties:
                          items: {type: array, items: {$ref: '#/components/schemas/ControlPlaneSnapshotWire'}}
                          total: {type: integer, format: int64}
                          limit: {type: integer}
                          offset: {type: integer}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
    post:
      operationId: createControlPlaneSnapshot
      tags: [Clusters]
      summary: Queue a control-plane snapshot
      x-astronomer-operation-status-path: /api/v1/clusters/{cluster_id}/control-plane-snapshots/{id}/
      x-astronomer-route-class: public-api
      x-astronomer-auth: bearer-or-session
      x-astronomer-rbac: clusters:update
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreateControlPlaneSnapshotRequestWire'}
      responses:
        '202':
          description: Durable snapshot intent accepted
          headers:
            Location: {description: Exact snapshot status resource, schema: {type: string, format: uri-reference}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/ControlPlaneSnapshotWire'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
  /api/v1/clusters/{cluster_id}/control-plane-snapshots/{id}/:
    get:
      operationId: getControlPlaneSnapshot
      tags: [Clusters]
      summary: Get a control-plane snapshot intent
      x-astronomer-route-class: public-api
      x-astronomer-auth: bearer-or-session
      x-astronomer-rbac: clusters:read
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Snapshot intent
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/ControlPlaneSnapshotWire'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/clusters/{cluster_id}/control-plane-snapshots/{id}/restore-guidance/:
    get:
      operationId: getControlPlaneSnapshotRestoreGuidance
      tags: [Clusters]
      summary: Get offline control-plane restore guidance
      description: Returns a read-only runbook and never initiates a restore.
      x-astronomer-route-class: public-api
      x-astronomer-auth: bearer-or-session
      x-astronomer-rbac: clusters:read
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Distribution-specific offline restore runbook
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/ControlPlaneRestoreRunbookWire'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/backups/restores/{id}:
    get:
      operationId: getBackupsRestoresById
      tags: [Backups]
      summary: Read one backup restore operation
      description: Returns the durable Velero restore receipt for authorized polling. This is the dereferenceable target emitted in the restore creation Location header.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Current restore operation state
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/RestoreOperationResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/saved-searches:
    get:
      operationId: getLoggingSavedSearches
      tags: [Logging]
      summary: "Read logging/saved-searches"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: output_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingSavedSearchListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      operationId: postLoggingSavedSearches
      tags: [Logging]
      summary: "Create or execute logging/saved-searches"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreateLoggingSavedSearchRequest'}
      responses:
        '201':
          description: Saved search created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingSavedSearchEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/saved-searches/{id}:
    delete:
      operationId: deleteLoggingSavedSearchesById
      tags: [Logging]
      summary: "Delete logging/saved-searches/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Saved search deleted
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      operationId: putLoggingSavedSearchesById
      tags: [Logging]
      summary: "Replace logging/saved-searches/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateLoggingSavedSearchRequest'}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingSavedSearchEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/resources/discovery:
    get:
      operationId: getClustersByClusterIdResourcesDiscovery
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/resources/discovery"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ResourceDiscoveryEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/resources/schema:
    get:
      operationId: getClustersByClusterIdResourcesSchema
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/resources/schema"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - name: resource_type
          in: query
          required: true
          schema:
            type: string
            enum: [deployments, statefulsets, daemonsets, jobs, cronjobs, services, ingresses, gateways, configmaps, secrets, persistentvolumeclaims, namespaces, serviceaccounts, k8s-roles, k8s-rolebindings, networkpolicies, hpa, poddisruptionbudgets]
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ResourceSchemaEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/compliance/export:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminComplianceExport
      tags: [Compliance]
      summary: "Read admin/compliance/export"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: download
      parameters:
        - {name: from, in: query, required: true, schema: {type: string, format: date}}
        - {name: to, in: query, required: true, schema: {type: string, format: date}}
      responses:
        '200':
          description: Audited download payload
          content:
            application/zip:
              schema: {type: string, format: binary}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/compliance/exports/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminComplianceExportsById
      tags: [Compliance]
      summary: "Read admin/compliance/exports/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: download
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Audited download payload
          content:
            application/octet-stream:
              schema: {type: string, format: binary}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/dashboard-widgets:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminDashboardWidgets
      tags: [DashboardWidgets]
      summary: "Read admin/dashboard-widgets"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Paginated dashboard widget definitions
          content:
            application/json:
              schema: {$ref: '#/components/schemas/DashboardWidgetPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAdminDashboardWidgets
      tags: [DashboardWidgets]
      summary: "Create or execute admin/dashboard-widgets"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/DashboardWidgetRequest'}
      responses:
        '201':
          description: Dashboard widget created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/DashboardWidgetEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/dashboard-widgets/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteAdminDashboardWidgetsById
      tags: [DashboardWidgets]
      summary: "Delete admin/dashboard-widgets/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminDashboardWidgetsById
      tags: [DashboardWidgets]
      summary: "Read admin/dashboard-widgets/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Dashboard widget definition
          content:
            application/json:
              schema: {$ref: '#/components/schemas/DashboardWidgetEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putAdminDashboardWidgetsById
      tags: [DashboardWidgets]
      summary: "Replace admin/dashboard-widgets/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/DashboardWidgetRequest'}
      responses:
        '200':
          description: Dashboard widget replaced
          content:
            application/json:
              schema: {$ref: '#/components/schemas/DashboardWidgetEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/gitops-sources:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminGitopsSources
      tags: [GitopsSources]
      summary: "Read admin/gitops-sources"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
      responses:
        '200':
          description: Paginated GitOps registration sources
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GitOpsSourcePage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAdminGitopsSources
      tags: [GitopsSources]
      summary: "Create or execute admin/gitops-sources"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/GitOpsSourceRequest'}
      responses:
        '201':
          description: GitOps registration source created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GitOpsSourceEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/gitops-sources/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteAdminGitopsSourcesById
      tags: [GitopsSources]
      summary: "Delete admin/gitops-sources/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminGitopsSourcesById
      tags: [GitopsSources]
      summary: "Read admin/gitops-sources/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: GitOps registration source
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GitOpsSourceEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putAdminGitopsSourcesById
      tags: [GitopsSources]
      summary: "Replace admin/gitops-sources/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/GitOpsSourceRequest'}
      responses:
        '200':
          description: GitOps registration source updated
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GitOpsSourceEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/gitops-sources/{id}/clusters:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminGitopsSourcesByIdClusters
      tags: [GitopsSources]
      summary: "Read admin/gitops-sources/{id}/clusters"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
      responses:
        '200':
          description: Paginated clusters managed by this GitOps source
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GitOpsManagedClusterPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/gitops-sources/{id}/preview:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminGitopsSourcesByIdPreview
      tags: [GitopsSources]
      summary: "Read admin/gitops-sources/{id}/preview"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Side-effect-free GitOps reconciliation preview
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GitOpsPreviewResultEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/gitops-sources/{id}/sync:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAdminGitopsSourcesByIdSync
      x-astronomer-operation-status-path: /api/v1/admin/gitops-sources/{id}
      tags: [GitopsSources]
      summary: "Create or execute admin/gitops-sources/{id}/sync"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: GitOps source sync durably queued; identical replay returns this exact receipt.
          headers:
            Location: {description: Exact GitOps source status resource, schema: {type: string, format: uri-reference}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GitOpsSyncReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/prometheus-datasources:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminPrometheusDatasources
      tags: [PrometheusDatasources]
      summary: "Read admin/prometheus-datasources"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Paginated Prometheus datasource definitions
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrometheusDatasourcePage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAdminPrometheusDatasources
      tags: [PrometheusDatasources]
      summary: "Create or execute admin/prometheus-datasources"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PrometheusDatasourceRequest'}
      responses:
        '201':
          description: Prometheus datasource created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrometheusDatasourceEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/prometheus-datasources/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteAdminPrometheusDatasourcesById
      tags: [PrometheusDatasources]
      summary: "Delete admin/prometheus-datasources/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putAdminPrometheusDatasourcesById
      tags: [PrometheusDatasources]
      summary: "Replace admin/prometheus-datasources/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PrometheusDatasourceRequest'}
      responses:
        '200':
          description: Prometheus datasource replaced
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrometheusDatasourceEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/prometheus-datasources/{id}/test:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAdminPrometheusDatasourcesByIdTest
      tags: [PrometheusDatasources]
      summary: "Create or execute admin/prometheus-datasources/{id}/test"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Prometheus datasource reachability result
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrometheusDatasourceTestResultEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/quota-plans:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminQuotaPlans
      tags: [QuotaPlans]
      summary: "Read admin/quota-plans"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
      responses:
        '200':
          description: Paginated quota-plan catalog
          content:
            application/json:
              schema: {$ref: '#/components/schemas/QuotaPlanPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAdminQuotaPlans
      tags: [QuotaPlans]
      summary: "Create or execute admin/quota-plans"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/QuotaPlanRequest'}
      responses:
        '201':
          description: Quota plan created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/QuotaPlanEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/quota-plans/{name}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteAdminQuotaPlansByName
      tags: [QuotaPlans]
      summary: "Delete admin/quota-plans/{name}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: name, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminQuotaPlansByName
      tags: [QuotaPlans]
      summary: "Read admin/quota-plans/{name}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: name, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Quota plan
          content:
            application/json:
              schema: {$ref: '#/components/schemas/QuotaPlanEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putAdminQuotaPlansByName
      tags: [QuotaPlans]
      summary: "Replace admin/quota-plans/{name}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: name, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/QuotaPlanRequest'}
      responses:
        '200':
          description: Quota plan replaced
          content:
            application/json:
              schema: {$ref: '#/components/schemas/QuotaPlanEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/quota-usage:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminQuotaUsage
      tags: [QuotaUsage]
      summary: "Read admin/quota-usage"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Fleet quota capacity and high-utilization tenants
          content:
            application/json:
              schema: {$ref: '#/components/schemas/QuotaUsageSnapshotEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/settings:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminSettings
      tags: [Settings]
      summary: "Read admin/settings"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Complete registry-backed platform settings snapshot
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PlatformSettingListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "admin-scope-and-handler-superuser"
      operationId: putAdminSettings
      tags: [Settings]
      summary: Atomically update platform settings
      description: Validates every registry key and value before committing the complete update set in one database statement.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PlatformSettingsBatchUpdateRequest'}
      responses:
        '200':
          description: Settings updated atomically
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PlatformSettingListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '500': {$ref: '#/components/responses/ErrorResponse'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/settings/{key}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteAdminSettingsByKey
      tags: [Settings]
      summary: "Delete admin/settings/{key}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: key, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Setting reset to its registry default
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PlatformSettingEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAdminSettingsByKey
      tags: [Settings]
      summary: "Read admin/settings/{key}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: key, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Registry-backed platform setting
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PlatformSettingEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putAdminSettingsByKey
      tags: [Settings]
      summary: "Replace admin/settings/{key}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: key, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PlatformSettingUpdateRequest'}
      responses:
        '200':
          description: Updated platform setting
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PlatformSettingEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/channels/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteAlertingChannelsById
      tags: [Alerting]
      summary: "Delete alerting/channels/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAlertingChannelsById
      tags: [Alerting]
      summary: "Read alerting/channels/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Notification channel
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NotificationChannelEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putAlertingChannelsById
      tags: [Alerting]
      summary: "Replace alerting/channels/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AlertChannelRequest'}
      responses:
        '200':
          description: Updated notification channel
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NotificationChannelEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/channels/{id}/test:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertingChannelsByIdTest
      tags: [Alerting]
      summary: "Create or execute alerting/channels/{id}/test"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Test notification accepted by the dispatcher
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NotificationChannelTestResultEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/events/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAlertingEventsById
      tags: [Alerting]
      summary: "Read alerting/events/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Alert event
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertEventEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/events/{id}/acknowledge:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertingEventsByIdAcknowledge
      tags: [Alerting]
      summary: "Create or execute alerting/events/{id}/acknowledge"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Acknowledged alert event
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertEventEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/events/{id}/resolve:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertingEventsByIdResolve
      tags: [Alerting]
      summary: "Create or execute alerting/events/{id}/resolve"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Resolved alert event
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertEventEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/rules:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAlertingRules
      tags: [Alerting]
      summary: "Read alerting/rules"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: clusterId, in: query, schema: {type: string, format: uuid}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Alert rules page
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertRulePage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertingRules
      tags: [Alerting]
      summary: "Create or execute alerting/rules"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AlertRuleRequest'}
      responses:
        '201':
          description: Created alert rule
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertRuleEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/rules/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteAlertingRulesById
      tags: [Alerting]
      summary: "Delete alerting/rules/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAlertingRulesById
      tags: [Alerting]
      summary: "Read alerting/rules/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Alert rule
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertRuleEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putAlertingRulesById
      tags: [Alerting]
      summary: "Replace alerting/rules/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AlertRuleRequest'}
      responses:
        '200':
          description: Updated alert rule
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertRuleEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/rules/{id}/disable:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertingRulesByIdDisable
      tags: [Alerting]
      summary: "Create or execute alerting/rules/{id}/disable"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Disabled alert rule
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertRuleEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/rules/{id}/enable:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertingRulesByIdEnable
      tags: [Alerting]
      summary: "Create or execute alerting/rules/{id}/enable"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Enabled alert rule
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertRuleEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/silences:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAlertingSilences
      tags: [Alerting]
      summary: "Read alerting/silences"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Alert silences page
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertSilencePage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertingSilences
      tags: [Alerting]
      summary: "Create or execute alerting/silences"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AlertSilenceRequest'}
      responses:
        '201':
          description: Created alert silence
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertSilenceEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/silences/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteAlertingSilencesById
      tags: [Alerting]
      summary: "Delete alerting/silences/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerting/silences/{id}/expire:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertingSilencesByIdExpire
      tags: [Alerting]
      summary: "Create or execute alerting/silences/{id}/expire"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Expired alert silence
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertSilenceEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerts/rules/{id}/disable:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertsRulesByIdDisable
      tags: [Alerts]
      summary: "Create or execute alerts/rules/{id}/disable"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: compatibility-alias
      deprecated: true
      x-astronomer-sunset: '2027-08-23'
      x-astronomer-successor: '/api/v1/alerting/rules/{id}/disable'
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Disabled alert rule
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertRuleEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerts/rules/{id}/enable:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertsRulesByIdEnable
      tags: [Alerts]
      summary: "Create or execute alerts/rules/{id}/enable"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: compatibility-alias
      deprecated: true
      x-astronomer-sunset: '2027-08-23'
      x-astronomer-successor: '/api/v1/alerting/rules/{id}/enable'
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Enabled alert rule
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertRuleEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/alerts/silences/{id}/expire:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postAlertsSilencesByIdExpire
      tags: [Alerts]
      summary: "Create or execute alerts/silences/{id}/expire"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: compatibility-alias
      deprecated: true
      x-astronomer-sunset: '2027-08-23'
      x-astronomer-successor: '/api/v1/alerting/silences/{id}/expire'
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Expired alert silence
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertSilenceEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/anomaly-baselines:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAnomalyBaselines
      tags: [AnomalyBaselines]
      summary: "Read anomaly-baselines"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: clusterId, in: query, schema: {type: string, format: uuid}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 50}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Authorized anomaly baseline page
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AnomalyBaselinePage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/anomaly-baselines/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAnomalyBaselinesById
      tags: [AnomalyBaselines]
      summary: "Read anomaly-baselines/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Authorized anomaly baseline
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AnomalyBaselineEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/auth/me/quota:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getAuthMeQuota
      tags: [Auth]
      summary: "Read auth/me/quota"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Effective quota and usage for the authenticated user
          content:
            application/json:
              schema: {$ref: '#/components/schemas/UserQuotaSnapshotEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/catalog/installed/{id}/revisions:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getCatalogInstalledByIdRevisions
      tags: [Catalog]
      summary: "Read catalog/installed/{id}/revisions"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Helm revision history for the installed release
          content:
            application/json:
              schema: {$ref: '#/components/schemas/HelmReleaseHistoryEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charts/{chart_id}/ratings:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getChartsByChartIdRatings
      tags: [Charts]
      summary: List a chart's ratings
      description: Returns the chart's ratings in a DRF-style paginated envelope.
      x-astronomer-route-class: public-api
      parameters:
        - {name: chart_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Paginated chart ratings.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/PaginatedEnvelope'}
                  - properties:
                      data:
                        type: array
                        items: {$ref: '#/components/schemas/ChartRating'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '500': {$ref: '#/components/responses/ErrorResponse'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postChartsByChartIdRatings
      tags: [Charts]
      summary: Create or replace the caller's chart rating
      description: Creates a rating, or updates the existing rating for the same user and installation/chart identity.
      x-astronomer-route-class: public-api
      parameters:
        - {name: chart_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ChartRatingWriteRequest'}
      responses:
        '200':
          description: Existing rating updated.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ChartRating'}
        '201':
          description: Rating created.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ChartRating'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '500': {$ref: '#/components/responses/ErrorResponse'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charts/{chart_id}/ratings/{rating_id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteChartsByChartIdRatingsByRatingId
      tags: [Charts]
      summary: "Delete charts/{chart_id}/ratings/{rating_id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: chart_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: rating_id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putChartsByChartIdRatingsByRatingId
      tags: [Charts]
      summary: Update a chart rating
      description: Updates a rating owned by the caller, or any rating for a superuser.
      x-astronomer-route-class: public-api
      parameters:
        - {name: chart_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: rating_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ChartRatingWriteRequest'}
      responses:
        '200':
          description: Updated rating.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ChartRating'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charts/{chart_id}/ratings/aggregate:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getChartsByChartIdRatingsAggregate
      tags: [Charts]
      summary: Get a chart's rating aggregate
      description: Returns the current average, Bayesian score, and five-bucket histogram.
      x-astronomer-route-class: public-api
      parameters:
        - {name: chart_id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Current rating aggregate.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ChartRatingAggregate'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charts/{chart_id}/ratings/mine:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getChartsByChartIdRatingsMine
      tags: [Charts]
      summary: Get the caller's rating for a chart
      description: Returns 404 when the caller has not rated the chart.
      x-astronomer-route-class: public-api
      parameters:
        - {name: chart_id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: The caller's rating.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ChartRating'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/gateway-classes:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdGatewayClasses
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/gateway-classes"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Page of mirrored GatewayClass resources
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MirroredGatewayClassPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/ingress-classes:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdIngressClasses
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/ingress-classes"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Page of mirrored IngressClass resources
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MirroredIngressClassPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/k8s/*:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteClustersByClusterIdK8sProxy
      tags: [Clusters]
      summary: "Delete clusters/{cluster_id}/k8s/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdK8sProxy
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/k8s/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubernetesProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    head:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: headClustersByClusterIdK8sProxy
      tags: [Clusters]
      summary: "HEAD clusters/{cluster_id}/k8s/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubernetesProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    options:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: optionsClustersByClusterIdK8sProxy
      tags: [Clusters]
      summary: "OPTIONS clusters/{cluster_id}/k8s/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubernetesProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    patch:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: patchClustersByClusterIdK8sProxy
      tags: [Clusters]
      summary: "Update clusters/{cluster_id}/k8s/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      x-astronomer-request-schema-status: passthrough
      requestBody:
        required: true
        content:
          application/json:
            schema: {}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubernetesProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postClustersByClusterIdK8sProxy
      tags: [Clusters]
      summary: "Create or execute clusters/{cluster_id}/k8s/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      x-astronomer-request-schema-status: passthrough
      requestBody:
        required: false
        content:
          application/json:
            schema: {}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubernetesProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putClustersByClusterIdK8sProxy
      tags: [Clusters]
      summary: "Replace clusters/{cluster_id}/k8s/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      x-astronomer-request-schema-status: passthrough
      requestBody:
        required: true
        content:
          application/json:
            schema: {}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubernetesProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    trace:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: traceClustersByClusterIdK8sProxy
      tags: [Clusters]
      summary: "TRACE clusters/{cluster_id}/k8s/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubernetesProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/limit-ranges:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdLimitRanges
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/limit-ranges"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: query, schema: {type: string}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Page of mirrored LimitRange resources
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MirroredLimitRangePage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/network-policies:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdNetworkPolicies
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/network-policies"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: query, schema: {type: string}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Page of mirrored NetworkPolicy resources
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MirroredNetworkPolicyPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/proxy/service/{namespace}/{service_port}/*:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteClustersByClusterIdProxyServiceByNamespaceByServicePortProxy
      tags: [Clusters]
      summary: "Delete clusters/{cluster_id}/proxy/service/{namespace}/{service_port}/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: service_port, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdProxyServiceByNamespaceByServicePortProxy
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/proxy/service/{namespace}/{service_port}/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: service_port, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Successful operation
          content:
            '*/*':
              schema: {$ref: '#/components/schemas/ServiceProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    head:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: headClustersByClusterIdProxyServiceByNamespaceByServicePortProxy
      tags: [Clusters]
      summary: "HEAD clusters/{cluster_id}/proxy/service/{namespace}/{service_port}/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: service_port, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Successful operation
          content:
            '*/*':
              schema: {$ref: '#/components/schemas/ServiceProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    options:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: optionsClustersByClusterIdProxyServiceByNamespaceByServicePortProxy
      tags: [Clusters]
      summary: "OPTIONS clusters/{cluster_id}/proxy/service/{namespace}/{service_port}/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: service_port, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Successful operation
          content:
            '*/*':
              schema: {$ref: '#/components/schemas/ServiceProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    patch:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: patchClustersByClusterIdProxyServiceByNamespaceByServicePortProxy
      tags: [Clusters]
      summary: "Update clusters/{cluster_id}/proxy/service/{namespace}/{service_port}/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: service_port, in: path, required: true, schema: {type: string, minLength: 1}}
      x-astronomer-request-schema-status: passthrough
      requestBody:
        required: true
        content:
          application/json:
            schema: {}
      responses:
        '200':
          description: Successful operation
          content:
            '*/*':
              schema: {$ref: '#/components/schemas/ServiceProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postClustersByClusterIdProxyServiceByNamespaceByServicePortProxy
      tags: [Clusters]
      summary: "Create or execute clusters/{cluster_id}/proxy/service/{namespace}/{service_port}/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: service_port, in: path, required: true, schema: {type: string, minLength: 1}}
      x-astronomer-request-schema-status: passthrough
      requestBody:
        required: false
        content:
          application/json:
            schema: {}
      responses:
        '200':
          description: Successful operation
          content:
            '*/*':
              schema: {$ref: '#/components/schemas/ServiceProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putClustersByClusterIdProxyServiceByNamespaceByServicePortProxy
      tags: [Clusters]
      summary: "Replace clusters/{cluster_id}/proxy/service/{namespace}/{service_port}/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: service_port, in: path, required: true, schema: {type: string, minLength: 1}}
      x-astronomer-request-schema-status: passthrough
      requestBody:
        required: true
        content:
          application/json:
            schema: {}
      responses:
        '200':
          description: Successful operation
          content:
            '*/*':
              schema: {$ref: '#/components/schemas/ServiceProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    trace:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: traceClustersByClusterIdProxyServiceByNamespaceByServicePortProxy
      tags: [Clusters]
      summary: "TRACE clusters/{cluster_id}/proxy/service/{namespace}/{service_port}/*"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: proxy
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: service_port, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Successful operation
          content:
            '*/*':
              schema: {$ref: '#/components/schemas/ServiceProxyResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/resource-quotas:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdResourceQuotas
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/resource-quotas"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: query, schema: {type: string}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Page of mirrored ResourceQuota resources
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MirroredResourceQuotaPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/security/policy:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdSecurityPolicy
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/security/policy"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Security policy assigned to the cluster
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ClusterSecurityPolicyEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/security/scans:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdSecurityScans
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/security/scans"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Page of cluster security scans
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CISScanListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/security/scans/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdSecurityScansById
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/security/scans/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Cluster security scan
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CISScanEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/security/scans/{id}/cancel:
    post:
      operationId: postClustersByClusterIdSecurityScansByIdCancel
      tags: [Clusters]
      summary: "Create or execute clusters/{cluster_id}/security/scans/{id}/cancel"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "clusters:read and security:update on the cluster-qualified scan"
      x-astronomer-auth: "bearer/session auth with write-clusters token scope"
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Cancelled cluster security scan
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CISScanEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/tools/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdToolsStatus
      tags: [Clusters]
      summary: List tool status for a cluster
      description: Returns the complete enabled-tool catalog with installed and in-flight operation state for the selected cluster.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Complete tool status list.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/ListEnvelope'}
                  - properties:
                      data:
                        type: array
                        items: {$ref: '#/components/schemas/ClusterToolStatus'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/vulnerabilities/diff:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdVulnerabilitiesDiff
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/vulnerabilities/diff"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: prior_hours, in: query, schema: {type: integer, minimum: 1, maximum: 8760, default: 24}}
      responses:
        '200':
          description: Latest vulnerability totals compared with a prior snapshot
          content:
            application/json:
              schema: {$ref: '#/components/schemas/VulnerabilityDiffEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/vulnerabilities/export.csv:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdVulnerabilitiesExportCsv
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/vulnerabilities/export.csv"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: download
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Audited download payload
          content:
            text/csv:
              schema: {type: string, format: binary}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/vulnerabilities/history:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdVulnerabilitiesHistory
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/vulnerabilities/history"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: since_hours, in: query, schema: {type: integer, minimum: 1, maximum: 8760, default: 720}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 1000, default: 200}}
      responses:
        '200':
          description: Cluster vulnerability scan history
          content:
            application/json:
              schema: {$ref: '#/components/schemas/VulnerabilityHistoryEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/vulnerabilities/progress:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdVulnerabilitiesProgress
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/vulnerabilities/progress"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Live Trivy scan progress and persisted report count
          content:
            application/json:
              schema: {$ref: '#/components/schemas/VulnerabilityScanProgressEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/vulnerabilities/reports/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdVulnerabilitiesReportsById
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/vulnerabilities/reports/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: severity, in: query, schema: {type: string}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 500, default: 50}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Vulnerability report with a filtered page of CVEs
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ImageVulnerabilityReportDetailEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/vulnerabilities/reports/{report_id}/history:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdVulnerabilitiesReportsByReportIdHistory
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/vulnerabilities/reports/{report_id}/history"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: report_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 1000, default: 100}}
      responses:
        '200':
          description: Per-image vulnerability report history
          content:
            application/json:
              schema: {$ref: '#/components/schemas/VulnerabilityReportHistoryEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/vulnerabilities/rescan:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postClustersByClusterIdVulnerabilitiesRescan
      tags: [Clusters]
      summary: "Create or execute clusters/{cluster_id}/vulnerabilities/rescan"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Durable Trivy rescan operation accepted
          headers:
            Location: {description: Pollable workload operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/VulnerabilityRescanResultEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{cluster_id}/workloads/{kind}/{namespace}/{name}/metrics:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByClusterIdWorkloadsByKindByNamespaceByNameMetrics
      tags: [Clusters]
      summary: "Read clusters/{cluster_id}/workloads/{kind}/{namespace}/{name}/metrics"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: kind, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: name, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: range, in: query, schema: {type: string, enum: [1h, 6h, 24h, 7d], default: 1h}}
      responses:
        '200':
          description: Workload metric series
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringMetricsEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/logging/outputs/{output_id}/rotate-token:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postClustersByIdLoggingOutputsByOutputIdRotateToken
      tags: [Clusters]
      summary: "Create or execute clusters/{id}/logging/outputs/{output_id}/rotate-token"
      description: Atomically rotates the hosted-Loki token, records audit intent, and enqueues durable output reconciliation. The plaintext token is returned only in this accepted receipt.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: output_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Token rotation and durable reconciliation operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingTokenRotationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/logging/outputs/attach-astronomer:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByIdLoggingOutputsAttachAstronomer
      tags: [Clusters]
      summary: "Read clusters/{id}/logging/outputs/attach-astronomer"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingAttachStatusEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postClustersByIdLoggingOutputsAttachAstronomer
      tags: [Clusters]
      summary: "Create or execute clusters/{id}/logging/outputs/attach-astronomer"
      description: Returns the existing attachment synchronously when no write is needed; otherwise atomically persists the hosted-Loki output and token, records audit intent, and enqueues durable reconciliation.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: rotate, in: query, required: false, schema: {type: boolean, default: false}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '200':
          description: Existing system output was already attached.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingAttachResultEnvelope'}
        '202':
          description: Hosted-Loki attachment or rotation and durable reconciliation operation accepted. A newly minted plaintext token is returned only in this receipt.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingAttachMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/monitoring/config:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByIdMonitoringConfig
      tags: [Clusters]
      summary: "Read clusters/{id}/monitoring/config"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Cluster monitoring configuration, or an empty data object when not configured.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ClusterMonitoringConfigEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putClustersByIdMonitoringConfig
      tags: [Clusters]
      summary: "Replace clusters/{id}/monitoring/config"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateClusterMonitoringConfigRequest'}
      responses:
        '200':
          description: Cluster monitoring configuration replaced.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ClusterMonitoringConfigEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/monitoring/stack/install:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postClustersByIdMonitoringStackInstall
      tags: [Clusters]
      summary: "Create or execute clusters/{id}/monitoring/stack/install"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/MonitoringStackRequest'}
      responses:
        '202':
          description: Monitoring-stack installation durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/monitoring/stack/preview:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postClustersByIdMonitoringStackPreview
      tags: [Clusters]
      summary: "Create or execute clusters/{id}/monitoring/stack/preview"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/MonitoringStackRequest'}
      responses:
        '200':
          description: Sanitized rendered Helm values and replacement impact.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringStackPreviewEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/monitoring/stack/replace:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postClustersByIdMonitoringStackReplace
      tags: [Clusters]
      summary: "Create or execute clusters/{id}/monitoring/stack/replace"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/MonitoringStackRequest'}
      responses:
        '202':
          description: Monitoring-stack replacement durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/monitoring/stack/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getClustersByIdMonitoringStackStatus
      tags: [Clusters]
      summary: "Read clusters/{id}/monitoring/stack/status"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Desired, observed, drift, and latest-operation status.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringStackStatusEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/monitoring/stack/uninstall:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteClustersByIdMonitoringStackUninstall
      tags: [Clusters]
      summary: "Delete clusters/{id}/monitoring/stack/uninstall"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Monitoring-stack uninstall durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/monitoring/stack/upgrade:
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putClustersByIdMonitoringStackUpgrade
      tags: [Clusters]
      summary: "Replace clusters/{id}/monitoring/stack/upgrade"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/MonitoringStackRequest'}
      responses:
        '202':
          description: Monitoring-stack upgrade durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/compliance/posture:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getCompliancePosture
      tags: [Compliance]
      summary: "Read compliance/posture"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Fleet compliance posture rollup
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CompliancePostureEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/controllers/alerts:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getControllersAlerts
      tags: [Controllers]
      summary: "Read controllers/alerts"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: status, in: query, schema: {type: string}}
        - {name: controller, in: query, schema: {type: string}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 50}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Page of control-plane alerts
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ControlPlaneAlertPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/controllers/alerts/{id}/acknowledge:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postControllersAlertsByIdAcknowledge
      tags: [Controllers]
      summary: "Create or execute controllers/alerts/{id}/acknowledge"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Acknowledged control-plane alert
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ControlPlaneAlertEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/controllers/policy:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getControllersPolicy
      tags: [Controllers]
      summary: "Read controllers/policy"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Active control-plane health policy
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ControlPlanePolicyEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putControllersPolicy
      tags: [Controllers]
      summary: "Replace controllers/policy"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ControlPlanePolicyRequest'}
      responses:
        '200':
          description: Updated control-plane health policy
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ControlPlanePolicyEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/controllers/silences:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getControllersSilences
      tags: [Controllers]
      summary: "Read controllers/silences"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 50}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Page of control-plane silences
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ControlPlaneSilencePage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postControllersSilences
      tags: [Controllers]
      summary: "Create or execute controllers/silences"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ControlPlaneSilenceRequest'}
      responses:
        '201':
          description: Created control-plane silence
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ControlPlaneSilenceEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/controllers/silences/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteControllersSilencesById
      tags: [Controllers]
      summary: "Delete controllers/silences/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/controllers/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getControllersStatus
      tags: [Controllers]
      summary: "Read controllers/status"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Aggregate controller health and active thresholds
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ControlPlaneStatusEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/dashboards/clusters/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getDashboardsClustersById
      tags: [Dashboards]
      summary: "Read dashboards/clusters/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Rendered dashboard widgets authorized for the cluster
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RenderedDashboardWidgetListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/dashboards/global:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getDashboardsGlobal
      tags: [Dashboards]
      summary: "Read dashboards/global"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Rendered global dashboard widgets
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RenderedDashboardWidgetListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/dashboards/projects/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getDashboardsProjectsById
      tags: [Dashboards]
      summary: "Read dashboards/projects/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Rendered dashboard widgets authorized for the project
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RenderedDashboardWidgetListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/gitops/sources/{id}/webhook:
    post:
      x-astronomer-auth: "gitops-webhook-secret"
      x-astronomer-rbac: "external-shared-secret"
      security:
        - gitOpsWebhookSecret: []
      operationId: postGitopsSourcesByIdWebhook
      x-astronomer-async-202-exception:
        reason: trusted_webhook_ingest
      tags: [Gitops]
      summary: "Create or execute gitops/sources/{id}/webhook"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '202':
          description: GitOps source sync durably queued
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GitOpsSyncResultEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/controller/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getLoggingControllerStatus
      tags: [Logging]
      summary: "Read logging/controller/status"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Authorized logging controller health and reconciliation summary.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingControllerStatusEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/operations:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getLoggingOperations
      tags: [Logging]
      summary: "Read logging/operations"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
        - {name: targetType, in: query, required: false, schema: {type: string, enum: [output, pipeline]}}
        - {name: targetKey, in: query, required: false, schema: {type: string}}
        - {name: status, in: query, required: false, schema: {type: string, enum: [pending, running, completed, failed, superseded]}}
      responses:
        '200':
          description: Page of the full authorized, filtered operation result.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingOperationListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/operations/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getLoggingOperationsById
      tags: [Logging]
      summary: "Read logging/operations/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Authorized operation detail with bounded event history when available.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/operations/{id}/retry:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postLoggingOperationsByIdRetry
      tags: [Logging]
      summary: "Create or execute logging/operations/{id}/retry"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Existing failed operation requeued after durable audit commit.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/outputs:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getLoggingOutputs
      tags: [Logging]
      summary: "Read logging/outputs"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
        - {name: cluster_id, in: query, required: false, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Page of authorized logging outputs with exact authorized total.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingOutputPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postLoggingOutputs
      tags: [Logging]
      summary: "Create or execute logging/outputs"
      description: Atomically commits output desired state, durable reconciliation operation, and audit intent.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: query, required: false, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LoggingOutputWriteRequest'}
      responses:
        '202':
          description: Output desired state and durable reconciliation operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingOutputMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/outputs/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteLoggingOutputsById
      tags: [Logging]
      summary: "Delete logging/outputs/{id}"
      description: Atomically commits output deletion, durable reconciliation operation, and audit intent. The accepted receipt includes the deleted desired-state representation for correlation.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Output deletion and durable reconciliation operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingOutputMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putLoggingOutputsById
      tags: [Logging]
      summary: "Replace logging/outputs/{id}"
      description: Atomically replaces output desired state and records the durable reconciliation operation and audit intent.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LoggingOutputWriteRequest'}
      responses:
        '202':
          description: Output replacement and durable reconciliation operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingOutputMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/outputs/{id}/disable:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postLoggingOutputsByIdDisable
      tags: [Logging]
      summary: "Create or execute logging/outputs/{id}/disable"
      description: Atomically disables output desired state and records the durable reconciliation operation and audit intent.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Output disable operation accepted for asynchronous reconciliation.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingOutputMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/outputs/{id}/enable:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postLoggingOutputsByIdEnable
      tags: [Logging]
      summary: "Create or execute logging/outputs/{id}/enable"
      description: Atomically enables output desired state and records the durable reconciliation operation and audit intent.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Output enable operation accepted for asynchronous reconciliation.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingOutputMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/outputs/{id}/query:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postLoggingOutputsByIdQuery
      tags: [Logging]
      summary: "Create or execute logging/outputs/{id}/query"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LoggingQueryRequest'}
      responses:
        '200':
          description: Bounded provider-normalized query result.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingQueryEnvelope'}
        '422': {$ref: '#/components/responses/ErrorResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/outputs/{id}/rotate-token:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postLoggingOutputsByIdRotateToken
      tags: [Logging]
      summary: "Create or execute logging/outputs/{id}/rotate-token"
      description: Atomically rotates the hosted-Loki token, records audit intent, and enqueues durable output reconciliation. The plaintext token is returned only in this accepted receipt.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Token rotation and durable reconciliation operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingTokenRotationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/outputs/{id}/test:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postLoggingOutputsByIdTest
      tags: [Logging]
      summary: "Create or execute logging/outputs/{id}/test"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Apply-test operation accepted for asynchronous reconciliation.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingTestEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/pipelines:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getLoggingPipelines
      tags: [Logging]
      summary: "Read logging/pipelines"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
        - {name: cluster_id, in: query, required: false, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingPipelinePage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postLoggingPipelines
      tags: [Logging]
      summary: "Create or execute logging/pipelines"
      description: Commits the pipeline desired state, durable reconciliation operation, and audit intent atomically. The operation URL is pollable until reconciliation reaches a terminal state.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: query, required: false, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LoggingPipelineWriteRequest'}
      responses:
        '202':
          description: Pipeline desired state and durable reconciliation operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingPipelineMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/pipelines/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteLoggingPipelinesById
      tags: [Logging]
      summary: "Delete logging/pipelines/{id}"
      description: Commits pipeline deletion, a durable reconciliation operation, and audit intent atomically. The receipt includes the deleted desired-state representation for correlation.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Pipeline deletion and durable reconciliation operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingPipelineMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putLoggingPipelinesById
      tags: [Logging]
      summary: "Replace logging/pipelines/{id}"
      description: Atomically replaces pipeline desired state and records the durable reconciliation operation and audit intent.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LoggingPipelineWriteRequest'}
      responses:
        '202':
          description: Pipeline replacement and durable reconciliation operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingPipelineMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/pipelines/{id}/disable:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postLoggingPipelinesByIdDisable
      tags: [Logging]
      summary: "Create or execute logging/pipelines/{id}/disable"
      description: Atomically disables pipeline desired state and records the durable reconciliation operation and audit intent.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Pipeline disable operation accepted for asynchronous reconciliation.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingPipelineMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/pipelines/{id}/enable:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postLoggingPipelinesByIdEnable
      tags: [Logging]
      summary: "Create or execute logging/pipelines/{id}/enable"
      description: Atomically enables pipeline desired state and records the durable reconciliation operation and audit intent.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Pipeline enable operation accepted for asynchronous reconciliation.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingPipelineMutationReceiptEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/logging/pipelines/{id}/fluentbit-config:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getLoggingPipelinesByIdFluentbitConfig
      tags: [Logging]
      summary: "Read logging/pipelines/{id}/fluentbit-config"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LoggingFluentBitConfigEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/monitoring/endpoints:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getMonitoringEndpoints
      tags: [Monitoring]
      summary: "Read monitoring/endpoints"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Page containing the configured default monitoring backend
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringBackendPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postMonitoringEndpoints
      tags: [Monitoring]
      summary: "Create or execute monitoring/endpoints"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateMonitoringBackendRequest'}
      responses:
        '200':
          description: Created or replaced default monitoring backend
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringBackendEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/monitoring/endpoints/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteMonitoringEndpointsById
      tags: [Monitoring]
      summary: Delete the default monitoring endpoint
      description: Requires all per-cluster and managed shared monitoring stacks to be uninstalled; terminal per-cluster configuration records are removed with the backend.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '204':
          description: Monitoring endpoint deleted
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getMonitoringEndpointsById
      tags: [Monitoring]
      summary: "Read monitoring/endpoints/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Monitoring backend
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringBackendEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putMonitoringEndpointsById
      tags: [Monitoring]
      summary: "Replace monitoring/endpoints/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateMonitoringBackendRequest'}
      responses:
        '200':
          description: Updated monitoring backend
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringBackendEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/monitoring/metrics/cluster-overview/{cluster_id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getMonitoringMetricsClusterOverviewByClusterId
      tags: [Monitoring]
      summary: "Read monitoring/metrics/cluster-overview/{cluster_id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Legacy cluster metrics result
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LegacyMonitoringMetricsEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/monitoring/metrics/node/{cluster_id}/{node}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getMonitoringMetricsNodeByClusterIdByNode
      tags: [Monitoring]
      summary: "Read monitoring/metrics/node/{cluster_id}/{node}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: node, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Legacy node metrics result
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LegacyMonitoringMetricsEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/monitoring/metrics/query/{cluster_id}:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postMonitoringMetricsQueryByClusterId
      tags: [Monitoring]
      summary: "Create or execute monitoring/metrics/query/{cluster_id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Cluster metrics
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringMetricsEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/monitoring/metrics/workload/{cluster_id}/{namespace}/{workload}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getMonitoringMetricsWorkloadByClusterIdByNamespaceByWorkload
      tags: [Monitoring]
      summary: "Read monitoring/metrics/workload/{cluster_id}/{namespace}/{workload}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: namespace, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: workload, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: range, in: query, schema: {type: string, enum: [1h, 6h, 24h, 7d], default: 1h}}
      responses:
        '200':
          description: Legacy workload metrics result
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LegacyMonitoringMetricsEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/native-rbac-rules:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getNativeRbacRules
      tags: [NativeRbacRules]
      summary: "Read native-rbac-rules"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: userId, in: query, schema: {type: string, format: uuid}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 500, default: 100}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Native Kubernetes RBAC rules
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NativeRBACRuleListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postNativeRbacRules
      tags: [NativeRbacRules]
      summary: "Create or execute native-rbac-rules"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NativeRBACRuleRequest'}
      responses:
        '201':
          description: Created native Kubernetes RBAC rule
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NativeRBACRuleEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/native-rbac-rules/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteNativeRbacRulesById
      tags: [NativeRbacRules]
      summary: "Delete native-rbac-rules/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/observability/grafana-ticket:
    get:
      operationId: getObservabilityGrafanaTicket
      tags: [Observability]
      summary: "Read observability/grafana-ticket"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "monitoring:read at any scope"
      x-astronomer-auth: "bearer/session auth"
      parameters:
        - name: return
          in: query
          required: false
          description: Allow-listed Grafana return URL. Defaults to the configured Grafana root.
          schema: {type: string, format: uri}
      responses:
        '302':
          description: Redirect to Grafana with a one-use ticket in the query string.
          headers:
            Location:
              schema: {type: string, format: uri}
          content:
            text/html:
              schema: {type: string, description: Standard HTTP redirect body}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/observability/grafana-ticket/redeem:
    post:
      operationId: postObservabilityGrafanaTicketRedeem
      tags: [Observability]
      summary: "Create or execute observability/grafana-ticket/redeem"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "none; Take() of one-use ticket"
      x-astronomer-auth: "ticket is the only credential"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/GrafanaTicketRedeemRequest'}
      responses:
        '200':
          description: Grafana identity and session constraints bound to the consumed ticket
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GrafanaTicketIdentityEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/projects/{id}/quota:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getProjectsByIdQuota
      tags: [Projects]
      summary: "Read projects/{id}/quota"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Effective project quota and current usage
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProjectQuotaSnapshotEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/controller/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSecurityControllerStatus
      tags: [Security]
      summary: "Read security/controller/status"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Security controller health and workload summary
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityControllerStatusEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/policies:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSecurityPolicies
      tags: [Security]
      summary: "Read security/policies"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
      responses:
        '200':
          description: Authorized security policy page
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/PaginatedEnvelope'}
                  - type: object
                    properties:
                      data: {type: array, items: {$ref: '#/components/schemas/ClusterSecurityPolicy'}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSecurityPolicies
      tags: [Security]
      summary: "Create or execute security/policies"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ClusterSecurityPolicyCreateRequest'}
      responses:
        '201':
          description: Security policy assignment created
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/ClusterSecurityPolicy'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/policies/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteSecurityPoliciesById
      tags: [Security]
      summary: "Delete security/policies/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/policies/{id}/apply:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSecurityPoliciesByIdApply
      tags: [Security]
      summary: "Create or execute security/policies/{id}/apply"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Security policy marked for application
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/ClusterSecurityPolicy'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/profiles:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSecurityProfiles
      tags: [Security]
      summary: List installed CIS profiles
      description: Returns profiles discovered from the adopted cluster, with an explicit static fallback when the operator is unavailable.
      x-astronomer-route-class: public-api
      parameters:
        - {name: cluster_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Available profiles and their discovery source
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/CISProfilesResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/scans:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSecurityScans
      tags: [Security]
      summary: List visible CIS scans
      description: Pages only scans on active clusters visible to the caller.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
      responses:
        '200':
          description: Authorized CIS scan page
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CISScanListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSecurityScans
      tags: [Security]
      summary: Start a CIS scan
      description: Atomically creates the durable scan and tunnel task intent, then creates the cis-operator ClusterScan resource.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CISScanCreateRequest'}
      responses:
        '201':
          description: Durable scan receipt
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CISScanEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/scans/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSecurityScansById
      tags: [Security]
      summary: "Read security/scans/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CISScanEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/scans/{id}/report.csv:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSecurityScansByIdReportCsv
      tags: [Security]
      summary: "Read security/scans/{id}/report.csv"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: download
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Audited download payload
          content:
            text/csv:
              schema: {type: string, format: binary}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/templates:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSecurityTemplates
      tags: [Security]
      summary: "Read security/templates"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
      responses:
        '200':
          description: Pod Security Admission template page
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/PaginatedEnvelope'}
                  - type: object
                    properties:
                      data: {type: array, items: {$ref: '#/components/schemas/PodSecurityTemplate'}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSecurityTemplates
      tags: [Security]
      summary: "Create or execute security/templates"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PodSecurityTemplateWriteRequest'}
      responses:
        '201':
          description: Pod Security Admission template created
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/PodSecurityTemplate'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/templates/{id}:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteSecurityTemplatesById
      tags: [Security]
      summary: "Delete security/templates/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSecurityTemplatesById
      tags: [Security]
      summary: "Read security/templates/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Pod Security Admission template
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/PodSecurityTemplate'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putSecurityTemplatesById
      tags: [Security]
      summary: "Replace security/templates/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PodSecurityTemplateWriteRequest'}
      responses:
        '200':
          description: Pod Security Admission template replaced
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - type: object
                    properties:
                      data: {$ref: '#/components/schemas/PodSecurityTemplate'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/vulnerabilities/summary:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSecurityVulnerabilitiesSummary
      tags: [Security]
      summary: "Read security/vulnerabilities/summary"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Fleet vulnerability aggregate
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetVulnerabilityAggregateEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/security/vulnerabilities/top-clusters:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSecurityVulnerabilitiesTopClusters
      tags: [Security]
      summary: "Read security/vulnerabilities/top-clusters"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 10}}
      responses:
        '200':
          description: Clusters with the highest vulnerability counts
          content:
            application/json:
              schema: {$ref: '#/components/schemas/VulnerableClusterSummaryListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/banner:
    get:
      x-astronomer-auth: "public"
      x-astronomer-rbac: "public"
      security: []
      operationId: getSettingsBanner
      tags: [Settings]
      summary: "Read settings/banner"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Public login and global banner settings
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PublicBannerSettingsEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/branding:
    get:
      x-astronomer-auth: "public"
      x-astronomer-rbac: "public"
      security: []
      operationId: getSettingsBranding
      tags: [Settings]
      summary: "Read settings/branding"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Public product branding settings
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PublicBrandingSettingsEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/alertmanager/install:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSettingsMonitoringAlertmanagerInstall
      tags: [Settings]
      summary: "Create or execute settings/monitoring/alertmanager/install"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedAlertmanagerStackRequest'}
      responses:
        '202':
          description: Shared Alertmanager installation durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/alertmanager/preview:
    post:
      operationId: postSettingsMonitoringAlertmanagerPreview
      tags: [Settings]
      summary: "Create or execute settings/monitoring/alertmanager/preview"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "monitoring:read"
      x-astronomer-auth: "bearer/session auth"
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedAlertmanagerStackRequest'}
      responses:
        '200':
          description: Sanitized rendered Helm values and replacement impact.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringStackPreviewEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/alertmanager/replace:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSettingsMonitoringAlertmanagerReplace
      tags: [Settings]
      summary: "Create or execute settings/monitoring/alertmanager/replace"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedAlertmanagerStackRequest'}
      responses:
        '202':
          description: Shared Alertmanager replacement durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/alertmanager/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSettingsMonitoringAlertmanagerStatus
      tags: [Settings]
      summary: "Read settings/monitoring/alertmanager/status"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Desired, observed, drift, and latest-operation status.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringStackStatusEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/alertmanager/uninstall:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteSettingsMonitoringAlertmanagerUninstall
      tags: [Settings]
      summary: "Delete settings/monitoring/alertmanager/uninstall"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: clusterId, in: query, required: false, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Shared Alertmanager uninstall durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/alertmanager/upgrade:
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putSettingsMonitoringAlertmanagerUpgrade
      tags: [Settings]
      summary: "Replace settings/monitoring/alertmanager/upgrade"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedAlertmanagerStackRequest'}
      responses:
        '202':
          description: Shared Alertmanager upgrade durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/backend:
    get:
      operationId: getSettingsMonitoringBackend
      tags: [Settings]
      summary: "Read settings/monitoring/backend"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "monitoring:read"
      x-astronomer-auth: "bearer/session auth"
      responses:
        '200':
          description: Redacted default monitoring backend.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringBackendEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putSettingsMonitoringBackend
      tags: [Settings]
      summary: "Replace settings/monitoring/backend"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateMonitoringBackendRequest'}
      responses:
        '200':
          description: Redacted default monitoring backend replaced.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringBackendEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/grafana/install:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSettingsMonitoringGrafanaInstall
      tags: [Settings]
      summary: "Create or execute settings/monitoring/grafana/install"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedGrafanaStackRequest'}
      responses:
        '202':
          description: Shared Grafana installation durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/grafana/preview:
    post:
      operationId: postSettingsMonitoringGrafanaPreview
      tags: [Settings]
      summary: "Create or execute settings/monitoring/grafana/preview"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "monitoring:read"
      x-astronomer-auth: "bearer/session auth"
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedGrafanaStackRequest'}
      responses:
        '200':
          description: Sanitized rendered Helm values and replacement impact.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringStackPreviewEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/grafana/replace:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSettingsMonitoringGrafanaReplace
      tags: [Settings]
      summary: "Create or execute settings/monitoring/grafana/replace"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedGrafanaStackRequest'}
      responses:
        '202':
          description: Shared Grafana replacement durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/grafana/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSettingsMonitoringGrafanaStatus
      tags: [Settings]
      summary: "Read settings/monitoring/grafana/status"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Desired, observed, drift, and latest-operation status.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringStackStatusEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/grafana/uninstall:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteSettingsMonitoringGrafanaUninstall
      tags: [Settings]
      summary: "Delete settings/monitoring/grafana/uninstall"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: clusterId, in: query, required: false, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Shared Grafana uninstall durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/grafana/upgrade:
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putSettingsMonitoringGrafanaUpgrade
      tags: [Settings]
      summary: "Replace settings/monitoring/grafana/upgrade"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedGrafanaStackRequest'}
      responses:
        '202':
          description: Shared Grafana upgrade durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/loki/install:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSettingsMonitoringLokiInstall
      tags: [Settings]
      summary: "Create or execute settings/monitoring/loki/install"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedLokiStackRequest'}
      responses:
        '202':
          description: Shared Loki installation durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/loki/preview:
    post:
      operationId: postSettingsMonitoringLokiPreview
      tags: [Settings]
      summary: "Create or execute settings/monitoring/loki/preview"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "monitoring:read"
      x-astronomer-auth: "bearer/session auth; feature.hosted_loki FeatureGateDefault false"
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedLokiStackRequest'}
      responses:
        '200':
          description: Sanitized rendered Helm values and replacement impact.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringStackPreviewEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/loki/replace:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSettingsMonitoringLokiReplace
      tags: [Settings]
      summary: "Create or execute settings/monitoring/loki/replace"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedLokiStackRequest'}
      responses:
        '202':
          description: Shared Loki replacement durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/loki/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSettingsMonitoringLokiStatus
      tags: [Settings]
      summary: "Read settings/monitoring/loki/status"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Desired, observed, drift, and latest-operation status.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringStackStatusEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/loki/uninstall:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteSettingsMonitoringLokiUninstall
      tags: [Settings]
      summary: "Delete settings/monitoring/loki/uninstall"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: clusterId, in: query, required: false, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Shared Loki uninstall durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/loki/upgrade:
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putSettingsMonitoringLokiUpgrade
      tags: [Settings]
      summary: "Replace settings/monitoring/loki/upgrade"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedLokiStackRequest'}
      responses:
        '202':
          description: Shared Loki upgrade durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/operations:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSettingsMonitoringOperations
      tags: [Settings]
      summary: "Read settings/monitoring/operations"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: targetType, in: query, required: false, schema: {type: string, enum: [cluster_stack, shared_thanos, shared_alertmanager, shared_grafana, shared_loki]}}
        - {name: targetKey, in: query, required: false, schema: {type: string}}
        - {name: status, in: query, required: false, schema: {type: string, enum: [pending, running, completed, failed, superseded]}}
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
      responses:
        '200':
          description: Authorized monitoring-operation page, newest first.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/operations/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSettingsMonitoringOperationsById
      tags: [Settings]
      summary: "Read settings/monitoring/operations/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Monitoring operation with stage events.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/operations/{id}/retry:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSettingsMonitoringOperationsByIdRetry
      x-astronomer-operation-status-path: /api/v1/settings/monitoring/operations/{id}
      tags: [Settings]
      summary: "Create or execute settings/monitoring/operations/{id}/retry"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Existing failed or superseded operation requeued; identical replay returns this exact receipt.
          headers:
            Location: {description: Exact monitoring operation status resource, schema: {type: string, format: uri-reference}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/sizer:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSettingsMonitoringSizer
      tags: [Settings]
      summary: "Read settings/monitoring/sizer"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Read-only management-cluster capacity verdicts and inputs.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringSizerEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/thanos/install:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSettingsMonitoringThanosInstall
      tags: [Settings]
      summary: "Create or execute settings/monitoring/thanos/install"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedThanosStackRequest'}
      responses:
        '202':
          description: Shared Thanos installation durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/thanos/preview:
    post:
      operationId: postSettingsMonitoringThanosPreview
      tags: [Settings]
      summary: "Create or execute settings/monitoring/thanos/preview"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "monitoring:read"
      x-astronomer-auth: "bearer/session auth"
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedThanosStackRequest'}
      responses:
        '200':
          description: Sanitized rendered Helm values and replacement impact.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringStackPreviewEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/thanos/replace:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postSettingsMonitoringThanosReplace
      tags: [Settings]
      summary: "Create or execute settings/monitoring/thanos/replace"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedThanosStackRequest'}
      responses:
        '202':
          description: Shared Thanos replacement durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/thanos/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSettingsMonitoringThanosStatus
      tags: [Settings]
      summary: "Read settings/monitoring/thanos/status"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Desired, observed, drift, and latest-operation status.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringStackStatusEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/thanos/uninstall:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteSettingsMonitoringThanosUninstall
      tags: [Settings]
      summary: "Delete settings/monitoring/thanos/uninstall"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {name: clusterId, in: query, required: false, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Shared Thanos uninstall durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/monitoring/thanos/upgrade:
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putSettingsMonitoringThanosUpgrade
      tags: [Settings]
      summary: "Replace settings/monitoring/thanos/upgrade"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/MonitoringManagementClusterID'}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SharedThanosStackRequest'}
      responses:
        '202':
          description: Shared Thanos upgrade durably queued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/registration:
    get:
      x-astronomer-auth: "public"
      x-astronomer-rbac: "public"
      security: []
      operationId: getSettingsRegistration
      tags: [Settings]
      summary: "Read settings/registration"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Public adopted-cluster registration TLS settings
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PublicRegistrationSettingsEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/settings/sso/presets:
    get:
      x-astronomer-auth: "public"
      x-astronomer-rbac: "public"
      security: []
      operationId: getSettingsSsoPresets
      tags: [Settings]
      summary: "Read settings/sso/presets"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Supported SSO provider presets
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SSOPresetListEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/streams/tickets:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postStreamsTickets
      tags: [Streams]
      summary: Create a single-use stream authentication ticket
      description: Creates a short-lived, single-use ticket bound to the caller, stream kind, and optional cluster. Non-event streams require a cluster and enforce the same RBAC/scope gate as their weakest ticket redeemer.
      x-astronomer-route-class: public-api
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/StreamTicketRequest'}
      responses:
        '201':
          description: Ticket issued
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/StreamTicketResponse'
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/support-bundle:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getSupportBundle
      tags: [SupportBundle]
      summary: "Read support-bundle"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: download
      responses:
        '200':
          description: Audited download payload
          content:
            application/octet-stream:
              schema: {type: string, format: binary}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/{slug}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getToolsBySlug
      tags: [Tools]
      summary: Get a tool by slug
      x-astronomer-route-class: public-api
      parameters:
        - {name: slug, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Tool definition.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ClusterTool'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/{slug}/adopt:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postToolsBySlugAdopt
      tags: [Tools]
      summary: Adopt an existing Helm release as a managed tool
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - {name: slug, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ToolActionRequest'}
      responses:
        '202':
          description: Adoption operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ToolOperation'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/{slug}/install:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postToolsBySlugInstall
      tags: [Tools]
      summary: Install a managed tool
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - {name: slug, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ToolActionRequest'}
      responses:
        '202':
          description: Installation operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ToolOperation'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/{slug}/preview:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postToolsBySlugPreview
      tags: [Tools]
      summary: Preview a managed tool installation
      x-astronomer-route-class: public-api
      parameters:
        - {name: slug, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ToolActionRequest'}
      responses:
        '200':
          description: Resolved chart and values preview.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ToolPreview'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/{slug}/uninstall:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteToolsBySlugUninstall
      tags: [Tools]
      summary: Uninstall a managed tool
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - {name: slug, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ToolUninstallRequest'}
      responses:
        '202':
          description: Uninstall operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ToolOperation'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/{slug}/upgrade:
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: putToolsBySlugUpgrade
      tags: [Tools]
      summary: Upgrade a managed tool
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - {name: slug, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ToolActionRequest'}
      responses:
        '202':
          description: Upgrade operation accepted.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ToolOperation'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/controller/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getToolsControllerStatus
      tags: [Tools]
      summary: "Read tools/controller/status"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: public-api
      responses:
        '200':
          description: Tool controller queue, catalog, and failure status
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ToolControllerStatusEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/operations:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getToolsOperations
      tags: [Tools]
      summary: List tool operations
      x-astronomer-route-class: public-api
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 50}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
        - {name: targetType, in: query, schema: {type: string}}
        - {name: targetKey, in: query, schema: {type: string}}
        - {name: status, in: query, schema: {type: string}}
      responses:
        '200':
          description: Authorized tool operation page.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/ListEnvelope'}
                  - properties:
                      data:
                        type: array
                        items: {$ref: '#/components/schemas/ToolOperation'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/operations/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getToolsOperationsById
      tags: [Tools]
      summary: Get a tool operation and its events
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Tool operation detail.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ToolOperation'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/operations/{id}/retry:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postToolsOperationsByIdRetry
      tags: [Tools]
      summary: Retry a failed tool operation
      x-astronomer-route-class: public-api
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '202':
          description: Operation requeued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ToolOperation'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/tools/slug/{slug}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getToolsSlugBySlug
      tags: [Tools]
      summary: Get a tool by slug compatibility route
      x-astronomer-route-class: public-api
      parameters:
        - {name: slug, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '200':
          description: Tool definition.
          content:
            application/json:
              schema:
                allOf:
                  - {$ref: '#/components/schemas/DataEnvelope'}
                  - properties:
                      data: {$ref: '#/components/schemas/ClusterTool'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/delivery/sources/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliverySources
      tags: [Delivery]
      summary: List reusable project-scoped delivery sources
      x-rbac: delivery_sources:list
      parameters:
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
        - {name: status, in: query, schema: {type: string, enum: [pending, ready, degraded, revoked]}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0}}
      responses:
        '200': {description: Paginated source metadata with no credential material, content: {application/json: {schema: {$ref: '#/components/schemas/DeliverySourcePage'}}}}
    post:
      x-astronomer-auth: "browser JWT or API token with projects:write scope; idempotent request UUID"
      x-astronomer-route-class: public-api
      operationId: postDeliverySources
      tags: [Delivery]
      summary: Create a reusable delivery source and seal write-only credentials
      x-rbac: delivery_sources:create
      parameters:
        - {name: Idempotency-Key, in: header, schema: {type: string, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliverySourceWrite'}}}
      responses:
        '201': {description: Source created, content: {application/json: {schema: {$ref: '#/components/schemas/DeliverySourceEnvelope'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
  /api/v1/delivery/sources/{id}/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliverySourcesById
      tags: [Delivery]
      summary: Get credential-free delivery source metadata
      x-rbac: delivery_sources:read
      responses:
        '200': {description: Source metadata, content: {application/json: {schema: {$ref: '#/components/schemas/DeliverySourceEnvelope'}}}}
        '404': {$ref: '#/components/responses/NotFound'}
    patch:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: patchDeliverySourcesById
      tags: [Delivery]
      summary: Update credential-free delivery source metadata
      x-rbac: delivery_sources:update
      parameters:
        - {name: Idempotency-Key, in: header, schema: {type: string, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliverySourcePatch'}}}
      responses:
        '200': {description: Source metadata updated, content: {application/json: {schema: {$ref: '#/components/schemas/DeliverySourceEnvelope'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '404': {$ref: '#/components/responses/NotFound'}
    delete:
      x-astronomer-auth: "browser JWT or API token with projects:write scope"
      x-astronomer-route-class: public-api
      operationId: deleteDeliverySourcesById
      tags: [Delivery]
      summary: Delete an unreferenced delivery source
      x-rbac: delivery_sources:delete
      responses:
        '204': {description: Source deleted}
        '409': {description: Source is still referenced}
  /api/v1/delivery/sources/{id}/rotate-credential/:
    post:
      x-astronomer-auth: "browser JWT or API token with projects:write scope; idempotent request UUID"
      x-astronomer-route-class: public-api
      operationId: postDeliverySourcesByIdRotateCredential
      tags: [Delivery]
      summary: Replace write-only credentials and increment the source epoch
      x-rbac: delivery_sources:update
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: Idempotency-Key, in: header, schema: {type: string, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliverySourceCredentialRotate'}}}
      responses:
        '200': {description: Credential metadata updated, content: {application/json: {schema: {$ref: '#/components/schemas/DeliverySourceEnvelope'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
  /api/v1/delivery/sources/{id}/verify/:
    post:
      x-astronomer-auth: "browser JWT or API token with projects:write scope; idempotency middleware"
      x-astronomer-route-class: public-api
      operationId: postDeliverySourcesByIdVerify
      tags: [Delivery]
      summary: Queue an authenticated source resolution and trust verification
      x-rbac: delivery_sources:update
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliverySourceVerify'}}}
      responses:
        '202':
          description: Verification queued with a durable receipt
          headers:
            Location: {description: Existing source status resource, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryAsyncOperationReceiptEnvelope'}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/delivery/bundles/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryBundles
      tags: [Delivery]
      summary: List stable component bundle identities
      x-rbac: delivery_bundles:list
      parameters:
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0}}
      responses:
        '200': {description: Paginated bundles, content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryBundlePage'}}}}
    post:
      x-astronomer-auth: "browser JWT or API token with projects:write scope; idempotent request UUID"
      x-astronomer-route-class: public-api
      operationId: postDeliveryBundles
      tags: [Delivery]
      summary: Create a stable component bundle identity
      x-rbac: delivery_bundles:create
      parameters:
        - {name: Idempotency-Key, in: header, schema: {type: string, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryBundleWrite'}}}
      responses:
        '201': {description: Bundle created, content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryBundleEnvelope'}}}}
  /api/v1/delivery/bundles/{id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryBundlesById
      tags: [Delivery]
      summary: Get one component bundle identity
      x-rbac: delivery_bundles:read
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200': {description: Bundle metadata, content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryBundleEnvelope'}}}}
        '404': {$ref: '#/components/responses/NotFound'}
    patch:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: patchDeliveryBundlesById
      tags: [Delivery]
      summary: Update stable component bundle metadata
      x-rbac: delivery_bundles:update
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
        - {name: Idempotency-Key, in: header, schema: {type: string, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryBundleWrite'}}}
      responses:
        '200': {description: Bundle metadata updated, content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryBundleEnvelope'}}}}
        '404': {$ref: '#/components/responses/NotFound'}
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: deleteDeliveryBundlesById
      tags: [Delivery]
      summary: Delete an unreferenced component bundle
      x-rbac: delivery_bundles:delete
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '204': {description: Bundle deleted}
        '409': {description: Bundle is still referenced}
  /api/v1/delivery/bundles/{id}/versions/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryBundlesByIdVersions
      tags: [Delivery]
      summary: List immutable versions of one component bundle
      x-rbac: delivery_bundles:list
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0}}
      responses:
        '200': {description: Paginated immutable versions, content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryBundleVersionPage'}}}}
    post:
      x-astronomer-auth: "browser JWT or API token with projects:write scope; idempotent request UUID"
      x-astronomer-route-class: public-api
      operationId: postDeliveryBundlesByIdVersions
      tags: [Delivery]
      summary: Append a credential-free version and queue immutable source resolution
      x-rbac: delivery_bundles:create
      parameters:
        - {name: Idempotency-Key, in: header, schema: {type: string, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryBundleVersionWrite'}}}
      responses:
        '201': {description: Resolving version created, content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryBundleVersionEnvelope'}}}}
        '409': {description: Source is revoked or immutable version identity conflicts}
  /api/v1/delivery/bundles/{id}/versions/{versionId}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryBundlesByIdVersionsByVersionId
      tags: [Delivery]
      summary: Get one immutable component bundle version
      x-rbac: delivery_bundles:read
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: versionId, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200': {description: Immutable version, content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryBundleVersionEnvelope'}}}}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/delivery/targets/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryTargets
      tags: [Delivery]
      summary: List delivery targets in one project
      x-rbac: delivery_targets:list
      parameters:
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
        - {name: name, in: query, schema: {type: string, minLength: 1, maxLength: 128}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0}}
      responses:
        '200':
          description: Stable paginated target list
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryTargetPage'}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      x-astronomer-auth: "browser JWT or projects:write API token; idempotency middleware"
      x-astronomer-route-class: public-api
      operationId: postDeliveryTargets
      tags: [Delivery]
      summary: Create a project-scoped delivery target
      x-rbac: delivery_targets:create
      parameters:
        - {name: Idempotency-Key, in: header, required: false, schema: {type: string, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryTargetWrite'}}}
      responses:
        '201':
          description: Target created, with a strong resource-version ETag
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryTargetEnvelope'}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/delivery/targets/{id}/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryTargetsById
      tags: [Delivery]
      summary: Get a delivery target
      x-rbac: delivery_targets:read
      responses:
        '200':
          description: Target and strong resource-version ETag
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryTargetEnvelope'}}}
        '404': {$ref: '#/components/responses/NotFound'}
    patch:
      x-astronomer-auth: "browser JWT or projects:write API token; strong If-Match and idempotency middleware"
      x-astronomer-route-class: public-api
      operationId: patchDeliveryTargetsById
      tags: [Delivery]
      summary: Compare-and-swap target configuration
      x-rbac: delivery_targets:update
      parameters:
        - {name: If-Match, in: header, required: true, schema: {type: string, pattern: '^"[0-9]+"$'}}
        - {name: Idempotency-Key, in: header, required: false, schema: {type: string, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryTargetPatch'}}}
      responses:
        '200':
          description: Updated target and new ETag
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryTargetEnvelope'}}}
        '412': {description: Stale resource version}
        '428': {description: If-Match required}
    delete:
      x-astronomer-auth: "browser JWT or projects:write API token; strong If-Match and idempotency middleware"
      x-astronomer-route-class: public-api
      operationId: deleteDeliveryTargetsById
      tags: [Delivery]
      summary: Begin downstream target deletion
      x-rbac: delivery_targets:delete
      parameters:
        - {name: If-Match, in: header, required: true, schema: {type: string, pattern: '^"[0-9]+"$'}}
        - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
      responses:
        '202':
          description: Deletion intent persisted and deployment tombstones created
          headers:
            Location: {description: Existing target status resource, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryAsyncOperationReceiptEnvelope'}}}
        '412': {description: Stale resource version}
        '428': {description: If-Match required}
  /api/v1/delivery/targets/{id}/preview/:
    post:
      x-astronomer-auth: "browser JWT or API token"
      x-astronomer-route-class: public-api
      operationId: postDeliveryTargetsByIdPreview
      tags: [Delivery]
      summary: Evaluate authoritative placement and capability blockers
      x-rbac: delivery_targets:read
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
        - {name: page_size, in: query, schema: {type: integer, minimum: 1, maximum: 500, default: 100}}
        - {name: cursor, in: query, schema: {type: string, maxLength: 4096}}
      responses:
        '200':
          description: Stable decisions, risks, counts, and preview digest
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryTargetPreviewEnvelope'}}}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Cursor digest no longer matches the authoritative preview}
  /api/v1/delivery/targets/{id}/rollouts/:
    post:
      x-astronomer-auth: "browser JWT or projects:write API token; required Idempotency-Key and target-generation If-Match"
      x-astronomer-route-class: public-api
      operationId: postDeliveryTargetsByIdRollouts
      tags: [Delivery]
      summary: Freeze a preview and start a rollout
      x-rbac: delivery_rollouts:create
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: If-Match, in: header, required: true, schema: {type: string, pattern: '^"[1-9][0-9]*"$'}}
        - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryRolloutStart'}}}
      responses:
        '202':
          description: Immutable rollout plan created and queued
          headers: *durable-polling-headers
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryFrozenRolloutEnvelope'}}}
        '409': {description: 'Preview stale, target changed, or idempotency conflict'}
        '428': {description: If-Match required}
  /api/v1/delivery/targets/{id}/orphan/:
    post:
      x-astronomer-auth: "browser JWT or projects:write API token; strong target If-Match and idempotency middleware"
      x-astronomer-route-class: public-api
      operationId: postDeliveryTargetsByIdOrphan
      tags: [Delivery]
      summary: Finalize deletion while retaining downstream resources
      x-rbac: delivery_orphans:orphan
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
        - {name: If-Match, in: header, required: true, schema: {type: string, pattern: '^"[0-9]+"$'}}
        - {name: Idempotency-Key, in: header, schema: {type: string, maxLength: 128}}
      responses:
        '200':
          description: Orphan decision persisted
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryTargetDeletionEnvelope'}}}
        '412': {description: Stale resource version}
  /api/v1/delivery/rollouts/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryRollouts
      tags: [Delivery]
      summary: List project rollouts
      x-rbac: delivery_rollouts:list
      parameters:
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
        - {name: state, in: query, schema: {type: string}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0}}
      responses:
        '200':
          description: Stable paginated rollout list
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryRolloutPage'}}}
  /api/v1/delivery/rollouts/{id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryRolloutsById
      tags: [Delivery]
      summary: Get frozen rollout, counters, approvals, and timeline
      x-rbac: delivery_rollouts:read
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Rollout detail and strong fencing-generation ETag
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryRolloutDetailEnvelope'}}}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/delivery/rollouts/{id}/clusters/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryRolloutsByIdClusters
      tags: [Delivery]
      summary: List frozen per-cluster rollout state
      x-rbac: delivery_rollouts:read
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Paginated rollout cluster state
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryRolloutClusterPage'}}}
  /api/v1/delivery/rollouts/{id}/events/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryRolloutsByIdEvents
      tags: [Delivery]
      summary: List durable rollout events
      x-rbac: delivery_rollouts:read
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Paginated rollout timeline
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryRolloutEventPage'}}}
  /api/v1/delivery/rollouts/{id}/pause/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      <<: *delivery-rollout-action
      operationId: postDeliveryRolloutsByIdPause
  /api/v1/delivery/rollouts/{id}/resume/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      <<: *delivery-rollout-action
      operationId: postDeliveryRolloutsByIdResume
  /api/v1/delivery/rollouts/{id}/abort/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      <<: *delivery-rollout-action
      operationId: postDeliveryRolloutsByIdAbort
  /api/v1/delivery/rollouts/{id}/retry/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      <<: *delivery-rollout-action
      operationId: postDeliveryRolloutsByIdRetry
  /api/v1/delivery/rollouts/{id}/rollback/:
    post:
      x-astronomer-auth: "browser JWT or projects:write API token; strong fencing-generation If-Match and idempotency middleware"
      x-astronomer-route-class: public-api
      operationId: postDeliveryRolloutsByIdRollback
      tags: [Delivery]
      summary: Roll back to every cluster's frozen previous known-good version
      x-rbac: delivery_rollbacks:rollback
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: If-Match, in: header, required: true, schema: {type: string, pattern: '^"[1-9][0-9]*"$'}}
        - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
      requestBody:
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryRolloutAction'}}}
      responses:
        '202':
          description: Rollback transition and outbox intent committed
          headers:
            Location: {description: Existing rollout status resource, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryAsyncOperationReceiptEnvelope'}}}
        '412': {description: Stale fencing generation}
  /api/v1/delivery/rollouts/{id}/approve/:
    post:
      x-astronomer-auth: "browser JWT or projects:write API token; strong fencing-generation If-Match and idempotency middleware"
      x-astronomer-route-class: public-api
      operationId: postDeliveryRolloutsByIdApprove
      tags: [Delivery]
      summary: Decide one exact expiring frozen approval gate
      x-rbac: delivery_approvals:approve
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: If-Match, in: header, required: true, schema: {type: string, pattern: '^"[1-9][0-9]*"$'}}
        - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryRolloutApproval'}}}
      responses:
        '202':
          description: Approval decision, event, and wake-up intent committed
          headers:
            Location: {description: Existing rollout status resource, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryAsyncOperationReceiptEnvelope'}}}
        '412': {description: Stale fencing generation}
  /api/v1/delivery/deployments/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryDeployments
      tags: [Delivery]
      summary: List current project target-by-cluster deployments
      x-rbac: delivery_deployments:list
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
        - {name: cluster_id, in: query, schema: {type: string, format: uuid}}
        - {name: phase, in: query, schema: {type: string}}
      responses:
        '200':
          description: Stable paginated deployment list
          content: {application/json: {schema: {$ref: '#/components/schemas/ClusterDeploymentPage'}}}
  /api/v1/delivery/deployments/{id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryDeploymentsById
      tags: [Delivery]
      summary: Get normalized deployment status and recent events
      x-rbac: delivery_deployments:read
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Deployment and strong desired-generation ETag
          content: {application/json: {schema: {$ref: '#/components/schemas/ClusterDeploymentDetailEnvelope'}}}
  /api/v1/delivery/deployments/{id}/events/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryDeploymentsByIdEvents
      tags: [Delivery]
      summary: List coalesced deployment status events
      x-rbac: delivery_deployments:read
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Paginated deployment events
          content: {application/json: {schema: {$ref: '#/components/schemas/ClusterDeploymentEventPage'}}}
  /api/v1/delivery/deployments/{id}/reconcile/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    post:
      x-astronomer-auth: "browser JWT or projects:write API token; strong desired-generation If-Match and idempotency middleware"
      x-astronomer-rbac: "delivery_deployments:update on the normalized project scope"
      x-astronomer-route-class: public-api
      <<: *delivery-deployment-action
      operationId: postDeliveryDeploymentsByIdReconcile
  /api/v1/delivery/deployments/{id}/suspend/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    post:
      x-astronomer-auth: "browser JWT or projects:write API token; strong desired-generation If-Match and idempotency middleware"
      x-astronomer-rbac: "delivery_deployments:update on the normalized project scope"
      x-astronomer-route-class: public-api
      <<: *delivery-deployment-action
      operationId: postDeliveryDeploymentsByIdSuspend
  /api/v1/delivery/deployments/{id}/resume/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      <<: *delivery-deployment-action
      operationId: postDeliveryDeploymentsByIdResume
  /api/v1/delivery/clusters/{clusterId}/inventory/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryClustersByClusterIdInventory
      tags: [Delivery]
      summary: Get project-scoped Flux inventory and cluster deployments
      x-rbac: delivery_inventory:read
      parameters:
        - {name: clusterId, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: project_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Controller inventory and deployment summary
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryClusterInventoryEnvelope'}}}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/delivery/system/compatibility/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliverySystemCompatibility
      tags: [Delivery]
      summary: Get the supported release matrix and observed inventory counts
      x-rbac: delivery_platform:read
      responses:
        '200':
          description: Runtime compatibility contract and observed status counts
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliverySystemCompatibilityEnvelope'}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/delivery/fleet/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "delivery_inventory:read"
      x-astronomer-route-class: compatibility-alias
      deprecated: true
      x-astronomer-sunset: '2027-08-23'
      x-astronomer-successor: '/api/v1/delivery/estate/'
      operationId: getDeliveryFleetLegacy
      tags: [Delivery]
      summary: Get the estate-wide Flux delivery scoreboard (deprecated path)
      description: Response-compatible alias retained for v1 clients during the published terminology migration window.
      responses:
        '200':
          description: Estate tiles, per-cluster rows, attention, and distributions
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryEstateEnvelope'}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/delivery/estate/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getDeliveryEstate
      tags: [Delivery]
      summary: Get the estate-wide Flux delivery scoreboard
      description: |
        Cross-project summary of adopted Flux-managed clusters. The local
        host-only cluster is included in the table and excluded from
        adopted/Flux tiles. Requires a global delivery_inventory:read grant.
      x-rbac: delivery_inventory:read
      responses:
        '200':
          description: Estate tiles, per-cluster rows, attention, and distributions
          content: {application/json: {schema: {$ref: '#/components/schemas/DeliveryEstateEnvelope'}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /scim/v2/Users:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: postScimUsers
      tags:
        - SCIM
      summary: Provision (create) a user via SCIM 2.0
      description: |
        SCIM 2.0 User create. Authenticated with a static SCIM bearer
        token (NOT the platform JWT). Maps onto the users table. If a
        user with the same `userName` already exists the existing
        resource is returned (idempotent re-provisioning).
      responses:
        '201':
          description: User created
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMUser'}
        '401':
          description: Missing or invalid SCIM bearer token
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: getScimUsers
      tags:
        - SCIM
      summary: List users via SCIM 2.0
      description: |
        SCIM 2.0 ListResponse. Supports `startIndex`/`count` pagination
        and the `userName eq "x"` filter every IdP issues before create.
      responses:
        '200':
          description: SCIM ListResponse of users
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMListResponse'}
        '401':
          description: Missing or invalid SCIM bearer token
  /scim/v2/Users/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: getScimUsersById
      tags:
        - SCIM
      summary: Get a user via SCIM 2.0
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM User resource
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMUser'}
        '404':
          description: User not found
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: putScimUsersById
      tags:
        - SCIM
      summary: Replace a user via SCIM 2.0 (de/reactivate)
      description: |
        Full-resource replace. Applies core attributes and the `active`
        flag; `active:false` deactivates the user (blocks login), `active:true`
        reactivates. `userName` is immutable and falls back to the stored value.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Updated SCIM User resource
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMUser'}
        '400':
          description: Invalid request body or user id
        '404':
          description: User not found
    patch:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: patchScimUsersById
      tags:
        - SCIM
      summary: Partially update a user via SCIM 2.0 (RFC 7644 PatchOp)
      description: |
        Applies a SCIM PatchOp. Supports the `replace` operations real IdPs
        (Okta, Azure AD) emit: `replace` of `active` (`active:false`
        deactivates — blocks login — and `active:true` reactivates), and
        `replace` of core attributes (`displayName`/`name`, `emails`,
        `userName`), as both path-set ops and a path-less merge of a partial
        User object. `op` is matched case-insensitively; `add`/`remove` ops
        and unknown paths are ignored. The updated resource is returned.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/scim+json:
            schema:
              type: object
              properties:
                schemas:
                  type: array
                  items:
                    type: string
                  example:
                    - urn:ietf:params:scim:api:messages:2.0:PatchOp
                Operations:
                  type: array
                  items:
                    type: object
                    properties:
                      op:
                        type: string
                      path:
                        type: string
                      value: {}
      responses:
        '200':
          description: Updated SCIM User resource
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMUser'}
        '400':
          description: Invalid request body, user id, or patch value
        '404':
          description: User not found
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: deleteScimUsersById
      tags:
        - SCIM
      summary: De-provision (delete) a user via SCIM 2.0
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: User deleted
        '404':
          description: User not found
  /scim/v2/Groups:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: getScimGroups
      tags:
        - SCIM
      summary: List groups via SCIM 2.0
      description: |
        Read-only. Each distinct group name configured in
        identity_group_mappings becomes one SCIM Group resource.
      responses:
        '200':
          description: SCIM ListResponse of groups
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMListResponse'}
        '401':
          description: Missing or invalid SCIM bearer token
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postScimGroups
      tags: [Scim]
      summary: "Create or execute scim/v2/Groups"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: scim
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SCIMGroupCreateRequest'}
      responses:
        '200':
          description: Existing idempotent SCIM Group resource
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMGroup'}
        '201':
          description: Created SCIM Group resource
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMGroup'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /scim/v2/Groups/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: getScimGroupsById
      tags:
        - SCIM
      summary: Get a group via SCIM 2.0
      description: '`id` is the group displayName.'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM Group resource
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMGroup'}
        '404':
          description: Group not found
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: deleteScimGroupsById
      tags: [Scim]
      summary: "Delete scim/v2/Groups/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: scim
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      responses:
        '204':
          description: Resource deleted or already absent
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    patch:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: patchScimGroupsById
      tags: [Scim]
      summary: "Update scim/v2/Groups/{id}"
      description: Contract generated from the mounted route inventory; response and request envelopes are stable even when domain payload fields evolve additively.
      x-astronomer-route-class: scim
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, minLength: 1}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SCIMGroupPatchRequest'}
      responses:
        '200':
          description: Updated SCIM Group resource
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMGroup'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /scim/v2/ServiceProviderConfig:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: getScimServiceProviderConfig
      tags:
        - SCIM
      summary: SCIM 2.0 service provider configuration (RFC 7643 §5)
      description: |
        Static document advertising the SCIM features this provider
        supports: `patch` true, `filter` true (the `userName eq "x"`
        filter), `bulk`/`sort`/`etag`/`changePassword` false, and
        `authenticationSchemes` = oauthbearertoken. IdPs probe this
        before provisioning.
      responses:
        '200':
          description: SCIM ServiceProviderConfig
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMServiceProviderConfig'}
        '401':
          description: Missing or invalid SCIM bearer token
  /scim/v2/ResourceTypes:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: getScimResourceTypes
      tags:
        - SCIM
      summary: SCIM 2.0 resource types (RFC 7643 §6)
      description: |
        Read-only ListResponse describing the User and Group resource
        types this provider exposes.
      responses:
        '200':
          description: SCIM ListResponse of resource types
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMListResponse'}
        '401':
          description: Missing or invalid SCIM bearer token
  /scim/v2/Schemas:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: scim
      operationId: getScimSchemas
      tags:
        - SCIM
      summary: SCIM 2.0 schemas (RFC 7643 §7)
      description: |
        Read-only ListResponse of the core User and Group schema
        definitions, limited to the attributes this provider maps.
      responses:
        '200':
          description: SCIM ListResponse of schemas
          content:
            application/scim+json:
              schema: {$ref: '#/components/schemas/SCIMListResponse'}
        '401':
          description: Missing or invalid SCIM bearer token
  /api/v1/auth/login/:
    post:
      x-astronomer-auth: "public"
      x-astronomer-rbac: "public"
      x-astronomer-route-class: public-api
      operationId: postAuthLogin
      tags:
        - Auth
      summary: Email/password login
      description: |
        Returns a JWT access token + refresh token. Rate-limited at
        5/minute per source IP. If TOTP is required for the account
        the response carries a `challenge_token` instead and the
        client must POST to `/auth/totp/verify/`.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login succeeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '423':
          description: Password accepted; TOTP verification or enrollment is required.
          content:
            application/json:
              schema:
                type: object
                required: [error, challenge_token]
                properties:
                  error:
                    type: string
                    enum: [totp_required, totp_enrollment_required]
                  challenge_token:
                    type: string
  /api/v1/auth/me/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAuthMe
      tags:
        - Auth
      summary: Current authenticated user
      responses:
        '200':
          description: User profile
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /api/v1/auth/logout/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postAuthLogout
      tags:
        - Auth
      summary: Logout current session
      responses:
        '200':
          description: Session revoked or logout redirect returned
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
  /api/v1/settings/features/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getSettingsFeatures
      tags:
        - Settings
      summary: Authenticated feature flag snapshot
      description: |
        Returns only feature.* booleans so the SPA can mirror backend
        feature gates without exposing the full admin settings registry.
      responses:
        '200':
          description: Feature flags
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/FeatureFlags'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /api/v1/admin/read-audit-policies/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminReadAuditPoliciesList
      tags: [Settings]
      summary: List read-side audit policies
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Complete policy list and count.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - type: object
                    properties: {data: {$ref: '#/components/schemas/ReadAuditPolicyListResponse'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {description: Policy list failed, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "admin API scope and handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminReadAuditPolicyCreate
      tags: [Settings]
      summary: Create a read-side audit policy
      security: [{bearerJWT: []}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/ReadAuditPolicyCreateRequest'}}}
      responses:
        '201':
          description: Policy created.
          headers:
            Location: {description: Canonical policy URL., schema: {type: string}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - type: object
                    properties: {data: {$ref: '#/components/schemas/ReadAuditPolicyResponse'}}
        '400': {description: Invalid policy body or values, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {description: Policy creation failed, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/admin/read-audit-policies/{id}/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminReadAuditPolicyGet
      tags: [Settings]
      summary: Get a read-side audit policy
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Policy detail.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - type: object
                    properties: {data: {$ref: '#/components/schemas/ReadAuditPolicyResponse'}}
        '400': {description: Invalid policy ID, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '500': {description: Policy read failed, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "admin API scope and handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminReadAuditPolicyUpdate
      tags: [Settings]
      summary: Update a read-side audit policy
      security: [{bearerJWT: []}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/ReadAuditPolicyUpdateRequest'}}}
      responses:
        '200':
          description: Policy updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - type: object
                    properties: {data: {$ref: '#/components/schemas/ReadAuditPolicyResponse'}}
        '400': {description: "Invalid policy ID, body, or values", content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '500': {description: Policy update failed, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "admin API scope and handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminReadAuditPolicyDelete
      tags: [Settings]
      summary: Delete a read-side audit policy
      security: [{bearerJWT: []}]
      responses:
        '204': {description: Policy deleted.}
        '400': {description: Invalid policy ID, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '500': {description: Policy deletion failed, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/admin/backup-drill/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAdminBackupDrill
      tags:
        - Settings
      summary: Latest management-plane backup restore-drill result
      responses:
        '200':
          description: Backup drill status
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/BackupDrillLatestResponse'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/admin/backup-drill/history/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: getAdminBackupDrillHistory
      tags: [Settings]
      summary: List management-plane restore-drill history
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 0, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Paginated drill history.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties: {data: {type: array, items: {$ref: '#/components/schemas/BackupDrillResult'}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/admin/management-backup/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAdminManagementBackup
      tags:
        - Settings
      summary: Astronomer management-plane backup status
      description: >-
        Superuser-only status of Astronomer's own pg_dump: destinations
        (S3 buckets), CronJob schedule, last Job, restore drill. Credentials
        and wrapping passphrases are never returned.
      responses:
        '200':
          description: Management-plane backup status
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/ManagementBackupStatusResponse'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/admin/management-backup/destinations/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postAdminManagementBackupDestinations
      tags:
        - Settings
      summary: Add a management-plane backup S3 destination
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/ManagementBackupDestinationWriteRequest'}}}
      responses:
        '201':
          description: Destination created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/ManagementBackupDestination'}}
        '400': {description: Invalid destination, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {description: Transaction runner unavailable, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/admin/management-backup/destinations/{id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: getAdminManagementBackupDestinationsById
      tags: [Settings]
      summary: Get exact management-backup destination status
      description: Returns the exact durable desired and observed reconciliation state without credential material.
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Exact redacted destination status.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ManagementBackupDestinationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: putAdminManagementBackupDestinationsById
      tags:
        - Settings
      summary: Update a management-plane backup S3 destination
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/ManagementBackupDestinationWriteRequest'}}}
      responses:
        '200':
          description: Destination updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/ManagementBackupDestination'}}
        '400': {description: Invalid destination, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Destination reconciliation active, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {description: Transaction runner unavailable, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: deleteAdminManagementBackupDestinationsById
      x-astronomer-operation-status-path: /api/v1/admin/management-backup/destinations/{id}/
      tags:
        - Settings
      summary: Remove a management-plane backup S3 destination
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Desired-state deletion accepted for reconciliation; identical replay returns this exact receipt.
          headers:
            Location: {description: Exact management-backup destination status resource, schema: {type: string, format: uri-reference}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ManagementBackupDeleteReceiptEnvelope'}
        '400': {description: Invalid destination ID, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Destination reconciliation active, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {description: Transaction runner unavailable, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/admin/management-backup/destinations/{id}/test/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postAdminManagementBackupDestinationsByIdTest
      tags:
        - Settings
      summary: Probe a management-plane backup S3 destination
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema: {type: string, minLength: 1, maxLength: 128}
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Durable probe operation accepted
          headers:
            Location: {description: Poll URL for the durable operation., schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds., schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/ManagementBackupOperation'}}
  /api/v1/admin/management-backup/destinations/{id}/run/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postAdminManagementBackupDestinationsByIdRun
      tags:
        - Settings
      summary: Run and track a management-plane backup now
      description: The durable operation remains retrying while the Kubernetes Job is active and completes only after the Job reports success. A failed Job produces a failed operation receipt.
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema: {type: string, minLength: 1, maxLength: 128}
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Durable backup-run operation accepted
          headers:
            Location: {description: Poll URL for the durable operation., schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds., schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/ManagementBackupOperation'}}
  /api/v1/admin/management-backup/operations/{id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "superuser"
      x-astronomer-route-class: public-api
      operationId: getAdminManagementBackupOperationById
      tags: [Settings]
      summary: Get durable management-backup operation status
      parameters:
        - in: path
          name: id
          required: true
          schema: {type: string, format: uuid}
      responses:
        '200':
          description: Sanitized operation status
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/ManagementBackupOperation'}}
  /api/v1/extensions/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getExtensions
      tags:
        - Extensions
      summary: List installed UI extensions
      responses:
        '200':
          description: Installed extension registry
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/ExtensionRecord'
                      sample_manifest:
                        $ref: '#/components/schemas/ExtensionManifest'
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postExtensions
      tags:
        - Extensions
      summary: Install or update a UI extension manifest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - manifest
              properties:
                manifest:
                  $ref: '#/components/schemas/ExtensionManifest'
                source:
                  type: string
                enable:
                  type: boolean
      responses:
        '200':
          description: Installed extension
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ExtensionRecord'
  /api/v1/extensions/validate/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postExtensionsValidate
      tags:
        - Extensions
      summary: Validate a UI extension manifest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                manifest:
                  $ref: '#/components/schemas/ExtensionManifest'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ExtensionValidation'
  /api/v1/extensions/sample-manifest/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getExtensionsSampleManifest
      tags:
        - Extensions
      summary: Return the starter UI extension manifest
      responses:
        '200':
          description: Sample manifest
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ExtensionManifest'
  /api/v1/extensions/mounts/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getExtensionsMounts
      tags:
        - Extensions
      summary: List enabled extensions for the host loader
      description: >-
        Returns the render-only projection (§HostMounts) the host UI loader
        needs to mount enabled extensions: per-point manifests, mount points,
        and the derived tier (1 = declarative, 2 = signed bundle). Viewer-
        readable (settings:read, not admin). Only enabled, compatible
        extensions appear; Tier-2 points additionally require a verified bundle.
        Upstream dataSource paths are not exposed.
      responses:
        '200':
          description: Enabled-extensions mount surface
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ExtensionMountsResponse'
  /api/v1/extensions/{name}/data/{dataSourceId}/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postExtensionsByNameDataByDataSourceId
      tags:
        - Extensions
      summary: Proxy a manifest-declared data source under the caller's RBAC
      description: >-
        §DataProxy — fetches a manifest-DECLARED data source on behalf of a
        declarative widget (Tier 1) or, with an X-Extension-Ticket header, the
        sandboxed bridge (Tier 2). The call is enforced under two independent
        gates the extension can only NARROW, never widen: (a) the requesting
        user's own RBAC, re-checked via the same engine/bindings as the route
        middleware, and (b) the manifest allowlist — the dataSource must be
        declared in the stored, validated manifest of the enabled+compatible
        extension, and the upstream URL is rebuilt server-side from that
        manifest so no client field can redirect it. Effective access =
        manifest.permissions[] ∩ the user's RBAC. An extension can never read or
        write anything the logged-in user could not already do in the UI.
        Viewer-gated (settings:read, NOT admin); the real per-call check is the
        per-dataSource RBAC inside the handler.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
        - name: dataSourceId
          in: path
          required: true
          schema:
            type: string
        - name: X-Extension-Ticket
          in: header
          required: false
          description: >-
            Tier-2 single-use, ≤60s, scope-checked data ticket (§BridgeProtocol).
            Present only for sandboxed-iframe bridge calls; browser sessions use
            the normal session cookie instead.
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                context:
                  type: object
                  description: Cluster/project/namespace ids bound per the dataSource scope.
                  properties:
                    clusterId:
                      type: string
                      format: uuid
                    projectId:
                      type: string
                      format: uuid
                    namespace:
                      type: string
                pathParams:
                  type: object
                  additionalProperties:
                    type: string
                  description: >-
                    Path-template placeholder values, validated against the
                    template's allowed placeholders only.
                query:
                  type: object
                  additionalProperties:
                    type: string
                  description: >-
                    Overrides for declared query keys only; undeclared keys are
                    dropped.
                body:
                  type: object
                  description: POST form-submit body only, against the declared input schema.
      responses:
        '200':
          description: Proxied + projected data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      data:
                        type: object
                        description: rows (list), value (object/series).
                      shape:
                        type: string
                        enum: [list, object, series]
                      meta:
                        type: object
        '403':
          description: The requesting user's own RBAC denies this data source (extension_rbac_denied).
        '404':
          description: Unknown or undeclared data source (the manifest allowlist).
        '409':
          description: Extension is not enabled or not compatible.
  /api/v1/extensions/{name}/token/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postExtensionsByNameToken
      tags:
        - Extensions
      summary: Mint a short-lived, scoped Tier-2 bridge ticket
      description: >-
        §BridgeProtocol — backs the sandboxed iframe's ext/token.request. The
        host mints a single-use, ≤60s, scope-checked ticket bound to
        {user, extension, dataSource, cluster} and returns it; the iframe sends
        it as the X-Extension-Ticket header to the §DataProxy endpoint. The
        session JWT never crosses the bridge. Issuance is gated by the SAME two
        checks as the data path: the dataSource must be a Tier-2 bundle source
        declared in the stored, validated manifest of the enabled+compatible
        extension with a verified bundle, AND the requesting user's own RBAC
        must permit it (engine.CheckPermission over the user's bindings). The
        ticket conveys identity only — RBAC is re-derived on every data call —
        so it grants nothing the user could not already do. Viewer-gated
        (settings:read, NOT admin).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - dataSource
              properties:
                dataSource:
                  type: string
                  description: A Tier-2 bundle dataSource id declared in the manifest.
                context:
                  type: object
                  properties:
                    clusterId:
                      type: string
                      format: uuid
                    projectId:
                      type: string
                      format: uuid
                    namespace:
                      type: string
      responses:
        '201':
          description: A scoped, single-use bridge ticket
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                      dataSource:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
                      scope:
                        type: string
        '403':
          description: The requesting user's own RBAC denies this data source (extension_rbac_denied).
        '404':
          description: Unknown extension or undeclared Tier-2 data source.
        '409':
          description: Extension is not enabled, not compatible, or its bundle is not verified.
  /api/v1/extensions/verify-bundle/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postExtensionsVerifyBundle
      tags:
        - Extensions
      summary: Verify a signed extension bundle (Ed25519 + SHA-256)
      description: >-
        Verifies an executable extension bundle's SHA-256 checksum and Ed25519
        signature against the trusted public key configured on the server. Fails
        closed when no trusted key is configured.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - bundle
                - signature
              properties:
                bundle:
                  type: string
                  description: Base64 (std) encoded executable bundle bytes.
                signature:
                  type: string
                  description: Base64 (std) encoded Ed25519 signature over the bundle bytes.
                checksum:
                  type: string
                  description: Optional expected "sha256:<hex>" digest of the bundle.
                name:
                  type: string
                  description: >-
                    Optional installed-extension name. When supplied and the
                    verified checksum matches a bundle descriptor in that
                    extension's stored manifest, the §HostMounts Tier-2 gate is
                    lifted (bundle_verified set true) so the descriptor may
                    mount. Omit to verify without touching any stored row.
      responses:
        '200':
          description: Bundle verified
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      verified:
                        type: boolean
                      checksum:
                        type: string
                      gated:
                        type: boolean
                        description: >-
                          Whether the named extension's Tier-2 mount gate was
                          lifted (bundle_verified set true) by this verification.
  /api/v1/extensions/{name}/enable/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postExtensionsByNameEnable
      tags:
        - Extensions
      summary: Enable a compatible extension
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Updated extension
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ExtensionRecord'
  /api/v1/extensions/{name}/disable/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postExtensionsByNameDisable
      tags:
        - Extensions
      summary: Disable an extension
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Updated extension
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ExtensionRecord'
  /api/v1/clusters/{cluster_id}/service-mesh/inventory/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdServiceMeshInventory
      tags:
        - ServiceMesh
      summary: List Istio service-mesh resources for a cluster
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Mesh resource inventory
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ServiceMeshInventory'
  /api/v1/clusters/{cluster_id}/service-mesh/validate/:
    post:
      x-astronomer-request-schema-status: polymorphic
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "service_mesh:update plus clusters write API-token scope"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdServiceMeshValidate
      tags:
        - ServiceMesh
      summary: Validate an Istio service-mesh policy object before apply
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  properties:
                    yaml:
                      type: string
                - type: object
                  properties:
                    object:
                      type: object
                      additionalProperties: true
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ServiceMeshPolicyValidation'
  /api/v1/clusters/{cluster_id}/apiserver-audit/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdApiserverAudit
      x-astronomer-async-202-exception:
        reason: append_only_agent_audit_ingest
      tags:
        - Security
      summary: Ingest a batch of kube-apiserver audit events from the agent
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                events:
                  type: array
                  items:
                    type: object
                    additionalProperties: true
      responses:
        '202':
          description: Ingest result
          content:
            application/json:
              schema:
                type: object
                properties:
                  accepted:
                    type: integer
                  skipped:
                    type: integer
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdApiserverAudit
      tags:
        - Security
      summary: List collected kube-apiserver audit events for a cluster
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
        - name: offset
          in: query
          required: false
          schema:
            type: integer
      responses:
        '200':
          description: Paginated list of apiserver audit events
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  total:
                    type: integer
  /api/v1/cluster-agents/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClusterAgents
      tags:
        - Agents
      summary: List adopted-cluster agents
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
            maximum: 500
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Cluster-agent health summary and rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ClusterAgentResponse'
  /api/v1/cluster-agents/{cluster_id}/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-route-class: public-api
      operationId: getClusterAgentsByClusterId
      tags:
        - Agents
      summary: Get one Cluster Agent
      x-rbac: agents:read
      responses:
        '200':
          description: Cluster Agent identity, connection, and compatibility
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data]
                properties:
                  data: {$ref: '#/components/schemas/ClusterAgentItem'}
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/cluster-agents/{cluster_id}/diagnostics/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClusterAgentsByClusterIdDiagnostics
      tags:
        - Agents
      summary: Agent diagnostics for one cluster
      responses:
        '200':
          description: Redacted diagnostics payload
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AgentDiagnostics'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/cluster-agents/{cluster_id}/diagnostics/bundle/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: download
      operationId: getClusterAgentsByClusterIdDiagnosticsBundle
      tags:
        - Agents
      summary: Download redacted agent diagnostics bundle
      responses:
        '200':
          description: JSON attachment suitable for support triage
          headers:
            Content-Disposition:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentDiagnosticsBundle'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/cluster-agents/{cluster_id}/operations/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClusterAgentsByClusterIdOperations
      tags:
        - Agents
      summary: List agent lifecycle operations for one cluster
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Lifecycle operation history
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AgentLifecycleOperationsResponse'
  /api/v1/cluster-agents/{cluster_id}/self-test/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postClusterAgentsByClusterIdSelfTest
      tags:
        - Agents
      summary: Run an agent self-test
      responses:
        '200':
          description: Deterministic pass/warn/fail checks from inventory and live diagnostics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AgentSelfTest'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/cluster-agents/{cluster_id}/upgrade-plan/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postClusterAgentsByClusterIdUpgradePlan
      tags:
        - Agents
      summary: Build an agent upgrade plan
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentUpgradePlanRequest'
      responses:
        '200':
          description: Upgrade plan and validation gates
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AgentUpgradePlan'
  /api/v1/cluster-agents/{cluster_id}/upgrade/:
    parameters:
      - $ref: '#/components/parameters/RequiredIdempotencyKey'
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postClusterAgentsByClusterIdUpgrade
      tags:
        - Agents
      summary: Queue an agent upgrade lifecycle operation
      description: |
        Atomically persists operator-approved upgrade intent and its audit
        record. Actor/route/key and a canonical request digest are durably
        bound to the exact receipt; identical replay creates no duplicate
        operation or audit and changed key reuse returns 409.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentUpgradePlanRequest'
      responses:
        '202':
          description: Upgrade lifecycle operation queued
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AgentUpgradeOperationResponse'
        '409':
          description: Upgrade plan is blocked
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
  /api/v1/activity/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: getActivityLegacy
      deprecated: true
      x-astronomer-route-class: compatibility-alias
      x-astronomer-sunset: '2027-08-23'
      x-astronomer-successor: '/api/v1/activity'
      tags:
        - Clusters
      summary: Recent platform activity
      responses:
        '200':
          description: Activity feed
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
  /api/v1/tools/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getTools
      tags:
        - Tools
      summary: List available cluster tools
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Tool catalog
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ClusterToolPage'}
  /api/v1/alerting/events/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAlertingEvents
      tags:
        - Alerting
      summary: List alert events
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [firing, acknowledged, resolved, silenced]}}
        - {name: severity, in: query, schema: {type: string, enum: [critical, warning, info]}}
        - {name: clusterId, in: query, schema: {type: string, format: uuid}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Alert events page
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AlertEventPage'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClusters
      tags:
        - Clusters
      summary: List clusters
      description: |
        Returns all clusters the caller can read, excluding tombstoned
        rows (decommissioned_at IS NOT NULL).
      parameters:
        - name: search
          in: query
          schema:
            type: string
          description: Substring match on name + display_name
        - name: environment
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema: {type: string, maxLength: 128}
        - name: provider
          in: query
          schema: {type: string, maxLength: 128}
        - name: limit
          in: query
          schema: {type: integer, minimum: 1, maximum: 200, default: 20}
        - name: offset
          in: query
          schema: {type: integer, minimum: 0, default: 0}
      responses:
        '200':
          description: List of clusters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedClusters'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:create"
      x-astronomer-route-class: public-api
      operationId: postClusters
      tags:
        - Clusters
      summary: Create cluster (imported)
      description: |
        Records an existing Kubernetes cluster for agent-based management; it
        does not provision cluster infrastructure or attach a cluster template.
        Returns the cluster with `registration_phase=created` so the caller can
        record the explicit `install_baseline` choice and complete agent
        registration. When selected, the release-pinned components in
        `deploy/bundles/catalog.json` are later reconciled through the normal
        Flux-native delivery rollout path after the agent reports a Ready,
        compatible Flux inventory.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateClusterRequest'
      responses:
        '201':
          description: Cluster created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Cluster'
        '400':
          description: Validation error (e.g. non-RFC-1123 name)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{id}/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersById
      tags:
        - Clusters
      summary: Get cluster
      responses:
        '200':
          description: Cluster
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Cluster'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:delete"
      x-astronomer-route-class: public-api
      operationId: deleteClustersById
      tags:
        - Clusters
      summary: Decommission cluster
      parameters:
        - name: force
          in: query
          required: false
          description: Escalate an existing decommission and skip the cleanup grace window.
          schema: {type: boolean, default: false}
      description: |
        Soft-deletes the cluster: tombstones the row + enqueues an
        async reconciler that revokes the agent's registration token,
        archives the audit log for that cluster, and prunes dependent
        rows. The cluster's pod-side resources are NOT touched — the
        operator's responsibility.
      responses:
        '204':
          description: Decommission queued
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/clusters/{id}/ownership/takeover/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdOwnershipTakeover
      tags:
        - Clusters
      summary: Transfer CRD-owned cluster row to API ownership
      description: |
        Explicitly clears CRD ownership metadata from a cluster row so future
        UI/API updates can mutate fields that ordinary PUT/PATCH otherwise
        reject with `ownership_conflict`. Only CRD-owned rows can be taken
        over by this endpoint; system-owned rows remain protected.
      responses:
        '200':
          description: Ownership transfer result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/OwnershipTransferResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Row is not eligible for takeover
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/projects/{id}/ownership/takeover/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token projects:write plus projects:update"
      x-astronomer-route-class: public-api
      operationId: postProjectsByIdOwnershipTakeover
      tags:
        - Projects
      summary: Transfer CRD-owned project row to API ownership
      description: |
        Explicitly clears CRD ownership metadata from a project row so future
        UI/API updates can mutate fields that ordinary PUT/PATCH otherwise
        reject with `ownership_conflict`. Only CRD-owned rows can be taken
        over by this endpoint; system-owned rows remain protected.
      responses:
        '200':
          description: Ownership transfer result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/OwnershipTransferResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Row is not eligible for takeover
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{id}/manifest/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdManifest
      tags:
        - Clusters
      summary: Agent install manifest
      description: |
        Returns a kubectl-applyable YAML manifest that installs the
        Astronomer agent into the target cluster. Each call mints a
        fresh registration token bound to the cluster ID; tokens
        expire 24h after issue. The manifest includes the agent's
        ServiceAccount + ClusterRole + Secret + Deployment + Service +
        NetworkPolicy + PodDisruptionBudget.
      responses:
        '200':
          description: YAML manifest
          content:
            text/yaml:
              schema:
                type: string
  /api/v1/nodes/{cluster_id}/{node_name}/cordon/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: node_name
        in: path
        required: true
        schema:
          type: string
      - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postNodesByClusterIdByNodeNameCordon
      tags:
        - Nodes
      summary: Cordon a node
      responses:
        '202':
          description: Durable cordon operation accepted
          headers: *durable-polling-headers
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeOperationEnvelope'}
  /api/v1/nodes/{cluster_id}/{node_name}/uncordon/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: node_name
        in: path
        required: true
        schema:
          type: string
      - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postNodesByClusterIdByNodeNameUncordon
      tags:
        - Nodes
      summary: Uncordon a node
      responses:
        '202':
          description: Durable uncordon operation accepted
          headers: *durable-polling-headers
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeOperationEnvelope'}
  /api/v1/nodes/{cluster_id}/{node_name}/drain/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: node_name
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: true
        description: Required for both the read-only preview and durable drain. A durable replay by the same actor with the same request returns the original operation, while a changed request returns 409.
        schema: {type: string, minLength: 1, maxLength: 128}
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postNodesByClusterIdByNodeNameDrain
      tags:
        - Nodes
      summary: Cordon a node and evict eligible pods
      description: |
        Non-preview requests atomically commit the durable node operation,
        identifier-only worker task, and mandatory audit intent. The request
        performs no eviction. Poll the Location URL; an identical
        Idempotency-Key replay does not create another operation, task, audit,
        cordon, or eviction effect.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeDrainRequest'
      responses:
        '200':
          description: Dry-run, blocked, or no-op drain result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/NodeDrainResponse'
        '202':
          description: Durable, resumable drain operation accepted; no eviction occurs in the request.
          headers: *durable-polling-headers
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeOperationEnvelope'}
  /api/v1/nodes/{cluster_id}/{node_name}/labels/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: node_name
        in: path
        required: true
        schema:
          type: string
      - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postNodesByClusterIdByNodeNameLabels
      tags:
        - Nodes
      summary: Set a node label
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeKeyValueRequest'
      responses:
        '202':
          description: Durable label operation accepted
          headers: *durable-polling-headers
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeOperationEnvelope'}
  /api/v1/nodes/{cluster_id}/{node_name}/labels/remove/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: node_name
        in: path
        required: true
        schema:
          type: string
      - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postNodesByClusterIdByNodeNameLabelsRemove
      tags:
        - Nodes
      summary: Remove a node label
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeKeyRequest'
      responses:
        '202':
          description: Durable label removal accepted
          headers: *durable-polling-headers
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeOperationEnvelope'}
  /api/v1/nodes/{cluster_id}/{node_name}/annotations/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: node_name
        in: path
        required: true
        schema:
          type: string
      - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postNodesByClusterIdByNodeNameAnnotations
      tags:
        - Nodes
      summary: Set a node annotation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeKeyValueRequest'
      responses:
        '202':
          description: Durable annotation operation accepted
          headers: *durable-polling-headers
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeOperationEnvelope'}
  /api/v1/nodes/{cluster_id}/{node_name}/annotations/remove/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: node_name
        in: path
        required: true
        schema:
          type: string
      - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postNodesByClusterIdByNodeNameAnnotationsRemove
      tags:
        - Nodes
      summary: Remove a node annotation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeKeyRequest'
      responses:
        '202':
          description: Durable annotation removal accepted
          headers: *durable-polling-headers
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeOperationEnvelope'}
  /api/v1/nodes/{cluster_id}/{node_name}/taints/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: node_name
        in: path
        required: true
        schema:
          type: string
      - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postNodesByClusterIdByNodeNameTaints
      tags:
        - Nodes
      summary: Add or replace a node taint by key and effect
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeTaintRequest'
      responses:
        '202':
          description: Durable taint operation accepted
          headers: *durable-polling-headers
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeOperationEnvelope'}
  /api/v1/nodes/{cluster_id}/{node_name}/taints/remove/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: node_name
        in: path
        required: true
        schema:
          type: string
      - {name: Idempotency-Key, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postNodesByClusterIdByNodeNameTaintsRemove
      tags:
        - Nodes
      summary: Remove node taints by key and optional effect
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeTaintRemoveRequest'
      responses:
        '202':
          description: Durable taint removal operation accepted
          headers: *durable-polling-headers
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeOperationEnvelope'}
  /api/v1/nodes/{cluster_id}/{node_name}/operations/{id}/:
    get:
      operationId: getNodeOperation
      tags: [Nodes]
      summary: Poll a durable node mutation or drain
      description: Returns sanitized state and resumable drain evidence. Cluster and node identity must both match, otherwise the operation is hidden with 404.
      x-astronomer-route-class: public-api
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-auth: "bearer-or-session"
      parameters:
        - {name: cluster_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: node_name, in: path, required: true, schema: {type: string, minLength: 1}}
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Current durable node operation state and sanitized progress.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeOperationEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/registration/status/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdRegistrationStatus
      tags:
        - Registration
      summary: Registration status + step timeline
      responses:
        '200':
          description: Phase + steps
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/RegistrationStatus'
  /api/v1/clusters/{id}/registration/options/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: putClustersByIdRegistrationOptions
      tags:
        - Registration
      summary: Set wizard step-1 options
      description: |
        Records the operator's explicit `install_baseline` choice on the cluster
        row. `true` selects the default-enabled, release-pinned components in the
        built-in bundle catalog; it does not attach a cluster template or start
        delivery before the agent reports a Ready, compatible Flux inventory.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetOptionsRequest'
      responses:
        '200':
          description: Updated status
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data]
                properties:
                  data: {$ref: '#/components/schemas/RegistrationStatus'}
  /api/v1/clusters/{id}/registration/confirm/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdRegistrationConfirm
      tags:
        - Registration
      summary: Operator confirms manifest applied
      description: |
        Advances `created` → `awaiting_agent`. Confirmation does not install
        charts or enqueue a cluster-template task. If `install_baseline=true`
        was set on `/options/`, the cluster remains in registration until its
        authenticated agent reports a Ready, compatible local Flux inventory;
        Astronomer then creates ordinary immutable delivery targets and
        rollouts for the catalog's default-enabled built-in components.
      responses:
        '200':
          description: Updated status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/RegistrationStatus'
        '409':
          description: Illegal phase transition
  /api/v1/clusters/{id}/registration/retry/{step_id}/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: step_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdRegistrationRetryByStepId
      tags:
        - Registration
      summary: Retry a failed step
      description: |
        Re-fires the underlying cluster_template:apply task for a step
        currently in `failed`. The phase machine rewinds
        `failed` → `provisioning` before the apply re-runs.
      responses:
        '200':
          description: Updated status
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data]
                properties:
                  data: {$ref: '#/components/schemas/RegistrationStatus'}
        '409':
          description: Step is not in failed state
  /api/v1/clusters/{id}/vulnerabilities/summary/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdVulnerabilitiesSummary
      tags:
        - Vulnerabilities
      summary: Aggregate severity counts
      responses:
        '200':
          description: Severity totals across all current reports
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/VulnSummary'
  /api/v1/clusters/{id}/vulnerabilities/images/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdVulnerabilitiesImages
      tags:
        - Vulnerabilities
      summary: Per-image vulnerability reports
      parameters:
        - name: namespace
          in: query
          schema: {type: string}
        - name: limit
          in: query
          schema: {type: integer, minimum: 1, maximum: 200, default: 20}
        - name: offset
          in: query
          schema: {type: integer, minimum: 0, default: 0}
      responses:
        '200':
          description: Image vulnerability rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/VulnImage'
                  count:
                    type: integer
  /api/v1/clusters/{id}/shell/sessions/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdShellSessions
      tags:
        - Shell
      summary: List the caller's shell sessions on this cluster
      responses:
        '200':
          description: Sessions
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubectlSessionListEnvelope'}
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdShellSessions
      tags:
        - Shell
      summary: Open a kubectl shell session
      description: |
        Creates a ServiceAccount + ClusterRole + RoleBinding + Pod
        running `astronomer-shell` (alpine + kubectl built from
        dl.k8s.io). Returns when the Pod is Ready; the operator then
        attaches via WS at
        `/api/v1/ws/clusters/{cluster_id}/shell/sessions/{id}/`.
      responses:
        '201':
          description: Session record
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubectlSessionEnvelope'}
        '502':
          description: Open failed (e.g. agent disconnected, pod never reached Ready)
  /api/v1/clusters/{id}/shell/sessions/{session_id}/close/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update plus session ownership"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdShellSessionsBySessionIdClose
      tags:
        - Shell
      summary: Close a shell session early
      description: |
        Tears down the SA/Role/RB/Pod and marks the session row
        `status=closed`. The reaper would do this automatically on
        idle/hard-cap; calling this is the polite version.
      responses:
        '200':
          description: Session closed and cluster-side resources removed.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubectlCloseEnvelope'}
  /api/v1/clusters/{cluster_id}/shell/sessions/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdShellSessionsById
      summary: Get a kubectl shell session
      description: |
        Returns the current state of a single in-browser kubectl shell session
        scoped to the given cluster. Gated on clusters:update RBAC (opening or
        inspecting a privileged shell is treated as a write action). A caller may
        only read a session they own; superusers may read any session. The
        response includes the live command count and the configured idle timeout.
      tags:
        - kubectl-shell
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          description: Cluster the session belongs to.
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          description: Shell session id.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The shell session.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubectlSessionEnvelope'}
        '400':
          description: Invalid cluster_id or session id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Session belongs to another operator.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Session not found, or not found in this cluster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Database error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Kubectl shell is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/shell/sessions/{id}/commands:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update plus session ownership"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdShellSessionsByIdCommands
      summary: List recorded commands for a kubectl shell session
      description: |
        Returns the audit trail of shell commands recorded for a single kubectl
        session, newest-first, as a DRF-style paginated list. Gated on
        clusters:update RBAC. A caller may only read commands for a session they
        own; superusers may read any session's commands. Use the limit/offset
        query parameters to page; limit is clamped to the range 1-1000 (default
        100) and offset is clamped to a minimum of 0 (default 0).
      tags:
        - kubectl-shell
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          description: Cluster the session belongs to.
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          description: Shell session id.
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          description: Max items per page. Values below 1 fall back to 100; values above 1000 are clamped to 1000.
          schema:
            type: integer
            default: 100
            minimum: 1
            maximum: 1000
        - name: offset
          in: query
          required: false
          description: Number of items to skip. Negative values fall back to 0.
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        '200':
          description: Paginated page of recorded commands.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/KubectlRecordedCommandPage'}
        '400':
          description: Invalid cluster_id or session id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Session belongs to another operator.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Session not found, or not found in this cluster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Database error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Kubectl shell is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/alerting/channels/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAlertingChannels
      tags:
        - Alerting
      summary: List notification channels
      parameters:
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 20}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200':
          description: Array of channels
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NotificationChannelPage'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postAlertingChannels
      tags:
        - Alerting
      summary: Create a notification channel (channel_type, configuration)
      description: |
        Supported channel_type: slack, pagerduty, msteams, webhook, email.
        See the Alerting tag header for the configuration key conventions
        per channel type.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AlertChannelRequest'}
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NotificationChannelEnvelope'}
        '400':
          description: Unsupported channel_type
  /api/v1/admin/alerting/inhibitions/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAdminAlertingInhibitions
      tags:
        - Alerting
      summary: List alert inhibition rules (P-03)
      description: Requires alerts:read (or alerts:list) RBAC.
      security:
        - bearerJWT: []
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
      responses:
        '200':
          description: Array of inhibition rules.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/InhibitionPage'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postAdminAlertingInhibitions
      tags:
        - Alerting
      summary: Create an alert inhibition rule (P-03)
      description: |
        Alertmanager-style inhibition. A firing target alert is suppressed
        while a source alert (matching source_matchers) is firing and both
        share equal values on every equal_labels label. Requires the admin
        api-token scope and alerts:create RBAC.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InhibitionRequest'
      responses:
        '201':
          description: Created inhibition rule.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/InhibitionEnvelope'}
        '400':
          description: Invalid matchers or missing source/target.
  /api/v1/admin/alerting/inhibitions/{id}/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAdminAlertingInhibitionsById
      tags:
        - Alerting
      summary: Get an alert inhibition rule (P-03)
      description: Requires alerts:read (or alerts:list) RBAC.
      security:
        - bearerJWT: []
      responses:
        '200':
          description: The inhibition rule.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/InhibitionEnvelope'}
        '404':
          description: Inhibition not found.
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: putAdminAlertingInhibitionsById
      tags:
        - Alerting
      summary: Update an alert inhibition rule (P-03)
      description: Requires the admin api-token scope and alerts:update RBAC.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InhibitionRequest'
      responses:
        '200':
          description: Updated inhibition rule.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/InhibitionEnvelope'}
        '400':
          description: Invalid matchers or missing source/target.
        '404':
          description: Inhibition not found.
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: deleteAdminAlertingInhibitionsById
      tags:
        - Alerting
      summary: Delete an alert inhibition rule (P-03)
      description: Requires the admin api-token scope and alerts:delete RBAC.
      security:
        - bearerJWT: []
      responses:
        '204':
          description: Deleted.
        '404':
          description: Inhibition not found.
  /api/v1/clusters/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: patchClustersById
      summary: Update a cluster (partial; alias of PUT, same handler)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: Requires clusters:write api-token scope and clusters:update RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateClusterRequest'
      responses:
        '200':
          description: Updated cluster.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterResponse'
        '400':
          description: Invalid id or body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Cluster not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Ownership conflict (cluster managed elsewhere)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: putClustersById
      summary: Update a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: Requires clusters:write api-token scope and clusters:update RBAC. Same handler as PATCH.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateClusterRequest'
      responses:
        '200':
          description: Updated cluster.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterResponse'
        '400':
          description: Invalid id or body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Cluster not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Ownership conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/condition-remediation:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdConditionRemediation
      summary: List recent condition-remediation attempts for a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC. Returns up to 50 most-recent remediation rows, newest first.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The 50 most recent remediation attempts, newest first.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ClusterConditionRemediationListEnvelope'}
        '404':
          description: Cluster not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/conditions:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdConditions
      summary: List cluster conditions (Kubernetes-style)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC. Empty list (not 404) when no health-check tick has run yet.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: One entry per condition type.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ClusterConditionListEnvelope'}
        '400':
          description: Invalid id
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/decommission:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdDecommission
      summary: Get latest decommission status for a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC. Poll endpoint paired with DELETE /clusters/{id} (202).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Decommission status with per-phase progress.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/DecommissionStatusResponse'
        '404':
          description: No decommission for cluster
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/agent-token/rotate/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdAgentTokenRotate
      summary: Trigger durable agent-token rotation
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: >-
        clusters:update RBAC. Marks the cluster's durable agent token for
        rotation; the fresh token is minted and delivered on the agent's next
        CONNECT, with the old token honored during a grace window so there is no
        mid-rotation lockout. Identical key replay returns the exact original
        receipt without another state transition or audit; conflicting key
        reuse returns 409.
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Rotation pending; completes on the agent's next connect.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DataEnvelope'
        '404':
          description: Cluster not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: No agent token eligible for rotation, or a rotation is already in flight
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/agent-token/revoke/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdAgentTokenRevoke
      summary: Revoke the durable agent token
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: >-
        clusters:update RBAC. Hard-revokes the durable agent token (sets
        revoked_at, clears the grace previous_token_hash). The agent's next
        CONNECT is then denied; an operator must re-import the cluster to issue a
        fresh credential.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Token revoked.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DataEnvelope'
        '404':
          description: Cluster has no active agent token to revoke
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/generate_kubeconfig:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:read; route returns scoped cluster access material"
      operationId: postClustersByIdGenerateKubeconfigLegacy
      deprecated: true
      x-astronomer-route-class: compatibility-alias
      x-astronomer-sunset: '2027-08-23'
      x-astronomer-successor: '/api/v1/clusters/{id}/generate-kubeconfig'
      summary: Generate a proxy kubeconfig (YAML download, underscore alias)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: Legacy alias for generate-kubeconfig. Issues a one-hour, read-only API token and returns a YAML kubeconfig whose server is the audited Astronomer proxy. Fails closed if the revocable credential cannot be persisted.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Short-lived read-only kubeconfig YAML routed through the Astronomer proxy. Response headers identify proxy mode and credential expiry.
          content:
            application/x-yaml:
              schema:
                type: string
        '404':
          description: Cluster not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Short-lived credential issuer unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/generate-direct-kubeconfig:
    post:
      x-astronomer-auth: "bearer/session auth + CSRF write gate"
      x-astronomer-rbac: "clusters:update; issues portable member-cluster access"
      x-astronomer-route-class: download
      operationId: postClustersByIdGenerateDirectKubeconfig
      summary: Generate a short-lived direct kubeconfig (YAML download)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: Requires clusters:update. Persists a durable sanitized issuance intent before any network or TokenRequest call, then records a sanitized outcome. After validating the configured HTTPS Kubernetes API endpoint, TLS identity, reachability, connected-agent capability, and dedicated read-only ServiceAccount TokenRequest, returns a non-renewable kubeconfig valid for at most 15 minutes. Never reuses registration, agent, proxy, or administrator credentials and fails closed when any capability is unavailable.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Fifteen-minute direct read-only kubeconfig YAML. Includes direct mode and credential expiry response headers.
          content:
            application/x-yaml:
              schema: {type: string}
        '404':
          description: Cluster not found
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
        '409':
          description: Direct access is not configured or is unsupported for this cluster
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
        '503':
          description: Endpoint reachability, agent capability, TokenRequest, or mandatory audit storage unavailable
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
  /api/v1/clusters/{id}/generate-kubeconfig:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:read; route returns scoped cluster access material"
      x-astronomer-route-class: download
      operationId: postClustersByIdGenerateKubeconfig
      summary: Generate a proxy kubeconfig (YAML download)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC. Issues a one-hour, read-only API token and returns a YAML kubeconfig whose server is the audited Astronomer proxy. This API never returns direct cluster credentials or placeholder tokens and fails closed if issuance is unavailable.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Short-lived read-only proxy kubeconfig YAML. Includes X-Astronomer-Kubeconfig-Mode and X-Astronomer-Credential-Expires-At headers.
          content:
            application/x-yaml:
              schema:
                type: string
        '404':
          description: Cluster not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Short-lived credential issuer unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/health:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdHealth
      summary: Get cached cluster health status
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: monitoring:read RBAC. Returns the latest health snapshot row.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Health status (raw sqlc row; UNCERTAIN exact field names).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
        '404':
          description: Health status not found for cluster
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/kubeconfig:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:read; returns only the scoped Astronomer proxy preview"
      x-astronomer-route-class: compatibility-alias
      deprecated: true
      x-astronomer-sunset: '2027-08-23'
      x-astronomer-successor: '/api/v1/clusters/{id}/kubeconfig-preview'
      operationId: getClustersByIdKubeconfigLegacy
      summary: Preview a proxy kubeconfig (deprecated direct-access path)
      tags: [clusters]
      security: [{bearerJWT: []}]
      description: The legacy path is response-compatible but no longer returns portable member-cluster credentials. It returns the audited Astronomer proxy preview and advertises its successor and sunset in response headers.
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Proxy kubeconfig object.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {type: object}}
        '404':
          description: Cluster not found or proxy preview unavailable
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
  /api/v1/clusters/{id}/kubeconfig-preview:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: download
      operationId: getClustersByIdKubeconfigPreview
      summary: Preview the proxy kubeconfig (JSON, for UI display)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Proxy kubeconfig object.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
        '404':
          description: Cluster not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/metrics:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdMetrics
      summary: Get cluster metrics (Prometheus time-series via Monitoring)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: |
        monitoring:read RBAC. When the Monitoring dependency is wired (normal case) this serves Monitoring.ListMetrics returning Prometheus time-series; otherwise it falls back to the scalar Clusters.GetMetrics payload (cluster_id, cluster_name, status, is_connected, kubernetes_version, node_count, agent_version, cpu/memory/pod usage). UNCERTAIN: exact monitoring time-series schema is owned by the monitoring domain.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: range
          in: query
          required: false
          schema: {type: string, enum: [1h, 6h, 24h, 7d], default: 1h}
      responses:
        '200':
          description: Metrics payload.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
        '404':
          description: Cluster not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      operationId: postClustersByIdMetrics
      tags: [Clusters]
      summary: Query the current cluster metrics summary
      description: Returns live Prometheus-backed summary metrics when configured, otherwise a best-effort Kubernetes and persisted-health summary.
      x-astronomer-route-class: public-api
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Cluster metrics summary
          content:
            application/json:
              schema: {$ref: '#/components/schemas/MonitoringMetricsEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/clusters/{id}/metrics/summary:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdMetricsSummary
      summary: Get cluster metrics summary
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: |
        monitoring:read RBAC. Serves Monitoring.ListMetrics when Monitoring is wired, else the scalar Clusters.GetMetricsSummary fallback. UNCERTAIN: exact shape owned by monitoring domain.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Metrics summary payload.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
        '404':
          description: Cluster not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/register:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdRegister
      summary: Generate a cluster registration token
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC. Token valid 24h.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '201':
          description: Registration token (raw token returned once).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          id:
                            type: string
                            format: uuid
                          cluster_id:
                            type: string
                            format: uuid
                          token:
                            type: string
                            description: Plaintext token, returned only on creation.
                          expires_at:
                            type: string
                            format: date-time
        '404':
          description: Cluster not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/registration/cancel:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdRegistrationCancel
      summary: Cancel an in-progress cluster registration (superuser)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC; additionally gated to superusers at the handler level.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Updated registration status.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RegistrationStatus'
        '400':
          description: Invalid id
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Requires superuser
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Illegal state transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/registry:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdRegistry
      summary: Get the legacy single-row registry config for a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC.
      responses:
        '200':
          description: Registry config (raw sqlc row).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterRegistryConfigLegacy'
        '404':
          description: Registry config not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: putClustersByIdRegistry
      summary: Upsert the legacy single-row registry config
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRegistryConfigRequest'
      responses:
        '200':
          description: Upserted registry config.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterRegistryConfigLegacy'
        '400':
          description: Invalid id or body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: deleteClustersByIdRegistry
      summary: Delete the legacy single-row registry config
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      responses:
        '204':
          description: Deleted.
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/v2/pods:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:read"
      x-astronomer-route-class: public-api
      operationId: remoteV2ListPods
      summary: List pods via the v2 remotedialer tunnel (non-production demo)
      description: |
        Demonstration endpoint (remoteV2PodsHandler) proving the new
        remotedialer-based tunnel end-to-end: builds a client-go clientset whose
        transport is dialed through the agent WebSocket and lists pods in the
        requested namespace. Hard-gated OUT of production builds.

        Auth: bearer JWT/API-token; requires `clusters:read` RBAC.
      tags:
        - Platform
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Cluster UUID.
        - name: namespace
          in: query
          required: false
          schema:
            type: string
            default: default
          description: Namespace to list pods in (defaults to "default").
      responses:
        '200':
          description: Pod summary list (not enveloped — raw object).
          content:
            application/json:
              schema:
                type: object
                properties:
                  cluster_id:
                    type: string
                  namespace:
                    type: string
                  count:
                    type: integer
                  pods:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                        phase:
                          type: string
                        node:
                          type: string
                required:
                  - cluster_id
                  - namespace
                  - count
                  - pods
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '502':
          description: Pod list failed upstream.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '503':
          description: Agent not connected / transport build failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  /api/v1/register/{token}:
    get:
      x-astronomer-auth: "registration token in path is the credential"
      x-astronomer-rbac: "not user RBAC; valid unexpired registration token must resolve to a cluster"
      x-astronomer-route-class: public-api
      operationId: getRegisterByToken
      summary: Fetch the agent install manifest by registration token (public)
      tags:
        - clusters
      security: []
      description: |
        Unauthenticated. Consumed by `curl -sfL <server>/api/v1/register/<token>.yaml | kubectl apply --server-side --field-manager=astronomer-bootstrap -f -`. The `.yaml` suffix on the token is stripped server-side.
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Agent install manifest YAML.
          content:
            text/yaml:
              schema:
                type: string
        '400':
          description: Missing token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Token not found or expired
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/register/signed/{cluster_id}:
    get:
      x-astronomer-auth: "public"
      x-astronomer-rbac: "public"
      x-astronomer-route-class: public-api
      operationId: getRegisterSignedByClusterId
      summary: Fetch the agent install manifest via a short-TTL signed URL (public)
      tags:
        - clusters
      security: []
      description: |
        Unauthenticated. Guarded by an HMAC signature over (cluster_id, expiry) instead of a registration token. The wizard mints the URL with a 15-minute window; expired or tampered links return an opaque 404. On success a fresh registration token is minted and the install manifest YAML is returned.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: expires
          in: query
          required: true
          description: Unix-seconds expiry the signature was computed over.
          schema:
            type: integer
            format: int64
        - name: sig
          in: query
          required: true
          description: Hex-encoded HMAC-SHA256 over "cluster_id|expires".
          schema:
            type: string
      responses:
        '200':
          description: Agent install manifest YAML.
          content:
            text/yaml:
              schema:
                type: string
        '400':
          description: Invalid cluster ID or expiry
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Link invalid or expired
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Signed manifest URLs not enabled
  /api/v1/register/ca.crt:
    get:
      x-astronomer-auth: "public"
      x-astronomer-rbac: "public"
      x-astronomer-route-class: public-api
      operationId: getRegisterCaCrt
      summary: Download the registration CA bundle (public)
      tags:
        - clusters
      security: []
      description: |
        Unauthenticated. Returns the operator-provided PEM bundle from platform_settings["registration.ca_bundle"] as text/plain. 404 when none configured.
      responses:
        '200':
          description: PEM CA bundle.
          content:
            text/plain:
              schema:
                type: string
        '404':
          description: No CA bundle configured
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/cluster-templates:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClusterTemplates
      summary: List cluster templates
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: cluster-templates:list RBAC.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated templates.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ClusterTemplateResponse'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "cluster_templates:create"
      x-astronomer-route-class: public-api
      operationId: postClusterTemplates
      summary: Create a cluster template
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: cluster-templates:create RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateClusterTemplateRequest'
      responses:
        '201':
          description: Created template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterTemplateResponse'
        '400':
          description: Invalid body/spec
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Name conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/cluster-templates/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClusterTemplatesById
      summary: Get a cluster template
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: cluster-templates:read RBAC.
      responses:
        '200':
          description: Template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterTemplateResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "cluster_templates:update"
      x-astronomer-route-class: public-api
      operationId: patchClusterTemplatesById
      summary: Update a cluster template (alias of PUT)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: cluster-templates:update RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateClusterTemplateRequest'
      responses:
        '200':
          description: Updated template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterTemplateResponse'
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "cluster_templates:update"
      x-astronomer-route-class: public-api
      operationId: putClusterTemplatesById
      summary: Update a cluster template
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: cluster-templates:update RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateClusterTemplateRequest'
      responses:
        '200':
          description: Updated template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterTemplateResponse'
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "cluster_templates:delete"
      x-astronomer-route-class: public-api
      operationId: deleteClusterTemplatesById
      summary: Delete a cluster template
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: cluster-templates:delete RBAC.
      responses:
        '204':
          description: Deleted.
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/cluster-templates/{id}/clusters:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "cluster_templates:read and clusters:read"
      x-astronomer-route-class: public-api
      operationId: getClusterTemplatesByIdClusters
      summary: List clusters bound to a cluster template
      description: Requires read access to both cluster templates and clusters.
      tags:
        - clusters
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Bound cluster application states.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          type: object
                          required: [cluster_id, cluster_name, status]
                          properties:
                            cluster_id:
                              type: string
                              format: uuid
                            cluster_name:
                              type: string
                            status:
                              type: string
                              enum: [pending, applying, applied, failed]
                            last_applied_at:
                              type: string
                              format: date-time
                            message:
                              type: string
        '400':
          description: Invalid template ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing template or cluster read permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Template not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Bound-cluster list failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/template:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdTemplate
      summary: Get the template application bound to a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC.
      responses:
        '200':
          description: Template application.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterTemplateApplicationResponse'
        '404':
          description: No template bound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdTemplate
      summary: Bind/apply a template to a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC. The required actor/route-scoped Idempotency-Key is bound to a canonical request digest and exact receipt; changed payload reuse returns 409.
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApplyClusterTemplateRequest'
      responses:
        '202':
          description: Application enqueued.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterTemplateApplicationResponse'
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Cluster or template not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: deleteClustersByClusterIdTemplate
      summary: Detach the template from a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      responses:
        '204':
          description: Detached.
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/template/reapply:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdTemplateReapply
      summary: Re-apply the bound template to a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Re-apply enqueued.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterTemplateApplicationResponse'
        '404':
          description: No template bound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/cluster-groups:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClusterGroups
      summary: List cluster groups (tree)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:update RBAC. Returns the full tree (no real pagination; count == page length).
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
      responses:
        '200':
          description: Cluster group tree.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data, pagination]
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ClusterGroupResponse'
                  pagination:
                    $ref: '#/components/schemas/PaginationMetadata'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClusterGroups
      summary: Create a cluster group
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:update RBAC. Tree depth capped at 3.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateClusterGroupRequest'
      responses:
        '201':
          description: Created group.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterGroupResponse'
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Slug conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/cluster-groups/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClusterGroupsById
      summary: Get a cluster group
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:update RBAC.
      responses:
        '200':
          description: Cluster group with child/subtree counts.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterGroupResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: patchClusterGroupsById
      summary: Update a cluster group (alias of PUT)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:update RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateClusterGroupRequest'
      responses:
        '200':
          description: Updated group.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterGroupResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: putClusterGroupsById
      summary: Update a cluster group
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:update RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateClusterGroupRequest'
      responses:
        '200':
          description: Updated group.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterGroupResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: deleteClusterGroupsById
      summary: Delete a cluster group
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:update RBAC.
      responses:
        '204':
          description: Deleted.
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/cluster-groups/{id}/clusters:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClusterGroupsByIdClusters
      summary: List clusters in a group (subtree)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:update RBAC. Returns id+name refs for clusters in the group subtree.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Cluster references.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              format: uuid
                            name:
                              type: string
        '404':
          description: Group not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/cluster-groups/{id}/move:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClusterGroupsByIdMove
      summary: Bulk-assign clusters into a group
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:update RBAC. Unknown/invalid cluster ids are skipped (reported in response).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveClustersRequest'
      responses:
        '200':
          description: Move result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          moved:
                            type: integer
                          skipped:
                            type: array
                            items:
                              type: string
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Group not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/registries:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdRegistries
      summary: List registry configs for a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC. Returns {items:[...]} (not the paginated envelope).
      responses:
        '200':
          description: Registry configs.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              $ref: '#/components/schemas/ClusterRegistryResponse'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdRegistries
      summary: Create a registry config for a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClusterRegistryRequest'
      responses:
        '201':
          description: Created registry config.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterRegistryResponse'
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/registries/{id}:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdRegistriesById
      summary: Get a registry config
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC.
      responses:
        '200':
          description: Registry config.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterRegistryResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: putClustersByClusterIdRegistriesById
      summary: Update a registry config
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClusterRegistryRequest'
      responses:
        '200':
          description: Updated registry config.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterRegistryResponse'
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: deleteClustersByClusterIdRegistriesById
      summary: Delete a registry config
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      responses:
        '204':
          description: Deleted.
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/registries/{id}/test:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdRegistriesByIdTest
      summary: Test connectivity for a registry config
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Test result (ok=false carries the failure message).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ClusterRegistryTestResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/apiserver-allowlist:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdApiserverAllowlist
      summary: Get the apiserver allow-list for a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC. Returns an empty policy object when none configured (still 200).
      responses:
        '200':
          description: Allow-list policy.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/AllowlistResponse'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: putClustersByClusterIdApiserverAllowlist
      summary: Update the apiserver allow-list
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AllowlistUpdateRequest'
      responses:
        '200':
          description: Updated allow-list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/AllowlistResponse'
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/apiserver-allowlist/preview:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdApiserverAllowlistPreview
      summary: Preview the effective apiserver allow-list
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Allow-list preview.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/AllowlistResponse'
  /api/v1/clusters/{cluster_id}/apiserver-allowlist/reconcile:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdApiserverAllowlistReconcile
      summary: Trigger reconcile of the apiserver allow-list
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC. Returns 202 with {"status":"accepted"} (not enveloped).
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Reconcile accepted.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: accepted
        '404':
          description: Cluster or policy not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/apiserver-allowlist/snapshots:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdApiserverAllowlistSnapshots
      summary: List allow-list drift snapshots
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC. Returns {items:[...]} (not the paginated envelope).
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Snapshots.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              $ref: '#/components/schemas/AllowlistSnapshotResponse'
  /api/v1/clusters/{cluster_id}/service-mesh:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdServiceMesh
      summary: Get cached service-mesh detection for a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Service-mesh detection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ServiceMeshDetectionResponse'
  /api/v1/clusters/{cluster_id}/service-mesh/detect:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:read"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdServiceMeshDetect
      summary: Re-run live service-mesh detection
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC (note POST, but read-gated — detection is a refresh).
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Refreshed detection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ServiceMeshDetectionResponse'
  /api/v1/clusters/{cluster_id}/service-mesh/mtls:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdServiceMeshMtls
      summary: Get mTLS coverage breakdown
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: mTLS breakdown by namespace.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/MTLSBreakdownResponse'
  /api/v1/admin/network-policy-templates:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAdminNetworkPolicyTemplates
      summary: List network-policy templates
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: network-policies:list RBAC.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated templates.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/NetworkPolicyTemplateResponse'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "network_policies:create"
      x-astronomer-route-class: public-api
      operationId: postAdminNetworkPolicyTemplates
      summary: Create a network-policy template
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: network-policies:create RBAC.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateNetworkPolicyTemplateRequest'
      responses:
        '201':
          description: Created template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/NetworkPolicyTemplateResponse'
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/network-policy-templates/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAdminNetworkPolicyTemplatesById
      summary: Get a network-policy template
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: network-policies:read RBAC.
      responses:
        '200':
          description: Template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/NetworkPolicyTemplateResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "network_policies:update"
      x-astronomer-route-class: public-api
      operationId: putAdminNetworkPolicyTemplatesById
      summary: Update a network-policy template
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: network-policies:update RBAC. Builtin rows are read-only at the handler level.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateNetworkPolicyTemplateRequest'
      responses:
        '200':
          description: Updated template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/NetworkPolicyTemplateResponse'
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "network_policies:delete"
      x-astronomer-route-class: public-api
      operationId: deleteAdminNetworkPolicyTemplatesById
      summary: Delete a network-policy template
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: network-policies:delete RBAC.
      responses:
        '204':
          description: Deleted.
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/network-policies/applications:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdNetworkPoliciesApplications
      summary: List network-policy applications on a cluster
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:read RBAC.
      responses:
        '200':
          description: Applications.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/NetworkPolicyApplicationResponse'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdNetworkPoliciesApplications
      summary: Apply a network-policy template to a cluster (one or more namespaces)
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC. The required Idempotency-Key persists a canonical request digest and exact batch receipt; changed payload reuse returns 409.
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApplyNetworkPolicyRequest'
      responses:
        '202':
          description: Application(s) enqueued.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/NetworkPolicyApplicationResponse'
        '400':
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/network-policies/applications/{id}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: deleteClustersByClusterIdNetworkPoliciesApplicationsById
      summary: Delete a network-policy application
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Deleted.
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/network-policies/applications/{id}/reapply:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdNetworkPoliciesApplicationsByIdReapply
      summary: Re-apply a network-policy application
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: clusters:write api-token scope + clusters:update RBAC.
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Re-apply enqueued.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/NetworkPolicyApplicationResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{id}/gatekeeper/constraints/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByIdGatekeeperConstraints
      tags:
        - clusters
      summary: List Gatekeeper constraints (bundle + authored) (P-04)
      description: |
        Lists the embedded starter-bundle constraints plus operator-authored
        custom constraints for the cluster, with best-effort live violation
        counts. Requires clusters:read RBAC.
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Bundle + custom constraints.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data]
                properties:
                  data: {$ref: '#/components/schemas/GatekeeperConstraintList'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdGatekeeperConstraints
      tags:
        - clusters
      summary: Author a Gatekeeper constraint and queue reconciliation (P-04)
      description: |
        Validates the YAML (ConstraintTemplate or Constraint; ConstraintTemplate
        Rego is structurally checked), then atomically persists desired state,
        a durable tunnel-reconcile task, and mandatory audit intent. The worker
        applies it after commit and records a sanitized observed outcome.
        Requires the clusters:write api-token scope and clusters:update RBAC.
      security:
        - bearerJWT: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConstraintYAMLRequest'
      responses:
        '202':
          headers: *durable-polling-headers
          description: Desired state, durable task, and audit accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConstraintValidationEnvelope'
        '400':
          description: Invalid YAML / Rego, or the cluster rejected the constraint.
        '403':
          description: Caller lacks clusters:update.
  /api/v1/clusters/{id}/gatekeeper/constraints/validate/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postClustersByIdGatekeeperConstraintsValidate
      tags:
        - clusters
      summary: Validate a Gatekeeper constraint YAML without applying (P-04)
      description: Requires clusters:read RBAC. Never applies to the cluster.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConstraintYAMLRequest'
      responses:
        '200':
          description: Validation result (valid, errors, name, kind).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConstraintValidationEnvelope'
  /api/v1/clusters/{id}/gatekeeper/constraints/{name}/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: name
        in: path
        required: true
        schema:
          type: string
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: deleteClustersByIdGatekeeperConstraintsByName
      tags:
        - clusters
      summary: Delete an authored Gatekeeper constraint (P-04)
      description: |
        Atomically marks the authored constraint absent and persists a durable
        tunnel-reconcile task plus mandatory audit intent. The worker deletes
        the resource after commit; Kubernetes 404 is treated as converged.
        Requires the clusters:write api-token scope and clusters:update RBAC.
      security:
        - bearerJWT: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      responses:
        '202':
          description: Deletion desired state and task accepted.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GatekeeperConstraintMutationEnvelope'
        '403':
          description: Caller lacks clusters:update.
        '404':
          description: Authored constraint not found.
  /api/v1/cloud-credentials/providers:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCloudCredentialsProviders
      summary: List supported cloud-credential providers
      tags:
        - clusters
      security:
        - bearerJWT: []
      description: Authenticated; no extra RBAC scope. Returns {items:[ProviderSpec...]}.
      responses:
        '200':
          description: Provider specs.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              $ref: '#/components/schemas/CloudProviderSpec'
  /api/v1/clusters/{cluster_id}/snapshot-schedules:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdSnapshotSchedules
      summary: List snapshot schedules for a cluster
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of snapshot schedules.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              $ref: '#/components/schemas/SnapshotScheduleResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdSnapshotSchedules
      summary: Create a snapshot schedule for a cluster
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SnapshotScheduleRequest'
      responses:
        '201':
          description: Schedule created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/SnapshotScheduleResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/clusters/{cluster_id}/snapshot-schedules/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdSnapshotSchedulesById
      summary: Get a snapshot schedule
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The snapshot schedule.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/SnapshotScheduleResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: putClustersByClusterIdSnapshotSchedulesById
      summary: Update a snapshot schedule
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SnapshotScheduleUpdateRequest'
      responses:
        '200':
          description: Schedule updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/SnapshotScheduleResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: deleteClustersByClusterIdSnapshotSchedulesById
      summary: Delete a snapshot schedule
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Schedule deleted.
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/clusters/{cluster_id}/snapshots:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdSnapshots
      summary: List snapshots for a cluster
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of snapshots.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              $ref: '#/components/schemas/SnapshotResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdSnapshots
      x-astronomer-operation-status-path: /api/v1/clusters/{cluster_id}/snapshots/{id}
      summary: Create an ad-hoc snapshot
      description: |
        Creates the DB row and posts a Velero Backup CR to the member cluster. Returns 202 with the freshly inserted row (phase=New); the poller advances the phase asynchronously. Body embeds SnapshotSpec fields at the top level plus optional source/velero_name/velero_namespace.
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SnapshotCreateRequest'
      responses:
        '202':
          description: Durable snapshot intent accepted; poll the Location resource for exact status.
          headers:
            Location: {description: Exact snapshot status resource, schema: {type: string, format: uri-reference}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/SnapshotResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/clusters/{cluster_id}/snapshots/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdSnapshotsById
      summary: Get a snapshot
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The snapshot.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/SnapshotResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token clusters:write plus clusters:update"
      x-astronomer-route-class: public-api
      operationId: deleteClustersByClusterIdSnapshotsById
      summary: Delete a snapshot
      description: |
        Posts a Velero DeleteBackupRequest CR (best-effort) then drops the local row. Never waits for Velero.
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Snapshot deleted.
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/clusters/{cluster_id}/snapshots/{id}/restore:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "clusters:update"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdSnapshotsByIdRestore
      summary: Restore from a snapshot
      description: |
        Atomically commits a durable restore row, identifier-only worker task,
        mandatory audit intent, and the actor-scoped Idempotency-Key binding.
        The request performs no member-cluster mutation. target_cluster_id is
        optional and defaults to the snapshot's cluster. The snapshot must be
        Completed or PartiallyFailed. Cross-cluster restore requires a matching
        Velero BackupStorageLocation. Identical replays return the original
        receipt; changed target, namespace, or spec returns 409.
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SnapshotRestoreRequest'
      responses:
        '202':
          description: Durable restore accepted; poll the source snapshot URL while the worker converges the restore.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/SnapshotRestoreResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '409':
          description: Snapshot not ready, or target cluster has no Velero BSL.
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
        '502':
          $ref: '#/components/responses/ErrorResponse'
        '503':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/clusters/{cluster_id}/velero-status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdVeleroStatus
      summary: Velero pre-flight status for a cluster
      description: |
        Reports whether Velero is installed and whether any BackupStorageLocation is Available. Returns 200 even when the member cluster is unreachable (installed=false + reason).
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Velero status.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/VeleroStatusResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '503':
          description: Tunnel requester not configured.
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackups
      summary: List backups
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated backups.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/BackupResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated backup surface; explicit backup RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: postBackups
      summary: Create an on-demand backup
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BackupCreateRequest'
      responses:
        '201':
          description: Backup created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackupsById
      summary: Get a backup
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The backup.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated backup surface; explicit backup RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: deleteBackupsById
      summary: Delete a backup
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Backup deleted.
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/{id}/restore:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated backup surface; explicit restore RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: postBackupsByIdRestore
      summary: Restore from a backup
      description: |
        Creates a restore operation (status=pending) tracked against the backup. Body is optional; namespace_mapping defaults to {}.
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BackupRestoreRequest'
      responses:
        '201':
          description: Restore operation created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RestoreOperationResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/controller/status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackupsControllerStatus
      summary: Backup subsystem controller status
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Operational summary (reconciler/health/storage/backups/schedules/restores).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupControllerStatus'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/restores:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackupsRestores
      summary: List restore operations
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated restore operations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RestoreOperationResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/runs:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackupsRuns
      summary: List backup runs (alias of GET /api/v1/backups)
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated backups (same shape as GET /api/v1/backups).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/BackupResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/schedules:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackupsSchedules
      summary: List backup schedules
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated backup schedules.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/BackupScheduleResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated backup surface; explicit schedule RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: postBackupsSchedules
      summary: Create a backup schedule
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BackupScheduleRequest'
      responses:
        '201':
          description: Schedule created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupScheduleResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/schedules/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackupsSchedulesById
      summary: Get a backup schedule
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The backup schedule.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupScheduleResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "backups:update"
      x-astronomer-route-class: public-api
      operationId: putBackupsSchedulesById
      summary: Update a backup schedule
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BackupScheduleRequest'
      responses:
        '200':
          description: Schedule updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupScheduleResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "backups:delete"
      x-astronomer-route-class: public-api
      operationId: deleteBackupsSchedulesById
      summary: Delete a backup schedule
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Schedule deleted.
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/schedules/{id}/trigger-now:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated backup surface; explicit schedule RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: postBackupsSchedulesByIdTriggerNow
      summary: Trigger a backup schedule immediately
      description: |
        Creates a one-off Velero Backup CR and a backups row tracking it. Returns the created backup.
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '201':
          description: Manual backup triggered.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/storage:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackupsStorage
      summary: List backup storage configs
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated storage configs.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/BackupStorageConfigResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated backup surface; explicit backup storage RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: postBackupsStorage
      summary: Create a backup storage config
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BackupStorageConfigRequest'
      responses:
        '201':
          description: Storage config created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupStorageConfigResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/storage/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackupsStorageById
      summary: Get a backup storage config
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The storage config.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupStorageConfigResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated backup surface; explicit backup storage RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: putBackupsStorageById
      summary: Update a backup storage config
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BackupStorageConfigRequest'
      responses:
        '200':
          description: Storage config updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupStorageConfigResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated backup surface; explicit backup storage RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: deleteBackupsStorageById
      summary: Delete a backup storage config
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Storage config deleted.
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/storage/{id}/test:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "backups:update"
      x-astronomer-route-class: public-api
      operationId: postBackupsStorageByIdTest
      summary: Test a storage config connection
      description: |
        Issues an authenticated S3 list-objects probe against the bucket. Always returns 200 with {success, message}; success=false when the probe fails.
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Probe result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/StorageTestResult'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/storage/{id}/test-connection:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "backups:update"
      x-astronomer-route-class: public-api
      operationId: postBackupsStorageByIdTestConnection
      summary: Test a storage config connection (alias of /test)
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Probe result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/StorageTestResult'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/storage-configs:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackupsStorageConfigs
      summary: List backup storage configs (alias of /storage)
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated storage configs.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/BackupStorageConfigResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "backups:create"
      x-astronomer-route-class: public-api
      operationId: postBackupsStorageConfigs
      summary: Create a backup storage config (alias of /storage)
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BackupStorageConfigRequest'
      responses:
        '201':
          description: Storage config created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupStorageConfigResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/storage-configs/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getBackupsStorageConfigsById
      summary: Get a backup storage config (alias of /storage/{id})
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The storage config.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupStorageConfigResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "backups:update"
      x-astronomer-route-class: public-api
      operationId: putBackupsStorageConfigsById
      summary: Update a backup storage config (alias of /storage/{id})
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BackupStorageConfigRequest'
      responses:
        '200':
          description: Storage config updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/BackupStorageConfigResponse'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "backups:delete"
      x-astronomer-route-class: public-api
      operationId: deleteBackupsStorageConfigsById
      summary: Delete a backup storage config (alias of /storage/{id})
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Storage config deleted.
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/backups/storage-configs/{id}/test-connection:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "backups:update"
      x-astronomer-route-class: public-api
      operationId: postBackupsStorageConfigsByIdTestConnection
      summary: Test a storage config connection (alias of /storage/{id}/test)
      tags:
        - cluster-snapshots-backups
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Probe result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/StorageTestResult'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorResponse'
        '500':
          $ref: '#/components/responses/ErrorResponse'
  /api/v1/catalog/charts/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogCharts
      summary: List catalog charts
      description: |
        Lists Helm charts across synced repositories. Returns a DRF-style
        paginated envelope. Supports `?tag=` (filter on helm_chart_tags) and
        `?project_id=` (project-scoped union of globals + own + subscribed
        catalogs); these are mutually exclusive with `tag` taking precedence.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: tag
          in: query
          required: false
          schema:
            type: string
          description: Filter charts by helm_chart_tags tag.
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Select the project-scoped catalog union. Omit only for the globally managed catalog view.
      responses:
        '200':
          description: Paginated page of charts.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/HelmChart'
        '400':
          description: Invalid project_id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List/count failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/charts/{id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogChartsById
      summary: Get a catalog chart
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Select the project's catalog visibility. Omit only for a globally managed chart.
      responses:
        '200':
          description: The chart.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/HelmChart'
        '400':
          description: Invalid chart ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Chart not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/charts/{id}/readme/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogChartsByIdReadme
      summary: Get a chart README
      description: |
        Returns the README for the latest (or `?version=`) chart version.
        Lazily hydrates from the chart archive on cache miss.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Select the project's catalog visibility. Omit only for a globally managed chart.
        - name: version
          in: query
          required: false
          schema:
            type: string
          description: Specific chart version; defaults to latest.
      responses:
        '200':
          description: README payload.
          content:
            application/json:
              schema:
                type: object
                properties:
                  chart:
                    type: string
                  version:
                    type: string
                  readme:
                    type: string
        '400':
          description: Invalid chart ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Chart or version not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/charts/{id}/values/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogChartsByIdValues
      summary: Get chart default values and schema
      description: |
        Returns the default values YAML plus the JSON values schema for the
        latest (or `?version=`) chart version. Lazily hydrated on cache miss.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Select the project's catalog visibility. Omit only for a globally managed chart.
        - name: version
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Default values and schema.
          content:
            application/json:
              schema:
                type: object
                properties:
                  chart:
                    type: string
                  version:
                    type: string
                  default_values:
                    type: string
                    description: values.yaml as a raw string
                  values_schema:
                    type: object
                    description: JSON schema (raw JSON object)
        '400':
          description: Invalid chart ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Chart or version not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/charts/{id}/versions/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogChartsByIdVersions
      summary: List versions of a chart
      description: |
        Returns chart versions with explicit pagination metadata.
        `limit`/`offset` page the underlying query.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Select the project's catalog visibility. Omit only for a globally managed chart.
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Page of chart versions.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/HelmChartVersionPage'}
        '400':
          description: Invalid chart ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/recommendations/popular/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogRecommendationsPopular
      summary: List popular visible charts
      description: Returns ranked recommendations from the selected project's visible catalogs. Omit project_id only for the globally managed catalog view.
      tags: [catalog]
      security: [{bearerJWT: []}]
      parameters:
        - name: project_id
          in: query
          required: false
          schema: {type: string, format: uuid}
        - name: limit
          in: query
          required: false
          schema: {type: integer, minimum: 1, maximum: 50, default: 6}
      responses:
        '200':
          description: Ranked visible chart recommendations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items: {$ref: '#/components/schemas/ChartRecommendation'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/catalog/recommendations/similar/{chart_id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogRecommendationsSimilarByChartId
      summary: List similar visible charts
      description: Returns co-installation recommendations only when the source chart and results are visible in the selected project. Omit project_id only for the globally managed catalog view.
      tags: [catalog]
      security: [{bearerJWT: []}]
      parameters:
        - name: chart_id
          in: path
          required: true
          schema: {type: string, format: uuid}
        - name: project_id
          in: query
          required: false
          schema: {type: string, format: uuid}
        - name: limit
          in: query
          required: false
          schema: {type: integer, minimum: 1, maximum: 20, default: 5}
      responses:
        '200':
          description: Ranked visible chart recommendations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items: {$ref: '#/components/schemas/ChartRecommendation'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/catalog/controller/status/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogControllerStatus
      summary: Catalog reconciler controller status
      description: |
        Summary document for the catalog operations reconciler: operation
        counts by status, recent operations/failures, chart and installed
        counts. Counts are RBAC-filtered to the caller's visible clusters.
      tags:
        - catalog
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Controller status summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  reconciler:
                    type: object
                  catalog:
                    type: object
                    properties:
                      chartCount:
                        type: integer
                        format: int64
                      installedCount:
                        type: integer
                        format: int64
                        nullable: true
                  operations:
                    type: object
                    additionalProperties:
                      type: integer
                  recentFailureCount:
                    type: integer
                  recentOperations:
                    type: array
                    items:
                      type: object
                  latestFailure:
                    type: object
                    nullable: true
        '500':
          description: Failed to load catalog operations.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/installed/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogInstalled
      summary: List installed charts
      description: |
        Lists installed_charts rows (paginated envelope). When `?cluster_id=`
        is supplied the listing is scoped to that cluster.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated page of installed charts.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/InstalledChart'
        '500':
          description: List/count failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated catalog surface; explicit catalog RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: postCatalogInstalled
      summary: Create an installation (install a chart)
      description: |
        Enqueues a helm install on the target cluster. Cluster-mutating:
        requires the `clusters:write` api-token scope (legacy empty-scope and
        session tokens pass through) plus RBAC `catalog:create` on the cluster,
        enforced inside the handler. May be blocked/deferred by an active
        maintenance window.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - cluster_id
                - project_id
                - chart_version_id
                - release_name
                - namespace
              properties:
                cluster_id:
                  type: string
                  format: uuid
                project_id:
                  type: string
                  format: uuid
                chart_version_id:
                  type: string
                  format: uuid
                release_name:
                  type: string
                namespace:
                  type: string
                values_override:
                  type: string
                notes:
                  type: string
                tool_slug:
                  type: string
                preset_used:
                  type: string
      responses:
        '202':
          description: Installation accepted and enqueued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CatalogInstallationAcceptedEnvelope'}
        '400':
          description: Invalid body, missing required field, invalid ID, or vault resolution failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: The project is not assigned to the cluster, the repository is not visible to the project, or catalog:create is denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Chart version, chart, or repository not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Blocked by an active maintenance window.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create/enqueue failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/installed/{id}/:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated catalog surface; explicit catalog RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: deleteCatalogInstalledById
      summary: Uninstall an installed chart
      description: |
        Marks the release pending_uninstall and enqueues a helm uninstall.
        Cluster-mutating: requires `clusters:write` api-token scope plus RBAC
        `catalog:delete` on the cluster. May be blocked by a maintenance window.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Uninstall accepted (returns the catalog operation).
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CatalogOperationEnvelope'}
        '400':
          description: Invalid installation ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: RBAC catalog:delete denied for the cluster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Installation not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Blocked by an active maintenance window.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Update/enqueue failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/installed/{id}/rollback/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "catalog:update"
      x-astronomer-route-class: public-api
      operationId: postCatalogInstalledByIdRollback
      summary: Roll back an installed chart
      description: |
        Marks the release pending_rollback and enqueues a helm rollback. An
        optional positive `revision` selects any prior revision; otherwise the
        immediately previous revision is used. Cluster-mutating:
        `clusters:write` api-token scope plus RBAC `catalog:update` on the cluster.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                revision: {type: integer, minimum: 1}
      responses:
        '202':
          description: Rollback accepted (returns the catalog operation).
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CatalogOperationEnvelope'}
        '400':
          description: Invalid installed chart ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: RBAC catalog:update denied for the cluster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Installed chart not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Rollback/enqueue failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/installed/{id}/upgrade/:
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated catalog surface; explicit catalog RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: putCatalogInstalledByIdUpgrade
      summary: Upgrade an installed chart
      description: |
        Stages the selected version and new values_override (status
        pending_upgrade), then enqueues a helm upgrade. A supplied version must
        belong to the currently installed chart. Cluster-mutating:
        `clusters:write` api-token scope plus RBAC `catalog:update` on the cluster.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                chart_version_id:
                  type: string
                  format: uuid
                values_override:
                  type: string
      responses:
        '202':
          description: Upgrade accepted and enqueued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CatalogInstallationAcceptedEnvelope'}
        '400':
          description: Invalid installed chart ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: RBAC catalog:update denied for the cluster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Installed chart not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Resolve/update/enqueue failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/installed/{id}/values/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogInstalledByIdValues
      summary: Get an installed chart's values override
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The release name, namespace, and stored values override.
          content:
            application/json:
              schema:
                type: object
                properties:
                  release_name:
                    type: string
                  namespace:
                    type: string
                  values_override:
                    type: string
        '400':
          description: Invalid release ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Installed chart not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/operations/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogOperations
      summary: List catalog operations
      description: |
        Returns a bounded catalog-operation page, RBAC-filtered to the caller's
        visible clusters. Filterable by `targetType`, `targetKey`, and `status`.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: targetType
          in: query
          required: false
          schema:
            type: string
        - name: targetKey
          in: query
          required: false
          schema:
            type: string
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Catalog operation page.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ListEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/CatalogOperation'
        '500':
          description: List/permission failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/operations/{id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogOperationsById
      summary: Get a catalog operation
      description: |
        Returns the operation plus its event log under `events`. Requires RBAC
        `catalog:read` on the operation's target cluster.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The catalog operation with events.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/CatalogOperation'
                  - type: object
                    properties:
                      events:
                        type: array
                        items:
                          $ref: '#/components/schemas/CatalogOperationEvent'
        '400':
          description: Invalid operation ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: RBAC catalog:read denied for the cluster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Operation not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Resolve failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/operations/{id}/retry/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "catalog:update"
      x-astronomer-route-class: public-api
      operationId: postCatalogOperationsByIdRetry
      summary: Retry a catalog operation
      description: |
        Requeues a failed/retryable catalog operation and triggers the
        reconciler. Requires RBAC `catalog:update` on the target cluster.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Operation requeued.
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogOperation'
        '400':
          description: Invalid operation ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: RBAC catalog:update denied for the cluster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Operation not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Operation is not in a retryable state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Resolve/retry failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/repositories/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogRepositories
      summary: List helm repositories (catalogs)
      description: |
        Default admin view excludes project-owned (private) catalogs.
        `?include_project_owned=true` includes them; `?project_id=` switches to
        the project-scoped browse (returns a bare array, not paginated).
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: include_project_owned
          in: query
          required: false
          schema:
            type: boolean
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: |
            Paginated repositories (admin view), or a bare array when
            `?project_id=` is supplied.
          content:
            application/json:
              schema:
                oneOf:
                  - allOf:
                      - $ref: '#/components/schemas/PaginatedEnvelope'
                      - properties:
                          data:
                            type: array
                            items:
                              $ref: '#/components/schemas/HelmRepository'
                  - type: array
                    items:
                      $ref: '#/components/schemas/HelmRepository'
        '400':
          description: Invalid project_id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List/count failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "catalog:create"
      x-astronomer-route-class: public-api
      operationId: postCatalogRepositories
      summary: Create a helm repository (catalog)
      description: |
        Credentials go in `auth_config`, never at the top level of the body.
        A request carrying a top-level `username`, `password` or `token` is
        rejected with 400 rather than silently created without a credential.
      tags:
        - catalog
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - url
              properties:
                name:
                  type: string
                url:
                  type: string
                repo_type:
                  type: string
                  description: auto-detected as 'oci' for oci:// URLs when empty
                description:
                  type: string
                is_default:
                  type: boolean
                auth_type:
                  type: string
                  description: |
                    'none' | 'basic' | 'bearer'. Inferred from auth_config when
                    empty (username/password → basic, token/bearer → bearer):
                    a credential stored without an auth_type would never be
                    sent.
                auth_config:
                  $ref: '#/components/schemas/HelmRepositoryAuthConfig'
                enabled:
                  type: boolean
                  default: true
                  description: |
                    Defaults to true when absent. A disabled repository is
                    skipped by the scheduled sync sweep entirely.
      responses:
        '201':
          description: Repository created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/HelmRepository'
        '400':
          description: Invalid body or missing name/url.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/repositories/{id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getCatalogRepositoriesById
      summary: Get a helm repository
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The repository.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/HelmRepository'
        '400':
          description: Invalid repository ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Repository not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "catalog:update"
      x-astronomer-route-class: public-api
      operationId: putCatalogRepositoriesById
      summary: Update a helm repository
      description: |
        Every field is optional and **an absent field leaves the stored value
        unchanged** — a body of `{"name":"new"}` renames the repository and
        touches nothing else.

        `auth_config` in particular: omit it to keep the stored credential,
        send it to replace the whole credential document. Within a supplied
        document, a secret whose value is the redaction sentinel
        (`__ASTRONOMER_SECRET_UNCHANGED__`) or the empty string keeps its
        stored value, so a redacted GET body can be edited and PUT straight
        back. Send `auth_config: {}` to remove the credential outright.

        Credentials at the top level of the body (`username`, `password`,
        `token`) are rejected with 400.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                url:
                  type: string
                repo_type:
                  type: string
                description:
                  type: string
                is_default:
                  type: boolean
                auth_type:
                  type: string
                auth_config:
                  $ref: '#/components/schemas/HelmRepositoryAuthConfig'
                enabled:
                  type: boolean
      responses:
        '200':
          description: Repository updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/HelmRepository'
        '400':
          description: Invalid repository ID or body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Repository not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Update failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "catalog:delete"
      x-astronomer-route-class: public-api
      operationId: deleteCatalogRepositoriesById
      summary: Delete a helm repository
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Repository deleted (no content).
        '400':
          description: Invalid repository ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Delete failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/repositories/{id}/sync/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "catalog:update"
      x-astronomer-route-class: public-api
      operationId: postCatalogRepositoriesByIdSync
      x-astronomer-operation-status-path: /api/v1/catalog/repositories/{id}/
      summary: Sync a helm repository
      description: |
        Durably accepts a repository-specific catalog sync task. The worker
        fetches the repository index (or OCI artifacts), upserts chart and
        version rows, and stamps last_synced_at on success. Task intent and
        the mandatory request audit record commit atomically.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Sync task durably accepted; identical replay returns this exact receipt.
          headers:
            Location: {description: Exact catalog repository status resource, schema: {type: string, format: uri-reference}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CatalogRepositorySyncReceiptEnvelope'}
        '400':
          description: Invalid repository ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Repository not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409': {$ref: '#/components/responses/Conflict'}
        '500':
          description: Failed to create the durable sync task.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Mandatory audit storage is unavailable; no sync task was committed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/catalog/repositories/{id}/test-connection/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "catalog:update"
      x-astronomer-route-class: public-api
      operationId: postCatalogRepositoriesByIdTestConnection
      summary: Test a helm repository's connectivity
      description: |
        Probes the repository's index.yaml (or the OCI /v2/ ping endpoint) and
        reports reachability. Returning the credential-test result requires a
        synchronous mandatory audit write; upstream transport errors are not
        exposed verbatim.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Connection succeeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        '400':
          description: Invalid repository ID or URL.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Repository not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Repository/registry unreachable or returned an error status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
        '503':
          description: Mandatory audit storage is unavailable; the probe result is withheld.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/apps/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdApps
      summary: List installed apps on a cluster (enriched)
      description: |
        Per-cluster Apps tab listing: installed_charts rows for the cluster with
        LEFT-joined chart metadata. Tool installs (chart_version_id NULL) come
        back with empty chart_* fields, a non-empty tool_slug, and
        source_kind="tool". Requires RBAC `clusters:read`. Paginated envelope.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 500
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated page of enriched installed apps.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/InstalledAppEnriched'
        '400':
          description: Invalid cluster ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List/count failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/apps/failed/:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "catalog:delete"
      x-astronomer-route-class: public-api
      operationId: deleteClustersByClusterIdAppsFailed
      summary: Delete failed installs on a cluster
      description: |
        Rancher-style force delete: hard-deletes installed_charts rows stuck in
        failed_install / failed_uninstall on the cluster. Requires RBAC
        `catalog:delete` on the cluster. Healthy releases are untouched.
      tags:
        - catalog
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Count of failed installs removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: integer
                    format: int64
        '400':
          description: Invalid cluster ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: RBAC catalog:delete denied for the cluster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Delete failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getProjects
      summary: List projects
      description: DRF-style paginated list of all projects across clusters.
      tags:
        - projects
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 0
          description: Page size (defaults to 20).
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
            minimum: 0
          description: Row offset into the result set.
      responses:
        '200':
          description: Paginated page of projects.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Project'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List or count failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:create"
      x-astronomer-route-class: public-api
      operationId: postProjects
      summary: Create a project
      description: |
        Creates a project on a cluster. Seeds the project_namespaces sidecar
        from any namespaces given at create time and enqueues an enforcement
        reconcile for each. New projects default to the `baseline` Pod Security
        profile and unbounded resource quota.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectRequest'
      responses:
        '201':
          description: Project created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/Project'
        '400':
          description: Invalid body, missing name, bad cluster_id, or invalid pod_security_profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{id}/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getProjectsById
      summary: Get a project
      tags:
        - projects
      security:
        - bearerJWT: []
      responses:
        '200':
          description: The project.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/Project'
        '400':
          description: Invalid project ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:update"
      x-astronomer-route-class: public-api
      operationId: putProjectsById
      summary: Replace a project
      description: |
        Full update of a project. Omitted policy fields (pod_security_profile,
        resource_quota_*) are preserved from the existing row. Re-enqueues an
        enforcement reconcile for every current namespace. Blocked with 409 when
        the project is CRD-owned (transfer ownership first).
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectRequest'
      responses:
        '200':
          description: Project updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/Project'
        '400':
          description: Invalid body / ID / pod_security_profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Project is CRD-owned; edit the CR or transfer ownership.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Ownership-check failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token projects:write plus projects:update"
      x-astronomer-route-class: public-api
      operationId: patchProjectsById
      summary: Update a project (full-replacement semantics)
      description: |
        Routed to the same handler as PUT — despite the PATCH verb this is a
        full update, not a JSON-merge patch, and accepts the same body as PUT.
        Use PATCH /projects/{id}/policy/ for partial policy edits.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectRequest'
      responses:
        '200':
          description: Project updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/Project'
        '400':
          description: Invalid body / ID / pod_security_profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Project is CRD-owned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:delete"
      x-astronomer-route-class: public-api
      operationId: deleteProjectsById
      summary: Delete a project
      description: |
        Enqueues per-namespace cleanup of managed CRs before removing the row.
        Subject to the maintenance-window gate (migration 057) — may be
        refused or deferred during an active window on the parent cluster.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      responses:
        '204':
          description: Project deleted.
        '400':
          description: Invalid project ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Refused by an active maintenance window.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{id}/add-namespace/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:update"
      x-astronomer-route-class: public-api
      operationId: postProjectsByIdAddNamespace
      summary: Add a namespace to a project
      description: |
        Adds a namespace to the project's namespace list and the
        project_namespaces sidecar, then enqueues an enforcement reconcile.
        Rejects namespaces already claimed by this or another project on the
        same cluster.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectNamespaceRequest'
      responses:
        '200':
          description: Namespace added; updated project returned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/Project'
        '400':
          description: Invalid body / ID, or missing namespace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Namespace already in this project or claimed by another project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Encode or update failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{id}/remove-namespace/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:update"
      x-astronomer-route-class: public-api
      operationId: postProjectsByIdRemoveNamespace
      summary: Remove a namespace from a project
      description: |
        Enqueues cleanup of the namespace's managed CRs (before the DB update)
        and removes the namespace from the project's list.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectNamespaceRequest'
      responses:
        '200':
          description: Namespace removed; updated project returned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/Project'
        '400':
          description: Invalid body / ID, or missing namespace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found, or namespace not in this project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Encode or update failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{id}/policy/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
    patch:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:update"
      x-astronomer-route-class: public-api
      operationId: patchProjectsByIdPolicy
      summary: Update project policy
      description: |
        Partial update of just the per-project policy fields
        (pod_security_profile and the three resource_quota_* limits). Omitted
        fields keep their current value. Blocked with 409 when the project is
        CRD-owned.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectPolicyRequest'
      responses:
        '200':
          description: Policy updated; updated project returned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/Project'
        '400':
          description: Invalid body / ID / pod_security_profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Project is CRD-owned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Update or ownership-check failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{id}/quota-usage/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getProjectsByIdQuotaUsage
      summary: Live quota usage for a project
      description: |
        Fans out to each (cluster, namespace) the project owns and reads the
        managed `astronomer-project-quota` ResourceQuota's used + hard maps.
        Per-cluster errors are collected in `errors` rather than failing the
        whole response. Requires a configured cluster tunnel (503 otherwise).
      tags:
        - projects
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Per-namespace quota usage plus per-cluster errors.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          results:
                            type: array
                            items:
                              type: object
                              properties:
                                cluster_id:
                                  type: string
                                  format: uuid
                                cluster_name:
                                  type: string
                                namespace:
                                  type: string
                                used:
                                  type: object
                                  additionalProperties: true
                                  description: ResourceQuota status.used key/value pairs (e.g. cpu, memory, pods).
                                hard:
                                  type: object
                                  additionalProperties: true
                                  description: ResourceQuota status.hard (falls back to spec.hard) key/value pairs.
                          errors:
                            type: array
                            items:
                              type: object
                              properties:
                                cluster_id:
                                  type: string
                                  format: uuid
                                cluster_name:
                                  type: string
                                namespace:
                                  type: string
                                error:
                                  type: string
        '400':
          description: Invalid project ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to list project namespaces.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Cluster tunnel not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{id}/rbac/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getProjectsByIdRbac
      summary: Project RBAC matrix
      description: |
        Returns the project's role bindings (capped at 500), each enriched with
        the role's name/display + the principal's display name, plus a summary
        with user/group counts and per-role counts.
      tags:
        - projects
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Bindings and summary for the project.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          project_id:
                            type: string
                            format: uuid
                          bindings:
                            type: array
                            items:
                              type: object
                              properties:
                                id:
                                  type: string
                                  format: uuid
                                principal_kind:
                                  type: string
                                  enum:
                                    - user
                                    - group
                                principal_id:
                                  type: string
                                  description: User UUID for user bindings; omitted for groups.
                                principal_name:
                                  type: string
                                role_id:
                                  type: string
                                  format: uuid
                                role_name:
                                  type: string
                                role_display:
                                  type: string
                                is_builtin:
                                  type: boolean
                                created_at:
                                  type: string
                                  format: date-time
                          summary:
                            type: object
                            properties:
                              user_count:
                                type: integer
                              group_count:
                                type: integer
                              role_counts:
                                type: object
                                additionalProperties:
                                  type: integer
        '400':
          description: Invalid project ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to list bindings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{id}/clusters/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getProjectsByIdClusters
      summary: Clusters a project lives on
      description: |
        Distinct clusters the project is materialised on (derived from
        project_namespaces), each with a namespace count.
      tags:
        - projects
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Distinct clusters with namespace counts.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          project_id:
                            type: string
                            format: uuid
                          count:
                            type: integer
                          clusters:
                            type: array
                            items:
                              type: object
                              properties:
                                cluster_id:
                                  type: string
                                  format: uuid
                                cluster_name:
                                  type: string
                                namespace_count:
                                  type: integer
        '400':
          description: Invalid project ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to list project namespaces.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/projects/:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Cluster UUID.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdProjects
      summary: List projects on a cluster
      description: DRF-style paginated list of projects belonging to one cluster.
      tags:
        - projects
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 0
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        '200':
          description: Paginated page of projects on the cluster.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Project'
        '400':
          description: Invalid cluster ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List or count failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{id}/default-vault-connection/:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getProjectsByIdDefaultVaultConnection
      summary: Get the project's default Vault connection
      description: |
        Returns the project's selected default Vault connection (redacted) or
        nulls when none is set.
      tags:
        - projects
      security:
        - bearerJWT: []
      responses:
        '200':
          description: The default connection (or nulls).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          connection_id:
                            type: string
                            format: uuid
                            nullable: true
                          connection:
                            nullable: true
                            allOf:
                              - $ref: '#/components/schemas/VaultConnection'
        '400':
          description: Invalid project ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Lookup failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:update"
      x-astronomer-route-class: public-api
      operationId: putProjectsByIdDefaultVaultConnection
      summary: Set the project's default Vault connection
      description: |
        Sets (or clears, with null) the project's default Vault connection.
        Validates the referenced connection exists.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                connection_id:
                  type: string
                  format: uuid
                  nullable: true
                  description: Vault connection UUID, or null to clear the default.
      responses:
        '200':
          description: Default connection updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          connection_id:
                            type: string
                            format: uuid
                            nullable: true
        '400':
          description: Invalid body / project ID / connection_id (or connection not found).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Update failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{project_id}/catalogs/:
    parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getProjectsByProjectIdCatalogs
      summary: List visible Helm catalogs for a project
      description: |
        Returns global catalogs (always visible) plus the project's own
        (private) catalogs and any catalogs the project has subscribed to. The
        per-row `visibility` field discriminates the buckets.
      tags:
        - projects
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Visible catalogs (flat array under data).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ProjectCatalog'
        '400':
          description: Invalid project ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to list catalogs or subscriptions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:update"
      x-astronomer-route-class: public-api
      operationId: postProjectsByProjectIdCatalogs
      summary: Create a project-owned Helm catalog
      description: |
        Creates a private (project-owned) catalog and auto-subscribes the
        project so it appears in subsequent List responses with
        visibility="own".
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectCatalogRequest'
      responses:
        '201':
          description: Catalog created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ProjectCatalog'
        '400':
          description: Invalid body / ID, or missing name or URL.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to create catalog.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{project_id}/catalogs/{catalog_id}/:
    parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
      - name: catalog_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Catalog (helm_repository) UUID.
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token projects:write plus projects:delete"
      x-astronomer-route-class: public-api
      operationId: deleteProjectsByProjectIdCatalogsByCatalogId
      summary: Unsubscribe from or delete a catalog
      description: |
        When the catalog is owned by this project, deletes the catalog row
        entirely (cascade). Otherwise removes only the project's subscription
        row. Idempotent — returns 204 even when there was nothing to
        unsubscribe.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      responses:
        '204':
          description: Catalog deleted or subscription removed.
        '400':
          description: Invalid project ID or catalog ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Catalog not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to delete catalog or unsubscribe.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{project_id}/catalogs/{catalog_id}/charts/:
    parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
      - name: catalog_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Catalog (helm_repository) UUID.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getProjectsByProjectIdCatalogsByCatalogIdCharts
      summary: List charts in a catalog
      description: |
        Lists charts in the catalog. Requires the project to have visibility
        (own, subscribed, or global). Foreign-private catalogs return 403
        unless the caller is a superuser.
      tags:
        - projects
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 100
            minimum: 0
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        '200':
          description: Charts in the catalog (flat array under data).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/HelmChart'
        '400':
          description: Invalid project ID or catalog ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Catalog not accessible to this project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Catalog not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Visibility resolution or list failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{project_id}/catalogs/{catalog_id}/subscribe/:
    parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
      - name: catalog_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Catalog (helm_repository) UUID.
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:update"
      x-astronomer-route-class: public-api
      operationId: postProjectsByProjectIdCatalogsByCatalogIdSubscribe
      summary: Subscribe a project to a catalog
      description: |
        Subscribes the project to a public catalog (or another project's
        private catalog only when the caller is a superuser). Idempotent:
        re-subscribing returns the existing row with 200.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      responses:
        '200':
          description: Already subscribed; existing subscription returned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ProjectCatalogSubscription'
        '201':
          description: Subscription created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ProjectCatalogSubscription'
        '400':
          description: Invalid project ID or catalog ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Cannot subscribe to another project's private catalog.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project or catalog not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to subscribe.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{project_id}/cloud-credentials/:
    parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getProjectsByProjectIdCloudCredentials
      summary: List cloud credentials for a project
      description: Lists the project's cloud credentials with secret values redacted.
      tags:
        - projects
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Credentials wrapped under data.items.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              $ref: '#/components/schemas/CloudCredential'
        '400':
          description: Invalid project ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:update"
      x-astronomer-route-class: public-api
      operationId: postProjectsByProjectIdCloudCredentials
      summary: Create a cloud credential
      description: |
        Stores a provider credential (encrypted at rest) and upserts a
        materialization per target_ref, enqueuing apply tasks. Requires a
        configured encryptor (503 otherwise).
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloudCredentialRequest'
      responses:
        '201':
          description: Credential created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/CloudCredential'
        '400':
          description: Invalid body, name, provider, data, or target_refs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: A credential with that name already exists in this project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Encrypt or create failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Encryption key not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{project_id}/cloud-credentials/{id}/:
    parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Cloud credential UUID.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getProjectsByProjectIdCloudCredentialsById
      summary: Get a cloud credential
      description: Returns one credential (redacted) plus its per-target materialization status rows.
      tags:
        - projects
      security:
        - bearerJWT: []
      responses:
        '200':
          description: The credential with materialization status.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/CloudCredential'
        '400':
          description: Invalid project ID or credential ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Credential not found (or not under this project).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Decode failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:update"
      x-astronomer-route-class: public-api
      operationId: putProjectsByProjectIdCloudCredentialsById
      summary: Update a cloud credential
      description: |
        Updates description, data, and target_refs. Name and provider are
        immutable. Secret fields equal to the redaction sentinel are preserved
        from the stored blob. Dropped target_refs enqueue in-cluster Secret
        deletion. Requires a configured encryptor (503 otherwise).
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloudCredentialRequest'
      responses:
        '200':
          description: Credential updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/CloudCredential'
        '400':
          description: Invalid body, immutable name/provider, invalid data, or target_refs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Credential not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Encrypt or update failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Encryption key not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token projects:write plus projects:update"
      x-astronomer-route-class: public-api
      operationId: patchProjectsByProjectIdCloudCredentialsById
      summary: Update a cloud credential (alias of PUT)
      description: Routed to the same handler as PUT; same body and semantics.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloudCredentialRequest'
      responses:
        '200':
          description: Credential updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/CloudCredential'
        '400':
          description: Invalid body, immutable name/provider, invalid data, or target_refs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Credential not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Encryption key not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token projects:write plus projects:delete"
      x-astronomer-route-class: public-api
      operationId: deleteProjectsByProjectIdCloudCredentialsById
      summary: Delete a cloud credential
      description: |
        Enqueues in-cluster Secret deletion for every target_ref (before the
        row is purged), then deletes the credential.
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      responses:
        '204':
          description: Credential deleted.
        '400':
          description: Invalid project ID or credential ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Credential not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Delete failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/projects/{project_id}/cloud-credentials/{id}/test/:
    parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Project UUID.
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Cloud credential UUID.
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "projects:update"
      x-astronomer-route-class: public-api
      operationId: postProjectsByProjectIdCloudCredentialsByIdTest
      summary: Test a cloud credential
      description: |
        Decrypts the stored blob and validates it against the provider SDK
        (AWS/GCP/Azure). Generic credentials and unconfigured testers return
        ok=false with an explanatory message (still HTTP 200).
      tags:
        - projects
      security:
        - bearerJWT: []
      x-required-token-scope: projects:write
      responses:
        '200':
          description: Test outcome (ok + message).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          ok:
                            type: boolean
                          message:
                            type: string
        '400':
          description: Invalid project ID or credential ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Credential not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to decrypt stored credential.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/global-roles:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacGlobalRoles
      summary: List global RBAC roles
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated list of global roles.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RBACRole'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "rbac:create"
      x-astronomer-route-class: public-api
      operationId: postRbacGlobalRoles
      summary: Create a global RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `create` permission.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACRoleRequest'
      responses:
        '201':
          description: Role created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACRole'
        '400':
          description: Invalid body or missing name
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/global-roles/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacGlobalRolesById
      summary: Get a global RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The global role.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACRole'
        '400':
          description: Invalid role ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "rbac:update"
      x-astronomer-route-class: public-api
      operationId: putRbacGlobalRolesById
      summary: Update a global RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `update` permission. `is_builtin` is ignored on update.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACRoleRequest'
      responses:
        '200':
          description: Role updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACRole'
        '400':
          description: Invalid ID or body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Update failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "rbac:delete"
      x-astronomer-route-class: public-api
      operationId: deleteRbacGlobalRolesById
      summary: Delete a global RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `delete` permission. Cascades to all bindings of this role.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Role deleted.
        '400':
          description: Invalid role ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/cluster-roles:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacClusterRoles
      summary: List cluster RBAC roles
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated list of cluster roles.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RBACRole'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:create"
      x-astronomer-route-class: public-api
      operationId: postRbacClusterRoles
      summary: Create a cluster RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `create` permission.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACRoleRequest'
      responses:
        '201':
          description: Role created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACRole'
        '400':
          description: Invalid body or missing name
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/cluster-roles/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacClusterRolesById
      summary: Get a cluster RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The cluster role.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACRole'
        '400':
          description: Invalid role ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:update"
      x-astronomer-route-class: public-api
      operationId: putRbacClusterRolesById
      summary: Update a cluster RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `update` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACRoleRequest'
      responses:
        '200':
          description: Role updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACRole'
        '400':
          description: Invalid ID or body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Update failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:delete"
      x-astronomer-route-class: public-api
      operationId: deleteRbacClusterRolesById
      summary: Delete a cluster RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `delete` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Role deleted.
        '400':
          description: Invalid role ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/project-roles:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacProjectRoles
      summary: List project RBAC roles
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated list of project roles.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RBACRole'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:create"
      x-astronomer-route-class: public-api
      operationId: postRbacProjectRoles
      summary: Create a project RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `create` permission.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACRoleRequest'
      responses:
        '201':
          description: Role created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACRole'
        '400':
          description: Invalid body or missing name
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/project-roles/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacProjectRolesById
      summary: Get a project RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The project role.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACRole'
        '400':
          description: Invalid role ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:update"
      x-astronomer-route-class: public-api
      operationId: putRbacProjectRolesById
      summary: Update a project RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `update` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACRoleRequest'
      responses:
        '200':
          description: Role updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACRole'
        '400':
          description: Invalid ID or body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Update failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:delete"
      x-astronomer-route-class: public-api
      operationId: deleteRbacProjectRolesById
      summary: Delete a project RBAC role
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `delete` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Role deleted.
        '400':
          description: Invalid role ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/global-bindings:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacGlobalBindings
      summary: List global role bindings
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission. Returns a plain array under `data` (not the paginated envelope); `limit`/`offset` page the underlying query.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of global role bindings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RBACGlobalRoleBinding'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "rbac:create"
      x-astronomer-route-class: public-api
      operationId: postRbacGlobalBindings
      summary: Create a global role binding
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `create` permission. Provide `user_id` or `group`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACBindingRequest'
      responses:
        '201':
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACGlobalRoleBinding'
        '400':
          description: Invalid body or role ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/global-bindings/{id}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "rbac:delete"
      x-astronomer-route-class: public-api
      operationId: deleteRbacGlobalBindingsById
      summary: Delete a global role binding
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `delete` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Binding deleted.
        '400':
          description: Invalid binding ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Binding not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/global-role-bindings:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacGlobalRoleBindings
      summary: List global role bindings (alias)
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Alias of `GET /api/v1/rbac/global-bindings`. Requires the `rbac` resource `read` permission.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of global role bindings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RBACGlobalRoleBinding'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:create"
      x-astronomer-route-class: public-api
      operationId: postRbacGlobalRoleBindings
      summary: Create a global role binding (alias)
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Alias of `POST /api/v1/rbac/global-bindings`. Requires api-token scope `rbac:write` and the `rbac` resource `create` permission.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACBindingRequest'
      responses:
        '201':
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACGlobalRoleBinding'
        '400':
          description: Invalid body or role ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/global-role-bindings/{id}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:delete"
      x-astronomer-route-class: public-api
      operationId: deleteRbacGlobalRoleBindingsById
      summary: Delete a global role binding (alias)
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Alias of `DELETE /api/v1/rbac/global-bindings/{id}`. Requires api-token scope `rbac:write` and the `rbac` resource `delete` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Binding deleted.
        '400':
          description: Invalid binding ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Binding not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/cluster-bindings:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacClusterBindings
      summary: List cluster role bindings
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission. Returns a plain array under `data`. Optionally filter by `cluster_id`.
      parameters:
        - name: cluster_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of cluster role bindings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RBACClusterRoleBinding'
        '400':
          description: Invalid cluster_id
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "rbac:create"
      x-astronomer-route-class: public-api
      operationId: postRbacClusterBindings
      summary: Create a cluster role binding
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `create` permission. `cluster_id` is required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACClusterBindingRequest'
      responses:
        '201':
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACClusterRoleBinding'
        '400':
          description: Invalid body, role ID, or cluster ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/cluster-bindings/{id}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "rbac:delete"
      x-astronomer-route-class: public-api
      operationId: deleteRbacClusterBindingsById
      summary: Delete a cluster role binding
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `delete` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Binding deleted.
        '400':
          description: Invalid binding ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Binding not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/cluster-role-bindings:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacClusterRoleBindings
      summary: List cluster role bindings (alias)
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Alias of `GET /api/v1/rbac/cluster-bindings`. Requires the `rbac` resource `read` permission.
      parameters:
        - name: cluster_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of cluster role bindings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RBACClusterRoleBinding'
        '400':
          description: Invalid cluster_id
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:create"
      x-astronomer-route-class: public-api
      operationId: postRbacClusterRoleBindings
      summary: Create a cluster role binding (alias)
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Alias of `POST /api/v1/rbac/cluster-bindings`. Requires api-token scope `rbac:write` and the `rbac` resource `create` permission.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACClusterBindingRequest'
      responses:
        '201':
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACClusterRoleBinding'
        '400':
          description: Invalid body, role ID, or cluster ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/cluster-role-bindings/{id}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:delete"
      x-astronomer-route-class: public-api
      operationId: deleteRbacClusterRoleBindingsById
      summary: Delete a cluster role binding (alias)
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Alias of `DELETE /api/v1/rbac/cluster-bindings/{id}`. Requires api-token scope `rbac:write` and the `rbac` resource `delete` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Binding deleted.
        '400':
          description: Invalid binding ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Binding not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/project-bindings:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacProjectBindings
      summary: List project role bindings
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission. Returns a plain array under `data`. Optionally filter by `project_id`.
      parameters:
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of project role bindings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RBACProjectRoleBinding'
        '400':
          description: Invalid project_id
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "rbac:create"
      x-astronomer-route-class: public-api
      operationId: postRbacProjectBindings
      summary: Create a project role binding
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `create` permission. `project_id` is required. Subject to per-project member and per-user project quota caps when the quota enforcer is configured.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACProjectBindingRequest'
      responses:
        '201':
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACProjectRoleBinding'
        '400':
          description: Invalid body, role ID, or project ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '429':
          description: Project member or user-project quota exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/project-bindings/{id}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "rbac:delete"
      x-astronomer-route-class: public-api
      operationId: deleteRbacProjectBindingsById
      summary: Delete a project role binding
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires api-token scope `rbac:write` and the `rbac` resource `delete` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Binding deleted.
        '400':
          description: Invalid binding ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Binding not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/project-role-bindings:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacProjectRoleBindings
      summary: List project role bindings (alias)
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Alias of `GET /api/v1/rbac/project-bindings`. Requires the `rbac` resource `read` permission.
      parameters:
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of project role bindings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RBACProjectRoleBinding'
        '400':
          description: Invalid project_id
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: List failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:create"
      x-astronomer-route-class: public-api
      operationId: postRbacProjectRoleBindings
      summary: Create a project role binding (alias)
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Alias of `POST /api/v1/rbac/project-bindings`. Requires api-token scope `rbac:write` and the `rbac` resource `create` permission.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACProjectBindingRequest'
      responses:
        '201':
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACProjectRoleBinding'
        '400':
          description: Invalid body, role ID, or project ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '429':
          description: Quota exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Create failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/project-role-bindings/{id}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token rbac:write plus rbac:delete"
      x-astronomer-route-class: public-api
      operationId: deleteRbacProjectRoleBindingsById
      summary: Delete a project role binding (alias)
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Alias of `DELETE /api/v1/rbac/project-bindings/{id}`. Requires api-token scope `rbac:write` and the `rbac` resource `delete` permission.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Binding deleted.
        '400':
          description: Invalid binding ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing scope or permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Binding not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/my-roles:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacMyRoles
      summary: Get the current user's effective role bindings
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Any authenticated user. No special RBAC permission required.
      responses:
        '200':
          description: The caller's bindings with denormalised role rules.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          user_id:
                            type: string
                          bindings:
                            type: array
                            items:
                              type: object
                              properties:
                                user_id:
                                  type: string
                                group:
                                  type: string
                                cluster_id:
                                  type: string
                                  nullable: true
                                project_id:
                                  type: string
                                  nullable: true
                                rules:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      resource:
                                        type: string
                                      verbs:
                                        type: array
                                        items:
                                          type: string
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to load bindings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/my-roles/check:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacMyRolesCheck
      summary: Check whether the current user has a permission
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: 'Any authenticated user. Evaluates the caller''s bindings against the given resource/verb (optionally scoped by cluster_id/project_id). When the RBAC engine is not configured the check returns `allowed: true`.'
      parameters:
        - name: resource
          in: query
          required: true
          schema:
            type: string
        - name: verb
          in: query
          required: true
          schema:
            type: string
        - name: cluster_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Permission decision.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          allowed:
                            type: boolean
                          resource:
                            type: string
                          verb:
                            type: string
                          cluster_id:
                            type: string
                          project_id:
                            type: string
        '400':
          description: resource and verb are required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to load bindings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/my-permissions:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacMyPermissions
      summary: Get the current user's effective permissions
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Any authenticated user. Returns the caller's bindings and the merged, deduplicated permission grants, optionally evaluated against a selected context (cluster_id/project_id/namespace query params).
      parameters:
        - name: cluster_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: namespace
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Effective permissions for the caller.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RBACEffectivePermissionsEnvelope'}
        '400':
          description: Invalid context (cluster_id/project_id/namespace)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to load bindings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: RBAC binding lookup not configured
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/effective-permissions/{user_id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacEffectivePermissionsByUserId
      summary: Get effective permissions for a user
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission. Returns the target user's bindings and merged permission grants.
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cluster_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: project_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: namespace
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Effective permissions for the user.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RBACEffectivePermissionsEnvelope'}
        '400':
          description: Invalid user_id or context
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to load bindings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: RBAC binding lookup not configured
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/permission-preview:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "rbac:read"
      x-astronomer-route-class: public-api
      operationId: postRbacPermissionPreview
      summary: Preview the permissions implied by a role, template, or rule set
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Requires the `rbac` resource `read` permission. Exactly one of `template_name`, `role_id`, or `rules` must be supplied; `scope` defaults to `global`. Returns the resolved grants, computed risk level, sensitive flags, and warnings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RBACPermissionPreviewRequest'
      responses:
        '200':
          description: Preview result.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RBACPermissionPreviewEnvelope'}
        '400':
          description: Invalid body, scope, or preview inputs
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing rbac read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/templates:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacTemplates
      summary: List built-in RBAC role templates
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Any authenticated user. Static catalog metadata; no RBAC permission required.
      responses:
        '200':
          description: All templates in stable order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          templates:
                            type: array
                            items:
                              $ref: '#/components/schemas/RBACTemplate'
                          count:
                            type: integer
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Template catalog not loaded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/rbac/templates/{name}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getRbacTemplatesByName
      summary: Get a built-in RBAC role template by name
      tags:
        - rbac
      security:
        - bearerJWT: []
      description: Any authenticated user. Static catalog metadata; no RBAC permission required.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: The template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/RBACTemplate'
        '400':
          description: Template name is required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Template not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Template catalog not loaded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/change-password/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated current user"
      x-astronomer-route-class: public-api
      operationId: postAuthChangePassword
      summary: Change the authenticated user's password
      description: |
        Verifies the caller's current password, hashes the new one with bcrypt,
        and persists it. Clears the `must_change_password` flag on success.
        Response is NOT wrapped in the standard data envelope.
      tags:
        - auth
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - current_password
                - new_password
              properties:
                current_password:
                  type: string
                  format: password
                new_password:
                  type: string
                  format: password
      responses:
        '200':
          description: Password updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  detail:
                    type: string
                    example: Password updated
                  must_change_password:
                    type: boolean
                    example: false
        '400':
          description: Invalid body or missing fields.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Not authenticated or current password incorrect.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Password updates not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/refresh/:
    post:
      x-astronomer-auth: "public"
      x-astronomer-rbac: "public"
      x-astronomer-route-class: public-api
      operationId: postAuthRefresh
      summary: Exchange a refresh token for a new access/refresh pair
      description: |
        PUBLIC. Accepts `{refresh}` in the body, or falls back to the
        `astronomer_refresh` cookie (CSRF-validated). Returns a new token pair.
      tags:
        - auth
      security: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                refresh:
                  type: string
                  description: Refresh JWT. Optional when the refresh cookie is present.
      responses:
        '200':
          description: New token pair (raw, unwrapped).
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                    description: New access JWT
                  refresh:
                    type: string
                    description: New refresh JWT
        '401':
          description: Missing/invalid/expired refresh token, wrong token type, or CSRF required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Token generation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/logout-done/:
    get:
      x-astronomer-auth: "public"
      x-astronomer-rbac: "public"
      x-astronomer-route-class: public-api
      operationId: getAuthLogoutDone
      summary: SSO single-logout landing endpoint
      description: |
        PUBLIC. The post_logout_redirect_uri the IdP bounces back to after
        RP-initiated logout. Sets a one-shot `astro_logged_out` cookie and
        issues a 303 redirect to `/dashboard/login`.
      tags:
        - auth
      security: []
      responses:
        '303':
          description: Redirect to the SPA login screen.
          headers:
            Location:
              schema:
                type: string
                example: /dashboard/login
  /api/v1/auth/password-reset/request/:
    post:
      x-astronomer-auth: "public by design; email address and reset token flow is the proof boundary"
      x-astronomer-rbac: "not applicable"
      x-astronomer-route-class: public-api
      x-astronomer-bodyless-success: true
      operationId: postAuthPasswordResetRequest
      x-astronomer-async-202-exception:
        reason: privacy_preserving_acknowledgement
      summary: Request a password-reset email
      description: |
        PUBLIC, rate-limited. ALWAYS returns 202 regardless of whether the
        email matched a user (email-enumeration safe). When matched and the
        reset store + email notifier are wired, an emailed reset link is sent.
      tags:
        - auth
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
      responses:
        '202':
          description: Accepted. No body. Returned in all cases (including unknown email or internal failure).
  /api/v1/auth/password-reset/complete/:
    post:
      x-astronomer-auth: "public by design; reset token in request body is the credential"
      x-astronomer-rbac: "not applicable"
      x-astronomer-route-class: public-api
      operationId: postAuthPasswordResetComplete
      summary: Complete a password reset using an emailed token
      description: |
        PUBLIC, rate-limited. Validates the token (unused, unexpired, password
        unchanged since issue), updates the password, wipes outstanding reset
        tokens, and invalidates existing sessions. Response is unwrapped.
      tags:
        - auth
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - token
                - new_password
              properties:
                token:
                  type: string
                  description: Opaque reset token from the email link.
                new_password:
                  type: string
                  format: password
                  minLength: 8
                  maxLength: 72
      responses:
        '200':
          description: Password updated (raw, unwrapped).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Password updated
        '400':
          description: Missing fields, invalid/expired/used token, or password length out of range.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Hash or update failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Password reset is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/tokens/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAuthTokens
      summary: List the authenticated user's API tokens
      description: |
        Paginated (DRF-style, always HTTP 200). Plaintext token values are
        never returned in the list.
      tags:
        - auth
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
          description: Page size.
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
          description: Page offset.
      responses:
        '200':
          description: Paginated list of API tokens.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ApiTokenListItem'
        '401':
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Token management not configured or listing failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated current user plus token quota"
      x-astronomer-route-class: public-api
      operationId: postAuthTokens
      summary: Create a new API token
      description: |
        Returns the plaintext token value ONCE in the response. Validates the
        optional CIDR allowlist; subject to the per-user token quota when
        enforcement is wired (429 on hard-exceed).
      tags:
        - auth
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                expires_in_days:
                  type: integer
                  description: 0 or omitted = no expiry.
                scopes:
                  type: array
                  items:
                    type: string
                allowed_cidrs:
                  type: string
                  description: Comma-separated CIDR/IP allowlist. Empty = no IP restriction.
      responses:
        '201':
          description: Token created. The `token` field carries the plaintext, shown only here.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/ApiTokenCreated'
        '400':
          description: Missing name or invalid allowed_cidrs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '429':
          description: Per-user token quota exceeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Token management not configured or creation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/tokens/{id}/:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated current user and token ownership"
      x-astronomer-route-class: public-api
      operationId: deleteAuthTokensById
      summary: Revoke an API token
      description: Revokes a token owned by the authenticated user. 404 when the token does not exist or belongs to another user.
      tags:
        - auth
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Token revoked. No content.
        '400':
          description: Invalid token ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Token not found (or not owned by caller).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Token management not configured or revoke failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/totp/verify/:
    post:
      x-astronomer-auth: "public by design; signed challenge token and TOTP/recovery code are the credentials"
      x-astronomer-rbac: "not applicable"
      x-astronomer-route-class: public-api
      operationId: postAuthTotpVerify
      summary: Verify a TOTP/recovery code and obtain a session
      description: |
        PUBLIC, rate-limited. Proof of identity is the `challenge_token` issued
        by Login. On success returns the same shape as Login (token, refresh,
        user). Tries the TOTP code first, then falls back to a recovery code
        unless `use_recovery` is set.
      tags:
        - auth
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - challenge_token
                - code
              properties:
                challenge_token:
                  type: string
                  description: Short-lived TOTP challenge JWT from Login.
                code:
                  type: string
                  description: 6-digit TOTP code or a recovery code.
                use_recovery:
                  type: boolean
                  description: Force recovery-code path (e.g. lost device).
      responses:
        '200':
          description: Session pair + user (same shape as Login).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '400':
          description: Invalid body or missing fields.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Invalid challenge, wrong purpose, not enrolled, or bad code.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Stored secret unreadable or token generation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/totp/enroll/start/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated current user"
      x-astronomer-route-class: public-api
      operationId: postAuthTotpEnrollStart
      summary: Begin TOTP enrollment
      description: |
        Generates a fresh secret, otpauth URL, and QR PNG, parking the
        encrypted secret in a short-lived challenge. Nothing is persisted until
        the confirm step.
      tags:
        - auth
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Enrollment package (raw object).
          content:
            application/json:
              schema:
                type: object
                properties:
                  otpauth_url:
                    type: string
                  qr_data_url:
                    type: string
                    description: 'data: URL PNG of the QR code.'
                  challenge_token:
                    type: string
                    description: 5-minute purpose-bound JWT to echo back on confirm.
                  challenge:
                    type: string
                    description: Opaque encrypted-secret blob to echo back on confirm.
                  issuer:
                    type: string
        '401':
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Secret/QR generation or token minting failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: TOTP not configured (no encryptor).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/totp/enroll/confirm/:
    post:
      x-astronomer-auth: "bearer/session auth plus enrollment challenge token"
      x-astronomer-rbac: "authenticated current user"
      x-astronomer-route-class: public-api
      operationId: postAuthTotpEnrollConfirm
      summary: Confirm TOTP enrollment
      description: |
        Validates the challenge, verifies the supplied code, persists the
        enrollment, and returns 10 one-time recovery codes (shown ONCE).
      tags:
        - auth
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - challenge_token
                - challenge
                - code
              properties:
                challenge_token:
                  type: string
                challenge:
                  type: string
                code:
                  type: string
                  description: 6-digit code from the authenticator app.
      responses:
        '200':
          description: Enrollment confirmed; recovery codes returned once.
          content:
            application/json:
              schema:
                type: object
                properties:
                  recovery_codes:
                    type: array
                    items:
                      type: string
                  enrolled:
                    type: boolean
                    example: true
        '400':
          description: Invalid body, missing fields, malformed challenge, or invalid code.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Not authenticated or invalid/mismatched challenge.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Persist or recovery-code generation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: TOTP not configured (no encryptor).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/totp/disable/:
    post:
      x-astronomer-auth: "bearer/session auth plus current password or valid TOTP proof"
      x-astronomer-rbac: "authenticated current user"
      x-astronomer-route-class: public-api
      operationId: postAuthTotpDisable
      summary: Disable TOTP for the authenticated user
      description: |
        Requires BOTH the current password AND a valid TOTP code. Wipes the
        enrollment and recovery codes. Response is unwrapped.
      tags:
        - auth
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - password
                - code
              properties:
                password:
                  type: string
                  format: password
                code:
                  type: string
      responses:
        '200':
          description: TOTP disabled (raw, unwrapped).
          content:
            application/json:
              schema:
                type: object
                properties:
                  detail:
                    type: string
                    example: TOTP disabled
        '400':
          description: Invalid body or missing fields.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Not authenticated, wrong password, or invalid code.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: User not found or TOTP not enabled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Secret unreadable or disable persist failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/totp/recovery-codes/regenerate/:
    post:
      x-astronomer-auth: "bearer/session auth plus current password or valid TOTP proof"
      x-astronomer-rbac: "authenticated current user"
      x-astronomer-route-class: public-api
      operationId: postAuthTotpRecoveryCodesRegenerate
      summary: Regenerate TOTP recovery codes
      description: Requires a fresh TOTP code (not a recovery code). Replaces the existing recovery sheet and returns the new codes once.
      tags:
        - auth
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
                  description: Current 6-digit TOTP code.
      responses:
        '200':
          description: New recovery codes (shown once).
          content:
            application/json:
              schema:
                type: object
                properties:
                  recovery_codes:
                    type: array
                    items:
                      type: string
        '400':
          description: Invalid body or missing code.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Not authenticated or invalid code.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: TOTP not enabled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Secret unreadable or persist failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/totp/status/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAuthTotpStatus
      summary: Get TOTP enrollment status
      description: Cheap status read for the account-security page. When not enrolled, returns `{enrolled:false}`.
      tags:
        - auth
      security:
        - bearerJWT: []
      responses:
        '200':
          description: TOTP status (raw object).
          content:
            application/json:
              schema:
                type: object
                properties:
                  enrolled:
                    type: boolean
                  last_used_at:
                    type: string
                    format: date-time
                    nullable: true
                  recovery_codes_remaining:
                    type: integer
                    format: int64
        '401':
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/users/{id}/disable-totp/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: postAdminUsersByIdDisableTotp
      summary: Admin force-disable TOTP for a user
      description: |
        Superuser-gated. For the lost-device case where the user cannot satisfy
        the password+code Disable requirement. Wipes the target user's
        enrollment and recovery codes. Response is unwrapped.
      tags:
        - auth
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Target user ID.
      responses:
        '200':
          description: TOTP disabled for the user (raw, unwrapped).
          content:
            application/json:
              schema:
                type: object
                properties:
                  detail:
                    type: string
                    example: TOTP disabled for user
        '400':
          description: Invalid user ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Superuser required (requireScope admin + handler superuser gate).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target user not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Disable persist failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/dex/connector-types/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAuthDexConnectorTypes
      summary: List supported Dex connector types
      description: |
        Returns the connector-type registry (oidc, okta, microsoft, github,
        gitlab, bitbucket, google, saml, ldap, oauth) with each type's required,
        optional, secret, and nested field requirements for the UI wizard.
      tags:
        - auth
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Connector-type catalog.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/DexConnectorType'
  /api/v1/auth/dex/connectors/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAuthDexConnectors
      summary: List Dex connectors
      description: Returns every persisted Dex connector. Secret config fields are redacted (blank with a `__<field>_set` marker).
      tags:
        - auth
      security:
        - bearerJWT: []
      responses:
        '200':
          description: List of connectors.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/DexConnector'
        '500':
          description: Listing failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated settings surface; explicit settings RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: postAuthDexConnectors
      summary: Create a Dex connector
      description: Validates the config against the connector-type registry, encrypts secret fields, and persists the row.
      tags:
        - auth
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DexConnectorRequest'
      responses:
        '201':
          description: Connector created (secrets redacted in the response).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/DexConnector'
        '400':
          description: Invalid body, missing name, unknown type, or failed field validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: A connector with that name already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Encrypt, encode, or persist failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/dex/connectors/{id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAuthDexConnectorsById
      summary: Get a Dex connector
      tags:
        - auth
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Connector (secrets redacted).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/DexConnector'
        '400':
          description: Invalid connector ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Connector not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated settings surface; explicit settings RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: patchAuthDexConnectorsById
      summary: Update a Dex connector
      description: |
        Partial update; omitted fields keep their current value. When `config`
        is present it is treated as a full replacement, except secret fields
        left empty are preserved from the existing row.
      tags:
        - auth
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DexConnectorRequest'
      responses:
        '200':
          description: Connector updated (secrets redacted).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/DexConnector'
        '400':
          description: Invalid ID/body, unknown type, or field validation failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Connector not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Encrypt, encode, or update failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated settings surface; explicit settings RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: deleteAuthDexConnectorsById
      summary: Delete a Dex connector
      tags:
        - auth
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Connector deleted.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          deleted:
                            type: string
                            format: uuid
        '400':
          description: Invalid connector ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Connector not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Delete failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/dex/settings/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAuthDexSettings
      summary: Get Dex singleton settings
      description: Returns the singleton Dex settings row. On a fresh install (no row), returns zero-value defaults with `configured:false`.
      tags:
        - auth
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Dex settings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/DexSettings'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated settings surface; explicit settings RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: putAuthDexSettings
      summary: Upsert Dex singleton settings
      description: Upserts the Dex settings row. `issuer_url` is required. For bundled Dex, this endpoint binds only issuer and target cluster; namespace, chart release, Deployment, Service, and runtime Secret are immutable chart-derived identity. Operator-managed Dex may set the custom identity fields. The deprecated `configmap_name` request alias is accepted for one compatibility release but never causes a ConfigMap write.
      tags:
        - auth
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DexSettingsRequest'
      responses:
        '200':
          description: Settings saved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/DexSettings'
        '400':
          description: Invalid body, missing issuer_url, or invalid cluster_id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Save failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/dex/apply/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated settings surface; explicit settings RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: postAuthDexApply
      summary: Queue a durable Dex runtime apply
      description: |
        Atomically records the requested generation, tunnel-worker task, and
        mandatory audit intent. The worker owns all Kubernetes reconciliation.
        Identical actor-bound key replays return the original operation.
      tags:
        - auth
      security:
        - bearerJWT: []
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Durable operation accepted; no Kubernetes call is made before this response.
          headers:
            Location: {description: Exact operation status resource, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/DexOperation'
        '400':
          description: Settings not configured, missing issuer_url, or missing cluster_id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Connector listing or config render failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Durable operation persistence or encryption unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/dex/register-as-sso/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated settings surface; explicit settings RBAC audit remains open"
      x-astronomer-route-class: public-api
      operationId: postAuthDexRegisterAsSso
      summary: Queue durable Dex SSO registration
      description: |
        Atomically stages encrypted desired client settings, disables live SSO
        for the new generation, and records the operation, worker task, and
        mandatory audit intent. The worker verifies the runtime before enabling SSO.
      tags:
        - auth
      security:
        - bearerJWT: []
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                client_id:
                  type: string
                  default: astronomer
                client_secret:
                  type: string
                  format: password
                  description: Plaintext; encrypted before storage.
                display_name:
                  type: string
                  default: Sign in with Dex
      responses:
        '202':
          description: Desired SSO state and its durable reconcile operation were committed atomically.
          headers:
            Location: {description: Exact operation status resource, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/DexOperation'
        '400':
          description: Invalid body or Dex settings not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Encrypt or save failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Durable operation persistence or encryption unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/dex/operations/{operation_id}/:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin SSO update permission"
      x-astronomer-route-class: public-api
      operationId: getAuthDexOperation
      summary: Get exact Dex operation status
      tags: [auth]
      security:
        - bearerJWT: []
      parameters:
        - name: operation_id
          in: path
          required: true
          schema: {type: string, format: uuid}
      responses:
        '200':
          description: Durable operation state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/DexOperation'
        '404':
          description: Operation not found.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
  /api/v1/users:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getUsers
      summary: List users
      tags:
        - users-settings
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated list of users. Each item is the UI-shaped user object (not the full User schema).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/UsersSettingsUserListItem'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token admin scope plus users:create"
      x-astronomer-route-class: public-api
      operationId: postUsers
      summary: Create a user
      tags:
        - users-settings
      description: Requires the `admin` API-token scope (requireScope) plus users:create RBAC.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - password
              properties:
                email:
                  type: string
                  format: email
                username:
                  type: string
                  description: Defaults to email when omitted.
                first_name:
                  type: string
                last_name:
                  type: string
                password:
                  type: string
                  format: password
                is_active:
                  type: boolean
                  nullable: true
                  description: Defaults to true.
                is_staff:
                  type: boolean
                is_superuser:
                  type: boolean
      responses:
        '201':
          description: Created user (UI-shaped object).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/UsersSettingsUserListItem'
        '400':
          description: Validation error (missing email/password or invalid body).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/users/{id}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getUsersById
      summary: Get a user by ID
      tags:
        - users-settings
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: User (UI-shaped object).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/UsersSettingsUserListItem'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token admin scope plus users:update"
      x-astronomer-route-class: public-api
      operationId: putUsersById
      summary: Update a user (full)
      tags:
        - users-settings
      description: Requires the `admin` API-token scope plus users:update RBAC. Empty/omitted fields keep their current value.
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UsersSettingsUpdateUserRequest'
      responses:
        '200':
          description: Updated user (UI-shaped object).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/UsersSettingsUserListItem'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token admin scope plus users:update"
      x-astronomer-route-class: public-api
      operationId: patchUsersById
      summary: Update a user (partial)
      tags:
        - users-settings
      description: Same handler as PUT. Requires the `admin` API-token scope plus users:update RBAC.
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UsersSettingsUpdateUserRequest'
      responses:
        '200':
          description: Updated user (UI-shaped object).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/UsersSettingsUserListItem'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token admin scope plus users:delete"
      x-astronomer-route-class: public-api
      operationId: deleteUsersById
      summary: Delete a user
      tags:
        - users-settings
      description: Requires the `admin` API-token scope plus users:delete RBAC.
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: User deleted (no content).
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/users/{id}/reset-password:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin user-management route"
      x-astronomer-route-class: public-api
      operationId: postUsersByIdResetPassword
      summary: Reset a user's password
      tags:
        - users-settings
      description: |
        Requires the `admin` API-token scope plus users:update RBAC. Body is optional; when `password` is omitted a temporary password is generated and returned once in `temporary_password`.
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                password:
                  type: string
                  format: password
      responses:
        '200':
          description: Password reset result. `temporary_password` present only when generated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          success:
                            type: boolean
                          message:
                            type: string
                          temporary_password:
                            type: string
                            description: Returned exactly once, only when a password was auto-generated.
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/users/{id}/force-logout:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: postAdminUsersByIdForceLogout
      summary: Force-logout a user (invalidate all sessions)
      tags:
        - users-settings
      description: |
        Stamps tokens_invalidated_at so every JWT issued before now is rejected, and clears any SSO sessions. Requires the `admin` API-token scope and superuser (gated in-handler). Response is a bare (un-enveloped) object.
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Sessions invalidated. Un-enveloped body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                  tokens_invalidated_at:
                    type: string
                    format: date-time
                  sso_sessions_cleared:
                    type: integer
                  sso_backchannel_ok:
                    type: integer
                  sso_backchannel_failed:
                    type: integer
        '403':
          description: Superuser privileges required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/users/{id}/unlock:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: postAdminUsersByIdUnlock
      summary: Unlock a locked-out user account
      tags:
        - users-settings
      description: |
        Clears failed_login_count, locked_until and locked_reason. Requires the `admin` API-token scope and superuser (gated in-handler). Response is a bare (un-enveloped) object.
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: User unlocked. Un-enveloped body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        '403':
          description: Superuser privileges required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/users/{id}/resync-groups/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: postAdminUsersByIdResyncGroups
      summary: Reconcile a user's role bindings from the last IdP group snapshot
      description: |
        Replays the target user's last persisted IdP-group snapshot against the
        current group mappings and invalidates their RBAC cache when bindings
        change. The response is a bare, un-enveloped object.
      tags:
        - users-settings
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Group-derived role bindings reconciled. Un-enveloped body.
          content:
            application/json:
              schema:
                type: object
                required: [success, user_id, added_count, removed_count, groups]
                properties:
                  success:
                    type: boolean
                  user_id:
                    type: string
                    format: uuid
                  added_count:
                    type: integer
                  removed_count:
                    type: integer
                  groups:
                    type: array
                    items:
                      type: string
        '400':
          description: Invalid user ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Admin scope or superuser privileges required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: User has no persisted IdP-group snapshot yet.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Snapshot loading or group reconciliation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/scim-tokens/:
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: postAdminScimTokens
      summary: Mint a SCIM provisioning bearer token
      tags:
        - users-settings
      description: |
        Generates a new `astro_scim_<random>` bearer token for the
        `/scim/v2/*` provisioning chain. The plaintext token is returned
        exactly ONCE in this response under `token`; only its SHA-256 hash
        is persisted. Superuser only (gated in-handler).
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  description: Operator-facing label for the token.
      responses:
        '201':
          description: Token created. The plaintext `token` is shown only here.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SCIMTokenCreatedEnvelope'
        '400':
          description: Invalid body or missing name.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Superuser privileges required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getAdminScimTokens
      summary: List SCIM provisioning tokens (metadata only)
      tags:
        - users-settings
      description: |
        Returns metadata for every SCIM token. The secret is never
        included — only id, name, display prefix, and timestamps.
        Superuser only (gated in-handler).
      security:
        - bearerJWT: []
      responses:
        '200':
          description: List of SCIM token metadata.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SCIMTokenListEnvelope'
        '403':
          description: Superuser privileges required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/scim-tokens/{id}/:
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: deleteAdminScimTokensById
      summary: Revoke a SCIM provisioning token
      tags:
        - users-settings
      description: Deletes the SCIM token by id. Superuser only (gated in-handler).
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Token revoked.
        '400':
          description: Invalid token id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Superuser privileges required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getSettings
      summary: Get general settings (alias of /settings/general)
      tags:
        - users-settings
      description: Resolves to the same handler as GET /settings/general.
      security:
        - bearerJWT: []
      responses:
        '200':
          description: General platform settings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/UsersSettingsGeneral'
  /api/v1/settings/general:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getSettingsGeneral
      summary: Get general settings
      tags:
        - users-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: General platform settings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/UsersSettingsGeneral'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token admin scope plus settings:update"
      x-astronomer-route-class: public-api
      operationId: putSettingsGeneral
      summary: Update general settings
      tags:
        - users-settings
      description: |
        Requires the `admin` API-token scope plus settings:update RBAC. Only platformName has a backing column; the other toggles are echoed back. metricsCollection maps to the telemetry_enabled column.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                platformName:
                  type: string
                  nullable: true
                platform_name:
                  type: string
                  nullable: true
                  description: Snake-case alias for platformName.
                agentHeartbeatInterval:
                  type: integer
                  nullable: true
                defaultSessionTimeout:
                  type: integer
                  nullable: true
                enableAuditLogging:
                  type: boolean
                  nullable: true
                metricsCollection:
                  type: boolean
                  nullable: true
      responses:
        '200':
          description: Updated general settings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/UsersSettingsGeneral'
  /api/v1/settings/audit-logs:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "audit_logs:read or audit_logs:list"
      x-astronomer-route-class: public-api
      operationId: getSettingsAuditLogs
      summary: List audit logs
      tags:
        - users-settings
      description: Requires auditLogs:read or auditLogs:list RBAC (requireAnyPermission).
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated audit-log entries.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/UsersSettingsAuditLogEntry'
  /api/v1/settings/sso:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getSettingsSso
      summary: List enabled SSO providers
      tags:
        - users-settings
      description: Public-readable. Response is a bare JSON array (not enveloped).
      responses:
        '200':
          description: SSO providers.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UsersSettingsSSOProvider'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token admin scope plus sso:create"
      x-astronomer-route-class: public-api
      operationId: postSettingsSso
      summary: Create an SSO provider
      tags:
        - users-settings
      description: Requires the `admin` API-token scope plus sso:create RBAC.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
                - name
                - config
              properties:
                type:
                  type: string
                  enum:
                    - github
                    - google
                    - oidc
                name:
                  type: string
                enabled:
                  type: boolean
                  nullable: true
                  description: Defaults to true.
                config:
                  type: object
                  required:
                    - client_id
                    - client_secret
                  properties:
                    client_id:
                      type: string
                    client_secret:
                      type: string
                    metadata_url:
                      type: string
                      description: Required for oidc.
                    allowed_organizations:
                      type: string
                      description: Comma-separated.
                    auto_create_users:
                      type: boolean
                      nullable: true
                      description: Defaults to true.
      responses:
        '201':
          description: Created SSO provider.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/UsersSettingsSSOProvider'
        '400':
          description: Validation / unsupported provider type.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: A provider with this key already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/sso/{id}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "API token admin scope plus sso:delete"
      x-astronomer-route-class: public-api
      operationId: deleteSettingsSsoById
      summary: Delete an SSO provider
      tags:
        - users-settings
      description: Requires the `admin` API-token scope plus sso:delete RBAC.
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Provider deleted (no content).
        '404':
          description: SSO provider not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/tokens:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getSettingsTokens
      summary: List the caller's API tokens
      tags:
        - users-settings
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Paginated API tokens for the authenticated user (no plaintext).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/UsersSettingsTokenListItem'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated user token owner"
      x-astronomer-route-class: public-api
      operationId: postSettingsTokens
      summary: Create an API token
      tags:
        - users-settings
      description: Returns the plaintext token exactly once.
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                expires_in_days:
                  type: integer
                  description: 0 = never expires.
                scopes:
                  type: array
                  items:
                    type: string
                allowed_cidrs:
                  type: string
                  description: Comma-separated CIDR/IP allowlist; empty = no restriction.
      responses:
        '201':
          description: Created token, including the one-time plaintext.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/UsersSettingsCreatedToken'
        '400':
          description: Validation error (missing name / invalid allowed_cidrs).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '429':
          description: Per-user token quota exceeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/tokens/{id}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated user token owner"
      x-astronomer-route-class: public-api
      operationId: deleteSettingsTokensById
      summary: Revoke an API token
      tags:
        - users-settings
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Token revoked.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
        '404':
          description: Token not found (or not owned by the caller).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/activity:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getActivity
      summary: Activity feed
      tags:
        - users-settings
      description: |
        Recent activity derived from the audit log. Response is a bare JSON array (not enveloped). Public when the query store is unconfigured (returns an empty array).
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Activity items.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uuid
                    type:
                      type: string
                      example: system
                    action:
                      type: string
                    message:
                      type: string
                    resource:
                      type: string
                    timestamp:
                      type: string
                      format: date-time
  /api/v1/admin/agents/cluster-admin-posture:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminAgentsClusterAdminPosture
      summary: List clusters whose agent still resolves to the cluster-admin privilege profile
      description: |
        Read-only rollout aid. Enumerates every managed cluster whose stored agent privilege-profile annotation resolves to the cluster-admin (`admin`) profile, so operators can find and re-profile them. Superuser-gated inside the handler (401 anonymous / 403 non-superuser).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Posture report. `data` holds only the admin-profile clusters; `total_clusters` is the full scanned estate size.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          admin_profile_clusters:
                            type: integer
                          total_clusters:
                            type: integer
                          items:
                            type: array
                            items:
                              type: object
                              properties:
                                cluster_id:
                                  type: string
                                  format: uuid
                                cluster_name:
                                  type: string
                                cluster_status:
                                  type: string
                                is_local:
                                  type: boolean
                                privilege_profile:
                                  type: string
                                  example: admin
                              required:
                                - cluster_id
                                - cluster_name
                                - cluster_status
                                - is_local
                                - privilege_profile
                        required:
                          - admin_profile_clusters
                          - total_clusters
                          - items
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Cluster-agent inventory is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/charlie/commands/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: listCharlieCommands
      summary: List versioned Astronomer-owned Charlie chat shortcuts
      description: Commands are presentation and repeatable workflow metadata only. Every agent command still uses the active scope, mode, disclosed capabilities, approval rules, and audit boundary.
      tags: [charlie]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: The command catalog available to the authenticated user.
          content: {application/json: {schema: {$ref: '#/components/schemas/CharlieCommandCatalog'}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/charlie/threads/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus owner isolation"
      x-astronomer-route-class: public-api
      operationId: listCharlieThreads
      summary: List the authenticated user's current and archived Charlie conversations
      tags: [charlie]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Owner-scoped conversation metadata; content remains in Charlie.
          content:
            application/json:
              schema:
                type: object
                required: [threads]
                properties:
                  threads: {type: array, maxItems: 100, items: {$ref: '#/components/schemas/CharlieThreadMetadata'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/charlie/threads/active/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus owner isolation"
      x-astronomer-route-class: public-api
      operationId: getActiveCharlieThread
      summary: Get the authenticated user's active Charlie conversation
      tags: [charlie]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Active conversation metadata, or a null thread when none exists.
          content: {application/json: {schema: {type: object, additionalProperties: true}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/charlie/threads/new/:
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:create plus owner isolation"
      x-astronomer-route-class: public-api
      operationId: createCharlieThread
      summary: Archive the current conversation and start a clean one
      tags: [charlie]
      security: [{bearerJWT: []}]
      responses:
        '201':
          description: New owner-scoped active conversation metadata.
          content: {application/json: {schema: {type: object, additionalProperties: true}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/charlie/threads/messages/:
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:create plus owner and disclosed-resource authorization"
      x-astronomer-route-class: public-api
      operationId: createCharlieThreadMessage
      x-astronomer-async-202-exception:
        reason: interactive_message_acceptance
      summary: Send natural language or a validated slash command on the active conversation
      description: The server reparses visible slash text and rejects command identity, version, or argument drift before forwarding it through the Product Bridge.
      tags: [charlie]
      security: [{bearerJWT: []}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CharlieThreadMessageRequest'}}}
      responses:
        '202':
          description: Active thread metadata and the accepted Charlie turn receipt.
          content: {application/json: {schema: {type: object, additionalProperties: true}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charlie/threads/{thread_id}/history/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus owner isolation"
      x-astronomer-route-class: public-api
      operationId: getCharlieThreadHistory
      summary: Read stitched history for one owner-scoped Charlie conversation
      description: Archived transcripts are read-only by default and are never silently injected into a new turn.
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: thread_id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Retention-bounded redacted items stitched across the conversation's Charlie sessions.
          content: {application/json: {schema: {type: object, additionalProperties: true}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/charlie/sessions/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus live owner/resource authorization"
      x-astronomer-route-class: public-api
      operationId: listCharlieSessions
      summary: List Charlie sessions currently visible to the authenticated user
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 20}}
      responses:
        '200':
          description: Owner-filtered private metadata plus resource-authorized incident metadata; conversation content remains in Charlie.
          content:
            application/json:
              schema:
                type: object
                required: [sessions, mode]
                properties:
                  sessions: {type: array, maxItems: 100, items: {$ref: '#/components/schemas/CharlieSessionMetadata'}}
                  mode: {type: string, enum: [disabled, read_only, approval, auto]}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:create plus live read authorization for every disclosed resource"
      x-astronomer-route-class: public-api
      operationId: createCharlieSession
      summary: Create or replay a private Charlie session
      tags: [charlie]
      security: [{bearerJWT: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CharlieSessionCreateRequest'}
      responses:
        '201':
          description: Session created after local authorization was persisted.
          content:
            application/json:
              schema:
                type: object
                required: [session, replayed]
                properties:
                  session: {$ref: '#/components/schemas/CharlieSessionMetadata'}
                  replayed: {type: boolean}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charlie/sessions/{session_id}/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus live owner/resource authorization"
      x-astronomer-route-class: public-api
      operationId: getCharlieSession
      summary: Fetch an authorized Charlie session on demand
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: session_id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Local metadata plus authorized central session state.
          content:
            application/json:
              schema:
                type: object
                required: [session, remote]
                properties:
                  session: {$ref: '#/components/schemas/CharlieSessionMetadata'}
                  remote: {type: object, additionalProperties: true}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/charlie/sessions/{session_id}/history/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus live owner/resource authorization"
      x-astronomer-route-class: public-api
      operationId: getCharlieSessionHistory
      summary: Proxy retention-bounded Charlie history after live authorization
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: session_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {name: cursor, in: query, schema: {type: string, maxLength: 128}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 50}}
      responses:
        '200':
          description: Charlie-managed encrypted history; no local content copy is made.
          content:
            application/json:
              schema: {type: array, maxItems: 100, items: {type: object, additionalProperties: true}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/charlie/sessions/{session_id}/events/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus live owner/resource authorization before and during streaming"
      x-astronomer-route-class: public-api
      operationId: streamCharlieSessionEvents
      summary: Stream live events for one currently authorized Charlie session
      description: The server rechecks session ownership and disclosed-resource authorization before opening the bounded SSE stream.
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: session_id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: A bounded text/event-stream relayed from Charlie without local content persistence.
          content:
            text/event-stream:
              schema: {type: string}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charlie/sessions/{session_id}/messages/:
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:create plus live owner/resource authorization"
      x-astronomer-route-class: public-api
      operationId: createCharlieSessionMessage
      x-astronomer-async-202-exception:
        reason: interactive_message_acceptance
      summary: Submit one idempotent user turn to Charlie
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: session_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CharlieMessageRequest'}
      responses:
        '202':
          description: Central turn receipt, including replay of an existing client message ID.
          content: {application/json: {schema: {type: object, additionalProperties: true}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charlie/sessions/{session_id}/abort/:
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:create plus live owner/resource authorization"
      x-astronomer-route-class: public-api
      operationId: abortCharlieSession
      summary: Revoke local session authority before requesting central abort
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: session_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CharlieAbortRequest'}
      responses:
        '204': {description: Locally and centrally aborted.}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charlie/context/search/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus live product authorization for each returned resource"
      x-astronomer-route-class: public-api
      operationId: searchCharlieContext
      summary: Browse or search product-owned scopes available to Charlie
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: q, in: query, required: true, schema: {type: string, maxLength: 128}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 20, default: 20}}
      responses:
        '200':
          description: Only resource metadata the caller can currently read.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items: {type: array, maxItems: 20, items: {$ref: '#/components/schemas/CharlieContextSearchResult'}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/charlie/operations/{operation_id}:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus receipt owner and live resource authorization"
      x-astronomer-route-class: public-api
      operationId: getCharlieOperationStatus
      summary: Fetch metadata-only status for one authorized Charlie action receipt
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: operation_id, in: path, required: true, schema: {type: string, minLength: 1, maxLength: 255}}
      responses:
        '200':
          description: Action state with arguments, evidence, credentials, and raw results excluded.
          content: {application/json: {schema: {$ref: '#/components/schemas/CharlieOperationStatus'}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charlie/approvals/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read to reach the route; each candidate independently requires live session, account, charlie:approve, and target authorization before eligibility"
      x-astronomer-route-class: public-api
      operationId: listCharlieApprovals
      summary: List exact pending Charlie actions the current user may review
      description: Astronomer verifies each signed manifest and rechecks live session, account, charlie:approve, and target authorization before returning bounded display metadata.
      tags: [charlie]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Current approval candidates; manifest and signature material are not returned.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items: {type: array, maxItems: 200, items: {$ref: '#/components/schemas/CharlieApprovalSummary'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/charlie/approvals/{approval_id}/decision/:
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates; idempotent request UUID"
      x-astronomer-rbac: "charlie:approve plus live session, account, and underlying target permission rechecked before each decision"
      x-astronomer-route-class: public-api
      operationId: decideCharlieApproval
      summary: Approve or reject one exact signed Charlie action
      description: The product re-fetches and verifies the signed manifest, rederives scope and permissions, persists the exact authority row, and fails closed if Charlie does not confirm the same digest and state.
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: approval_id, in: path, required: true, schema: {type: string, minLength: 1, maxLength: 128}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CharlieApprovalDecisionRequest'}}}
      responses:
        '200':
          description: Final bounded decision state.
          content:
            application/json:
              schema:
                type: object
                required: [approval]
                properties:
                  approval: {$ref: '#/components/schemas/CharlieApprovalSummary'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {description: 'Approval is stale, ineligible, mismatched, or not confirmed; no action was authorized.', content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/charlie/findings/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus live authorization for every affected resource"
      x-astronomer-route-class: public-api
      operationId: listCharlieFindings
      summary: List bounded actionable Charlie finding summaries
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [open, acknowledged, dismissed, resolved]}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 20}}
      responses:
        '200':
          description: Only findings whose complete affected-resource scope the caller can currently read.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items: {type: array, maxItems: 100, items: {$ref: '#/components/schemas/CharlieFindingSummary'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/charlie/findings/{finding_id}/:
    get:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:read plus live authorization for every affected resource"
      x-astronomer-route-class: public-api
      operationId: getCharlieFinding
      summary: Fetch one finding and authorized Charlie-managed evidence on demand
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: finding_id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: Bounded local correlation plus optional central detail; central credentials and authorization references are never returned.
          content:
            application/json:
              schema:
                type: object
                required: [finding]
                properties:
                  finding: {$ref: '#/components/schemas/CharlieFindingDetail'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/charlie/findings/{finding_id}/acknowledge/:
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:update plus live authorization for every affected resource"
      x-astronomer-route-class: public-api
      operationId: acknowledgeCharlieFinding
      summary: Acknowledge one finding without executing its recommendation
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: finding_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CharlieFindingTransitionRequest'}}}
      responses:
        '200': {description: Finding acknowledged., content: {application/json: {schema: {type: object, required: [finding], properties: {finding: {$ref: '#/components/schemas/CharlieFindingDetail'}}}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Finding state changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/charlie/findings/{finding_id}/dismiss/:
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:update plus live authorization for every affected resource"
      x-astronomer-route-class: public-api
      operationId: dismissCharlieFinding
      summary: Dismiss one finding without executing its recommendation
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: finding_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CharlieFindingTransitionRequest'}}}
      responses:
        '200': {description: Finding dismissed., content: {application/json: {schema: {type: object, required: [finding], properties: {finding: {$ref: '#/components/schemas/CharlieFindingDetail'}}}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Finding state changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/charlie/findings/{finding_id}/start-remediation/:
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:update plus live authorization for every affected resource"
      x-astronomer-route-class: public-api
      operationId: startCharlieFindingRemediation
      summary: Record that product-owned manual remediation has started
      description: This lifecycle decision grants no Charlie execution authority.
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: finding_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CharlieFindingTransitionRequest'}}}
      responses:
        '200': {description: Remediation marked in progress., content: {application/json: {schema: {type: object, required: [finding], properties: {finding: {$ref: '#/components/schemas/CharlieFindingDetail'}}}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Finding state changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/charlie/findings/{finding_id}/request-verification/:
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:update plus live authorization for every affected resource"
      x-astronomer-route-class: public-api
      operationId: requestCharlieFindingVerification
      summary: Record manual completion and request product-owned verification
      description: Astronomer re-reads current state before resolution; this lifecycle decision grants no Charlie execution authority.
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: finding_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CharlieFindingTransitionRequest'}}}
      responses:
        '200': {description: Verification requested., content: {application/json: {schema: {type: object, required: [finding], properties: {finding: {$ref: '#/components/schemas/CharlieFindingDetail'}}}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Finding state changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/charlie/findings/{finding_id}/resolve/:
    post:
      x-astronomer-auth: "browser JWT only; feature and live connection gates"
      x-astronomer-rbac: "charlie:update plus live authorization for every affected resource"
      x-astronomer-route-class: public-api
      operationId: resolveCharlieFinding
      summary: Resolve one finding after independent operator verification
      tags: [charlie]
      security: [{bearerJWT: []}]
      parameters:
        - {name: finding_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CharlieFindingTransitionRequest'}}}
      responses:
        '200': {description: Finding resolved., content: {application/json: {schema: {type: object, required: [finding], properties: {finding: {$ref: '#/components/schemas/CharlieFindingDetail'}}}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Finding state changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/admin/charlie/onboarding/validate/:
    post:
      x-astronomer-auth: "bearer/session auth; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieOnboardingValidate
      summary: Verify a signed Charlie onboarding package locally
      description: |
        Prefer a Charlie endpoint plus one-time connect token. Validates the
        pinned schema, expiry, endpoint binding, central CA, artifact digests,
        and Ed25519 signature without contacting Charlie central.
      tags: [platform-admin-settings]
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CharlieOnboardingRequest'
      responses:
        '200':
          description: Content-free package verification status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CharlieOnboardingStatus'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '413':
          description: Onboarding request exceeds 256 KiB.
        '415':
          description: Content-Type is not application/json.
  /api/v1/admin/charlie/onboarding/consume/:
    post:
      x-astronomer-auth: "bearer/session auth; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieOnboardingConsume
      summary: Transactionally consume a verified Charlie onboarding package
      description: |
        Repeats local verification, creates installation-bound local trust,
        writes agent material directly to a Kubernetes Secret, and consumes the
        package only after all writes succeed. Replays do not recreate material.
      tags: [platform-admin-settings]
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CharlieOnboardingRequest'
      responses:
        '200':
          description: Content-free package consumption status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CharlieOnboardingStatus'
        '400':
          $ref: '#/components/responses/ErrorResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: Package could not be consumed safely and remains retryable.
        '413':
          description: Onboarding request exceeds 256 KiB.
        '415':
          description: Content-Type is not application/json.
  /api/v1/charlie/activation/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: charlieActivation
      summary: Whether Charlie is connected and user-facing surfaces should activate
      tags: [charlie]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Dormant until a signed package has been consumed.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [activated]
                properties:
                  activated: {type: boolean}
                  endpoint: {type: string, format: uri}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/admin/charlie/status/:
    get:
      x-astronomer-auth: "browser JWT only; remains available for safe inspection while feature.charlie is disabled"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieStatus
      summary: Read the redacted aggregate state of the Charlie integration
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200': {description: 'Connection, product-agent, and authoritative mode status.', content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminStatus'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/disconnect/:
    post:
      x-astronomer-auth: "browser JWT only; remains available while feature.charlie is disabled; exact confirmation phrase"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieDisconnect
      summary: Disconnect Charlie after typed confirmation
      description: |
        Deactivates the local Charlie connection after the operator types
        DISCONNECT CHARLIE. Charlie surfaces hide, mode is disabled, and a new
        signed onboarding package is required to reconnect. This is distinct
        from emergency disable, which fails closed while keeping the connection.
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminActionRequest'}}}}
      responses:
        '200': {description: 'Disconnected connection, product-agent, and mode status.', content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminStatus'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/mode/:
    patch:
      x-astronomer-auth: "browser JWT only; emergency disable remains available while feature.charlie is disabled; other changes require the feature; optimistic mode revision"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieModeUpdate
      summary: Change Charlie authority mode, emergency-disable it, or acknowledge disclosure
      description: Mode changes use an optimistic revision. Disclosure acknowledgement is a separate request shape and grants no authority by itself.
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CharlieModeRequest'}}}}
      responses:
        '200': {description: Current locally safe and centrally authoritative mode., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminMode'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Revision or mode prerequisites changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/kubernetes-visibility/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminCharlieKubernetesVisibilityGet
      summary: Read the product-owned Kubernetes observation boundary
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200': {description: Current Kubernetes visibility profile and hard prohibitions., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieKubernetesVisibility'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminCharlieKubernetesVisibilityUpdate
      summary: Replace the product-owned Kubernetes observation boundary
      description: A change closes admission, forces Charlie authority to disabled, clears capability acknowledgement, and never enables downstream targets or interactive Kubernetes transports.
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CharlieKubernetesVisibilityRequest'}}}}
      responses:
        '200': {description: Updated Kubernetes visibility profile., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieKubernetesVisibility'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: 'Revision, connection, or visibility prerequisites changed.', content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/trigger-rules/:
    get:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieTriggerRulesList
      summary: List Charlie automation trigger rules and current identity state
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200': {description: Bounded automation configuration., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminAutomation'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    post:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieTriggerRuleCreate
      summary: Create a Charlie automation trigger rule
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminTriggerRule'}}}}
      responses:
        '201': {description: Created trigger rule., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminTriggerRule'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Trigger-rule prerequisites changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/admin/charlie/alert-policy/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminCharlieAlertPolicyGet
      summary: Get product-owned Charlie finding alert policy
      description: Returns bounded local thresholds and channel references. Destination values, credentials, approvals, and action authority are never returned.
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200': {description: Current local alert policy and redacted channel choices., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminAlertPolicy'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminCharlieAlertPolicyUpdate
      summary: Replace product-owned Charlie finding alert policy
      description: Configures severity, dedupe, escalation, quiet hours, and references to local Astronomer notification channels. This endpoint grants no approval or dispatch authority.
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminAlertPolicyInput'}}}}
      responses:
        '200': {description: Updated local alert policy., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminAlertPolicy'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Channel references or policy bounds changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/alert-deliveries/:
    get:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieAlertDeliveryProofs
      summary: Read metadata-only delivery proof for one Charlie finding
      description: Returns only finding/delivery identifiers, lifecycle counts and timestamps, fixed template identity, and server-computed deep-link/content-free verdicts. Raw notification content, channel/destination metadata, dedupe buckets, and provider errors are never returned.
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      parameters:
        - {name: finding_id, in: query, required: true, schema: {type: string, format: uuid}}
      responses:
        '200': {description: Bounded current-deployment delivery proof., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminAlertDeliveryProofView'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/qualification/discovery/:
    post:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate; caller-selected capabilities are not accepted"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieDiscoveryQualification
      summary: Qualify Astronomer's fixed discovery compiler cases
      description: Operator-started, non-mutating qualification of one embedded mixed or malformed catalog candidate. Caller-controlled capability data is never accepted and current activation is never changed.
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CharlieDiscoveryQualificationRequest'}}}}
      responses:
        '200': {description: Fixed discovery generation verdict., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminDiscoveryQualification'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Qualification prerequisites are not active., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/trigger-rules/{rule_id}/:
    patch:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieTriggerRuleUpdate
      summary: Update or suppress one Charlie automation trigger rule
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      parameters:
        - {name: rule_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminTriggerRule'}}}}
      responses:
        '200': {description: Updated trigger rule., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminTriggerRule'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Trigger-rule state changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
    delete:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate; exact confirmation phrase"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieTriggerRuleDelete
      summary: Delete one Charlie automation trigger rule
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      parameters:
        - {name: rule_id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminActionRequest'}}}}
      responses:
        '204': {description: Trigger rule deleted.}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Trigger-rule state changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/admin/charlie/action-policies/{capability}/:
    put:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate; exact capability must remain independently allowlisted by Charlie"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieActionPolicyUpdate
      summary: Configure one bounded product-local Charlie automation policy
      description: Enabling a local policy requires the exact auto-eligible capability to be present in Charlie's independently reviewed central allowlist. This endpoint cannot add to that central allowlist.
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      parameters:
        - {name: capability, in: path, required: true, schema: {type: string, pattern: '^[a-z][a-z0-9_.:-]{0,127}$'}}
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminActionPolicyInput'}}}}
      responses:
        '200': {description: Current safe action policy., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminActionPolicy'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: 'Capability is not auto eligible, centrally allowlisted, or policy bounds are invalid.', content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/trigger-events/:
    get:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieTriggerEventsList
      summary: List bounded Charlie trigger lifecycle metadata
      description: Defaults to dead-letter events. Stored summary, fingerprint, origin references, session identifiers, and task payloads are never returned.
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      parameters:
        - {name: state, in: query, schema: {type: string, default: dead, enum: [pending, dispatching, dispatched, retry, dead, completed, suppressed]}}
        - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 20}}
      responses:
        '200':
          description: UI-safe trigger event metadata.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items: {type: array, maxItems: 100, items: {$ref: '#/components/schemas/CharlieAdminTriggerEvent'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/trigger-events/{event_id}/retry/:
    post:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate; UUID request id"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieTriggerEventRetry
      summary: Create one idempotent retry attempt for a dead-letter trigger
      description: The dead source event stays immutable. request_id identifies a fresh retry attempt that preserves the source fingerprint and creates a new stable session idempotency boundary.
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      parameters:
        - {name: event_id, in: path, required: true, schema: {type: string, format: uuid}}
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CharlieTriggerRetryRequest'}}}
      responses:
        '202':
          description: Retry attempt durably created with its outbox record.
          headers:
            Location: {description: Exact retry-event status resource, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/CharlieTriggerRetryReceipt'}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: 'Source is not dead, a retry is active, or integration state changed.', content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/trigger-events/{event_id}/:
    get:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieTriggerEventGet
      summary: Get one exact bounded Charlie trigger lifecycle record
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      parameters:
        - {name: event_id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '200':
          description: UI-safe trigger event metadata.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/CharlieAdminTriggerEvent'}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/access/:
    get:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieAccessGet
      summary: Preview the dedicated Charlie automation identity's effective access
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200': {description: Effective and explicitly granted permissions., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminAccess'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
    put:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieAccessUpdate
      summary: Enable or disable the dedicated Charlie automation identity
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAccessRequest'}}}}
      responses:
        '200': {description: Effective and explicitly granted permissions., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminAccess'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Access state changed., content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/charlie/diagnostics/run/:
    post:
      x-astronomer-auth: "browser JWT only; feature.charlie opt-in gate"
      x-astronomer-rbac: "admin API token scope plus charlie:manage"
      x-astronomer-route-class: public-api
      operationId: adminCharlieDiagnosticsRun
      summary: Run bounded redacted Charlie connectivity and policy diagnostics
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200': {description: Redacted diagnostic checks and correlation ID., content: {application/json: {schema: {$ref: '#/components/schemas/CharlieAdminDiagnostics'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/key-status:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminKeyStatus
      summary: Encryption and JWT signing key-rotation status
      description: |
        Returns the number of loaded encryption + JWT signing keys. A count > 1 means a key rotation is mid-flight. Superuser-gated inside the handler. Response is NOT wrapped in a data envelope (raw top-level object).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Live key counts.
          content:
            application/json:
              schema:
                type: object
                properties:
                  encryption_keys:
                    type: integer
                  jwt_keys:
                    type: integer
                  insecure_dev_keys:
                    type: array
                    items: {type: string}
                  as_of:
                    type: string
                    format: date-time
                required:
                  - encryption_keys
                  - jwt_keys
                  - insecure_dev_keys
                  - as_of
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/admin/emails:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminEmailsList
      summary: List email-message delivery audit rows
      description: |
        Paginated metadata-only view of the outbound email audit log (no message bodies). Superuser-gated inside the handler. Returns a custom `{data:{items,total,limit,offset}}` object (not the DRF paginated envelope).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 50
            minimum: 1
            maximum: 200
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        '200':
          description: Page of email-message audit rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              type: object
                              properties:
                                id:
                                  type: string
                                  format: uuid
                                to_address:
                                  type: string
                                subject:
                                  type: string
                                template:
                                  type: string
                                status:
                                  type: string
                                attempts:
                                  type: integer
                                last_error:
                                  type: string
                                sent_at:
                                  type: string
                                  format: date-time
                                  nullable: true
                                created_at:
                                  type: string
                                  format: date-time
                                user_id:
                                  type: string
                                  format: uuid
                                  nullable: true
                          total:
                            type: integer
                            format: int64
                          limit:
                            type: integer
                          offset:
                            type: integer
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/admin/smtp:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminSmtpGet
      summary: Get SMTP settings
      description: |
        Returns the singleton SMTP settings. The password is always the sentinel `<encrypted>`; `password_configured` reports whether one is actually set. Superuser-gated inside the handler. Returns empty defaults when no row exists.
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: SMTP settings (password redacted to sentinel).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/SmtpSettings'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: adminSmtpUpdate
      summary: Update SMTP settings
      description: |
        Partial update (all body fields optional). A password equal to the sentinel `<encrypted>` preserves the stored ciphertext; any other value is re-encrypted. Refuses to disable SMTP (enabled=false or blank host) when the active compliance baseline requires SMTP (409). Superuser-gated.
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                host:
                  type: string
                port:
                  type: integer
                username:
                  type: string
                password:
                  type: string
                  description: Send `<encrypted>` to keep existing; empty string to clear.
                from_address:
                  type: string
                  format: email
                from_name:
                  type: string
                auth_mechanism:
                  type: string
                  enum:
                    - plain
                    - login
                    - cram-md5
                    - none
                encryption:
                  type: string
                  enum:
                    - starttls
                    - tls
                    - none
                require_tls:
                  type: boolean
                timeout_seconds:
                  type: integer
                  minimum: 1
                  maximum: 600
      responses:
        '200':
          description: Saved settings (password redacted to sentinel).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/SmtpSettings'
        '400':
          description: Invalid body or validation error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: SMTP is required by the active compliance baseline; cannot disable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Encryptor not configured; cannot store password.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/smtp/test:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: adminSmtpTest
      summary: Send a test email using the live SMTP settings
      description: |
        Sends a templated test message to the supplied recipient using the stored (live) SMTP settings. Superuser-gated. Response is NOT wrapped in a data envelope.
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - recipient
              properties:
                recipient:
                  type: string
                  format: email
      responses:
        '200':
          description: Test message sent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  recipient:
                    type: string
                    format: email
        '400':
          description: Invalid recipient, missing recipient, or SMTP disabled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '502':
          description: The test send failed at the SMTP layer.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: SMTP settings are not configured yet.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/vault-connections:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminVaultConnectionsList
      summary: List Vault connections
      description: |
        Lists all Vault connections. Auth secrets are method-specific redacted to the `<encrypted>` sentinel. Superuser-gated inside the handler. Returns `{data:{items:[...]}}` (not paginated).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: All Vault connections.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              $ref: '#/components/schemas/VaultConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated admin surface; superuser/permission audit remains open"
      x-astronomer-route-class: public-api
      operationId: adminVaultConnectionsCreate
      summary: Create a Vault connection
      description: |
        Creates a Vault connection. The `auth` map is method-specific (token / approle / kubernetes) and is Fernet-encrypted at rest. Superuser-gated. Requires the encryptor to be configured (503 otherwise).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VaultConnectionRequest'
      responses:
        '201':
          description: Created connection (auth redacted).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/VaultConnection'
        '400':
          description: Invalid name, addr, auth, or body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: A connection with that name already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Encryption key not configured; cannot store auth.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/vault-connections/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminVaultConnectionGet
      summary: Get a Vault connection
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: The connection (auth redacted).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/VaultConnection'
        '400':
          description: Invalid connection ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated admin surface; superuser/permission audit remains open"
      x-astronomer-route-class: public-api
      operationId: adminVaultConnectionUpdate
      summary: Update a Vault connection
      description: |
        Updates a Vault connection. Any `auth` field equal to the `<encrypted>` sentinel preserves the stored value. Superuser-gated; requires encryptor.
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VaultConnectionRequest'
      responses:
        '200':
          description: Updated connection (auth redacted).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/VaultConnection'
        '400':
          description: Invalid ID, addr, auth, or body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: Encryption key not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated admin surface; superuser/permission audit remains open"
      x-astronomer-route-class: public-api
      operationId: adminVaultConnectionDelete
      summary: Delete a Vault connection
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '204':
          description: Deleted.
        '400':
          description: Invalid connection ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/admin/vault-connections/{id}/health:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated admin/secret configuration surface; handler validates connection ownership/configuration"
      x-astronomer-route-class: public-api
      operationId: adminVaultConnectionHealth
      summary: Auth-only health probe for a Vault connection
      description: |
        Authenticates against the connection (no kv read) and stamps last_health_*. Superuser-gated. Always returns HTTP 200; probe outcome is carried in the body (`ok`).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Probe result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          ok:
                            type: boolean
                          latency_ms:
                            type: integer
                            format: int64
                          message:
                            type: string
        '400':
          description: Invalid connection ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: Vault probe not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/vault-connections/{id}/test:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "authenticated admin surface; superuser/permission audit remains open"
      x-astronomer-route-class: public-api
      operationId: adminVaultConnectionTest
      summary: Auth + kv-probe test for a Vault connection
      description: |
        Authenticates AND performs a kv GET against an optional `probe_path` (defaults to `_health`). The secret value is never returned — only timing + reachability. Superuser-gated. Always returns HTTP 200; outcome is carried in the body (`ok`).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                probe_path:
                  type: string
                  description: Optional; defaults to `_health`.
      responses:
        '200':
          description: Probe result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/VaultTestResult'
        '400':
          description: Invalid connection ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: Vault probe not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/shell-sessions:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: adminShellSessionsList
      summary: List all active kubectl shell sessions
      description: |
        Returns every active in-browser kubectl session across all clusters and owners. Superuser-gated inside the handler. `data` is a bare array of session objects (not paginated).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Active shell sessions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ShellSession'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Kubectl shell is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/shell-sessions/{id}/commands:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          description: Shell session id
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: adminShellSessionCommands
      summary: List recorded commands for any shell session
      description: |
        Returns the recorded command audit lines for the given session, regardless of owner. Superuser-gated. DRF-paginated (the handler reads up to 1000 rows). Each item is `{command_at, command_line}`.
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Recorded command lines (paginated envelope).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          type: object
                          properties:
                            command_at:
                              type: string
                              format: date-time
                            command_line:
                              type: string
        '400':
          description: Invalid session id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Kubectl shell is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/compliance-baselines/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminComplianceBaselinesList
      summary: List compliance baselines
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Compliance baselines.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data: {type: array, items: {$ref: '#/components/schemas/ComplianceBaseline'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/admin/compliance-baselines/active/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminComplianceBaselineActive
      summary: Get the active compliance baseline application
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Active application, if any.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        additionalProperties: false
                        required: [active]
                        properties:
                          active:
                            nullable: true
                            allOf: [{$ref: '#/components/schemas/ComplianceBaselineApplication'}]
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/admin/compliance-baselines/{id}/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminComplianceBaselineGet
      summary: Get a compliance baseline
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Compliance baseline.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/ComplianceBaseline'}}
        '400': {description: Invalid baseline ID, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/admin/compliance-baselines/{id}/diff/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminComplianceBaselineDiff
      summary: Preview a compliance baseline diff
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Current-to-target diff.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/ComplianceBaselineDiff'}}
        '400': {description: Invalid baseline ID, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/admin/compliance-baselines/{id}/apply/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "admin API scope and handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminComplianceBaselineApply
      summary: Apply a compliance baseline
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody:
        required: false
        content: {application/json: {schema: {$ref: '#/components/schemas/ComplianceBaselineApplyRequest'}}}
      responses:
        '200':
          description: Synchronous application receipt.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/ComplianceBaselineApplyReceipt'}}
        '400': {description: Invalid baseline or request, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Baseline conflicts with current state, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {description: Transactional apply is unavailable, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/admin/compliance-baseline-applications/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminComplianceBaselineApplicationsList
      summary: List compliance baseline application history
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Application history.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data: {type: array, items: {$ref: '#/components/schemas/ComplianceBaselineApplication'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/admin/compliance-baseline-applications/{id}/revert/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "admin API scope and handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminComplianceBaselineApplicationRevert
      summary: Revert a compliance baseline application
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Synchronous revert receipt.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/ComplianceBaselineRevertReceipt'}}
        '400': {description: Invalid application ID, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Application cannot be reverted, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {description: Transactional revert is unavailable, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/admin/siem-forwarders/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminSIEMForwardersList
      summary: List SIEM forwarders
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: SIEM forwarders.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/SIEMForwarderList'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminSIEMForwardersCreate
      summary: Create a SIEM forwarder
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/SIEMForwarderRequest'}}}
      responses:
        '201':
          description: Created SIEM forwarder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/SIEMForwarderResponse'}}
        '400': {description: Invalid request, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Forwarder name already exists, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '503': {description: Encryption unavailable, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
  /api/v1/admin/siem-forwarders/{id}/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminSIEMForwarderGet
      summary: Get a SIEM forwarder
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: SIEM forwarder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/SIEMForwarderResponse'}}
        '400': {description: Invalid forwarder ID, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminSIEMForwarderUpdate
      summary: Update a SIEM forwarder
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/SIEMForwarderRequest'}}}
      responses:
        '200':
          description: Updated SIEM forwarder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/SIEMForwarderResponse'}}
        '400': {description: Invalid request, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {description: Encryption unavailable, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminSIEMForwarderDelete
      summary: Delete a SIEM forwarder
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '204': {description: Deleted.}
        '400': {description: Invalid forwarder ID, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/admin/siem-forwarders/{id}/test/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminSIEMForwarderTest
      summary: Queue a SIEM forwarder test
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
      responses:
        '202':
          description: Durable SIEM test operation receipt.
          headers:
            Location: {description: Exact SIEM test operation status resource, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/SIEMForwarderTestReceipt'}}
        '400': {description: Invalid forwarder ID, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/admin/siem-forwarders/{id}/test-operations/{operation_id}/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      - {name: operation_id, in: path, required: true, schema: {type: string, format: uuid}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminSIEMForwarderTestOperationGet
      summary: Get one durable SIEM test operation
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Current or terminal SIEM test operation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/SIEMForwarderTestReceipt'}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/admin/siem-forwarders/{id}/status/:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminSIEMForwarderStatus
      summary: Get SIEM forwarder delivery status
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Delivery status.
          content: {application/json: {schema: {$ref: '#/components/schemas/SIEMForwarderStatusResponse'}}}
        '400': {description: Invalid forwarder ID, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/admin/notification-templates/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminNotificationTemplatesList
      summary: List notification templates
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Registry templates and override state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        additionalProperties: false
                        required: [items, total]
                        properties:
                          items: {type: array, items: {$ref: '#/components/schemas/NotificationTemplateListItem'}}
                          total: {type: integer, minimum: 0}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/admin/notification-templates/{key}/:
    parameters:
      - {name: key, in: path, required: true, schema: {type: string}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminNotificationTemplateGet
      summary: Get a merged notification template
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Template defaults merged with any override.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/NotificationTemplateDetail'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "admin API scope and handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminNotificationTemplateUpdate
      summary: Upsert a notification template override
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/NotificationTemplateUpsertRequest'}}}
      responses:
        '200':
          description: Updated merged template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/NotificationTemplateDetail'}}
        '400': {description: Invalid body or body format, content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "admin API scope and handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminNotificationTemplateReset
      summary: Reset a notification template to its built-in default
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '204': {description: Override removed.}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/admin/notification-templates/{key}/preview/:
    parameters:
      - {name: key, in: path, required: true, schema: {type: string}}
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "admin API scope and handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminNotificationTemplatePreview
      summary: Render a candidate notification template
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/NotificationTemplatePreviewRequest'}}}
      responses:
        '200':
          description: Rendered subject and body.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties: {data: {$ref: '#/components/schemas/NotificationTemplatePreviewResponse'}}
        '400': {description: "Invalid template, format, or variables", content: {application/json: {schema: {$ref: '#/components/schemas/ErrorEnvelope'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/admin/notification-templates/{key}/variables/:
    parameters:
      - {name: key, in: path, required: true, schema: {type: string}}
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler superuser gate"
      x-astronomer-route-class: public-api
      operationId: adminNotificationTemplateVariables
      summary: List variables supported by a notification template
      tags: [platform-admin-settings]
      security: [{bearerJWT: []}]
      responses:
        '200':
          description: Template key and variable specifications.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        additionalProperties: false
                        required: [key, variables]
                        properties:
                          key: {type: string}
                          variables: {type: array, items: {$ref: '#/components/schemas/NotificationTemplateVariable'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/admin/webhooks:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminWebhooksList
      summary: List webhook subscriptions
      description: |
        Lists all webhook subscriptions. The secret is always the `<encrypted>` sentinel. Superuser-gated inside the handler. Returns `{data:{items,total}}` (not paginated).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: All webhook subscriptions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              $ref: '#/components/schemas/WebhookSubscription'
                          total:
                            type: integer
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: adminWebhooksCreate
      summary: Create a webhook subscription
      description: |
        Creates a webhook subscription. The secret is required on create and is Fernet-encrypted at rest. Superuser-gated; additionally requires the api-token `admin` scope (requireScope).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookSubscriptionRequest'
      responses:
        '201':
          description: Created subscription (secret redacted).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WebhookSubscription'
        '400':
          description: Validation error or invalid body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: A subscription with this name already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Encryptor not configured; cannot store secret.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/webhooks/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminWebhookGet
      summary: Get a webhook subscription
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: The subscription (secret redacted).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WebhookSubscription'
        '400':
          description: Invalid subscription id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: adminWebhookUpdate
      summary: Update a webhook subscription
      description: |
        Partial update. A secret equal to the `<encrypted>` sentinel preserves the stored value; an empty string is rejected. Superuser-gated; additionally requires the api-token `admin` scope (requireScope).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookSubscriptionRequest'
      responses:
        '200':
          description: Updated subscription (secret redacted).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WebhookSubscription'
        '400':
          description: Validation error or invalid body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: Encryptor unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: adminWebhookDelete
      summary: Delete a webhook subscription
      description: |
        Deletes the subscription (delivery history is cascade-removed). Superuser-gated; additionally requires the api-token `admin` scope. Refused (409) when the subscription is required by the active compliance baseline.
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '204':
          description: Deleted.
        '400':
          description: Invalid subscription id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Subscription is required by the active compliance baseline.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/webhooks/{id}/deliveries:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: adminWebhookDeliveries
      summary: List delivery history for a webhook subscription
      description: |
        Paginated delivery audit view for the subscription. Superuser-gated. Returns a custom `{data:{items,total,limit,offset}}` object (not the DRF paginated envelope).
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 50
            minimum: 1
            maximum: 200
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        '200':
          description: Page of delivery rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              $ref: '#/components/schemas/WebhookDelivery'
                          total:
                            type: integer
                            format: int64
                          limit:
                            type: integer
                          offset:
                            type: integer
        '400':
          description: Invalid subscription id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/admin/webhooks/{id}/deliveries/{delivery_id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          description: Subscription id
      - name: delivery_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          description: Delivery id
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: adminWebhookGetDelivery
      summary: Get one durable webhook delivery
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      responses:
        '200':
          description: Exact webhook delivery state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WebhookDelivery'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/admin/webhooks/{id}/deliveries/{delivery_id}/retry:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          description: Subscription id
      - name: delivery_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          description: Delivery id
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: adminWebhookRetryDelivery
      summary: Force re-dispatch of a webhook delivery
      description: |
        Atomically re-queues the delivery, stores mandatory audit intent, and
        binds the required Idempotency-Key before returning 202. Replays return
        the same delivery without another retry or audit. Poll Location for the
        exact delivery state. The delivery must belong to the subscription in
        the path. Superuser-gated; API tokens also require the `admin` scope.
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      responses:
        '202':
          description: Delivery re-queued.
          headers:
            Location: {description: Exact webhook delivery URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WebhookDelivery'
        '400':
          description: Invalid subscription or delivery id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Delivery not found or does not belong to this subscription.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/admin/webhooks/{id}/test:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "admin API token scope plus superuser check inside handler"
      x-astronomer-route-class: public-api
      operationId: adminWebhookTest
      summary: Enqueue a synthetic test event for a webhook subscription
      description: |
        Atomically enqueues a `webhook.test_ping` delivery, stores mandatory
        audit intent, and binds the required Idempotency-Key before returning
        202. Replays return the same delivery without another event or audit.
        Poll Location for the exact delivery state. Superuser-gated; API tokens
        also require the `admin` scope.
      tags:
        - platform-admin-settings
      security:
        - bearerJWT: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
      responses:
        '202':
          description: Test ping queued.
          headers:
            Location: {description: Exact webhook delivery URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WebhookDelivery'
        '400':
          description: Invalid subscription id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/audit/:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "audit_logs:read or audit_logs:list"
      x-astronomer-route-class: public-api
      summary: List audit log entries
      description: |
        Returns a DRF-style paginated page of audit log entries. Supports a large set of optional filter query params (see below). When the backing store implements the composable filtered reader, all filters compose; otherwise a single-field fallback path is used (precedence: action_class, since, user_id, resource_type, action, then unfiltered). Note: the `since` cursor mode returns a count equal to the page length, not the global total.
      tags:
        - audit
      operationId: listAuditLogs
      security:
        - bearerJWT: []
      parameters:
        - name: limit
          in: query
          description: |
            Page size. Aliases pageSize / page_size are also accepted. Defaults to 20; values <1 reset to 20; capped at 500.
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 500
        - name: offset
          in: query
          description: Number of items to skip. Defaults to 0.
          required: false
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: user_id
          in: query
          description: Filter by actor user UUID. Invalid UUID returns 400.
          required: false
          schema:
            type: string
            format: uuid
        - name: actor
          in: query
          description: Filter by actor (free-text actor match; filtered reader only).
          required: false
          schema:
            type: string
        - name: q
          in: query
          description: Free-text search across actor, action, resource, and request metadata (filtered reader only).
          required: false
          schema:
            type: string
        - name: audience
          in: query
          description: Restrict entries to human actors, system actors, or both.
          required: false
          schema:
            type: string
            enum: [people, system, all]
        - name: resource_type
          in: query
          required: false
          schema:
            type: string
        - name: resource_id
          in: query
          description: Filtered reader only.
          required: false
          schema:
            type: string
        - name: resource_name
          in: query
          description: Filtered reader only.
          required: false
          schema:
            type: string
        - name: target
          in: query
          description: Filtered reader only.
          required: false
          schema:
            type: string
        - name: action
          in: query
          required: false
          schema:
            type: string
        - name: action_class
          in: query
          description: Alias actionClass also accepted.
          required: false
          schema:
            type: string
        - name: result
          in: query
          description: Outcome filter. Must be one of success, failure, error.
          required: false
          schema:
            type: string
            enum:
              - success
              - failure
              - error
        - name: source
          in: query
          description: Filtered reader only.
          required: false
          schema:
            type: string
        - name: correlation_id
          in: query
          description: Alias correlationId also accepted. Filtered reader only.
          required: false
          schema:
            type: string
        - name: request_id
          in: query
          description: Alias requestId also accepted. Filtered reader only.
          required: false
          schema:
            type: string
        - name: cluster_id
          in: query
          description: Alias clusterId also accepted. Filtered reader only.
          required: false
          schema:
            type: string
        - name: project_id
          in: query
          description: Alias projectId also accepted. Filtered reader only.
          required: false
          schema:
            type: string
        - name: status_code
          in: query
          description: HTTP status code filter (0-599). Filtered reader only.
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 599
        - name: from
          in: query
          description: Lower time bound (inclusive), RFC3339.
          required: false
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Upper time bound (inclusive), RFC3339.
          required: false
          schema:
            type: string
            format: date-time
        - name: since
          in: query
          description: |
            Cursor UUID; returns entries after the given audit log id. When set, count is the page length rather than the global total.
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Paginated audit log page.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/AuditLogEntry'
        '400':
          description: Invalid filter, user_id, since cursor, status_code, result, or timestamp.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: |
            Caller lacks audit_logs read or list permission (RBAC requireAnyPermission).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to list or count audit logs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/audit/export/:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "audit_logs:read or audit_logs:list"
      x-astronomer-route-class: download
      summary: Export audit log entries as CSV
      description: |
        Streams matching audit log entries as a CSV attachment (filename audit_log_export.csv). Accepts the same filters as the list endpoint. Only format=csv is supported; the `since` cursor is not supported for export. The response is a CSV stream, not the JSON data envelope.
      tags:
        - audit
      operationId: exportAuditLogs
      security:
        - bearerJWT: []
      parameters:
        - name: format
          in: query
          description: Export format. Only 'csv' is supported. Defaults to csv.
          required: false
          schema:
            type: string
            enum:
              - csv
            default: csv
        - name: user_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: actor
          in: query
          required: false
          schema:
            type: string
        - name: q
          in: query
          description: Free-text search across actor, action, resource, and request metadata.
          required: false
          schema:
            type: string
        - name: audience
          in: query
          description: Restrict entries to human actors, system actors, or both.
          required: false
          schema:
            type: string
            enum: [people, system, all]
        - name: resource_type
          in: query
          required: false
          schema:
            type: string
        - name: resource_id
          in: query
          required: false
          schema:
            type: string
        - name: resource_name
          in: query
          required: false
          schema:
            type: string
        - name: target
          in: query
          required: false
          schema:
            type: string
        - name: action
          in: query
          required: false
          schema:
            type: string
        - name: action_class
          in: query
          description: Alias actionClass also accepted.
          required: false
          schema:
            type: string
        - name: result
          in: query
          required: false
          schema:
            type: string
            enum:
              - success
              - failure
              - error
        - name: source
          in: query
          required: false
          schema:
            type: string
        - name: correlation_id
          in: query
          description: Alias correlationId also accepted.
          required: false
          schema:
            type: string
        - name: request_id
          in: query
          description: Alias requestId also accepted.
          required: false
          schema:
            type: string
        - name: cluster_id
          in: query
          description: Alias clusterId also accepted.
          required: false
          schema:
            type: string
        - name: project_id
          in: query
          description: Alias projectId also accepted.
          required: false
          schema:
            type: string
        - name: status_code
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 599
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: CSV stream of audit log entries.
          content:
            text/csv:
              schema:
                type: string
                format: binary
                description: |
                  CSV with header row, columns: id, created_at, user_id, source, correlation_id, action, action_class, resource_type, resource_id, resource_name, http_method, path, status_code, duration_ms, ip_address, user_agent, request_id, detail.
        '400':
          description: Unsupported format, invalid filter, or unsupported since cursor.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller lacks audit_logs read or list permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/audit/{id}/:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "audit_logs:read"
      x-astronomer-route-class: public-api
      summary: Get a single audit log entry
      description: Returns one audit log entry by UUID.
      tags:
        - audit
      operationId: getAuditLog
      security:
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          description: Audit log entry UUID.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The audit log entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/AuditLogEntry'
        '400':
          description: Invalid audit log ID (not a UUID).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller lacks audit_logs read permission (RBAC requirePermission).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Audit log entry not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/nodes/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdNodes
      tags:
        - logging-monitoring
      summary: List cluster nodes
      description: |
        Tunnel-proxies the managed cluster's `GET /api/v1/nodes` and
        enriches each node with a per-node pod count. RBAC: nodes:list.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema: {type: integer, minimum: 1, maximum: 200, default: 20}
        - name: offset
          in: query
          schema: {type: integer, minimum: 0, default: 0}
      responses:
        '200':
          description: Node summaries
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NodeSummaryListEnvelope'}
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Cluster tunnel/proxy error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/nodes/{node_name}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdNodesByNodeName
      tags:
        - logging-monitoring
      summary: Get node detail
      description: |
        Returns capacity + live CPU/memory usage (from the shared
        metrics provider), the node's pods, recent Node events, taints,
        conditions, and image inventory. RBAC: nodes:read.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: node_name
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Node detail
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/NodeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Cluster tunnel/proxy error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/pods/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdPods
      tags:
        - logging-monitoring
      summary: List pods
      description: |
        Tunnel-proxies pods from the managed cluster. When `namespace`
        is supplied the listing is scoped to that namespace, otherwise
        all namespaces are returned. RBAC: pods:list.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: namespace
          in: query
          required: false
          description: Restrict to a single namespace.
          schema:
            type: string
      responses:
        '200':
          description: Pod summaries
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Pod'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Cluster tunnel/proxy error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/pods/watch/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: stream
      operationId: getClustersByClusterIdPodsWatch
      tags:
        - logging-monitoring
      summary: Watch pods (SSE)
      description: |
        Streams live pod changes through the agent tunnel as Server-Sent
        Events instead of the UI polling the list endpoint. Each Kubernetes
        watch event becomes one SSE frame whose `event:` is the watch verb
        (`ADDED`, `MODIFIED`, `DELETED`) and whose `data:` is the pod object
        JSON. Authenticate with a one-use stream `ticket` query parameter
        (scoped to this cluster) or a normal token. RBAC: pods:read.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: namespace
          in: query
          required: false
          description: Restrict the watch to a single namespace.
          schema:
            type: string
        - name: ticket
          in: query
          required: false
          description: One-use stream ticket (scoped to this cluster).
          schema:
            type: string
      responses:
        '200':
          description: SSE stream of pod watch events
          content:
            text/event-stream:
              schema:
                type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '501':
          description: Pod watch streaming not configured
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '503':
          description: Cluster tunnel/proxy error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/namespaces/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdNamespaces
      tags:
        - logging-monitoring
      summary: List namespaces
      description: |
        Lists namespaces with per-namespace pod counts. CPU/memory
        usage+limit fields are present but currently always 0.
        RBAC: clusters:read.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Namespace summaries
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Namespace'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Cluster tunnel/proxy error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/events/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdEvents
      tags:
        - logging-monitoring
      summary: List cluster events
      description: |
        Tunnel-proxies the managed cluster's core v1 events. The `limit`
        query is forwarded to the kubelet/apiserver as-is.
        RBAC: clusters:read.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          description: Forwarded to the upstream k8s events list as ?limit=.
          schema:
            type: integer
      responses:
        '200':
          description: Cluster events
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ClusterEvent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Cluster tunnel/proxy error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/workloads/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdWorkloads
      tags:
        - logging-monitoring
      summary: List workloads
      description: |
        Aggregates Deployments, StatefulSets, DaemonSets, Jobs and
        CronJobs (or only `kind` when supplied) across the cluster, with
        optional namespace and case-insensitive name/namespace `search`
        filtering. Returns a DRF-style paginated envelope (always 200).
        RBAC: workloads:list.
      parameters:
        - {$ref: '#/components/parameters/Limit'}
        - {$ref: '#/components/parameters/Offset'}
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: namespace
          in: query
          required: false
          schema:
            type: string
        - name: kind
          in: query
          required: false
          description: One of Deployment, StatefulSet, DaemonSet, Job, CronJob.
          schema:
            type: string
        - name: search
          in: query
          required: false
          description: Case-insensitive substring match on name or namespace.
          schema:
            type: string
      responses:
        '200':
          description: Workload page
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Workload'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Cluster tunnel/proxy error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/workloads/{kind}/{namespace}/{name}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdWorkloadsByKindByNamespaceByName
      tags:
        - logging-monitoring
      summary: Get a workload
      description: |
        Fetches a single workload resource and projects it to the
        Astronomer workload shape. RBAC: workloads:read.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: kind
          in: path
          required: true
          description: Deployment, StatefulSet, DaemonSet, ReplicaSet, Job, or CronJob (case-insensitive).
          schema:
            type: string
        - name: namespace
          in: path
          required: true
          schema:
            type: string
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Workload
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/Workload'
        '400':
          description: Unsupported workload kind
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Cluster tunnel/proxy error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "workloads:delete"
      x-astronomer-route-class: public-api
      operationId: deleteClustersByClusterIdWorkloadsByKindByNamespaceByName
      tags:
        - logging-monitoring
      summary: Delete a workload
      description: |
        Enqueues an asynchronous `delete` workload operation and returns
        the queued operation record (202). RBAC: workloads:delete. For
        API-token callers also requires the `write:clusters` scope.
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: kind
          in: path
          required: true
          schema:
            type: string
        - name: namespace
          in: path
          required: true
          schema:
            type: string
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        '202':
          description: Delete operation enqueued
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WorkloadOperation'
        '400':
          description: Unsupported workload kind
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          description: Failed to enqueue the operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/workloads/{kind}/{namespace}/{name}/pods/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getClustersByClusterIdWorkloadsByKindByNamespaceByNamePods
      tags:
        - logging-monitoring
      summary: List a workload's pods
      description: |
        Resolves the workload's selector and lists the matching pods.
        RBAC: pods:read.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: kind
          in: path
          required: true
          schema:
            type: string
        - name: namespace
          in: path
          required: true
          schema:
            type: string
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Pods owned by the workload
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Pod'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Cluster tunnel/proxy error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/workloads/{kind}/{namespace}/{name}/restart/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "workloads:restart"
      x-astronomer-route-class: public-api
      operationId: postClustersByClusterIdWorkloadsByKindByNamespaceByNameRestart
      tags:
        - logging-monitoring
      summary: Restart a workload
      description: |
        Enqueues an asynchronous `restart` operation (stamps a
        kubectl.kubernetes.io/restartedAt annotation on the pod
        template). Returns the queued operation record (202).
        RBAC: workloads:restart. For API-token callers also requires the
        `write:clusters` scope. No request body.
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: kind
          in: path
          required: true
          schema:
            type: string
        - name: namespace
          in: path
          required: true
          schema:
            type: string
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        '202':
          description: Restart operation enqueued
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WorkloadOperation'
        '400':
          description: Unsupported workload kind
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          description: Failed to enqueue the operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/workloads/{kind}/{namespace}/{name}/scale/:
    patch:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "workloads:scale"
      x-astronomer-route-class: public-api
      operationId: patchClustersByClusterIdWorkloadsByKindByNamespaceByNameScale
      tags:
        - logging-monitoring
      summary: Scale a workload
      description: |
        Enqueues an asynchronous `scale` operation to set the desired
        replica count. Returns the queued operation record (202).
        RBAC: workloads:scale. For API-token callers also requires the
        `write:clusters` scope.
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: kind
          in: path
          required: true
          schema:
            type: string
        - name: namespace
          in: path
          required: true
          schema:
            type: string
        - name: name
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                replicas:
                  type: integer
                  format: int32
                  description: Desired replica count.
              required:
                - replicas
      responses:
        '202':
          description: Scale operation enqueued
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WorkloadOperation'
        '400':
          description: Invalid JSON body or unsupported workload kind
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          description: Failed to enqueue the operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/workloads/controller/status/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getWorkloadsControllerStatus
      tags:
        - logging-monitoring
      summary: Workload reconciler controller status
      description: |
        Returns reconciler health plus a status-count summary of recent
        workload operations (RBAC-filtered to clusters the caller can
        read). RBAC: workloads:read.
      responses:
        '200':
          description: Controller status
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: object
                        properties:
                          reconciler:
                            type: object
                            additionalProperties: true
                            description: Reconciler enabled flag, queue depth, stale-running count, recent previews.
                          operations:
                            type: object
                            additionalProperties:
                              type: integer
                            description: Count of operations by status.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          description: Failed to load controller status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/workloads/operations/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getWorkloadsOperations
      tags:
        - logging-monitoring
      summary: List workload operations
      description: |
        Lists workload operations (scale/restart/delete) with optional
        target/status filtering, RBAC-filtered to readable clusters.
        RBAC: workloads:read.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
        - name: targetType
          in: query
          required: false
          schema:
            type: string
        - name: targetKey
          in: query
          required: false
          schema:
            type: string
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Workload operations
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/WorkloadOperation'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          description: Failed to list operations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '503':
          description: Workload store not configured
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/workloads/operations/{id}/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getWorkloadsOperationsById
      tags:
        - logging-monitoring
      summary: Get a workload operation
      description: |
        Returns a single workload operation, including its lifecycle
        events when available. RBAC: workloads:read on the operation's
        target cluster.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Workload operation with events
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        allOf:
                          - $ref: '#/components/schemas/WorkloadOperation'
                          - type: object
                            properties:
                              events:
                                type: array
                                description: Present when operation events could be loaded.
                                items:
                                  $ref: '#/components/schemas/WorkloadOperationEvent'
        '400':
          description: Invalid operation ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workloads/operations/{id}/retry/:
    post:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "workloads:update"
      x-astronomer-route-class: public-api
      operationId: postWorkloadsOperationsByIdRetry
      tags:
        - logging-monitoring
      summary: Retry a workload operation
      description: |
        Requeues a failed/superseded operation and triggers the
        reconciler. Returns the requeued operation (202). RBAC:
        workloads:update on the target cluster. For API-token callers
        also requires the `write:clusters` scope. No request body.
      parameters:
        - {$ref: '#/components/parameters/RequiredIdempotencyKey'}
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Operation requeued
          headers:
            Location: {description: Pollable operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer, minimum: 1}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WorkloadOperation'
        '400':
          description: Invalid operation ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Operation is not in a retryable state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Failed to requeue the operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/workloads/pods/{cluster_id}/{namespace}/{pod}/:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "pods:delete"
      x-astronomer-route-class: public-api
      operationId: deleteWorkloadsPodsByClusterIdByNamespaceByPod
      tags:
        - logging-monitoring
      summary: Delete a pod
      description: |
        Atomically commits a durable pod-delete operation, identifier-only
        tunnel task, and mandatory audit intent before returning 202. The
        managed-cluster delete runs asynchronously; poll the Location URL for
        completion. Kubernetes 404 converges as success. Reusing the required
        Idempotency-Key replays the committed operation without duplicating the
        effect. RBAC: pods:delete. API-token callers also require the
        `write:clusters` scope.
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKey'
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: namespace
          in: path
          required: true
          schema:
            type: string
        - name: pod
          in: path
          required: true
          schema:
            type: string
      responses:
        '202':
          description: Durable pod-delete operation accepted; the cluster effect has not yet been claimed as complete.
          headers:
            Location: {description: Pollable workload operation URL, schema: {type: string}}
            Retry-After: {description: Suggested polling delay in seconds, schema: {type: integer}}
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        $ref: '#/components/schemas/WorkloadOperation'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          description: Failed to persist the operation or tunnel-task intent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/workloads/pods/{cluster_id}/{namespace}/{pod}/logs/:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: public-api
      operationId: getWorkloadsPodsByClusterIdByNamespaceByPodLogs
      tags:
        - logging-monitoring
      summary: Get pod logs
      description: |
        Tunnel-proxies pod logs (timestamps forced on) and returns them
        as an array of parsed {timestamp, message, container} entries.
        RBAC: pods:logs.
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: namespace
          in: path
          required: true
          schema:
            type: string
        - name: pod
          in: path
          required: true
          schema:
            type: string
        - name: container
          in: query
          required: false
          description: Target container; echoed back in each log entry.
          schema:
            type: string
        - name: tailLines
          in: query
          required: false
          description: Number of trailing lines (alias tail_lines accepted).
          schema:
            type: integer
        - name: sinceSeconds
          in: query
          required: false
          description: Return logs newer than N seconds (alias since_seconds accepted).
          schema:
            type: integer
        - name: follow
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Parsed log lines
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/PodLogEntry'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: Cluster tunnel/proxy error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/clusters/{cluster_id}/resources/{resource_type}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: listNamedClusterResources
      summary: List a known namespaced/cluster resource type (flattened rows)
      description: Lists one of a fixed set of supported resource types on the target cluster and returns UI-ready flattened rows under `data`. A 404 from the cluster (e.g. an uninstalled CRD such as v1alpha2 Gateway API routes) is normalized to an empty list with HTTP 200. Each row always includes name, namespace, clusterId, labels, annotations and createdAt, plus type-specific fields.
      tags:
        - resources-explorer
      security:
        - bearerAuth: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          description: Target cluster UUID.
          schema:
            type: string
        - name: resource_type
          in: path
          required: true
          description: Supported resource type (route-validated by regex).
          schema:
            type: string
            enum:
              - services
              - ingresses
              - networkpolicies
              - persistentvolumes
              - persistentvolumeclaims
              - storageclasses
              - gateways
              - httproutes
              - gatewayclasses
              - grpcroutes
              - tcproutes
              - udproutes
              - tlsroutes
              - referencegrants
        - name: namespace
          in: query
          required: false
          description: Restrict the listing to a single namespace. Ignored for cluster-scoped types (persistentvolumes, storageclasses, gatewayclasses).
          schema:
            type: string
      responses:
        '200':
          description: List of flattened resource rows under `data` (empty array when the type is not installed on the cluster).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ResourceRow'
        '400':
          description: Unsupported resource type (code `invalid_resource`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller lacks `<resource_type>:list` permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Tunnel requester not configured or proxy/parse failure (code `proxy_error`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "resource-family create permission such as services:create, ingresses:create, network_policies:create, or storage:create"
      x-astronomer-route-class: proxy
      operationId: createNamedClusterResource
      summary: Request durable creation of a resource
      description: Atomically commits an encrypted manifest, identifier-only tunnel task, and mandatory audit intent. Returns a pollable receipt before any member-cluster effect. The worker converges creation through server-side apply.
      tags:
        - resources-explorer
      security:
        - bearerAuth: []
      x-required-scope: write:clusters
      parameters:
        - name: cluster_id
          in: path
          required: true
          description: Target cluster UUID.
          schema:
            type: string
        - name: resource_type
          in: path
          required: true
          description: Creatable resource type (route-validated by regex).
          schema:
            type: string
            enum:
              - services
              - ingresses
              - networkpolicies
              - persistentvolumeclaims
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable caller key. Reuse with a different manifest or target returns 409.
          schema: {type: string, minLength: 1, maxLength: 128}
      requestBody:
        required: true
        description: A Kubernetes resource manifest. The handler reads it verbatim and only inspects `metadata.namespace` to build the target API path; all other fields are passed through to the cluster API server unchanged.
        content:
          application/json:
            schema:
              type: object
              description: Kubernetes resource manifest (apiVersion/kind/metadata/spec/...).
              properties:
                apiVersion:
                  type: string
                kind:
                  type: string
                metadata:
                  type: object
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  additionalProperties: true
                spec:
                  type: object
                  additionalProperties: true
              additionalProperties: true
      responses:
        '202':
          description: Durable operation accepted. Poll the Location URI; Retry-After is 2 seconds.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceOperationEnvelope'
        '400':
          description: Unreadable body (`invalid_body`), undeterminable namespace (`invalid_body`), or unsupported resource type (`invalid_resource`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller lacks `<resource_type>:create` permission, or the API token is missing the write:clusters scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Idempotency-Key is bound to a different request, or another mutation is active for the target object.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
        '413':
          description: Manifest exceeds the 2 MiB request ceiling.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
        '503':
          description: Tunnel requester not configured or proxy failure (code `proxy_error`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/resources/{resource_type}/{namespace}/{name}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "resource-family delete permission such as services:delete, ingresses:delete, network_policies:delete, or storage:delete"
      x-astronomer-route-class: public-api
      operationId: deleteNamedClusterResource
      summary: Request durable deletion of a namespaced resource
      description: Commits a durable operation, identifier-only tunnel task, and mandatory audit before the worker performs an idempotent DELETE. Kubernetes 404 converges successfully.
      tags:
        - resources-explorer
      security:
        - bearerAuth: []
      x-required-scope: write:clusters
      parameters:
        - name: cluster_id
          in: path
          required: true
          description: Target cluster UUID.
          schema:
            type: string
        - name: resource_type
          in: path
          required: true
          description: Deletable namespaced resource type (route-validated by regex).
          schema:
            type: string
            enum:
              - services
              - ingresses
              - networkpolicies
              - persistentvolumeclaims
        - name: namespace
          in: path
          required: true
          description: Namespace of the resource.
          schema:
            type: string
        - name: name
          in: path
          required: true
          description: Name of the resource.
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema: {type: string, minLength: 1, maxLength: 128}
      responses:
        '202':
          description: Durable delete operation accepted.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceOperationEnvelope'
        '400':
          description: Unsupported resource type or missing namespace (code `invalid_resource`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller lacks `<resource_type>:delete` permission, or the API token is missing the write:clusters scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Idempotency-Key is bound to a different request, or another mutation is active for the target object.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
        '503':
          description: Tunnel requester not configured or proxy failure (code `proxy_error`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/resources/generic/{resource_type}:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "secrets:list when resource_type=secrets"
      x-astronomer-route-class: public-api
      operationId: listGenericClusterResources
      summary: List a generic resource type (flattened rows)
      description: Lists an arbitrary supported resource type from `resourceDefs` (e.g. configmaps, secrets, jobs, cronjobs, hpa, resourcequotas, limitranges, poddisruptionbudgets, crds, RBAC kinds, workload kinds) and returns flattened rows under `data`. Each row carries the base fields plus type-specific extras (e.g. dataCount for configmaps/secrets, schedule for cronjobs, minReplicas/maxReplicas for hpa). Listing secrets triggers a read-audit hook.
      tags:
        - resources-explorer
      security:
        - bearerAuth: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          description: Target cluster UUID.
          schema:
            type: string
        - name: resource_type
          in: path
          required: true
          description: Any key of the server-side resourceDefs table (e.g. configmaps, secrets, deployments, daemonsets, statefulsets, jobs, cronjobs, hpa, resourcequotas, limitranges, poddisruptionbudgets, k8s-roles, k8s-rolebindings, k8s-clusterroles, k8s-clusterrolebindings, crds, endpoints, serviceaccounts, replicasets). UNCERTAIN whether the full set should be enumerated in the spec.
          schema:
            type: string
        - name: namespace
          in: query
          required: false
          description: Restrict the listing to a single namespace (ignored for cluster-scoped types).
          schema:
            type: string
      responses:
        '200':
          description: List of flattened generic resource rows under `data`.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ResourceRow'
        '400':
          description: Unsupported resource type (code `invalid_resource`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller lacks list permission for the mapped resource.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Tunnel requester not configured or proxy/parse failure (code `proxy_error`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/resources/persistentvolumes/{name}:
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "storage:delete"
      x-astronomer-route-class: public-api
      operationId: deletePersistentVolume
      summary: Request durable deletion of a cluster-scoped PersistentVolume
      description: Commits a durable delete receipt and mandatory delivery/audit intents before the idempotent tunnel effect.
      tags:
        - resources-explorer
      security:
        - bearerAuth: []
      x-required-scope: write:clusters
      parameters:
        - name: cluster_id
          in: path
          required: true
          description: Target cluster UUID.
          schema:
            type: string
        - name: name
          in: path
          required: true
          description: PersistentVolume name.
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema: {type: string, minLength: 1, maxLength: 128}
      responses:
        '202':
          description: Durable delete operation accepted.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceOperationEnvelope'
        '400':
          description: Invalid resource (code `invalid_resource`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller lacks persistentvolumes:delete permission, or the API token is missing the write:clusters scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Tunnel requester not configured or proxy failure (code `proxy_error`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/resources/{cluster_id}/{type}/{namespace}/{name}:
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: getNamedResource
      summary: Get a single resource (raw passthrough)
      description: Fetches a single resource by type/namespace/name and returns the raw Kubernetes object under `data`. The type must be a key of resourceDefs; for cluster-scoped types the namespace path segment is present in the route but ignored when building the API path.
      tags:
        - resources-explorer
      security:
        - bearerAuth: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          description: Target cluster UUID.
          schema:
            type: string
        - name: type
          in: path
          required: true
          description: Resource type (a key of the server-side resourceDefs table).
          schema:
            type: string
        - name: namespace
          in: path
          required: true
          description: Namespace of the resource. Required by the route; ignored for cluster-scoped types.
          schema:
            type: string
        - name: name
          in: path
          required: true
          description: Name of the resource.
          schema:
            type: string
      responses:
        '200':
          description: Raw Kubernetes object under `data`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DataEnvelope'
        '400':
          description: Missing path params (`invalid_request`), unsupported type or missing namespace for a namespaced type (`invalid_resource`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller lacks `<type>:read` permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Tunnel requester not configured or proxy failure (code `proxy_error`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "resource-family update permission such as workloads:update, services:update, ingresses:update, configmaps:update, secrets:update, storage:update, or clusters:update for unmapped custom resource types"
      x-astronomer-route-class: proxy
      operationId: updateNamedResource
      summary: Request durable server-side apply of a resource
      description: Normal requests atomically commit an encrypted manifest, identifier-only tunnel task, and mandatory audit intent, then return 202 before any member-cluster effect. `dry_run=true` remains synchronous Kubernetes no-effect validation and returns 200.
      tags:
        - resources-explorer
      security:
        - bearerAuth: []
      x-required-scope: write:clusters
      parameters:
        - name: cluster_id
          in: path
          required: true
          description: Target cluster UUID.
          schema:
            type: string
        - name: type
          in: path
          required: true
          description: Resource type (a key of the server-side resourceDefs table).
          schema:
            type: string
        - name: namespace
          in: path
          required: true
          description: Namespace of the resource (ignored for cluster-scoped types).
          schema:
            type: string
        - name: name
          in: path
          required: true
          description: Name of the resource.
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          description: Required for normal durable apply requests.
          schema: {type: string, minLength: 1, maxLength: 128}
        - name: dry_run
          in: query
          required: false
          schema: {type: boolean, default: false}
        - name: force
          in: query
          required: false
          schema: {type: boolean, default: false}
      requestBody:
        required: true
        description: Full Kubernetes resource manifest to replace the existing object. Passed through to the cluster API server unchanged.
        content:
          application/json:
            schema:
              type: object
              description: Kubernetes resource manifest (apiVersion/kind/metadata/spec/...).
              additionalProperties: true
      responses:
        '200':
          description: Synchronous Kubernetes validation result when dry_run=true.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DataEnvelope'
        '202':
          description: Durable apply operation accepted.
          headers: *durable-polling-headers
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ResourceOperationEnvelope'}
        '400':
          description: Unreadable body (`invalid_body`), missing path params (`invalid_request`), or unsupported type / missing namespace (`invalid_resource`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller lacks `<type>:update` permission, or the API token is missing the write:clusters scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Idempotency-Key is bound to a different request, or another mutation is active for the target object.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
        '413':
          description: Manifest exceeds the 2 MiB request ceiling.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
        '503':
          description: Tunnel requester not configured or proxy failure (code `proxy_error`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "resource-family delete permission such as workloads:delete, services:delete, ingresses:delete, configmaps:delete, secrets:delete, storage:delete, or clusters:update for unmapped custom resource types"
      x-astronomer-route-class: proxy
      operationId: deleteNamedResourceREST
      summary: Request durable deletion of a resource
      description: Commits the operation, identifier-only tunnel task, and mandatory audit atomically before the idempotent worker effect.
      tags:
        - resources-explorer
      security:
        - bearerAuth: []
      x-required-scope: write:clusters
      parameters:
        - name: cluster_id
          in: path
          required: true
          description: Target cluster UUID.
          schema:
            type: string
        - name: type
          in: path
          required: true
          description: Resource type (a key of the server-side resourceDefs table).
          schema:
            type: string
        - name: namespace
          in: path
          required: true
          description: Namespace of the resource (ignored for cluster-scoped types).
          schema:
            type: string
        - name: name
          in: path
          required: true
          description: Name of the resource.
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema: {type: string, minLength: 1, maxLength: 128}
      responses:
        '202':
          description: Durable delete operation accepted.
          headers: *durable-polling-headers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceOperationEnvelope'
        '400':
          description: Missing path params (`invalid_request`) or unsupported type / missing namespace (`invalid_resource`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller lacks `<type>:delete` permission, or the API token is missing the write:clusters scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Idempotency-Key is bound to a different request, or another mutation is active for the target object.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ErrorEnvelope'}
        '503':
          description: Tunnel requester not configured or proxy failure (code `proxy_error`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/resources/search:
    get:
      x-astronomer-auth: "bearer/session auth"
      x-astronomer-rbac: "secrets:list per target cluster when type=secrets"
      x-astronomer-route-class: public-api
      operationId: searchResourcesAcrossClusters
      summary: Cross-cluster resource search (fan-out)
      description: Fans a single list query out across every active cluster the caller is authorized to list the requested type on, merges results with a bounded top-K heap, and returns them along with per-cluster errors and counts under `data`. The endpoint is per-user rate limited. The `name` filter is applied client-side after merge; `label`/`field` selectors are passed verbatim to Kubernetes. The per-cluster limit is NOT pushed down — every cluster's full list is fetched and the global `limit` is applied after merge.
      tags:
        - resources-explorer
      security:
        - bearerAuth: []
      parameters:
        - name: type
          in: query
          required: true
          description: Resource type to search. One of the search-supported keys.
          schema:
            type: string
            enum:
              - pods
              - events
              - endpoints
              - services
              - configmaps
              - secrets
              - namespaces
              - nodes
              - persistentvolumes
              - persistentvolumeclaims
              - storageclasses
              - deployments
              - statefulsets
              - daemonsets
              - replicasets
              - jobs
              - cronjobs
              - ingresses
              - networkpolicies
              - gateways
              - httproutes
        - name: namespace
          in: query
          required: false
          description: Filter to a single namespace; ignored for cluster-scoped types.
          schema:
            type: string
        - name: label
          in: query
          required: false
          description: Kubernetes label selector, passed verbatim to the cluster API.
          schema:
            type: string
        - name: field
          in: query
          required: false
          description: Kubernetes field selector, passed verbatim to the cluster API.
          schema:
            type: string
        - name: name
          in: query
          required: false
          description: Case-insensitive substring filter applied to item names after merge.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Max items returned across all clusters after merge. Default 100, values above 1000 are clamped to 1000, non-positive values reset to 100.
          schema:
            type: integer
            default: 100
            minimum: 1
            maximum: 1000
      responses:
        '200':
          description: Merged search results, per-cluster errors and counts under `data`.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/DataEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ResourceSearchResult'
        '400':
          description: Missing `type` (code `invalid_request`) or unsupported `type` (code `unsupported_type`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Caller is not authorized to list this resource type on any cluster (code `permission_denied`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '429':
          description: Per-user search rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to list active clusters (`list_clusters_failed`) or evaluate permissions (`internal_error`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Cross-cluster search not configured (code `search_unavailable`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /health:
    get:
      x-astronomer-auth: "public"
      x-astronomer-rbac: "public"
      x-astronomer-route-class: diagnostic
      operationId: tunnelHealthCheck
      summary: Liveness/health probe
      description: |
        Public, unauthenticated health endpoint. Returns the running server
        version and current UTC time. Used by load balancers and the
        platform-health rollup.
      tags:
        - Platform
      security: []
      responses:
        '200':
          description: Server is healthy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                  version:
                    type: string
                  time:
                    type: string
                    format: date-time
                required:
                  - status
                  - version
                  - time
  /api/v1/clusters/{cluster_id}/k8s/{path}:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Target cluster UUID.
      - name: path
        in: path
        required: true
        schema:
          type: string
        description: |
          Opaque Kubernetes API path after `/k8s/` (chi wildcard `*`), e.g.
          `api/v1/namespaces/default/pods`. Forwarded verbatim (plus the raw
          query string) to the in-cluster apiserver through the tunnel.
    description: |
      Reverse proxy from the management server to a connected cluster's
      Kubernetes apiserver, tunneled over the agent WebSocket
      (tunnel.ProxyHandler.HandleK8sProxy). Watch-shaped requests
      (`?watch=true`, `Accept: ...stream=watch`, or a `/watch/` path segment)
      are streamed frame-by-frame with no server-side timeout once the first
      frame arrives; all other requests are unary with a 30s timeout.

      Auth: Astronomer bearer JWT or personal API token. On MUTATING methods
      (anything other than GET/HEAD/OPTIONS) the api-token must carry the
      `clusters:write` scope (requireK8sProxyScope); GET/HEAD/OPTIONS bypass the
      scope check. RBAC permission is derived dynamically from the k8s path
      (e.g. `pods:exec` for exec/attach/portforward subresources, secret reads
      are separately audited). Rate-limited under the k8s-proxy class.

      Note: CONNECT is also accepted by the route but is not representable in
      OpenAPI; the eight representable verbs are listed below.
    get:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: k8sProxyGet
      summary: Proxy a request to the cluster Kubernetes apiserver
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque request body forwarded unchanged to the apiserver.
        content:
          application/json:
            schema: {}
          application/yaml:
            schema:
              type: string
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied apiserver response (status, headers and body forwarded through).
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Missing cluster_id or malformed request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '502':
          description: Invalid response from the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Cluster agent not connected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Request timed out waiting for the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    head:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: k8sProxyHead
      summary: Proxy a request to the cluster Kubernetes apiserver
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque request body forwarded unchanged to the apiserver.
        content:
          application/json:
            schema: {}
          application/yaml:
            schema:
              type: string
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied apiserver response (status, headers and body forwarded through).
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Missing cluster_id or malformed request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '502':
          description: Invalid response from the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Cluster agent not connected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Request timed out waiting for the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    options:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: k8sProxyOptions
      summary: Proxy a request to the cluster Kubernetes apiserver
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque request body forwarded unchanged to the apiserver.
        content:
          application/json:
            schema: {}
          application/yaml:
            schema:
              type: string
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied apiserver response (status, headers and body forwarded through).
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Missing cluster_id or malformed request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '502':
          description: Invalid response from the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Cluster agent not connected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Request timed out waiting for the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: k8sProxyPost
      summary: Proxy a mutating request (POST) to the cluster apiserver. Requires clusters:write scope.
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque request body forwarded unchanged to the apiserver.
        content:
          application/json:
            schema: {}
          application/yaml:
            schema:
              type: string
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied apiserver response (status, headers and body forwarded through).
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Missing cluster_id or malformed request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '502':
          description: Invalid response from the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Cluster agent not connected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Request timed out waiting for the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: k8sProxyPut
      summary: Proxy a mutating request (PUT) to the cluster apiserver. Requires clusters:write scope.
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque request body forwarded unchanged to the apiserver.
        content:
          application/json:
            schema: {}
          application/yaml:
            schema:
              type: string
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied apiserver response (status, headers and body forwarded through).
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Missing cluster_id or malformed request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '502':
          description: Invalid response from the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Cluster agent not connected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Request timed out waiting for the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: k8sProxyPatch
      summary: Proxy a mutating request (PATCH) to the cluster apiserver. Requires clusters:write scope.
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque request body forwarded unchanged to the apiserver.
        content:
          application/json:
            schema: {}
          application/yaml:
            schema:
              type: string
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied apiserver response (status, headers and body forwarded through).
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Missing cluster_id or malformed request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '502':
          description: Invalid response from the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Cluster agent not connected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Request timed out waiting for the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: k8sProxyDelete
      summary: Proxy a mutating request (DELETE) to the cluster apiserver. Requires clusters:write scope.
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque request body forwarded unchanged to the apiserver.
        content:
          application/json:
            schema: {}
          application/yaml:
            schema:
              type: string
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied apiserver response (status, headers and body forwarded through).
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Missing cluster_id or malformed request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '502':
          description: Invalid response from the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Cluster agent not connected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Request timed out waiting for the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    trace:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: k8sProxyTrace
      summary: Proxy a request to the cluster Kubernetes apiserver
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque request body forwarded unchanged to the apiserver.
        content:
          application/json:
            schema: {}
          application/yaml:
            schema:
              type: string
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied apiserver response (status, headers and body forwarded through).
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Missing cluster_id or malformed request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '502':
          description: Invalid response from the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Cluster agent not connected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Request timed out waiting for the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/proxy/service/{namespace}/{service_port}:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: namespace
        in: path
        required: true
        schema:
          type: string
        description: |
          Target service namespace. Must be a safe RFC-1123 name; the sensitive
          namespaces kube-system/kube-public/kube-node-lease are rejected.
      - name: service_port
        in: path
        required: true
        schema:
          type: string
        description: |
          `<service>` or `<service>:<port>` (port defaults to 80). Proxied via
          the apiserver service-proxy subresource
          (`/api/v1/namespaces/{ns}/services/http:{svc}:{port}/proxy`).
    description: |
      Reverse proxy to an in-cluster HTTP Service through the tunnel
      (handler.ServiceProxyHandler.ServeHTTP). The target service+port must be
      on the enabled-tools allowlist (ListEnabledTools / service_proxy_allowed
      presets) or the request is rejected 403. On mutating methods the api-token
      must carry `clusters:write` (requireServiceProxyScope); reads bypass it.
      RBAC is derived from method (clusters:read/update). CONNECT is accepted but
      not representable in OpenAPI.
    get:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxyGet
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    head:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxyHead
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    options:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxyOptions
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxyPost
      summary: Proxy a mutating request (POST) to an in-cluster Service. Requires clusters:write scope.
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxyPut
      summary: Proxy a mutating request (PUT) to an in-cluster Service. Requires clusters:write scope.
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxyPatch
      summary: Proxy a mutating request (PATCH) to an in-cluster Service. Requires clusters:write scope.
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxyDelete
      summary: Proxy a mutating request (DELETE) to an in-cluster Service. Requires clusters:write scope.
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    trace:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxyTrace
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/clusters/{cluster_id}/proxy/service/{namespace}/{service_port}/{path}:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: namespace
        in: path
        required: true
        schema:
          type: string
      - name: service_port
        in: path
        required: true
        schema:
          type: string
        description: '`<service>` or `<service>:<port>`.'
      - name: path
        in: path
        required: true
        schema:
          type: string
        description: Opaque sub-path appended to the service-proxy URL (chi wildcard `*`).
    description: |
      Same as the service-proxy base route but with a trailing sub-path
      forwarded to the upstream service. See
      `/api/v1/clusters/{cluster_id}/proxy/service/{namespace}/{service_port}`.
      CONNECT accepted but not representable in OpenAPI.
    get:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxySubGet
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    head:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxySubHead
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    options:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxySubOptions
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxySubPost
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxySubPut
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxySubPatch
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxySubDelete
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    trace:
      x-astronomer-request-schema-status: passthrough
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: proxy
      operationId: serviceProxySubTrace
      summary: Proxy a request to an in-cluster Service
      tags:
        - Platform
      security:
        - bearerJWT: []
      requestBody:
        required: false
        description: Opaque body forwarded to the upstream service.
        content:
          application/json:
            schema: {}
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Proxied service response.
          content:
            application/json:
              schema: {}
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid service-proxy target or unreadable body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Service-proxy target not allowlisted or RBAC denied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Service proxy not configured or upstream error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/connect/{cluster_id}:
    parameters:
      - name: cluster_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: |
          Cluster UUID. Also accepted via `?cluster_id=` query or the
          `X-Cluster-ID` header (the header is preferred).
    description: |
      WebSocket reverse-tunnel endpoint for cluster agents
      (tunnel2.RemoteServer.ServeHTTP, remotedialer-based). The agent connects
      and HTTP-Upgrades to a persistent WebSocket; the management server then
      dials in-cluster services back through it. Authorization: a cluster
      registration token presented as `Authorization: Bearer <token>`, whose
      `cluster_id` MUST match the requested cluster. This is an agent-only
      endpoint, not a user API. Registered outside the request-timeout group
      because the connection is long-lived. CONNECT accepted but not
      representable in OpenAPI.
    get:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: stream
      operationId: tunnelConnectGet
      summary: Agent reverse-tunnel WebSocket upgrade
      tags:
        - Platform
      security:
        - bearerJWT: []
      responses:
        '101':
          description: Switching Protocols — WebSocket tunnel established.
        '401':
          description: Missing/invalid registration token or cluster mismatch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    head:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: stream
      operationId: tunnelConnectHead
      summary: Agent reverse-tunnel WebSocket upgrade
      tags:
        - Platform
      security:
        - bearerJWT: []
      responses:
        '101':
          description: Switching Protocols — WebSocket tunnel established.
        '401':
          description: Missing/invalid registration token or cluster mismatch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    options:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: stream
      operationId: tunnelConnectOptions
      summary: Agent reverse-tunnel WebSocket upgrade
      tags:
        - Platform
      security:
        - bearerJWT: []
      responses:
        '101':
          description: Switching Protocols — WebSocket tunnel established.
        '401':
          description: Missing/invalid registration token or cluster mismatch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: stream
      operationId: tunnelConnectPost
      summary: Agent reverse-tunnel WebSocket upgrade
      tags:
        - Platform
      security:
        - bearerJWT: []
      responses:
        '101':
          description: Switching Protocols — WebSocket tunnel established.
        '401':
          description: Missing/invalid registration token or cluster mismatch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: stream
      operationId: tunnelConnectPut
      summary: Agent reverse-tunnel WebSocket upgrade
      tags:
        - Platform
      security:
        - bearerJWT: []
      responses:
        '101':
          description: Switching Protocols — WebSocket tunnel established.
        '401':
          description: Missing/invalid registration token or cluster mismatch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: stream
      operationId: tunnelConnectPatch
      summary: Agent reverse-tunnel WebSocket upgrade
      tags:
        - Platform
      security:
        - bearerJWT: []
      responses:
        '101':
          description: Switching Protocols — WebSocket tunnel established.
        '401':
          description: Missing/invalid registration token or cluster mismatch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: stream
      operationId: tunnelConnectDelete
      summary: Agent reverse-tunnel WebSocket upgrade
      tags:
        - Platform
      security:
        - bearerJWT: []
      responses:
        '101':
          description: Switching Protocols — WebSocket tunnel established.
        '401':
          description: Missing/invalid registration token or cluster mismatch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    trace:
      x-astronomer-auth: "bearer-or-session"
      x-astronomer-rbac: "handler-and-route-policy"
      x-astronomer-route-class: stream
      operationId: tunnelConnectTrace
      summary: Agent reverse-tunnel WebSocket upgrade
      tags:
        - Platform
      security:
        - bearerJWT: []
      responses:
        '101':
          description: Switching Protocols — WebSocket tunnel established.
        '401':
          description: Missing/invalid registration token or cluster mismatch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/ws/clusters/{cluster_id}/shell/sessions/{id}:
    get:
      x-astronomer-auth: "stream ticket or Authorization bearer token"
      x-astronomer-rbac: "session ownership plus cluster update permission on session creation"
      x-astronomer-route-class: stream
      operationId: kubectlShellWebSocket
      summary: Kubectl break-glass shell WebSocket
      description: |
        WebSocket upgrade for an in-browser kubectl shell session
        (handler.KubectlShellHandler.HandleWS). The session row identified by
        `{id}` must already exist (created via
        `POST /api/v1/clusters/{cluster_id}/shell/sessions/`), belong to the
        caller (or caller is superuser), be in the named cluster, and be
        `active`. After validation the WS is upgraded in place and frames are
        proxied to the cluster agent's exec relay; typed command lines are
        recorded for audit.

        Auth: because browsers cannot set Authorization headers on a WS
        handshake, the JWT/API-token is passed as a short-lived `?ticket=`
        query parameter (StreamKindShell) or via the Authorization header.
        Rate-limited under the exec/logs class.
      tags:
        - Platform
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Kubectl shell session id.
        - name: ticket
          in: query
          required: false
          schema:
            type: string
          description: Short-lived one-use stream ticket (browser auth path).
      responses:
        '101':
          description: Switching Protocols — shell WebSocket established.
        '401':
          description: Authentication required / invalid ticket.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Session belongs to another operator.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Session not found in this cluster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Session is not active.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Kubectl shell or exec proxy not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/ws/exec/{cluster_id}/{namespace}/{pod}/{container}:
    get:
      x-astronomer-auth: "stream ticket or Authorization bearer token"
      x-astronomer-rbac: "stream authorization plus pod exec permission on higher-level entry points"
      x-astronomer-route-class: stream
      operationId: podExecWebSocket
      summary: Pod exec (interactive terminal) WebSocket
      description: |
        WebSocket upgrade that relays an interactive `/bin/sh` exec session
        between the browser (xterm.js) and a pod container through the tunnel
        (tunnel.ExecConsumer.HandleExec). Browser→server frames are JSON
        envelopes (`{"type":"stdin"|"resize"|"auth"|"end",...}`); server→browser
        frames are `{"type":"output"|"error"|"end",...}`.

        Auth: bearer JWT/API-token via Authorization header, or a short-lived
        `?ticket=` (StreamKindExec) for the browser path. Rate-limited under the
        exec/logs class.
      tags:
        - Platform
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: namespace
          in: path
          required: true
          schema:
            type: string
        - name: pod
          in: path
          required: true
          schema:
            type: string
        - name: container
          in: path
          required: true
          schema:
            type: string
        - name: ticket
          in: query
          required: false
          schema:
            type: string
          description: Short-lived one-use stream ticket (browser auth path).
      responses:
        '101':
          description: Switching Protocols — exec WebSocket established.
        '400':
          description: Missing cluster_id/namespace/pod/container.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/ws/logs/{cluster_id}/{namespace}/{pod}/{container}:
    get:
      x-astronomer-auth: "stream ticket or Authorization bearer token"
      x-astronomer-rbac: "stream authorization plus pod logs permission on higher-level entry points"
      x-astronomer-route-class: stream
      operationId: podLogsWebSocket
      summary: Pod log stream WebSocket
      description: |
        WebSocket upgrade that streams pod container logs from the cluster agent
        to the browser (tunnel.LogsConsumer.HandleLogs). Each server→browser
        frame is a JSON `{timestamp, message, container}` object. Log options are
        passed as query parameters and forwarded to kubelet.

        Auth: bearer JWT/API-token via Authorization header, or a short-lived
        `?ticket=` (StreamKindLogs) for the browser path. Rate-limited under the
        exec/logs class.
      tags:
        - Platform
      security:
        - bearerJWT: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: namespace
          in: path
          required: true
          schema:
            type: string
        - name: pod
          in: path
          required: true
          schema:
            type: string
        - name: container
          in: path
          required: true
          schema:
            type: string
        - name: follow
          in: query
          required: false
          schema:
            type: string
            enum:
              - 'true'
              - 'false'
          description: Stream new lines as they arrive when "true".
        - name: tail_lines
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
          description: Number of trailing lines to return (alias tailLines). Mutually exclusive with since_seconds.
        - name: since_seconds
          in: query
          required: false
          schema:
            type: integer
            format: int64
            minimum: 1
          description: Return logs newer than this many seconds (alias sinceSeconds). Mutually exclusive with tail_lines.
        - name: ticket
          in: query
          required: false
          schema:
            type: string
          description: Short-lived one-use stream ticket (browser auth path).
      responses:
        '101':
          description: Switching Protocols — log stream WebSocket established.
        '400':
          description: Missing cluster_id/namespace/pod/container.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /internal/tunnel/k8s/{cluster_id}:
    post:

      x-astronomer-auth: "X-Astronomer-Internal-PSK derived from shared encryption key"
      x-astronomer-rbac: "server-internal only; caller workflow owns user RBAC"
      x-astronomer-route-class: internal
      operationId: internalTunnelK8s
      summary: Cross-pod internal Kubernetes request forward (server-to-server)
      description: |
        Server-internal endpoint (tunnel.InternalK8sHandler.Handle) used by a
        sibling server replica to forward a K8sRequest to whichever pod owns the
        cluster agent's WebSocket. NOT a user API and mounted outside the JWT
        chain.

        Auth: a pre-shared key in the `X-Astronomer-Internal-PSK` header
        (derived from the platform encryption key) plus the in-band marker
        `X-Astronomer-Internal-Source: sibling-server-pod`. Requests lacking the
        marker or with a bad PSK get 403; when no encryption key is configured
        the endpoint is disabled (503). The optional
        `X-Astronomer-Forwarded-User` header carries the originating user UUID
        for audit attribution.
      tags:
        - Platform
      security: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: X-Astronomer-Internal-PSK
          in: header
          required: true
          schema:
            type: string
          description: Shared-secret PSK derived from the platform encryption key.
        - name: X-Astronomer-Internal-Source
          in: header
          required: true
          schema:
            type: string
            enum:
              - sibling-server-pod
        - name: X-Astronomer-Forwarded-User
          in: header
          required: false
          schema:
            type: string
            format: uuid
          description: Originating user UUID for audit attribution.
      requestBody:
        required: true
        description: JSON-encoded K8sRequestPayload.
        content:
          application/json:
            schema:
              type: object
              properties:
                method:
                  type: string
                path:
                  type: string
                headers:
                  type: object
                  additionalProperties:
                    type: string
                body:
                  type: string
                  description: base64-encoded request body
                caller_user:
                  type: string
                  description: Canonical user or machine subject stamped by the originating server.
                caller_groups:
                  type: array
                  items:
                    type: string
                  description: Canonical role-group subjects, when resolved by the originating server.
                caller_request_id:
                  type: string
                  description: Server-generated request correlation identifier; never copied from caller input.
                caller_origin:
                  type: string
                  enum:
                    - user
                    - machine
                  description: Positive discriminator for the stamped caller identity.
              required:
                - method
                - path
      responses:
        '200':
          description: JSON-encoded K8sResponsePayload from the agent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status_code:
                    type: integer
                  headers:
                    type: object
                    additionalProperties:
                      type: string
                  body:
                    type: string
                    description: base64-encoded response body
                required:
                  - status_code
        '400':
          description: Missing cluster_id or undecodable payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing sibling-source marker or invalid PSK.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Agent send/decode failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Internal endpoint disabled or agent not connected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Timed out waiting for the agent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /internal/tunnel/helm/{cluster_id}:
    post:
      x-astronomer-auth: "X-Astronomer-Internal-PSK derived from shared encryption key"
      x-astronomer-rbac: "server-internal only; caller workflow owns user RBAC"
      x-astronomer-route-class: internal
      operationId: internalTunnelHelm
      summary: Cross-pod internal Helm request forward (server-to-server)
      description: |
        Server-internal endpoint (tunnel.InternalHelmHandler.Handle) used by a
        sibling server replica to forward a Helm operation
        (install/upgrade/uninstall/rollback/status) to the pod owning the
        cluster agent's WebSocket. NOT a user API; mounted outside the JWT chain.

        Auth: same PSK + `X-Astronomer-Internal-Source: sibling-server-pod`
        contract as `/internal/tunnel/k8s/{cluster_id}`. Mutating ops are
        audited as `cluster.helm_proxy.forwarded`, attributed via
        `X-Astronomer-Forwarded-User`. Helm waits up to 10 minutes.
      tags:
        - Platform
      security: []
      parameters:
        - name: cluster_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: X-Astronomer-Internal-PSK
          in: header
          required: true
          schema:
            type: string
        - name: X-Astronomer-Internal-Source
          in: header
          required: true
          schema:
            type: string
            enum:
              - sibling-server-pod
        - name: X-Astronomer-Forwarded-User
          in: header
          required: false
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        description: |
          InternalHelmRequest — the Helm message type plus the HelmRequestPayload.
        content:
          application/json:
            schema:
              type: object
              properties:
                msg_type:
                  type: string
                  description: Helm message type (HELM_INSTALL/HELM_UPGRADE/HELM_UNINSTALL/HELM_ROLLBACK/HELM_STATUS).
                payload:
                  type: object
                  properties:
                    release_name:
                      type: string
                    namespace:
                      type: string
                    chart_url:
                      type: string
                    chart_name:
                      type: string
                    repo_url:
                      type: string
                    version:
                      type: string
                    values:
                      type: object
                      additionalProperties: true
                    revision:
                      type: integer
                      description: for rollback
                    timeout:
                      type: integer
                      description: seconds
                  required:
                    - release_name
                    - namespace
              required:
                - msg_type
                - payload
      responses:
        '200':
          description: JSON-encoded HelmResultPayload from the agent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  release_name:
                    type: string
                  namespace:
                    type: string
                  status:
                    type: string
                  revision:
                    type: integer
                  error:
                    type: string
                required:
                  - success
        '400':
          description: Missing cluster_id, undecodable payload, or invalid helm message type.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Missing sibling-source marker or invalid PSK.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Agent send/decode failure or stream closed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Internal endpoint disabled or agent not connected.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Timed out waiting for the agent (helm cap 10m).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
