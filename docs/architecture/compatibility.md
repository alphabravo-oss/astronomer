# Release compatibility

<!-- Generated by scripts/compatibility-contract.py; do not edit by hand. -->

This table describes the `1.0.x` release line. Install mode is **fresh only**; pre-v1 databases are not upgraded in place.

## Runtime matrix

| Surface | Supported contract |
| --- | --- |
| Kubernetes | v1.33, v1.34, v1.35; latest patch of each minor |
| Flux distribution | `v2.9.3` |
| Agent protocol | `astronomer.delivery` versions 2-2 |
| PostgreSQL | 16, 17; TLS required in production |
| Browsers | Chrome 130+, Edge 130+, Firefox 128+, Safari 18.0+; current and previous stable at release |

## Flux components

Images are resolved to immutable digests by the release distribution; tags below identify the qualified upstream component release.

| Component | Upstream image | APIs consumed by Astronomer |
| --- | --- | --- |
| `source-controller` | `ghcr.io/fluxcd/source-controller@sha256:ff8f3c92f1bcb433e858c948040c3a3393fe73f5dd72048a4502bfaf0a4c26cd` (upstream `v1.9.3`) | `source.toolkit.fluxcd.io/v1/GitRepository`<br>`source.toolkit.fluxcd.io/v1/HelmRepository`<br>`source.toolkit.fluxcd.io/v1/OCIRepository` |
| `kustomize-controller` | `ghcr.io/fluxcd/kustomize-controller@sha256:2b8bec54ffb6caf421bd2a6c005d27f567d5dd4db7feb55794fb51fcabd69b8f` (upstream `v1.9.4`) | `kustomize.toolkit.fluxcd.io/v1/Kustomization` |
| `helm-controller` | `ghcr.io/fluxcd/helm-controller@sha256:16ada99456385100698a5d7adf90aba8a2089d987ab541c9566b6d7b0e897038` (upstream `v1.6.3`) | `helm.toolkit.fluxcd.io/v2/HelmRelease` |

Excluded components: `notification-controller`, `image-reflector-controller`, `image-automation-controller`, `source-watcher`.

## Source types

| Type | Immutable identity | Authentication modes |
| --- | --- | --- |
| `git` | `commit` | `none`, `basic`, `ssh`, `workload_identity` |
| `oci_artifact` | `digest` | `none`, `docker_config`, `workload_identity` |
| `helm_http` | `version_and_sha256` | `none`, `basic`, `tls_client_certificate` |
| `helm_oci` | `manifest_digest` | `none`, `docker_config`, `workload_identity` |

## Hard limits

| Limit | Value |
| --- | ---: |
| Assignment snapshot | 1 MiB |
| Status snapshot | 512 KiB |
| Events in one status snapshot | 100 |
| Source artifact | 512 MiB |
| Helm chart | 100 MiB |
| Rendered objects per deployment | 10000 |
| Concurrent assignments per cluster | 32 |

## Service objectives

| Objective | Target |
| --- | ---: |
| Management API monthly availability | 99.95% |
| Connected assignment acknowledgement p99 | 60 seconds |
| Flux transition visible centrally p99 | 30 seconds |
| Rollout scheduler decision p99 | 15 seconds |
| Management database RPO | 5 minutes |
| Management plane RTO | 60 minutes |
| Control-plane outage behavior | Last accepted generation continues reconciling locally |

## Kubernetes qualification

Every advertised minor has an explicit executable live qualification command:

- Kubernetes 1.33: `bash ./scripts/k3d-version-matrix-test.sh v1.33.5-k3s1`
- Kubernetes 1.34: `bash ./scripts/k3d-version-matrix-test.sh v1.34.1-k3s1`
- Kubernetes 1.35: `bash ./scripts/k3d-version-matrix-test.sh v1.35.0-k3s1`

Validate this contract and generated page with:

```bash
./scripts/compatibility-contract.py check
```
